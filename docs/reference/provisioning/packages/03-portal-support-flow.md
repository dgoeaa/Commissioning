# 03-portal-support.flow

> **design package — specified, not deployed.** Every value below is read from
> [`docs/deployment/power-automate-flows/03-portal-support.flow.json`](../../../deployment/power-automate-flows/03-portal-support.flow.json) and printed as that package carries it.
> Generated by `scripts/build-provisioning-reference.mjs` — do not edit by hand.

## 1. Identity

| Field | As provisioned |
|---|---|
| Display name | `03-portal-support.flow` |
| Internal name | `null` |
| Workflow id | _not carried by this package_ |
| Environment | `null` |
| Full resource id | `null` — the export records the field and leaves it null |
| Estate | Document portal — reachable by anonymous callers — the package sits in `docs/deployment/power-automate-flows/` |
| Package family | design package — specified, not deployed |
| Exported at (UTC) | `null` |
| Exported by | `null` |
| Source package | `docs/deployment/power-automate-flows/03-portal-support.flow.json` |

## 2. Definition envelope

| Field | As provisioned |
|---|---|
| `$schema` | `https://schema.management.azure.com/providers/Microsoft.Logic/schemas/2016-06-01/workflowdefinition.json#` |
| `contentVersion` | `1.0.0.0` |
| `description` | _not declared_ |
| `outputs` | `{}` — declared and empty |
| Actions | 24 |

## 3. Definition parameters

| Parameter | Type | Default value |
|---|---|---|
| `$connections` | `Object` | `{}` |

## 4. Connections and references

**Connections the actions are bound to**, read from each action's `inputs.host`:

| Connection name | API id | Reference name | Operations used |
|---|---|---|---|
| `shared_sharepointonline` | `/providers/Microsoft.PowerApps/apis/shared_sharepointonline` | — | `GetItems`×1, `PatchItem`×1, `PostItem`×2 |

**`connectionReferences`, verbatim:**

```json
{
  "shared_sharepointonline": {
    "connectionName": "4b7f2a9e6c1d4f3e9a8b5c2d7e1f6a3b",
    "source": "Invoker",
    "id": "/providers/Microsoft.PowerApps/apis/shared_sharepointonline",
    "tier": "NotSpecified"
  }
}
```

**`allConnectionData`** — the connection each named action is bound to:

| Action | Reference key | Connection name | API | Connection id |
|---|---|---|---|---|
| `Get_Rate_Limit_Support` | `shared_sharepointonline` | `4b7f2a9e6c1d4f3e9a8b5c2d7e1f6a3b` | `/providers/Microsoft.PowerApps/apis/shared_sharepointonline` | `/providers/Microsoft.PowerApps/apis/shared_sharepointonline/connections/4b7f2a9e6c1d4f3e9a8b5c2d7e1f6a3b` |
| `Update_Rate_Limit_Support` | `shared_sharepointonline` | `4b7f2a9e6c1d4f3e9a8b5c2d7e1f6a3b` | `/providers/Microsoft.PowerApps/apis/shared_sharepointonline` | `/providers/Microsoft.PowerApps/apis/shared_sharepointonline/connections/4b7f2a9e6c1d4f3e9a8b5c2d7e1f6a3b` |
| `Create_Rate_Limit_Support` | `shared_sharepointonline` | `4b7f2a9e6c1d4f3e9a8b5c2d7e1f6a3b` | `/providers/Microsoft.PowerApps/apis/shared_sharepointonline` | `/providers/Microsoft.PowerApps/apis/shared_sharepointonline/connections/4b7f2a9e6c1d4f3e9a8b5c2d7e1f6a3b` |
| `Create_Support_Case` | `shared_sharepointonline` | `4b7f2a9e6c1d4f3e9a8b5c2d7e1f6a3b` | `/providers/Microsoft.PowerApps/apis/shared_sharepointonline` | `/providers/Microsoft.PowerApps/apis/shared_sharepointonline/connections/4b7f2a9e6c1d4f3e9a8b5c2d7e1f6a3b` |

## 5. Trigger — the endpoint this flow exposes

| Field | As provisioned |
|---|---|
| Trigger name | `manual` |
| `type` | `Request` |
| `kind` | `Http` |
| `method` | `POST` |
| `relativePath` | _not declared — the trigger accepts the bare invoke path_ |
| `triggerAuthenticationType` | _absent from the export — no posture is recorded_ |
| `operationOptions` | _not declared_ |
| `conditions` | _not declared_ |
| `splitOn` | _not declared_ |
| `correlation` | _not declared_ |
| `recurrence` | _not declared_ |
| Declared request headers | _not declared_ |
| Declared query parameters | _not declared_ |
| `description` | _not declared_ |

### 5.1 Request payload — declared properties

| Property | Type | Required | Format | Enum | Default | Constraints | Description |
|---|---|---|---|---|---|---|---|
| `name` | `string \| null` | no | — | — | — | — | The submitter's full name. |
| `email` | `string \| null` | no | `email` | — | — | — | Reply address for the case. |
| `topic` | `string \| null` | no | — | — | — | — | The closest support topic label. |
| `aboutReference` | `string \| null` | no | — | — | — | — | An optional tracking ID the case concerns. |
| `message` | `string \| null` | no | — | — | — | — | The case description. |

Root `required`: _empty — the schema marks nothing mandatory_. Root `additionalProperties`: `true`.

### 5.2 Request payload — schema as provisioned

```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "title": "Portal Support Case Request",
  "description": "A helpdesk case raised from the NITDA Intelligent Portal support page.",
  "properties": {
    "name": {
      "type": [
        "string",
        "null"
      ],
      "description": "The submitter's full name."
    },
    "email": {
      "type": [
        "string",
        "null"
      ],
      "format": "email",
      "description": "Reply address for the case."
    },
    "topic": {
      "type": [
        "string",
        "null"
      ],
      "description": "The closest support topic label."
    },
    "aboutReference": {
      "type": [
        "string",
        "null"
      ],
      "description": "An optional tracking ID the case concerns."
    },
    "message": {
      "type": [
        "string",
        "null"
      ],
      "description": "The case description."
    }
  },
  "required": [],
  "additionalProperties": true
}
```

## 6. Responses returned to the caller

| Action | Status code | Runs after | On statuses |
|---|---|---|---|
| `Scope_Support_Main/Condition_Validate_Request/Condition_RL_Support_Allowed/Response_Support_Created` | `200` | `Create_Support_Case` | Succeeded |
| `Scope_Support_Main/Condition_Validate_Request/Condition_RL_Support_Allowed/else/Response_Support_Rate_Limited` | `429` | _first action_ | — |
| `Scope_Support_Main/Condition_Validate_Request/else/Response_Support_Invalid_Request` | `400` | _first action_ | — |
| `Scope_Support_Catch/Response_Support_Processing_Failed` | `500` | _first action_ | — |

### 6.1 `Scope_Support_Main/Condition_Validate_Request/Condition_RL_Support_Allowed/Response_Support_Created`

| Field | As provisioned |
|---|---|
| `statusCode` | `200` |
| `operationOptions` | _not declared_ |

| Header | Configured value |
|---|---|
| `Content-Type` | `application/json` |
| `Access-Control-Allow-Origin` | `*` |
| `Access-Control-Allow-Methods` | `POST, OPTIONS` |
| `Access-Control-Allow-Headers` | `Content-Type, X-Correlation-ID` |

**Response body, verbatim:**

```json
{
  "caseRef": "@outputs('Compose_Case_Ref')"
}
```

### 6.2 `Scope_Support_Main/Condition_Validate_Request/Condition_RL_Support_Allowed/else/Response_Support_Rate_Limited`

| Field | As provisioned |
|---|---|
| `statusCode` | `429` |
| `operationOptions` | _not declared_ |

| Header | Configured value |
|---|---|
| `Content-Type` | `application/json` |
| `Access-Control-Allow-Origin` | `*` |
| `Access-Control-Allow-Methods` | `POST, OPTIONS` |
| `Access-Control-Allow-Headers` | `Content-Type, X-Correlation-ID` |

**Response body, verbatim:**

```json
{
  "delivered": false,
  "reason": "too-many-requests"
}
```

### 6.3 `Scope_Support_Main/Condition_Validate_Request/else/Response_Support_Invalid_Request`

| Field | As provisioned |
|---|---|
| `statusCode` | `400` |
| `operationOptions` | _not declared_ |

| Header | Configured value |
|---|---|
| `Content-Type` | `application/json` |
| `Access-Control-Allow-Origin` | `*` |
| `Access-Control-Allow-Methods` | `POST, OPTIONS` |
| `Access-Control-Allow-Headers` | `Content-Type, X-Correlation-ID` |

**Response body, verbatim:**

```json
{
  "delivered": false,
  "reason": "invalid-request"
}
```

### 6.4 `Scope_Support_Catch/Response_Support_Processing_Failed`

| Field | As provisioned |
|---|---|
| `statusCode` | `500` |
| `operationOptions` | _not declared_ |

| Header | Configured value |
|---|---|
| `Content-Type` | `application/json` |
| `Access-Control-Allow-Origin` | `*` |
| `Access-Control-Allow-Methods` | `POST, OPTIONS` |
| `Access-Control-Allow-Headers` | `Content-Type, X-Correlation-ID` |

**Response body, verbatim:**

```json
{
  "delivered": false,
  "reason": "support-processing-failed"
}
```

## 7. Termination

_No `Terminate` action. The run ends when the action graph does._

## 8. Error handling and run-time configuration

**Actions wired to run on something other than success.** These are the flow's catch
paths — an action that runs after `Failed`, `Skipped` or `TimedOut` is error handling,
whatever it is named.

| Action | Type | Runs after | On statuses |
|---|---|---|---|
| `Scope_Support_Catch` | `Scope` | `Scope_Support_Main` | Failed, TimedOut |

_No action carries a retry policy, secure-data setting, operation option, tracked
property or loop limit. Every call runs on the platform defaults._

## 9. Variables

_This package initialises no variables._

## 10. SharePoint operations

Each row is one connector call. `table` is printed as configured; the resolved column
names the list that GUID belongs to per `docs/reference/sharepoint-list-index.json`,
and says so plainly when the index does not carry it.

| Action | Operation | Connection | `dataset` (site) | `table` (list) | Resolves to |
|---|---|---|---|---|---|
| `Scope_Support_Main/Condition_Validate_Request/Get_Rate_Limit_Support` | `GetItems` | `shared_sharepointonline` | `https://nitdanigeria.sharepoint.com/sites/NEDMS` | `NITDA_Portal_RateLimits` | _not in the list index_ |
| `Scope_Support_Main/Condition_Validate_Request/Condition_RL_Support_Allowed/Condition_RL_Support_Bucket_Exists/Update_Rate_Limit_Support` | `PatchItem` | `shared_sharepointonline` | `https://nitdanigeria.sharepoint.com/sites/NEDMS` | `NITDA_Portal_RateLimits` | _not in the list index_ |
| `Scope_Support_Main/Condition_Validate_Request/Condition_RL_Support_Allowed/Condition_RL_Support_Bucket_Exists/else/Create_Rate_Limit_Support` | `PostItem` | `shared_sharepointonline` | `https://nitdanigeria.sharepoint.com/sites/NEDMS` | `NITDA_Portal_RateLimits` | _not in the list index_ |
| `Scope_Support_Main/Condition_Validate_Request/Condition_RL_Support_Allowed/Create_Support_Case` | `PostItem` | `shared_sharepointonline` | `https://nitdanigeria.sharepoint.com/sites/NEDMS` | `NITDA_Portal_SupportCases` | _not in the list index_ |

## 11. Outbound HTTP calls

_This package makes no raw HTTP call._

## 12. Action dependency register

Every action in the package, in graph order, with the edge that starts it. `↳` marks
nesting depth; the path is the address used everywhere else on this page.

| Action path | Type | Kind | Runs after | On statuses |
|---|---|---|---|---|
| `Scope_Support_Main` | `Scope` | — | _first in its scope_ | — |
| ↳`Scope_Support_Main/Compose_Source_Ip` | `Compose` | — | _first in its scope_ | — |
| ↳`Scope_Support_Main/Compose_Name` | `Compose` | — | `Compose_Source_Ip` | Succeeded |
| ↳`Scope_Support_Main/Compose_Email` | `Compose` | — | `Compose_Name` | Succeeded |
| ↳`Scope_Support_Main/Compose_Topic` | `Compose` | — | `Compose_Email` | Succeeded |
| ↳`Scope_Support_Main/Compose_About_Reference` | `Compose` | — | `Compose_Topic` | Succeeded |
| ↳`Scope_Support_Main/Compose_Message` | `Compose` | — | `Compose_About_Reference` | Succeeded |
| ↳`Scope_Support_Main/Condition_Validate_Request` | `If` | — | `Compose_Message` | Succeeded |
| ↳↳`Scope_Support_Main/Condition_Validate_Request/Get_Rate_Limit_Support` | `OpenApiConnection` | — | _first in its scope_ | — |
| ↳↳`Scope_Support_Main/Condition_Validate_Request/Compose_RL_Support_Exists` | `Compose` | — | `Get_Rate_Limit_Support` | Succeeded |
| ↳↳`Scope_Support_Main/Condition_Validate_Request/Compose_RL_Support_Window_Expired` | `Compose` | — | `Compose_RL_Support_Exists` | Succeeded |
| ↳↳`Scope_Support_Main/Condition_Validate_Request/Compose_RL_Support_Effective_Count` | `Compose` | — | `Compose_RL_Support_Window_Expired` | Succeeded |
| ↳↳`Scope_Support_Main/Condition_Validate_Request/Compose_RL_Support_New_Count` | `Compose` | — | `Compose_RL_Support_Effective_Count` | Succeeded |
| ↳↳`Scope_Support_Main/Condition_Validate_Request/Condition_RL_Support_Allowed` | `If` | — | `Compose_RL_Support_New_Count` | Succeeded |
| ↳↳↳`Scope_Support_Main/Condition_Validate_Request/Condition_RL_Support_Allowed/Condition_RL_Support_Bucket_Exists` | `If` | — | _first in its scope_ | — |
| ↳↳↳↳`Scope_Support_Main/Condition_Validate_Request/Condition_RL_Support_Allowed/Condition_RL_Support_Bucket_Exists/Update_Rate_Limit_Support` | `OpenApiConnection` | — | _first in its scope_ | — |
| ↳↳↳↳↳`Scope_Support_Main/Condition_Validate_Request/Condition_RL_Support_Allowed/Condition_RL_Support_Bucket_Exists/else/Create_Rate_Limit_Support` | `OpenApiConnection` | — | _first in its scope_ | — |
| ↳↳↳`Scope_Support_Main/Condition_Validate_Request/Condition_RL_Support_Allowed/Compose_Case_Ref` | `Compose` | — | `Condition_RL_Support_Bucket_Exists` | Succeeded |
| ↳↳↳`Scope_Support_Main/Condition_Validate_Request/Condition_RL_Support_Allowed/Create_Support_Case` | `OpenApiConnection` | — | `Compose_Case_Ref` | Succeeded |
| ↳↳↳`Scope_Support_Main/Condition_Validate_Request/Condition_RL_Support_Allowed/Response_Support_Created` | `Response` | `Http` | `Create_Support_Case` | Succeeded |
| ↳↳↳↳`Scope_Support_Main/Condition_Validate_Request/Condition_RL_Support_Allowed/else/Response_Support_Rate_Limited` | `Response` | `Http` | _first in its scope_ | — |
| ↳↳↳`Scope_Support_Main/Condition_Validate_Request/else/Response_Support_Invalid_Request` | `Response` | `Http` | _first in its scope_ | — |
| `Scope_Support_Catch` | `Scope` | — | `Scope_Support_Main` | Failed/TimedOut |
| ↳`Scope_Support_Catch/Response_Support_Processing_Failed` | `Response` | `Http` | _first in its scope_ | — |

## 13. Configured values, action by action

The configured input of every action, verbatim. An action with no `inputs` key — a
`Scope`, an `If`, a `Foreach` — is documented by its control expression instead.

### `Scope_Support_Main`

| Field | As provisioned |
|---|---|
| Type | `Scope` |
| Kind | — |
| Description | _not declared_ |
| Runs after | _first in its scope_ |

_This action declares no `inputs`._

### `Scope_Support_Main/Compose_Source_Ip`

| Field | As provisioned |
|---|---|
| Type | `Compose` |
| Kind | — |
| Description | _not declared_ |
| Runs after | _first in its scope_ |

**`inputs`, verbatim:**

```json
"@trim(first(split(coalesce(triggerOutputs()?['headers']?['X-Forwarded-For'], triggerOutputs()?['headers']?['X-Azure-ClientIP'], 'unknown'), ',')))"
```

### `Scope_Support_Main/Compose_Name`

| Field | As provisioned |
|---|---|
| Type | `Compose` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Compose_Source_Ip` (Succeeded) |

**`inputs`, verbatim:**

```json
"@trim(coalesce(triggerBody()?['name'], ''))"
```

### `Scope_Support_Main/Compose_Email`

| Field | As provisioned |
|---|---|
| Type | `Compose` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Compose_Name` (Succeeded) |

**`inputs`, verbatim:**

```json
"@toLower(trim(coalesce(triggerBody()?['email'], '')))"
```

### `Scope_Support_Main/Compose_Topic`

| Field | As provisioned |
|---|---|
| Type | `Compose` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Compose_Email` (Succeeded) |

**`inputs`, verbatim:**

```json
"@trim(coalesce(triggerBody()?['topic'], ''))"
```

### `Scope_Support_Main/Compose_About_Reference`

| Field | As provisioned |
|---|---|
| Type | `Compose` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Compose_Topic` (Succeeded) |

**`inputs`, verbatim:**

```json
"@toUpper(trim(coalesce(triggerBody()?['aboutReference'], '')))"
```

### `Scope_Support_Main/Compose_Message`

| Field | As provisioned |
|---|---|
| Type | `Compose` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Compose_About_Reference` (Succeeded) |

**`inputs`, verbatim:**

```json
"@trim(coalesce(triggerBody()?['message'], ''))"
```

### `Scope_Support_Main/Condition_Validate_Request`

| Field | As provisioned |
|---|---|
| Type | `If` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Compose_Message` (Succeeded) |

_This action declares no `inputs`._

**`expression`, verbatim:**

```json
{
  "and": [
    {
      "equals": [
        "@and(greater(length(outputs('Compose_Name')), 1), and(contains(outputs('Compose_Email'), '@'), equals(indexOf(outputs('Compose_Email'), '@'), lastIndexOf(outputs('Compose_Email'), '@')), greater(indexOf(outputs('Compose_Email'), '@'), 0), less(indexOf(outputs('Compose_Email'), '@'), sub(length(outputs('Compose_Email')), 3)), contains(substring(outputs('Compose_Email'), add(indexOf(outputs('Compose_Email'), '@'), 1)), '.'), not(contains(outputs('Compose_Email'), ' '))), greater(length(outputs('Compose_Topic')), 0), greater(length(outputs('Compose_Message')), 19), less(length(outputs('Compose_Message')), 4000))",
        true
      ]
    }
  ]
}
```

### `Scope_Support_Main/Condition_Validate_Request/Get_Rate_Limit_Support`

| Field | As provisioned |
|---|---|
| Type | `OpenApiConnection` |
| Kind | — |
| Description | _not declared_ |
| Runs after | _first in its scope_ |
| Connector | `shared_sharepointonline` |
| Operation | `GetItems` |

**`inputs`, verbatim:**

```json
{
  "parameters": {
    "dataset": "https://nitdanigeria.sharepoint.com/sites/NEDMS",
    "table": "NITDA_Portal_RateLimits",
    "$filter": "Title eq '@{replace(concat('SUPPORT:', outputs('Compose_Source_Ip')), '''', '''''')}'",
    "$top": 1
  },
  "host": {
    "apiId": "/providers/Microsoft.PowerApps/apis/shared_sharepointonline",
    "operationId": "GetItems",
    "connectionName": "shared_sharepointonline"
  }
}
```

### `Scope_Support_Main/Condition_Validate_Request/Compose_RL_Support_Exists`

| Field | As provisioned |
|---|---|
| Type | `Compose` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Get_Rate_Limit_Support` (Succeeded) |

**`inputs`, verbatim:**

```json
"@greater(length(outputs('Get_Rate_Limit_Support')?['body/value']), 0)"
```

### `Scope_Support_Main/Condition_Validate_Request/Compose_RL_Support_Window_Expired`

| Field | As provisioned |
|---|---|
| Type | `Compose` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Compose_RL_Support_Exists` (Succeeded) |

**`inputs`, verbatim:**

```json
"@if(outputs('Compose_RL_Support_Exists'), greater(sub(ticks(utcNow()), ticks(first(outputs('Get_Rate_Limit_Support')?['body/value']?['WindowStartUtc']))), 6000000000), true)"
```

### `Scope_Support_Main/Condition_Validate_Request/Compose_RL_Support_Effective_Count`

| Field | As provisioned |
|---|---|
| Type | `Compose` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Compose_RL_Support_Window_Expired` (Succeeded) |

**`inputs`, verbatim:**

```json
"@if(outputs('Compose_RL_Support_Window_Expired'), 0, if(outputs('Compose_RL_Support_Exists'), first(outputs('Get_Rate_Limit_Support')?['body/value']?['RequestCount']), 0))"
```

### `Scope_Support_Main/Condition_Validate_Request/Compose_RL_Support_New_Count`

| Field | As provisioned |
|---|---|
| Type | `Compose` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Compose_RL_Support_Effective_Count` (Succeeded) |

**`inputs`, verbatim:**

```json
"@add(outputs('Compose_RL_Support_Effective_Count'), 1)"
```

### `Scope_Support_Main/Condition_Validate_Request/Condition_RL_Support_Allowed`

| Field | As provisioned |
|---|---|
| Type | `If` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Compose_RL_Support_New_Count` (Succeeded) |

_This action declares no `inputs`._

**`expression`, verbatim:**

```json
{
  "and": [
    {
      "equals": [
        "@less(outputs('Compose_RL_Support_Effective_Count'), 5)",
        true
      ]
    }
  ]
}
```

### `Scope_Support_Main/Condition_Validate_Request/Condition_RL_Support_Allowed/Condition_RL_Support_Bucket_Exists`

| Field | As provisioned |
|---|---|
| Type | `If` |
| Kind | — |
| Description | _not declared_ |
| Runs after | _first in its scope_ |

_This action declares no `inputs`._

**`expression`, verbatim:**

```json
{
  "and": [
    {
      "equals": [
        "@outputs('Compose_RL_Support_Exists')",
        true
      ]
    }
  ]
}
```

### `Scope_Support_Main/Condition_Validate_Request/Condition_RL_Support_Allowed/Condition_RL_Support_Bucket_Exists/Update_Rate_Limit_Support`

| Field | As provisioned |
|---|---|
| Type | `OpenApiConnection` |
| Kind | — |
| Description | _not declared_ |
| Runs after | _first in its scope_ |
| Connector | `shared_sharepointonline` |
| Operation | `PatchItem` |

**`inputs`, verbatim:**

```json
{
  "parameters": {
    "dataset": "https://nitdanigeria.sharepoint.com/sites/NEDMS",
    "table": "NITDA_Portal_RateLimits",
    "id": "@first(outputs('Get_Rate_Limit_Support')?['body/value']?['ID'])",
    "item/RequestCount": "@outputs('Compose_RL_Support_New_Count')",
    "item/WindowStartUtc": "@if(outputs('Compose_RL_Support_Window_Expired'), utcNow(), first(outputs('Get_Rate_Limit_Support')?['body/value']?['WindowStartUtc']))",
    "item/UpdatedAtUtc": "@utcNow()"
  },
  "host": {
    "apiId": "/providers/Microsoft.PowerApps/apis/shared_sharepointonline",
    "operationId": "PatchItem",
    "connectionName": "shared_sharepointonline"
  }
}
```

### `Scope_Support_Main/Condition_Validate_Request/Condition_RL_Support_Allowed/Condition_RL_Support_Bucket_Exists/else/Create_Rate_Limit_Support`

| Field | As provisioned |
|---|---|
| Type | `OpenApiConnection` |
| Kind | — |
| Description | _not declared_ |
| Runs after | _first in its scope_ |
| Connector | `shared_sharepointonline` |
| Operation | `PostItem` |

**`inputs`, verbatim:**

```json
{
  "parameters": {
    "dataset": "https://nitdanigeria.sharepoint.com/sites/NEDMS",
    "table": "NITDA_Portal_RateLimits",
    "item/Title": "@concat('SUPPORT:', outputs('Compose_Source_Ip'))",
    "item/WindowStartUtc": "@utcNow()",
    "item/RequestCount": 1,
    "item/UpdatedAtUtc": "@utcNow()"
  },
  "host": {
    "apiId": "/providers/Microsoft.PowerApps/apis/shared_sharepointonline",
    "operationId": "PostItem",
    "connectionName": "shared_sharepointonline"
  }
}
```

### `Scope_Support_Main/Condition_Validate_Request/Condition_RL_Support_Allowed/Compose_Case_Ref`

| Field | As provisioned |
|---|---|
| Type | `Compose` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Condition_RL_Support_Bucket_Exists` (Succeeded) |

**`inputs`, verbatim:**

```json
"@concat('NITDA-S-', toUpper(substring(replace(guid(), '-', ''), 0, 6)))"
```

### `Scope_Support_Main/Condition_Validate_Request/Condition_RL_Support_Allowed/Create_Support_Case`

| Field | As provisioned |
|---|---|
| Type | `OpenApiConnection` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Compose_Case_Ref` (Succeeded) |
| Connector | `shared_sharepointonline` |
| Operation | `PostItem` |

**`inputs`, verbatim:**

```json
{
  "parameters": {
    "dataset": "https://nitdanigeria.sharepoint.com/sites/NEDMS",
    "table": "NITDA_Portal_SupportCases",
    "item/Title": "@outputs('Compose_Case_Ref')",
    "item/Name": "@outputs('Compose_Name')",
    "item/Email": "@outputs('Compose_Email')",
    "item/Topic": "@outputs('Compose_Topic')",
    "item/AboutReference": "@outputs('Compose_About_Reference')",
    "item/Message": "@outputs('Compose_Message')",
    "item/Status": "open",
    "item/SubmittedAtUtc": "@utcNow()",
    "item/SourceIp": "@outputs('Compose_Source_Ip')"
  },
  "host": {
    "apiId": "/providers/Microsoft.PowerApps/apis/shared_sharepointonline",
    "operationId": "PostItem",
    "connectionName": "shared_sharepointonline"
  }
}
```

### `Scope_Support_Main/Condition_Validate_Request/Condition_RL_Support_Allowed/Response_Support_Created`

| Field | As provisioned |
|---|---|
| Type | `Response` |
| Kind | `Http` |
| Description | _not declared_ |
| Runs after | `Create_Support_Case` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "statusCode": 200,
  "headers": {
    "Content-Type": "application/json",
    "Access-Control-Allow-Origin": "*",
    "Access-Control-Allow-Methods": "POST, OPTIONS",
    "Access-Control-Allow-Headers": "Content-Type, X-Correlation-ID"
  },
  "body": {
    "caseRef": "@outputs('Compose_Case_Ref')"
  }
}
```

### `Scope_Support_Main/Condition_Validate_Request/Condition_RL_Support_Allowed/else/Response_Support_Rate_Limited`

| Field | As provisioned |
|---|---|
| Type | `Response` |
| Kind | `Http` |
| Description | _not declared_ |
| Runs after | _first in its scope_ |

**`inputs`, verbatim:**

```json
{
  "statusCode": 429,
  "headers": {
    "Content-Type": "application/json",
    "Access-Control-Allow-Origin": "*",
    "Access-Control-Allow-Methods": "POST, OPTIONS",
    "Access-Control-Allow-Headers": "Content-Type, X-Correlation-ID"
  },
  "body": {
    "delivered": false,
    "reason": "too-many-requests"
  }
}
```

### `Scope_Support_Main/Condition_Validate_Request/else/Response_Support_Invalid_Request`

| Field | As provisioned |
|---|---|
| Type | `Response` |
| Kind | `Http` |
| Description | _not declared_ |
| Runs after | _first in its scope_ |

**`inputs`, verbatim:**

```json
{
  "statusCode": 400,
  "headers": {
    "Content-Type": "application/json",
    "Access-Control-Allow-Origin": "*",
    "Access-Control-Allow-Methods": "POST, OPTIONS",
    "Access-Control-Allow-Headers": "Content-Type, X-Correlation-ID"
  },
  "body": {
    "delivered": false,
    "reason": "invalid-request"
  }
}
```

### `Scope_Support_Catch`

| Field | As provisioned |
|---|---|
| Type | `Scope` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Scope_Support_Main` (Failed/TimedOut) |

_This action declares no `inputs`._

### `Scope_Support_Catch/Response_Support_Processing_Failed`

| Field | As provisioned |
|---|---|
| Type | `Response` |
| Kind | `Http` |
| Description | _not declared_ |
| Runs after | _first in its scope_ |

**`inputs`, verbatim:**

```json
{
  "statusCode": 500,
  "headers": {
    "Content-Type": "application/json",
    "Access-Control-Allow-Origin": "*",
    "Access-Control-Allow-Methods": "POST, OPTIONS",
    "Access-Control-Allow-Headers": "Content-Type, X-Correlation-ID"
  },
  "body": {
    "delivered": false,
    "reason": "support-processing-failed"
  }
}
```

