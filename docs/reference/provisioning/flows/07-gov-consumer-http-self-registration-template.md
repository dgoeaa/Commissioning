# 07 - GOV - Consumer HTTP Self-Registration Template

> **deployed export — the live tenant.** Every value below is read from
> [`docs/reference/flow-contracts/deployed/07 - GOV - Consumer HTTP Self-Registration Template__9ec66366-d8db-4eaa-8507-a7aea1dde327__full_definition.json`](../../flow-contracts/deployed/07 - GOV - Consumer HTTP Self-Registration Template__9ec66366-d8db-4eaa-8507-a7aea1dde327__full_definition.json) and printed as that package carries it.
> Generated by `scripts/build-provisioning-reference.mjs` — do not edit by hand.

## 1. Identity

| Field | As provisioned |
|---|---|
| Display name | `07 - GOV - Consumer HTTP Self-Registration Template` |
| Internal name | `9ec66366-d8db-4eaa-8507-a7aea1dde327` |
| Workflow id | _not carried by this package_ |
| Environment | `Default-ca6a4b3f-9123-49bc-bcb9-27085ebbf1a1` |
| Full resource id | `null` — the export records the field and leaves it null |
| Estate | Internal platform — no portal register names this flow |
| Package family | deployed export — the live tenant |
| Exported at (UTC) | `2026-09-08T23:51:24Z` |
| Exported by | `Power Automate package export (Paxkages_updated_Governance.zip)` |
| Source package | `docs/reference/flow-contracts/deployed/07 - GOV - Consumer HTTP Self-Registration Template__9ec66366-d8db-4eaa-8507-a7aea1dde327__full_definition.json` |

## 2. Definition envelope

| Field | As provisioned |
|---|---|
| `$schema` | `https://schema.management.azure.com/providers/Microsoft.Logic/schemas/2016-06-01/workflowdefinition.json#` |
| `contentVersion` | `1.0.0.0` |
| `description` | _not declared_ |
| `outputs` | `{}` — declared and empty |
| Actions | 19 |

## 3. Definition parameters

| Parameter | Type | Default value |
|---|---|---|
| `$authentication` | `SecureObject` | `{}` |
| `$connections` | `Object` | `{}` |

## 4. Connections and references

**Connections the actions are bound to**, read from each action's `inputs.host`:

| Connection name | API id | Reference name | Operations used |
|---|---|---|---|
| `shared_office365-1` | `/providers/Microsoft.PowerApps/apis/shared_office365` | — | `SendEmailV2`×1 |
| `shared_sharepointonline-1` | `/providers/Microsoft.PowerApps/apis/shared_sharepointonline` | — | `HttpRequest`×4 |

## 5. Trigger — the endpoint this flow exposes

| Field | As provisioned |
|---|---|
| Trigger name | `manual` |
| `type` | `Request` |
| `kind` | `Http` |
| `method` | `POST` |
| `relativePath` | _not declared — the trigger accepts the bare invoke path_ |
| `triggerAuthenticationType` | `All` |
| `operationOptions` | _not declared_ |
| `conditions` | _not declared_ |
| `splitOn` | _not declared_ |
| `correlation` | _not declared_ |
| `recurrence` | _not declared_ |
| Declared request headers | _not declared_ |
| Declared query parameters | _not declared_ |
| `description` | _not declared_ |

### 5.1 Request payload — declared properties

| Property | Type | Required | Format | Enum | Default | Constraints | Description |
|---|---|---|---|---|---|---|---|
| `action` | `string \| null` | no | — | — | — | — | — |
| `operation` | `string \| null` | no | — | — | — | — | — |
| `source` | `string \| null` | no | — | — | — | — | — |

Root `required`: _empty — the schema marks nothing mandatory_. Root `additionalProperties`: `true`.

### 5.2 Request payload — schema as provisioned

```json
{
  "type": "object",
  "properties": {
    "action": {
      "type": [
        "string",
        "null"
      ]
    },
    "operation": {
      "type": [
        "string",
        "null"
      ]
    },
    "source": {
      "type": [
        "string",
        "null"
      ]
    }
  },
  "additionalProperties": true
}
```

## 6. Responses returned to the caller

_No `Response` action. This flow returns nothing to its caller beyond the platform's
own acknowledgement._

## 7. Termination

| Action | `runStatus` | Error code | Error message | Runs after |
|---|---|---|---|---|
| `Scope_07_GOV_Consumer_HTTP_Self_Registration_Template_COMPLETE_UPDATED/Scope_Main/Validate_Input/else/Terminate_Invalid_Input` | `Failed` | `INVALID_CONSUMER_REGISTRATION_INPUT` | `registryKey, consumerName, ownerEmail, and a consumer identifier are required; values must be 255 characters or fewer; the consumer identifier must not contain a pipe; ownerEmail must contain @.` | _first action_ |
| `Scope_07_GOV_Consumer_HTTP_Self_Registration_Template_COMPLETE_UPDATED/Scope_Main/If_Registry_Active/else/Terminate_Registry_Unavailable` | `Failed` | `REGISTRY_NOT_ACTIVE` | `@concat('The referenced HTTP flow registry record is missing or not active: ',trim(string(triggerBody()?['registryKey'])),'.')` | _first action_ |
| `Scope_07_GOV_Consumer_HTTP_Self_Registration_Template_COMPLETE_UPDATED/If_Consumer_Registration_Failed/Terminate_Consumer_Registration_Failed` | `Failed` | `DGOConsumerRegistrationFailed` | `@concat('Consumer HTTP self-registration ended with status ',outputs('Compose_Final_Report_JSON')?['status'],'.')` | _first action_ |
| `Scope_07_GOV_Consumer_HTTP_Self_Registration_Template_COMPLETE_UPDATED/If_Consumer_Registration_Failed/else/Terminate_Consumer_Registration_Succeeded` | `Succeeded` | — | — | _first action_ |

## 8. Error handling and run-time configuration

**Actions wired to run on something other than success.** These are the flow's catch
paths — an action that runs after `Failed`, `Skipped` or `TimedOut` is error handling,
whatever it is named.

| Action | Type | Runs after | On statuses |
|---|---|---|---|
| `Scope_07_GOV_Consumer_HTTP_Self_Registration_Template_COMPLETE_UPDATED/Compose_Final_Report_JSON` | `Compose` | `Scope_Main` | Succeeded, Failed, TimedOut, Skipped |
| `Scope_07_GOV_Consumer_HTTP_Self_Registration_Template_COMPLETE_UPDATED/If_Consumer_Registration_Failed` | `If` | `Send_Final_Report_Email` | Succeeded, Failed, TimedOut |

_No action carries a retry policy, secure-data setting, operation option, tracked
property or loop limit. Every call runs on the platform defaults._

## 9. Variables

_This package initialises no variables._

## 10. SharePoint operations

Each row is one connector call. `table` is printed as configured; the resolved column
names the list that GUID belongs to per `docs/reference/sharepoint-list-index.json`,
and says so plainly when the index does not carry it.

| Action | Operation | Connection | `dataset` (site) | `table` (list) | Resolves to |
|---|---|---|---|---|---|
| `Scope_07_GOV_Consumer_HTTP_Self_Registration_Template_COMPLETE_UPDATED/Scope_Main/Find_Registry` | `HttpRequest` | `shared_sharepointonline-1` | `https://nitdanigeria.sharepoint.com/sites/DGO_ECM_GOVERNANCE` | `null` | _not in the list index_ |
| `Scope_07_GOV_Consumer_HTTP_Self_Registration_Template_COMPLETE_UPDATED/Scope_Main/If_Registry_Active/Find_Consumer` | `HttpRequest` | `shared_sharepointonline-1` | `https://nitdanigeria.sharepoint.com/sites/DGO_ECM_GOVERNANCE` | `null` | _not in the list index_ |
| `Scope_07_GOV_Consumer_HTTP_Self_Registration_Template_COMPLETE_UPDATED/Scope_Main/If_Registry_Active/Upsert_Consumer/Update_Consumer` | `HttpRequest` | `shared_sharepointonline-1` | `https://nitdanigeria.sharepoint.com/sites/DGO_ECM_GOVERNANCE` | `null` | _not in the list index_ |
| `Scope_07_GOV_Consumer_HTTP_Self_Registration_Template_COMPLETE_UPDATED/Scope_Main/If_Registry_Active/Upsert_Consumer/else/Create_Consumer` | `HttpRequest` | `shared_sharepointonline-1` | `https://nitdanigeria.sharepoint.com/sites/DGO_ECM_GOVERNANCE` | `null` | _not in the list index_ |

## 11. Outbound HTTP calls

_This package makes no raw HTTP call._

## 12. Action dependency register

Every action in the package, in graph order, with the edge that starts it. `↳` marks
nesting depth; the path is the address used everywhere else on this page.

| Action path | Type | Kind | Runs after | On statuses |
|---|---|---|---|---|
| `Scope_07_GOV_Consumer_HTTP_Self_Registration_Template_COMPLETE_UPDATED` | `Scope` | — | _first in its scope_ | — |
| ↳`Scope_07_GOV_Consumer_HTTP_Self_Registration_Template_COMPLETE_UPDATED/Scope_Main` | `Scope` | — | _first in its scope_ | — |
| ↳↳`Scope_07_GOV_Consumer_HTTP_Self_Registration_Template_COMPLETE_UPDATED/Scope_Main/Compose_Consumer_Key` | `Compose` | — | `Validate_Input` | Succeeded |
| ↳↳`Scope_07_GOV_Consumer_HTTP_Self_Registration_Template_COMPLETE_UPDATED/Scope_Main/Validate_Input` | `If` | — | _first in its scope_ | — |
| ↳↳↳`Scope_07_GOV_Consumer_HTTP_Self_Registration_Template_COMPLETE_UPDATED/Scope_Main/Validate_Input/Input_Validated` | `Compose` | — | _first in its scope_ | — |
| ↳↳↳↳`Scope_07_GOV_Consumer_HTTP_Self_Registration_Template_COMPLETE_UPDATED/Scope_Main/Validate_Input/else/Terminate_Invalid_Input` | `Terminate` | — | _first in its scope_ | — |
| ↳↳`Scope_07_GOV_Consumer_HTTP_Self_Registration_Template_COMPLETE_UPDATED/Scope_Main/Find_Registry` | `OpenApiConnection` | — | `Compose_Consumer_Key` | Succeeded |
| ↳↳`Scope_07_GOV_Consumer_HTTP_Self_Registration_Template_COMPLETE_UPDATED/Scope_Main/If_Registry_Active` | `If` | — | `Find_Registry` | Succeeded |
| ↳↳↳`Scope_07_GOV_Consumer_HTTP_Self_Registration_Template_COMPLETE_UPDATED/Scope_Main/If_Registry_Active/Find_Consumer` | `OpenApiConnection` | — | _first in its scope_ | — |
| ↳↳↳`Scope_07_GOV_Consumer_HTTP_Self_Registration_Template_COMPLETE_UPDATED/Scope_Main/If_Registry_Active/Upsert_Consumer` | `If` | — | `Find_Consumer` | Succeeded |
| ↳↳↳↳`Scope_07_GOV_Consumer_HTTP_Self_Registration_Template_COMPLETE_UPDATED/Scope_Main/If_Registry_Active/Upsert_Consumer/Update_Consumer` | `OpenApiConnection` | — | _first in its scope_ | — |
| ↳↳↳↳↳`Scope_07_GOV_Consumer_HTTP_Self_Registration_Template_COMPLETE_UPDATED/Scope_Main/If_Registry_Active/Upsert_Consumer/else/Create_Consumer` | `OpenApiConnection` | — | _first in its scope_ | — |
| ↳↳↳↳`Scope_07_GOV_Consumer_HTTP_Self_Registration_Template_COMPLETE_UPDATED/Scope_Main/If_Registry_Active/else/Terminate_Registry_Unavailable` | `Terminate` | — | _first in its scope_ | — |
| ↳`Scope_07_GOV_Consumer_HTTP_Self_Registration_Template_COMPLETE_UPDATED/Compose_Final_Report_JSON` | `Compose` | — | `Scope_Main` | Succeeded/Failed/TimedOut/Skipped |
| ↳`Scope_07_GOV_Consumer_HTTP_Self_Registration_Template_COMPLETE_UPDATED/Compose_Final_Report_HTML` | `Compose` | — | `Compose_Final_Report_JSON` | Succeeded |
| ↳`Scope_07_GOV_Consumer_HTTP_Self_Registration_Template_COMPLETE_UPDATED/Send_Final_Report_Email` | `OpenApiConnection` | — | `Compose_Final_Report_HTML` | Succeeded |
| ↳`Scope_07_GOV_Consumer_HTTP_Self_Registration_Template_COMPLETE_UPDATED/If_Consumer_Registration_Failed` | `If` | — | `Send_Final_Report_Email` | Succeeded/Failed/TimedOut |
| ↳↳`Scope_07_GOV_Consumer_HTTP_Self_Registration_Template_COMPLETE_UPDATED/If_Consumer_Registration_Failed/Terminate_Consumer_Registration_Failed` | `Terminate` | — | _first in its scope_ | — |
| ↳↳↳`Scope_07_GOV_Consumer_HTTP_Self_Registration_Template_COMPLETE_UPDATED/If_Consumer_Registration_Failed/else/Terminate_Consumer_Registration_Succeeded` | `Terminate` | — | _first in its scope_ | — |

## 13. Configured values, action by action

The configured input of every action, verbatim. An action with no `inputs` key — a
`Scope`, an `If`, a `Foreach` — is documented by its control expression instead.

### `Scope_07_GOV_Consumer_HTTP_Self_Registration_Template_COMPLETE_UPDATED`

| Field | As provisioned |
|---|---|
| Type | `Scope` |
| Kind | — |
| Description | _not declared_ |
| Runs after | _first in its scope_ |

_This action declares no `inputs`._

### `Scope_07_GOV_Consumer_HTTP_Self_Registration_Template_COMPLETE_UPDATED/Scope_Main`

| Field | As provisioned |
|---|---|
| Type | `Scope` |
| Kind | — |
| Description | _not declared_ |
| Runs after | _first in its scope_ |

_This action declares no `inputs`._

### `Scope_07_GOV_Consumer_HTTP_Self_Registration_Template_COMPLETE_UPDATED/Scope_Main/Compose_Consumer_Key`

| Field | As provisioned |
|---|---|
| Type | `Compose` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Validate_Input` (Succeeded) |

**`inputs`, verbatim:**

```json
"@concat(trim(string(triggerBody()?['registryKey'])),'|',trim(string(coalesce(triggerBody()?['consumerIdentifier'],triggerBody()?['consumerName']))))"
```

### `Scope_07_GOV_Consumer_HTTP_Self_Registration_Template_COMPLETE_UPDATED/Scope_Main/Validate_Input`

| Field | As provisioned |
|---|---|
| Type | `If` |
| Kind | — |
| Description | _not declared_ |
| Runs after | _first in its scope_ |

_This action declares no `inputs`._

**`expression`, verbatim:**

```json
{
  "and": [
    {
      "greater": [
        "@length(trim(string(triggerBody()?['registryKey'])))",
        0
      ]
    },
    {
      "greater": [
        "@length(trim(string(triggerBody()?['consumerName'])))",
        0
      ]
    },
    {
      "greater": [
        "@length(trim(string(triggerBody()?['ownerEmail'])))",
        0
      ]
    },
    {
      "greater": [
        "@length(trim(string(coalesce(triggerBody()?['consumerIdentifier'],triggerBody()?['consumerName']))))",
        0
      ]
    },
    {
      "lessOrEquals": [
        "@length(trim(string(triggerBody()?['registryKey'])))",
        255
      ]
    },
    {
      "lessOrEquals": [
        "@length(trim(string(triggerBody()?['consumerName'])))",
        255
      ]
    },
    {
      "lessOrEquals": [
        "@length(trim(string(triggerBody()?['ownerEmail'])))",
        255
      ]
    },
    {
      "lessOrEquals": [
        "@length(trim(string(coalesce(triggerBody()?['consumerIdentifier'],triggerBody()?['consumerName']))))",
        255
      ]
    },
    {
      "equals": [
        "@contains(trim(string(coalesce(triggerBody()?['consumerIdentifier'],triggerBody()?['consumerName']))),'|')",
        false
      ]
    },
    {
      "contains": [
        "@trim(string(triggerBody()?['ownerEmail']))",
        "@"
      ]
    }
  ]
}
```

### `Scope_07_GOV_Consumer_HTTP_Self_Registration_Template_COMPLETE_UPDATED/Scope_Main/Validate_Input/Input_Validated`

| Field | As provisioned |
|---|---|
| Type | `Compose` |
| Kind | — |
| Description | _not declared_ |
| Runs after | _first in its scope_ |

**`inputs`, verbatim:**

```json
{
  "status": "Validated"
}
```

### `Scope_07_GOV_Consumer_HTTP_Self_Registration_Template_COMPLETE_UPDATED/Scope_Main/Validate_Input/else/Terminate_Invalid_Input`

| Field | As provisioned |
|---|---|
| Type | `Terminate` |
| Kind | — |
| Description | _not declared_ |
| Runs after | _first in its scope_ |

**`inputs`, verbatim:**

```json
{
  "runStatus": "Failed",
  "runError": {
    "code": "INVALID_CONSUMER_REGISTRATION_INPUT",
    "message": "registryKey, consumerName, ownerEmail, and a consumer identifier are required; values must be 255 characters or fewer; the consumer identifier must not contain a pipe; ownerEmail must contain @."
  }
}
```

### `Scope_07_GOV_Consumer_HTTP_Self_Registration_Template_COMPLETE_UPDATED/Scope_Main/Find_Registry`

| Field | As provisioned |
|---|---|
| Type | `OpenApiConnection` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Compose_Consumer_Key` (Succeeded) |
| Connector | `shared_sharepointonline-1` |
| Operation | `HttpRequest` |

**`inputs`, verbatim:**

```json
{
  "parameters": {
    "dataset": "https://nitdanigeria.sharepoint.com/sites/DGO_ECM_GOVERNANCE",
    "parameters/method": "GET",
    "parameters/uri": "@concat('_api/web/lists/getByTitle(''DGO_HTTPFlowRegistry'')/items?$select=Id,RegistryKey,LifecycleStatus,AuthenticationMode&$filter=RegistryKey eq ''',replace(trim(string(triggerBody()?['registryKey'])),'''',''''''),'''&$top=1')",
    "parameters/headers": {
      "Accept": "application/json;odata=nometadata"
    }
  },
  "host": {
    "apiId": "/providers/Microsoft.PowerApps/apis/shared_sharepointonline",
    "connectionName": "shared_sharepointonline-1",
    "operationId": "HttpRequest"
  },
  "authentication": "@parameters('$authentication')"
}
```

### `Scope_07_GOV_Consumer_HTTP_Self_Registration_Template_COMPLETE_UPDATED/Scope_Main/If_Registry_Active`

| Field | As provisioned |
|---|---|
| Type | `If` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Find_Registry` (Succeeded) |

_This action declares no `inputs`._

**`expression`, verbatim:**

```json
{
  "and": [
    {
      "greater": [
        "@length(coalesce(body('Find_Registry')?['value'],json('[]')))",
        0
      ]
    },
    {
      "equals": [
        "@first(body('Find_Registry')?['value'])?['LifecycleStatus']",
        "Active"
      ]
    }
  ]
}
```

### `Scope_07_GOV_Consumer_HTTP_Self_Registration_Template_COMPLETE_UPDATED/Scope_Main/If_Registry_Active/Find_Consumer`

| Field | As provisioned |
|---|---|
| Type | `OpenApiConnection` |
| Kind | — |
| Description | _not declared_ |
| Runs after | _first in its scope_ |
| Connector | `shared_sharepointonline-1` |
| Operation | `HttpRequest` |

**`inputs`, verbatim:**

```json
{
  "parameters": {
    "dataset": "https://nitdanigeria.sharepoint.com/sites/DGO_ECM_GOVERNANCE",
    "parameters/method": "GET",
    "parameters/uri": "@concat('_api/web/lists/getByTitle(''DGO_HTTPFlowConsumerRegistry'')/items?$select=Id,IsApproved,ApprovedUtc,LifecycleStatus&$filter=ConsumerKey eq ''',replace(outputs('Compose_Consumer_Key'),'''',''''''),'''&$top=1')",
    "parameters/headers": {
      "Accept": "application/json;odata=nometadata"
    }
  },
  "host": {
    "apiId": "/providers/Microsoft.PowerApps/apis/shared_sharepointonline",
    "connectionName": "shared_sharepointonline-1",
    "operationId": "HttpRequest"
  },
  "authentication": "@parameters('$authentication')"
}
```

### `Scope_07_GOV_Consumer_HTTP_Self_Registration_Template_COMPLETE_UPDATED/Scope_Main/If_Registry_Active/Upsert_Consumer`

| Field | As provisioned |
|---|---|
| Type | `If` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Find_Consumer` (Succeeded) |

_This action declares no `inputs`._

**`expression`, verbatim:**

```json
{
  "and": [
    {
      "greater": [
        "@length(coalesce(body('Find_Consumer')?['value'],json('[]')))",
        0
      ]
    }
  ]
}
```

### `Scope_07_GOV_Consumer_HTTP_Self_Registration_Template_COMPLETE_UPDATED/Scope_Main/If_Registry_Active/Upsert_Consumer/Update_Consumer`

| Field | As provisioned |
|---|---|
| Type | `OpenApiConnection` |
| Kind | — |
| Description | _not declared_ |
| Runs after | _first in its scope_ |
| Connector | `shared_sharepointonline-1` |
| Operation | `HttpRequest` |

**`inputs`, verbatim:**

```json
{
  "parameters": {
    "dataset": "https://nitdanigeria.sharepoint.com/sites/DGO_ECM_GOVERNANCE",
    "parameters/method": "POST",
    "parameters/uri": "@concat('_api/web/lists/getByTitle(''DGO_HTTPFlowConsumerRegistry'')/items(',string(first(body('Find_Consumer')?['value'])?['Id']),')')",
    "parameters/headers": {
      "Accept": "application/json;odata=nometadata",
      "Content-Type": "application/json;odata=nometadata",
      "IF-MATCH": "*",
      "X-HTTP-Method": "MERGE"
    },
    "parameters/body": "@setProperty(setProperty(setProperty(setProperty(setProperty(setProperty(setProperty(setProperty(setProperty(setProperty(setProperty(setProperty(json('{}'),'Title',trim(string(triggerBody()?['consumerName']))),'ConsumerKey',outputs('Compose_Consumer_Key')),'RegistryKey',trim(string(triggerBody()?['registryKey']))),'ConsumerName',trim(string(triggerBody()?['consumerName']))),'ConsumerType',coalesce(triggerBody()?['consumerType'],'PowerAutomate')),'ConsumerIdentifier',trim(string(coalesce(triggerBody()?['consumerIdentifier'],triggerBody()?['consumerName'])))),'OwnerEmail',toLower(trim(string(triggerBody()?['ownerEmail'])))),'ExpectedAuthentication',coalesce(triggerBody()?['expectedAuthentication'],first(body('Find_Registry')?['value'])?['AuthenticationMode'],'Unknown')),'LastObservedUtc',utcNow()),'LifecycleStatus','Active'),'IsApproved',coalesce(first(body('Find_Consumer')?['value'])?['IsApproved'],false)),'ApprovedUtc',if(equals(coalesce(first(body('Find_Consumer')?['value'])?['IsApproved'],false),true),first(body('Find_Consumer')?['value'])?['ApprovedUtc'],null))"
  },
  "host": {
    "apiId": "/providers/Microsoft.PowerApps/apis/shared_sharepointonline",
    "connectionName": "shared_sharepointonline-1",
    "operationId": "HttpRequest"
  },
  "authentication": "@parameters('$authentication')"
}
```

### `Scope_07_GOV_Consumer_HTTP_Self_Registration_Template_COMPLETE_UPDATED/Scope_Main/If_Registry_Active/Upsert_Consumer/else/Create_Consumer`

| Field | As provisioned |
|---|---|
| Type | `OpenApiConnection` |
| Kind | — |
| Description | _not declared_ |
| Runs after | _first in its scope_ |
| Connector | `shared_sharepointonline-1` |
| Operation | `HttpRequest` |

**`inputs`, verbatim:**

```json
{
  "parameters": {
    "dataset": "https://nitdanigeria.sharepoint.com/sites/DGO_ECM_GOVERNANCE",
    "parameters/method": "POST",
    "parameters/uri": "_api/web/lists/getByTitle('DGO_HTTPFlowConsumerRegistry')/items",
    "parameters/headers": {
      "Accept": "application/json;odata=nometadata",
      "Content-Type": "application/json;odata=nometadata"
    },
    "parameters/body": "@setProperty(setProperty(setProperty(setProperty(setProperty(setProperty(setProperty(setProperty(setProperty(setProperty(setProperty(setProperty(json('{}'),'Title',trim(string(triggerBody()?['consumerName']))),'ConsumerKey',outputs('Compose_Consumer_Key')),'RegistryKey',trim(string(triggerBody()?['registryKey']))),'ConsumerName',trim(string(triggerBody()?['consumerName']))),'ConsumerType',coalesce(triggerBody()?['consumerType'],'PowerAutomate')),'ConsumerIdentifier',trim(string(coalesce(triggerBody()?['consumerIdentifier'],triggerBody()?['consumerName'])))),'OwnerEmail',toLower(trim(string(triggerBody()?['ownerEmail'])))),'ExpectedAuthentication',coalesce(triggerBody()?['expectedAuthentication'],first(body('Find_Registry')?['value'])?['AuthenticationMode'],'Unknown')),'LastObservedUtc',utcNow()),'LifecycleStatus','Active'),'IsApproved',false),'ApprovedUtc',null)"
  },
  "host": {
    "apiId": "/providers/Microsoft.PowerApps/apis/shared_sharepointonline",
    "connectionName": "shared_sharepointonline-1",
    "operationId": "HttpRequest"
  },
  "authentication": "@parameters('$authentication')"
}
```

### `Scope_07_GOV_Consumer_HTTP_Self_Registration_Template_COMPLETE_UPDATED/Scope_Main/If_Registry_Active/else/Terminate_Registry_Unavailable`

| Field | As provisioned |
|---|---|
| Type | `Terminate` |
| Kind | — |
| Description | _not declared_ |
| Runs after | _first in its scope_ |

**`inputs`, verbatim:**

```json
{
  "runStatus": "Failed",
  "runError": {
    "code": "REGISTRY_NOT_ACTIVE",
    "message": "@concat('The referenced HTTP flow registry record is missing or not active: ',trim(string(triggerBody()?['registryKey'])),'.')"
  }
}
```

### `Scope_07_GOV_Consumer_HTTP_Self_Registration_Template_COMPLETE_UPDATED/Compose_Final_Report_JSON`

| Field | As provisioned |
|---|---|
| Type | `Compose` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Scope_Main` (Succeeded/Failed/TimedOut/Skipped) |

**`inputs`, verbatim:**

```json
{
  "reportType": "DGO Power Automate Execution Report",
  "schemaVersion": "5.0.0",
  "flow": "07 - GOV - Consumer HTTP Self-Registration Template",
  "flowId": "@workflow()?['name']",
  "runId": "@workflow()?['run']?['name']",
  "environmentId": "@workflow()?['tags']?['environmentName']",
  "generatedAtUtc": "@utcNow()",
  "status": "@if(contains(string(result('Scope_Main')),'Failed'),'Failed',if(contains(string(result('Scope_Main')),'TimedOut'),'TimedOut','Succeeded'))",
  "details": {
    "consumerKey": "@concat(trim(string(triggerBody()?['registryKey'])),'|',trim(string(coalesce(triggerBody()?['consumerIdentifier'],triggerBody()?['consumerName']))))",
    "registryKey": "@trim(string(triggerBody()?['registryKey']))",
    "consumerName": "@trim(string(triggerBody()?['consumerName']))",
    "consumerType": "@coalesce(triggerBody()?['consumerType'],'PowerAutomate')",
    "ownerEmail": "@toLower(trim(string(triggerBody()?['ownerEmail'])))",
    "approvalPolicy": "Self-registration never grants approval"
  },
  "runUrl": "@concat('https://make.powerautomate.com/environments/',workflow()?['tags']?['environmentName'],'/flows/',workflow()?['name'],'/runs/',workflow()?['run']?['name'])"
}
```

### `Scope_07_GOV_Consumer_HTTP_Self_Registration_Template_COMPLETE_UPDATED/Compose_Final_Report_HTML`

| Field | As provisioned |
|---|---|
| Type | `Compose` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Compose_Final_Report_JSON` (Succeeded) |

**`inputs`, verbatim:**

```json
"@concat('<html><body style=\"font-family:Segoe UI,Arial;background:#f3f6f9;padding:24px\"><div style=\"max-width:900px;margin:auto;background:white;border-radius:12px;overflow:hidden\"><div style=\"background:#004578;color:white;padding:24px\"><h1>',outputs('Compose_Final_Report_JSON')?['flow'],'</h1></div><div style=\"padding:24px\"><h2>Status: ',outputs('Compose_Final_Report_JSON')?['status'],'</h2><p><b>Run ID:</b> ',outputs('Compose_Final_Report_JSON')?['runId'],'</p><pre style=\"white-space:pre-wrap;background:#f8fafc;padding:12px\">',string(outputs('Compose_Final_Report_JSON')?['details']),'</pre><p><a href=\"',outputs('Compose_Final_Report_JSON')?['runUrl'],'\">Open run</a></p></div></div></body></html>')"
```

### `Scope_07_GOV_Consumer_HTTP_Self_Registration_Template_COMPLETE_UPDATED/Send_Final_Report_Email`

| Field | As provisioned |
|---|---|
| Type | `OpenApiConnection` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Compose_Final_Report_HTML` (Succeeded) |
| Connector | `shared_office365-1` |
| Operation | `SendEmailV2` |

**`inputs`, verbatim:**

```json
{
  "parameters": {
    "emailMessage/To": "dgsRegistry@nitda.gov.ng",
    "emailMessage/Subject": "@concat('[',outputs('Compose_Final_Report_JSON')?['status'],'] ',outputs('Compose_Final_Report_JSON')?['flow'],' | ',outputs('Compose_Final_Report_JSON')?['runId'])",
    "emailMessage/Body": "@outputs('Compose_Final_Report_HTML')",
    "emailMessage/Attachments": [
      {
        "Name": "@concat(replace(workflow()?['name'],' ','_'),'_',workflow()?['run']?['name'],'_Report.json')",
        "ContentBytes": "@string(outputs('Compose_Final_Report_JSON'))"
      }
    ],
    "emailMessage/Importance": "@if(equals(outputs('Compose_Final_Report_JSON')?['status'],'Succeeded'),'Normal','High')"
  },
  "host": {
    "apiId": "/providers/Microsoft.PowerApps/apis/shared_office365",
    "connectionName": "shared_office365-1",
    "operationId": "SendEmailV2"
  },
  "authentication": "@parameters('$authentication')"
}
```

### `Scope_07_GOV_Consumer_HTTP_Self_Registration_Template_COMPLETE_UPDATED/If_Consumer_Registration_Failed`

| Field | As provisioned |
|---|---|
| Type | `If` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Send_Final_Report_Email` (Succeeded/Failed/TimedOut) |

_This action declares no `inputs`._

**`expression`, verbatim:**

```json
{
  "and": [
    {
      "not": {
        "equals": [
          "@outputs('Compose_Final_Report_JSON')?['status']",
          "Succeeded"
        ]
      }
    }
  ]
}
```

### `Scope_07_GOV_Consumer_HTTP_Self_Registration_Template_COMPLETE_UPDATED/If_Consumer_Registration_Failed/Terminate_Consumer_Registration_Failed`

| Field | As provisioned |
|---|---|
| Type | `Terminate` |
| Kind | — |
| Description | _not declared_ |
| Runs after | _first in its scope_ |

**`inputs`, verbatim:**

```json
{
  "runStatus": "Failed",
  "runError": {
    "code": "DGOConsumerRegistrationFailed",
    "message": "@concat('Consumer HTTP self-registration ended with status ',outputs('Compose_Final_Report_JSON')?['status'],'.')"
  }
}
```

### `Scope_07_GOV_Consumer_HTTP_Self_Registration_Template_COMPLETE_UPDATED/If_Consumer_Registration_Failed/else/Terminate_Consumer_Registration_Succeeded`

| Field | As provisioned |
|---|---|
| Type | `Terminate` |
| Kind | — |
| Description | _not declared_ |
| Runs after | _first in its scope_ |

**`inputs`, verbatim:**

```json
{
  "runStatus": "Succeeded"
}
```

