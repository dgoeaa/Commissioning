# Digital Hub Email AI Assist

> **deployed export — the live tenant.** Every value below is read from
> [`docs/reference/flow-contracts/deployed/Digital Hub Email AI Assist__34761736-eb9a-30f7-c43b-c9b2ea6ad297__full_definition.json`](../../flow-contracts/deployed/Digital Hub Email AI Assist__34761736-eb9a-30f7-c43b-c9b2ea6ad297__full_definition.json) and printed as that package carries it.
> Generated by `scripts/build-provisioning-reference.mjs` — do not edit by hand.

## 1. Identity

| Field | As provisioned |
|---|---|
| Display name | `Digital Hub Email AI Assist` |
| Internal name | `34761736-eb9a-30f7-c43b-c9b2ea6ad297` |
| Workflow id | _not carried by this package_ |
| Environment | `Default-ca6a4b3f-9123-49bc-bcb9-27085ebbf1a1` |
| Full resource id | `null` — the export records the field and leaves it null |
| Estate | Internal platform — no portal register names this flow |
| Package family | deployed export — the live tenant |
| Exported at (UTC) | `2026-08-19T06:09:15Z` |
| Exported by | `scripts/export-power-automate-flows.ps1` |
| Source package | `docs/reference/flow-contracts/deployed/Digital Hub Email AI Assist__34761736-eb9a-30f7-c43b-c9b2ea6ad297__full_definition.json` |

**Endpoint contract keys attributed to this flow** by
`docs/reference/internal-flow-register.json`:

| Contract key | Physical flow | Fixed action | Workflow id |
|---|---|---|---|
| `AI_EMAIL_ANALYSIS` | `AI_EMAIL_ANALYSIS` | `aiAnalyseEmail` | `fe794e0139784ac694768e5a716e0be7` |

## 2. Definition envelope

| Field | As provisioned |
|---|---|
| `$schema` | `https://schema.management.azure.com/providers/Microsoft.Logic/schemas/2016-06-01/workflowdefinition.json#` |
| `contentVersion` | `1.0.0.0` |
| `description` | _not declared_ |
| `outputs` | _not declared_ |
| Actions | 24 |

## 3. Definition parameters

| Parameter | Type | Default value |
|---|---|---|
| `$authentication` | `SecureObject` | `{}` |
| `$connections` | `Object` | `{}` |

## 4. Connections and references

**Connections the actions are bound to**, read from each action's `inputs.host`:

| Connection name | API id | Reference name | Operations used |
|---|---|---|---|
| `shared_office365` | `/providers/Microsoft.PowerApps/apis/shared_office365` | — | `GetAttachment_V2`×1, `GetEmailV2`×1 |

## 5. Trigger — the endpoint this flow exposes

| Field | As provisioned |
|---|---|
| Trigger name | `manual` |
| `type` | `Request` |
| `kind` | `PowerAppV2` |
| `method` | _not declared_ |
| `relativePath` | _not declared — the trigger accepts the bare invoke path_ |
| `triggerAuthenticationType` | _absent from the export — no posture is recorded_ |
| `operationOptions` | _not declared_ |
| `conditions` | _not declared_ |
| `splitOn` | _not declared_ |
| `correlation` | _not declared_ |
| `recurrence` | _not declared_ |
| Declared request headers | _not declared_ |
| Declared query parameters | _not declared_ |
| `description` | _not declared_ |

### 5.1 Request payload — declared properties

| Property | Type | Required | Format | Enum | Default | Constraints | Description |
|---|---|---|---|---|---|---|---|
| `text` | `string` | **yes** | — | — | — | — | Please enter your input |
| `text_1` | `string` | **yes** | — | — | — | — | Please enter your input |

Root `required`: `text`, `text_1`. Root `additionalProperties`: _not declared_.

### 5.2 Request payload — schema as provisioned

```json
{
  "type": "object",
  "properties": {
    "text": {
      "title": "EmailID",
      "type": "string",
      "x-ms-dynamically-added": true,
      "description": "Please enter your input",
      "x-ms-content-hint": "TEXT"
    },
    "text_1": {
      "title": "AIActionRequired",
      "type": "string",
      "x-ms-dynamically-added": true,
      "description": "Please enter your input",
      "x-ms-content-hint": "TEXT"
    }
  },
  "required": [
    "text",
    "text_1"
  ]
}
```

## 6. Responses returned to the caller

_No `Response` action. This flow returns nothing to its caller beyond the platform's
own acknowledgement._

## 7. Termination

_No `Terminate` action. The run ends when the action graph does._

## 8. Error handling and run-time configuration

_No action is wired to run on `Failed`, `Skipped` or `TimedOut`. Every dependency in
this flow is a success edge._

**Run-time configuration set on individual actions** — retry policy, secure data,
operation options, tracked properties and loop limits, as configured:

| Action | Type | `runtimeConfiguration` | `operationOptions` | `trackedProperties` | `limit` |
|---|---|---|---|---|---|
| `HTTP_Request_to_AI_API` | `Http` | `{"contentTransfer":{"transferMode":"Chunked"}}` | — | — | — |

## 9. Variables

**Declared, with the initial value each is provisioned with:**

| Variable | Type | Initial value | Declared by |
|---|---|---|---|
| `varResponse` | `string` | _no initial value_ | `Initialize_variable` |
| `varAttachmentsHTML` | `boolean` | _no initial value_ | `Initialize_variable_2` |

**Every write, with the expression or literal assigned:**

| Action | Operation | Variable | Assigned value |
|---|---|---|---|
| `Scope_Successful/Set_variable` | `SetVariable` | `varResponse` | `@{outputs('HTML_Values')}` |
| `Scope_Errror/Set_variable_varResponse_Error` | `SetVariable` | `varResponse` | `@{outputs('Parse_JSON_Body_Parse_JSON_Error')?['body']?['candidates'][0]?['content']?['parts'][0]?['text']}` |

## 10. SharePoint operations

_This package performs no SharePoint operation._

## 11. Outbound HTTP calls

### `HTTP_Request_to_AI_API`

| Field | As provisioned |
|---|---|
| `method` | `POST` |
| `uri` | `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-pro:generateContent` |
| `authentication` | _not declared_ |
| `retryPolicy` | _not declared — platform default_ |

| Request header | Configured value |
|---|---|
| `Content-Type` | `application/json` |
| `X-goog-api-key` | `REDACTED-GOOGLE-API-KEY` |

**Request body, verbatim:**

```json
"@outputs('HTTP_Body_3')"
```

## 12. Action dependency register

Every action in the package, in graph order, with the edge that starts it. `↳` marks
nesting depth; the path is the address used everywhere else on this page.

| Action path | Type | Kind | Runs after | On statuses |
|---|---|---|---|---|
| `Get_email_(V2)` | `OpenApiConnection` | — | `Initialize_variable` | Succeeded |
| `Condition` | `If` | — | `Get_email_(V2)` | Succeeded |
| ↳`Condition/Apply_to_each` | `Foreach` | — | _first in its scope_ | — |
| ↳↳`Condition/Apply_to_each/Get_Attachment_(V2)` | `OpenApiConnection` | — | _first in its scope_ | — |
| ↳↳`Condition/Apply_to_each/Condition_2` | `If` | — | `Get_Attachment_(V2)` | Succeeded |
| ↳↳↳↳`Condition/Apply_to_each/Condition_2/else/base64AttachmentContent` | `Compose` | — | _first in its scope_ | — |
| `HTTP_Request_to_AI_API` | `Http` | — | `HTTP_Body_3` | Succeeded |
| `Scope_Successful` | `Scope` | — | `HTTP_Request_to_AI_API` | Succeeded |
| ↳`Scope_Successful/Parse_JSON_Succesful` | `ParseJson` | — | _first in its scope_ | — |
| ↳`Scope_Successful/Parse_JSON` | `ParseJson` | — | `Clean_Parse_JSON_Successful_Text` | Succeeded |
| ↳`Scope_Successful/HTML_Values` | `Compose` | — | `Extract_Values_JSON` | Succeeded |
| ↳`Scope_Successful/Extract_Values_JSON` | `Compose` | — | `Parse_JSON` | Succeeded |
| ↳`Scope_Successful/Parse_JSON_Successful_Text` | `Compose` | — | `Parse_JSON_Succesful` | Succeeded |
| ↳`Scope_Successful/Clean_Parse_JSON_Successful_Text` | `Compose` | — | `Parse_JSON_Successful_Text` | Succeeded |
| ↳`Scope_Successful/Set_variable` | `SetVariable` | — | `HTML_Values` | Succeeded |
| `Scope_Errror` | `Scope` | — | `Scope_Successful` | Succeeded |
| ↳`Scope_Errror/Parse_JSON_Error` | `ParseJson` | — | _first in its scope_ | — |
| ↳`Scope_Errror/Parse_JSON_Body_Parse_JSON_Error` | `Compose` | — | `Parse_JSON_Error` | Succeeded |
| ↳`Scope_Errror/Set_variable_varResponse_Error` | `SetVariable` | — | `Parse_JSON_Body_Parse_JSON_Error` | Succeeded |
| `Scope_Response` | `Scope` | — | `Scope_Errror` | Succeeded |
| ↳`Scope_Response/HTML_with_Attachments` | `Compose` | — | _first in its scope_ | — |
| `HTTP_Body_3` | `Compose` | — | `Condition` | Succeeded |
| `Initialize_variable` | `InitializeVariable` | — | `Initialize_variable_2` | Succeeded |
| `Initialize_variable_2` | `InitializeVariable` | — | _first in its scope_ | — |

## 13. Configured values, action by action

The configured input of every action, verbatim. An action with no `inputs` key — a
`Scope`, an `If`, a `Foreach` — is documented by its control expression instead.

### `Get_email_(V2)`

| Field | As provisioned |
|---|---|
| Type | `OpenApiConnection` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Initialize_variable` (Succeeded) |
| Connector | `shared_office365` |
| Operation | `GetEmailV2` |

**`inputs`, verbatim:**

```json
{
  "parameters": {
    "messageId": "@triggerBody()['text']",
    "includeAttachments": true
  },
  "host": {
    "apiId": "/providers/Microsoft.PowerApps/apis/shared_office365",
    "connectionName": "shared_office365",
    "operationId": "GetEmailV2"
  },
  "authentication": {
    "value": "@json(decodeBase64(triggerOutputs().headers['X-MS-APIM-Tokens']))['$ConnectionKey']",
    "type": "Raw"
  }
}
```

### `Condition`

| Field | As provisioned |
|---|---|
| Type | `If` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Get_email_(V2)` (Succeeded) |

_This action declares no `inputs`._

**`expression`, verbatim:**

```json
{
  "equals": [
    "@outputs('Get_email_(V2)')?['body/hasAttachments']",
    true
  ]
}
```

### `Condition/Apply_to_each`

| Field | As provisioned |
|---|---|
| Type | `Foreach` |
| Kind | — |
| Description | _not declared_ |
| Runs after | _first in its scope_ |

_This action declares no `inputs`._

**`foreach`, verbatim:**

```json
"@outputs('Get_email_(V2)')?['body/attachments']"
```

### `Condition/Apply_to_each/Get_Attachment_(V2)`

| Field | As provisioned |
|---|---|
| Type | `OpenApiConnection` |
| Kind | — |
| Description | _not declared_ |
| Runs after | _first in its scope_ |
| Connector | `shared_office365` |
| Operation | `GetAttachment_V2` |

**`inputs`, verbatim:**

```json
{
  "parameters": {
    "messageId": "@outputs('Get_email_(V2)')?['body/id']",
    "attachmentId": "@items('Apply_to_each')?['id']"
  },
  "host": {
    "apiId": "/providers/Microsoft.PowerApps/apis/shared_office365",
    "connectionName": "shared_office365",
    "operationId": "GetAttachment_V2"
  },
  "authentication": {
    "value": "@json(decodeBase64(triggerOutputs().headers['X-MS-APIM-Tokens']))['$ConnectionKey']",
    "type": "Raw"
  }
}
```

### `Condition/Apply_to_each/Condition_2`

| Field | As provisioned |
|---|---|
| Type | `If` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Get_Attachment_(V2)` (Succeeded) |

_This action declares no `inputs`._

**`expression`, verbatim:**

```json
{
  "equals": [
    "@outputs('Get_Attachment_(V2)')?['body/isInline']",
    true
  ]
}
```

### `Condition/Apply_to_each/Condition_2/else/base64AttachmentContent`

| Field | As provisioned |
|---|---|
| Type | `Compose` |
| Kind | — |
| Description | _not declared_ |
| Runs after | _first in its scope_ |

**`inputs`, verbatim:**

```json
"@base64(outputs('Get_Attachment_(V2)')?['body/contentBytes'])"
```

### `HTTP_Request_to_AI_API`

| Field | As provisioned |
|---|---|
| Type | `Http` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `HTTP_Body_3` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "uri": "https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-pro:generateContent",
  "method": "POST",
  "headers": {
    "Content-Type": "application/json",
    "X-goog-api-key": "REDACTED-GOOGLE-API-KEY"
  },
  "body": "@outputs('HTTP_Body_3')"
}
```

**`runtimeConfiguration`, verbatim:**

```json
{
  "contentTransfer": {
    "transferMode": "Chunked"
  }
}
```

### `Scope_Successful`

| Field | As provisioned |
|---|---|
| Type | `Scope` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `HTTP_Request_to_AI_API` (Succeeded) |

_This action declares no `inputs`._

### `Scope_Successful/Parse_JSON_Succesful`

| Field | As provisioned |
|---|---|
| Type | `ParseJson` |
| Kind | — |
| Description | _not declared_ |
| Runs after | _first in its scope_ |

**`inputs`, verbatim:**

```json
{
  "content": "@body('HTTP_Request_to_AI_API')",
  "schema": {
    "type": "object",
    "properties": {
      "statusCode": {
        "type": "integer"
      },
      "headers": {
        "type": "object",
        "properties": {
          "Vary": {
            "type": "string"
          },
          "Date": {
            "type": "string"
          },
          "Server": {
            "type": "string"
          },
          "X-XSS-Protection": {
            "type": "string"
          },
          "X-Frame-Options": {
            "type": "string"
          },
          "X-Content-Type-Options": {
            "type": "string"
          },
          "Server-Timing": {
            "type": "string"
          },
          "Alt-Svc": {
            "type": "string"
          },
          "Transfer-Encoding": {
            "type": "string"
          },
          "Content-Type": {
            "type": "string"
          },
          "Content-Length": {
            "type": "string"
          }
        }
      },
      "body": {
        "type": "object",
        "properties": {
          "candidates": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "content": {
                  "type": "object",
                  "properties": {
                    "parts": {
                      "type": "array",
                      "items": {
                        "type": "object",
                        "properties": {
                          "text": {
                            "type": "string"
                          }
                        },
                        "required": [
                          "text"
                        ]
                      }
                    },
                    "role": {
                      "type": "string"
                    }
                  }
                },
                "finishReason": {
                  "type": "string"
                },
                "index": {
                  "type": "integer"
                }
              },
              "required": [
                "content",
                "finishReason",
                "index"
              ]
            }
          },
          "usageMetadata": {
            "type": "object",
            "properties": {
              "promptTokenCount": {
                "type": "integer"
              },
              "candidatesTokenCount": {
                "type": "integer"
              },
              "totalTokenCount": {
                "type": "integer"
              },
              "promptTokensDetails": {
                "type": "array",
                "items": {
                  "type": "object",
                  "properties": {
                    "modality": {
                      "type": "string"
                    },
                    "tokenCount": {
                      "type": "integer"
                    }
                  },
                  "required": [
                    "modality",
                    "tokenCount"
                  ]
                }
              },
              "thoughtsTokenCount": {
                "type": "integer"
              }
            }
          },
          "modelVersion": {
            "type": "string"
          },
          "responseId": {
            "type": "string"
          }
        }
      }
    }
  }
}
```

### `Scope_Successful/Parse_JSON`

| Field | As provisioned |
|---|---|
| Type | `ParseJson` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Clean_Parse_JSON_Successful_Text` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "content": "@outputs('Clean_Parse_JSON_Successful_Text')",
  "schema": {
    "type": "object",
    "properties": {
      "Subject": {
        "type": "string"
      },
      "SenderName": {
        "type": "string"
      },
      "SenderOrg": {
        "type": "string"
      },
      "Department": {
        "type": "string"
      },
      "Summary": {
        "type": "string"
      },
      "ActionRequired": {
        "type": "string"
      },
      "RecommendedAction": {
        "type": "string"
      },
      "Priority": {
        "type": "string"
      },
      "Status": {
        "type": "string"
      },
      "DueDate": {
        "type": "string"
      },
      "Attachments": {
        "type": "string"
      }
    }
  }
}
```

### `Scope_Successful/HTML_Values`

| Field | As provisioned |
|---|---|
| Type | `Compose` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Extract_Values_JSON` (Succeeded) |

**`inputs`, verbatim:**

```json
"<div style=\"font-size:14.5px; color:#2d3748; line-height:1.6; font-family:Segoe UI, Roboto, Arial, sans-serif;\">\n \n  <div style=\"margin-bottom:10px; padding:12px; background:#f0f7f4; border-radius:8px; border:1px solid #dce5dd; border-left:6px solid #3182ce;\">\n    <div style=\"font-weight:600; color:#1a202c; margin-bottom:4px;\">📧 From</div>\n    <div style=\"color:#2d3748;\">@{body('Parse_JSON')?['SenderName']}, @{body('Parse_JSON')?['SenderOrg']}</div>\n  </div>\n \n  <div style=\"margin-bottom:10px; padding:12px; background:#edf2f7; border-radius:8px; border:1px solid #dce5dd; border-left:6px solid #4a5568;\">\n    <div style=\"font-weight:600; color:#1a202c; margin-bottom:4px;\">📝 Summary</div>\n    <div style=\"color:#2d3748;\">@{body('Parse_JSON')?['Summary']}</div>\n  </div>\n \n  <div style=\"margin-bottom:10px; padding:12px; background:#fef2f2; border-radius:8px; border:1px solid #dce5dd; border-left:6px solid #e53e3e;\">\n    <div style=\"font-weight:600; color:#1a202c; margin-bottom:4px;\">⚠️ Action Required</div>\n    <div style=\"color:#c53030; font-weight:500;\">@{body('Parse_JSON')?['ActionRequired']}</div>\n  </div>\n \n  <div style=\"margin-bottom:10px; padding:12px; background:#f0fdf4; border-radius:8px; border:1px solid #dce5dd; border-left:6px solid #38a169;\">\n    <div style=\"font-weight:600; color:#1a202c; margin-bottom:4px;\">✅ Recommended Action</div>\n    <div style=\"color:#2f855a; font-weight:500;\">@{body('Parse_JSON')?['RecommendedAction']}</div>\n  </div>\n \n  <div style=\"margin-bottom:10px; padding:12px; background:#f0f7f4; border-radius:8px; border:1px solid #dce5dd; border-left:6px solid #2f855a;\">\n    <div style=\"font-weight:600; color:#1a202c; margin-bottom:4px;\">🏢 Department</div>\n    <div style=\"color:#2d3748;\">@{body('Parse_JSON')?['Department']}</div>\n  </div>\n \n</div>"
```

### `Scope_Successful/Extract_Values_JSON`

| Field | As provisioned |
|---|---|
| Type | `Compose` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Parse_JSON` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "Subject": "Example Subject@{body('Parse_JSON')?['Subject']}",
  "SenderName": "Example Sender@{body('Parse_JSON')?['SenderName']}",
  "SenderOrg": "@{body('Parse_JSON')?['SenderOrg']}",
  "Department": "",
  "Summary": "@{body('Parse_JSON')?['Summary']}",
  "ActionRequired": "@{body('Parse_JSON')?['ActionRequired']}",
  "RecommendedAction": "@{body('Parse_JSON')?['RecommendedAction']}",
  "Priority": "@{body('Parse_JSON')?['Priority']}",
  "Status": "@{body('Parse_JSON')?['Status']}",
  "DueDate": "@{body('Parse_JSON')?['DueDate']}",
  "Attachments": "@{body('Parse_JSON')?['Attachments']}"
}
```

### `Scope_Successful/Parse_JSON_Successful_Text`

| Field | As provisioned |
|---|---|
| Type | `Compose` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Parse_JSON_Succesful` (Succeeded) |

**`inputs`, verbatim:**

```json
"@body('Parse_JSON_Succesful')['candidates'][0]['content']['parts'][0]['text']"
```

### `Scope_Successful/Clean_Parse_JSON_Successful_Text`

| Field | As provisioned |
|---|---|
| Type | `Compose` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Parse_JSON_Successful_Text` (Succeeded) |

**`inputs`, verbatim:**

```json
"@replace(replace(outputs('Parse_JSON_Successful_Text'), '```json', ''), '```', '')"
```

### `Scope_Successful/Set_variable`

| Field | As provisioned |
|---|---|
| Type | `SetVariable` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `HTML_Values` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "name": "varResponse",
  "value": "@{outputs('HTML_Values')}"
}
```

### `Scope_Errror`

| Field | As provisioned |
|---|---|
| Type | `Scope` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Scope_Successful` (Succeeded) |

_This action declares no `inputs`._

### `Scope_Errror/Parse_JSON_Error`

| Field | As provisioned |
|---|---|
| Type | `ParseJson` |
| Kind | — |
| Description | _not declared_ |
| Runs after | _first in its scope_ |

**`inputs`, verbatim:**

```json
{
  "content": "@body('HTTP_Request_to_AI_API')",
  "schema": {
    "type": "object",
    "properties": {
      "candidates": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "content": {
              "type": "object",
              "properties": {
                "role": {
                  "type": "string"
                },
                "parts": {
                  "type": "array",
                  "items": {
                    "type": "object",
                    "properties": {
                      "text": {
                        "type": "string"
                      }
                    },
                    "required": [
                      "text"
                    ]
                  }
                }
              }
            }
          }
        }
      }
    }
  }
}
```

### `Scope_Errror/Parse_JSON_Body_Parse_JSON_Error`

| Field | As provisioned |
|---|---|
| Type | `Compose` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Parse_JSON_Error` (Succeeded) |

**`inputs`, verbatim:**

```json
"@body('Parse_JSON_Error')"
```

### `Scope_Errror/Set_variable_varResponse_Error`

| Field | As provisioned |
|---|---|
| Type | `SetVariable` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Parse_JSON_Body_Parse_JSON_Error` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "name": "varResponse",
  "value": "@{outputs('Parse_JSON_Body_Parse_JSON_Error')?['body']?['candidates'][0]?['content']?['parts'][0]?['text']}"
}
```

### `Scope_Response`

| Field | As provisioned |
|---|---|
| Type | `Scope` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Scope_Errror` (Succeeded) |

_This action declares no `inputs`._

### `Scope_Response/HTML_with_Attachments`

| Field | As provisioned |
|---|---|
| Type | `Compose` |
| Kind | — |
| Description | _not declared_ |
| Runs after | _first in its scope_ |

**`inputs`, verbatim:**

```json
"<div style=\"margin-bottom:10px; padding:12px; background:#edf2f7; border-radius:8px; border:1px solid #dce5dd; border-left:6px solid #4a5568;\">\n    <div style=\"font-weight:600; color:#1a202c; margin-bottom:4px;\">📝 Summary</div>\n    <div style=\"color:#2d3748;\">@{body('Parse_JSON')?['Summary']}</div>\n  </div>\n \n  <div style=\"margin-bottom:10px; padding:12px; background:#fef2f2; border-radius:8px; border:1px solid #dce53e; border-left:6px solid #e53e3e;\">\n    <div style=\"font-weight:600; color:#1a202c; margin-bottom:4px;\">⚠️ Action Required</div>\n    <div style=\"color:#c53030; font-weight:500;\">@{body('Parse_JSON')?['ActionRequired']}</div>\n  </div>\n \n  <div style=\"margin-bottom:10px; padding:12px; background:#f0fdf4; border-radius:8px; border:1px solid #dce5dd; border-left:6px solid #38a169;\">\n    <div style=\"font-weight:600; color:#1a202c; margin-bottom:4px;\">✅ Recommended Action</div>\n    <div style=\"color:#2f855a; font-weight:500;\">@{body('Parse_JSON')?['RecommendedAction']}</div>\n  </div>\n \n  <div style=\"margin-bottom:10px; padding:12px; background:#f0f7f4; border-radius:8px; border:1px solid #dce5dd; border-left:6px solid #2f855a;\">\n    <div style=\"font-weight:600; color:#1a202c; margin-bottom:4px;\">🏢 Department</div>\n    <div style=\"color:#2d3748;\">@{body('Parse_JSON')?['Department']}</div>\n  </div>\n \n  <div style=\"margin-bottom:10px; padding:12px; background:#e8f4fd; border-radius:8px; border:1px solid #dce5dd; border-left:6px solid #007bff;\">\n    <div style=\"font-weight:600; color:#1a202c; margin-bottom:4px;\">📎 Attachments</div>\n    <div style=\"color:#2d3748;\">\n      @{variables('varAttachmentsHTML')}\n    </div>\n  </div>\n</div>\n\n"
```

### `HTTP_Body_3`

| Field | As provisioned |
|---|---|
| Type | `Compose` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Condition` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "contents": [
    {
      "role": "user",
      "parts": [
        {
          "text": "You are an expert AI assistant tasked with summarizing emails for the DGCEO office. Your goal is to extract key information from a provided email body and present it in a clean, structured JSON format.\n\nThe summary must include the following fields:\n\n- **Subject**: The full subject line of the email.\n- **SenderName**: The name of the person or organization that sent the email.\n- **SenderOrg**: The organization associated with the sender. If the organization is not explicitly mentioned, use the domain from the sender's email address (e.g., nitda.gov.ng, or a more relevant company name if available).\n- **Department**: The most relevant department within NITDA for handling this email. If a specific department is not clear, use a general term like \"DGCEO Office.\"\n- **Summary**: A concise summary of the email's purpose, key details (event names, locations, dates, deadlines), and any important context.\n- **ActionRequired**: A clear, concise statement on what is required from the DGCEO office, based on the email's content.\n- **RecommendedAction**: An actionable recommendation for the DGCEO office (e.g., \"Delegate to relevant department,\" \"For DGCEO's attention,\" \"Respond to sender\").\n- **Priority**: A priority level for the email. Infer this based on deadlines or urgency (e.g., \"High,\" \"Medium,\" \"Low\"). If no urgency is mentioned, default to \"Medium.\"\n- **Status**: The current status of the task. Default this to \"Pending Review.\"\n- **DueDate**: A specific date if a deadline is mentioned in the email (e.g., \"22nd Sept. 2025\"). If no due date is provided, state \"N/A.\"\n- **Attachments**: State whether the email contains attachments (\"Yes\") or not (\"No\") based on any explicit mentions or headers.\n\nYour output must be a single JSON object. Do not include any text before or after the JSON.\n\nHere is the email to summarize:\n\nFrom: @{outputs('Get_email_(V2)')?['body/from']},To: @{outputs('Get_email_(V2)')?['body/toRecipients']}  ,CC: @{outputs('Get_email_(V2)')?['body/ccRecipients']}Bcc: @{outputs('Get_email_(V2)')?['body/bccRecipients']}, Subject: @{outputs('Get_email_(V2)')?['body/subject']}, Received Date: @{outputs('Get_email_(V2)')?['body/receivedDateTime']}, @{outputs('Get_email_(V2)')?['body/importance']},Body:@{outputs('Get_email_(V2)')?['body/body']}"
        }
      ]
    }
  ],
  "generationConfig": {
    "temperature": 0.2,
    "topK": 1,
    "topP": 1,
    "candidateCount": 1
  }
}
```

### `Initialize_variable`

| Field | As provisioned |
|---|---|
| Type | `InitializeVariable` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Initialize_variable_2` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "variables": [
    {
      "name": "varResponse",
      "type": "string"
    }
  ]
}
```

### `Initialize_variable_2`

| Field | As provisioned |
|---|---|
| Type | `InitializeVariable` |
| Kind | — |
| Description | _not declared_ |
| Runs after | _first in its scope_ |

**`inputs`, verbatim:**

```json
{
  "variables": [
    {
      "name": "varAttachmentsHTML",
      "type": "boolean"
    }
  ]
}
```

