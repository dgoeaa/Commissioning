# Portal_Verify

> **deployed export — the live tenant.** Every value below is read from
> [`docs/reference/flow-contracts/deployed/Portal_Verify__86897b2f-9770-4efa-8486-2642f24bb947__full_definition.json`](../../flow-contracts/deployed/Portal_Verify__86897b2f-9770-4efa-8486-2642f24bb947__full_definition.json) and printed as that package carries it.
> Generated by `scripts/build-provisioning-reference.mjs` — do not edit by hand.

## 1. Identity

| Field | As provisioned |
|---|---|
| Display name | `Portal_Verify` |
| Internal name | `86897b2f-9770-4efa-8486-2642f24bb947` |
| Workflow id | _not carried by this package_ |
| Environment | `Default-ca6a4b3f-9123-49bc-bcb9-27085ebbf1a1` |
| Full resource id | `null` — the export records the field and leaves it null |
| Estate | Document portal — reachable by anonymous callers — listed in `docs/deployment/sharepoint/flow-standard.json#portalFlows` |
| Package family | deployed export — the live tenant |
| Exported at (UTC) | `2026-08-19T06:08:36Z` |
| Exported by | `scripts/export-power-automate-flows.ps1` |
| Source package | `docs/reference/flow-contracts/deployed/Portal_Verify__86897b2f-9770-4efa-8486-2642f24bb947__full_definition.json` |

**Portal endpoint** this workflow answers, per `docs/reference/portal-endpoint-workflow-ids.json`:

| Endpoint key | Endpoint flow name | Workflow id | Note |
|---|---|---|---|
| `VERIFY` | `CG_Verification_Endpoint` | `ee627334d2e34ba7ac1b899bdb3ff2d0` | Reuses the Portal_Verify workflow — a rename in place. This is why its varData was declared String while the newer flows declare Object. |

## 2. Definition envelope

| Field | As provisioned |
|---|---|
| `$schema` | `https://schema.management.azure.com/providers/Microsoft.Logic/schemas/2016-06-01/workflowdefinition.json#` |
| `contentVersion` | `undefined` |
| `description` | _not declared_ |
| `outputs` | _not declared_ |
| Actions | 57 |

## 3. Definition parameters

| Parameter | Type | Default value |
|---|---|---|
| `$authentication` | `SecureObject` | `{}` |
| `$connections` | `Object` | `{}` |

## 4. Connections and references

**Connections the actions are bound to**, read from each action's `inputs.host`:

| Connection name | API id | Reference name | Operations used |
|---|---|---|---|
| `shared_flowmanagement-1` | `/providers/Microsoft.PowerApps/apis/shared_flowmanagement` | — | `GetFlow`×1 |
| `shared_office365` | `/providers/Microsoft.PowerApps/apis/shared_office365` | — | `SendEmailV2`×1 |
| `shared_office365-2` | `/providers/Microsoft.PowerApps/apis/shared_office365` | — | `SendEmailV2`×1 |
| `shared_sharepointonline` | `/providers/Microsoft.PowerApps/apis/shared_sharepointonline` | — | `GetItems`×1, `PatchItem`×1 |
| `shared_sharepointonline-1` | `/providers/Microsoft.PowerApps/apis/shared_sharepointonline` | — | `PostItem`×1 |

## 5. Trigger — the endpoint this flow exposes

| Field | As provisioned |
|---|---|
| Trigger name | `manual` |
| `type` | `Request` |
| `kind` | `Http` |
| `method` | `POST` |
| `relativePath` | _not declared — the trigger accepts the bare invoke path_ |
| `triggerAuthenticationType` | `All` |
| `operationOptions` | _not declared_ |
| `conditions` | _not declared_ |
| `splitOn` | _not declared_ |
| `correlation` | _not declared_ |
| `recurrence` | _not declared_ |
| Declared request headers | _not declared_ |
| Declared query parameters | _not declared_ |
| `description` | _not declared_ |

### 5.1 Request payload — declared properties

| Property | Type | Required | Format | Enum | Default | Constraints | Description |
|---|---|---|---|---|---|---|---|
| `action` | `string \| null` | no | — | — | — | minLength 1 | The high-level action to perform. |
| `operation` | `string \| null` | no | — | — | — | minLength 1 | The specific operation name. |
| `mode` | `string \| null` | no | — | — | — | — | Execution mode for the request. |
| `source` | `string \| null` | no | — | — | — | — | Source system initiating the request. |
| `userEmail` | `string \| null` | no | `email` | — | — | — | The initiating user's email address. |
| `method` | `string \| null` | no | — | — | — | — | Request or execution method. |
| `id` | `string \| integer \| null` | no | — | — | — | — | Primary identifier associated with the request. |
| `device` | `object \| null` | no | — | — | — | — | Device information related to the request. |
| `device.id` | `string \| integer \| null` | no | — | — | — | — | Device identifier. |
| `otp_code` | `string` | no | — | — | — | — | Required only for verification |

Root `required`: _empty — the schema marks nothing mandatory_. Root `additionalProperties`: `true`.

### 5.2 Request payload — schema as provisioned

```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "title": "Standardized Trigger Contract",
  "description": "Self-contained HTTP trigger schema. Includes the standard shared trigger fields and the flow-specific field.",
  "properties": {
    "action": {
      "type": [
        "string",
        "null"
      ],
      "minLength": 1,
      "description": "The high-level action to perform."
    },
    "operation": {
      "type": [
        "string",
        "null"
      ],
      "minLength": 1,
      "description": "The specific operation name."
    },
    "mode": {
      "type": [
        "string",
        "null"
      ],
      "description": "Execution mode for the request."
    },
    "source": {
      "type": [
        "string",
        "null"
      ],
      "description": "Source system initiating the request."
    },
    "userEmail": {
      "type": [
        "string",
        "null"
      ],
      "format": "email",
      "description": "The initiating user's email address."
    },
    "method": {
      "type": [
        "string",
        "null"
      ],
      "description": "Request or execution method."
    },
    "id": {
      "type": [
        "string",
        "integer",
        "null"
      ],
      "description": "Primary identifier associated with the request."
    },
    "device": {
      "type": [
        "object",
        "null"
      ],
      "description": "Device information related to the request.",
      "properties": {
        "id": {
          "type": [
            "string",
            "integer",
            "null"
          ],
          "description": "Device identifier."
        }
      },
      "additionalProperties": true
    },
    "otp_code": {
      "type": "string",
      "description": "Required only for verification"
    }
  },
  "required": [],
  "additionalProperties": true,
  "x-backward-compatible": true,
  "x-compatibility-note": "Shared and flow-specific trigger fields are fully built into this single schema without external references."
}
```

## 6. Responses returned to the caller

| Action | Status code | Runs after | On statuses |
|---|---|---|---|
| `Scope_Global/Response` | `@variables('varStatusCode')` | `Set_variable_varHTTPResponse` | Succeeded |

### 6.1 `Scope_Global/Response`

| Field | As provisioned |
|---|---|
| `statusCode` | `@variables('varStatusCode')` |
| `operationOptions` | _not declared_ |

| Header | Configured value |
|---|---|
| `Access-Control-Allow-Origin` | `https://your-host` |
| `Access-Control-Allow-Methods` | `GET, POST, OPTIONS` |
| `Access-Control-Allow-Headers` | `Content-Type, X-DGO-Trigger, X-Correlation-ID` |
| `Content-Type` | `application/json` |

**Response body, verbatim:**

```json
" @{variables('varHTTPResponse')}"
```

## 7. Termination

_No `Terminate` action. The run ends when the action graph does._

## 8. Error handling and run-time configuration

**Actions wired to run on something other than success.** These are the flow's catch
paths — an action that runs after `Failed`, `Skipped` or `TimedOut` is error handling,
whatever it is named.

| Action | Type | Runs after | On statuses |
|---|---|---|---|
| `Scope_Flow_Data_Capture` | `Scope` | `Scope_Global` | Succeeded, TimedOut, Skipped, Failed |
| `Scope_Global/Scope_Finalize_Response_State` | `Scope` | `Scope_Flow_Processing_Catch`, `Switch` | Succeeded, Failed, Skipped, TimedOut · Succeeded, TimedOut, Skipped, Failed |
| `Scope_Global/Scope_Flow_Processing_Catch` | `Scope` | `Switch` | TimedOut, Skipped, Failed |
| `Scope_Global/Compose__Standard_Response_Revised` | `Compose` | `Set_variable_varData` | Succeeded, TimedOut, Skipped, Failed |
| `Scope_Global/Set_variable_varData` | `SetVariable` | `Scope_Finalize_Response_State` | Succeeded, TimedOut, Skipped, Failed |

_No action carries a retry policy, secure-data setting, operation option, tracked
property or loop limit. Every call runs on the platform defaults._

## 9. Variables

**Declared, with the initial value each is provisioned with:**

| Variable | Type | Initial value | Declared by |
|---|---|---|---|
| `varCurrentTime` | `string` | `@utcNow()` | `Initialize_variable_varCurrentTime` |
| `varResponse` | `string` | _no initial value_ | `Initialize_variable_varResponse` |
| `varStatusCode` | `integer` | _no initial value_ | `Initialize_variable_varStatusCode` |
| `varRandomOTP` | `integer` | `@rand(100000, 999999)` | `Initialize_variable_varRandomOTP` |
| `varFailedCount` | `integer` | `0` | `Initialize_variable_varFailed` |
| `varRequestId` | `string` | `@coalesce(triggerBody()?['requestId'], guid())` | `Initialize_variable_varRequestId` |
| `varReceivedAtUtc` | `string` | `@utcNow()` | `Initialize_variable_varReceivedAtUtc` |
| `varStartTicks` | `integer` | `@ticks(utcNow())` | `Initialize_variable_varStartTicks` |
| `varResults` | `array` | `[]` | `Initialize_variable_varResults` |
| `varErrors` | `array` | `[]` | `Initialize_variable_varErrors` |
| `varData` | `string` | _no initial value_ | `Initialize_variable_varData` |
| `varCompletedAtUtc` | `string` | _no initial value_ | `Initialize_variable_varCompletedAtUtc` |
| `varDurationMs` | `integer` | _no initial value_ | `Initialize_variable_varDurationMs` |
| `varHTTPResponse` | `object` | _no initial value_ | `Initialize_variable_varHTTPResponse` |

**Every write, with the expression or literal assigned:**

| Action | Operation | Variable | Assigned value |
|---|---|---|---|
| `Scope_Global/Scope_Finalize_Response_State/Set_variable__varDurationMs` | `SetVariable` | `varDurationMs` | `@if(↵  equals(coalesce(variables('varStartTicks'), 0), 0),↵  0,↵  max(↵    div(↵      sub(↵        ticks(↵          coalesce(↵            variables('varCompletedAtUtc'),↵            utcNow()↵          )↵        ),↵        variables('varStartTicks')↵      ),↵      10000↵    ),↵    0↵  )↵)` |
| `Scope_Global/Scope_Finalize_Response_State/Set_variable_varCompletedAtUtc` | `SetVariable` | `varCompletedAtUtc` | `@utcNow()` |
| `Scope_Global/Scope_Finalize_Response_State/Set_variable_varStatusCode` | `SetVariable` | `varStatusCode` | `@if(or(greater(variables('varFailedCount'), 0), greater(length(coalesce(variables('varErrors'), json('[]'))), 0)), 400, 200)` |
| `Scope_Global/Scope_Flow_Processing_Catch/Append_to_array_variable_varErrrors_(array)_Flow_Processing_Catch` | `AppendToArrayVariable` | `varErrors` | _563 characters — printed verbatim in §13 under_ `Scope_Global/Scope_Flow_Processing_Catch/Append_to_array_variable_varErrrors_(array)_Flow_Processing_Catch` |
| `Scope_Global/Scope_Flow_Processing_Catch/Increment_variable_varFailedCount_Flow_Processing_Catch` | `IncrementVariable` | `varFailedCount` | `1` |
| `Scope_Global/Switch/default/Append_to_string_variable_Default` | `AppendToStringVariable` | `varResponse` | `{"valid":false,"message":"Unknown or invalid action: @{triggerBody()}"}` |
| `Scope_Global/Switch/default/Set_variable_Default` | `SetVariable` | `varStatusCode` | `400` |
| `Scope_Global/Switch/case:Case_Generate/Append_to_string_variable_varResponse_Generate_OTP` | `AppendToStringVariable` | `varResponse` | `{"status":"success","message":"OTP sent","expiry_minutes":"@outputs('Create_item_OTP_Record')?['body/Expires_At']"}` |
| `Scope_Global/Switch/case:Case_Generate/Set_variable_varStatusCode_Generate_OTP` | `SetVariable` | `varStatusCode` | `200` |
| `Scope_Global/Switch/case:Case_Verify/Condition/Set_variable_varStatusCode_No_Pending_OTP_Found` | `SetVariable` | `varStatusCode` | `404` |
| `Scope_Global/Switch/case:Case_Verify/Condition/Append_to_string_variable_varRsponse_No_Pending_OTP_Found` | `AppendToStringVariable` | `varResponse` | `No OTP found` |
| `Scope_Global/Switch/case:Case_Verify/Condition/else/Condition_1/Set_variable_Verification_Successful` | `SetVariable` | `varStatusCode` | `200` |
| `Scope_Global/Switch/case:Case_Verify/Condition/else/Condition_1/Append_to_string_variable_Verification_Successful` | `AppendToStringVariable` | `varResponse` | `{"valid":true,"message":"Verification successful"}` |
| `Scope_Global/Switch/case:Case_Verify/Condition/else/Condition_1/else/Set_variable_OTP__OTP_Invalid_or_Expired` | `SetVariable` | `varStatusCode` | `400` |
| `Scope_Global/Switch/case:Case_Verify/Condition/else/Condition_1/else/Append_to_string_variable__OTP_Invalid_or_Expired` | `AppendToStringVariable` | `varResponse` | `{"valid":false,"message":"Invalid code or code expired"}` |
| `Scope_Global/Set_variable_varData` | `SetVariable` | `varData` | `@variables('varResponse')` |
| `Scope_Global/Set_variable_varHTTPResponse` | `SetVariable` | `varHTTPResponse` | _2998 characters — printed verbatim in §13 under_ `Scope_Global/Set_variable_varHTTPResponse` |

## 10. SharePoint operations

Each row is one connector call. `table` is printed as configured; the resolved column
names the list that GUID belongs to per `docs/reference/sharepoint-list-index.json`,
and says so plainly when the index does not carry it.

| Action | Operation | Connection | `dataset` (site) | `table` (list) | Resolves to |
|---|---|---|---|---|---|
| `Scope_Global/Switch/case:Case_Generate/Create_item_OTP_Record` | `PostItem` | `shared_sharepointonline-1` | `https://nitdanigeria.sharepoint.com/sites/NITDADGO-EAAACTIVITYTRACKING` | `9421d473-8906-43b7-a41f-a213046683c1` | OTP_Transactions — NITDADGO-EAAACTIVITYTRACKING |
| `Scope_Global/Switch/case:Case_Verify/Get_items` | `GetItems` | `shared_sharepointonline` | `https://nitdanigeria.sharepoint.com/sites/NITDADGO-EAAACTIVITYTRACKING` | `9421d473-8906-43b7-a41f-a213046683c1` | OTP_Transactions — NITDADGO-EAAACTIVITYTRACKING |
| `Scope_Global/Switch/case:Case_Verify/Condition/else/Condition_1/Update_item` | `PatchItem` | `shared_sharepointonline` | `https://nitdanigeria.sharepoint.com/sites/NITDADGO-EAAACTIVITYTRACKING` | `9421d473-8906-43b7-a41f-a213046683c1` | OTP_Transactions — NITDADGO-EAAACTIVITYTRACKING |

## 11. Outbound HTTP calls

_This package makes no raw HTTP call._

## 12. Action dependency register

Every action in the package, in graph order, with the edge that starts it. `↳` marks
nesting depth; the path is the address used everywhere else on this page.

| Action path | Type | Kind | Runs after | On statuses |
|---|---|---|---|---|
| `Initialize_variable_varCurrentTime` | `InitializeVariable` | — | `Compose_1` | Succeeded |
| `Initialize_variable_varResponse` | `InitializeVariable` | — | `Initialize_variable_varCurrentTime` | Succeeded |
| `Initialize_variable_varStatusCode` | `InitializeVariable` | — | `Initialize_variable_varResponse` | Succeeded |
| `Initialize_variable_varRandomOTP` | `InitializeVariable` | — | `Initialize_variable_varStatusCode` | Succeeded |
| `Scope_Flow_Data_Capture` | `Scope` | — | `Scope_Global` | Succeeded/TimedOut/Skipped/Failed |
| ↳`Scope_Flow_Data_Capture/Send_Telemetry_Email` | `OpenApiConnection` | — | `Compose_Telemetry_Attachments` | Succeeded |
| ↳`Scope_Flow_Data_Capture/Compose_Telemetry_Attachments` | `Compose` | — | `Compose_Flow_Run_Record` | Succeeded |
| ↳`Scope_Flow_Data_Capture/Compose_Flow_Run_Record` | `Compose` | — | `Get_Flow_Definition` | Succeeded |
| ↳`Scope_Flow_Data_Capture/Get_Flow_Definition` | `OpenApiConnection` | — | _first in its scope_ | — |
| `Scope_Global` | `Scope` | — | `Initialize_variable_varFailed` | Succeeded |
| ↳`Scope_Global/Scope_Finalize_Response_State` | `Scope` | — | `Scope_Flow_Processing_Catch`, `Switch` | Succeeded/Failed/Skipped/TimedOut · Succeeded/TimedOut/Skipped/Failed |
| ↳↳`Scope_Global/Scope_Finalize_Response_State/Set_variable__varDurationMs` | `SetVariable` | — | `Set_variable_varCompletedAtUtc` | Succeeded |
| ↳↳`Scope_Global/Scope_Finalize_Response_State/Set_variable_varCompletedAtUtc` | `SetVariable` | — | `Set_variable_varStatusCode` | Succeeded |
| ↳↳`Scope_Global/Scope_Finalize_Response_State/Set_variable_varStatusCode` | `SetVariable` | — | _first in its scope_ | — |
| ↳`Scope_Global/Scope_Flow_Processing_Catch` | `Scope` | — | `Switch` | TimedOut/Skipped/Failed |
| ↳↳`Scope_Global/Scope_Flow_Processing_Catch/Append_to_array_variable_varErrrors_(array)_Flow_Processing_Catch` | `AppendToArrayVariable` | — | _first in its scope_ | — |
| ↳↳`Scope_Global/Scope_Flow_Processing_Catch/Increment_variable_varFailedCount_Flow_Processing_Catch` | `IncrementVariable` | — | `Append_to_array_variable_varErrrors_(array)_Flow_Processing_Catch` | Succeeded |
| ↳`Scope_Global/Switch` | `Switch` | — | _first in its scope_ | — |
| ↳↳↳`Scope_Global/Switch/default/Append_to_string_variable_Default` | `AppendToStringVariable` | — | `Set_variable_Default` | Succeeded |
| ↳↳↳`Scope_Global/Switch/default/Set_variable_Default` | `SetVariable` | — | _first in its scope_ | — |
| ↳↳↳`Scope_Global/Switch/case:Case_Generate/Compose_OTP_Expiry` | `Compose` | — | _first in its scope_ | — |
| ↳↳↳`Scope_Global/Switch/case:Case_Generate/Create_item_OTP_Record` | `OpenApiConnection` | — | `Compose_OTP_Expiry` | Succeeded |
| ↳↳↳`Scope_Global/Switch/case:Case_Generate/Append_to_string_variable_varResponse_Generate_OTP` | `AppendToStringVariable` | — | `Send_an_email_(V2)` | Succeeded |
| ↳↳↳`Scope_Global/Switch/case:Case_Generate/Set_variable_varStatusCode_Generate_OTP` | `SetVariable` | — | `Append_to_string_variable_varResponse_Generate_OTP` | Succeeded |
| ↳↳↳`Scope_Global/Switch/case:Case_Generate/Send_an_email_(V2)` | `OpenApiConnection` | — | `Create_item_OTP_Record` | Succeeded |
| ↳↳↳`Scope_Global/Switch/case:Case_Generate/Compose_OTP_Identifier` | `Compose` | — | `Set_variable_varStatusCode_Generate_OTP` | Succeeded |
| ↳↳↳`Scope_Global/Switch/case:Case_Verify/Get_items` | `OpenApiConnection` | — | _first in its scope_ | — |
| ↳↳↳`Scope_Global/Switch/case:Case_Verify/Condition` | `If` | — | `Compose_Count_Items_Check` | Succeeded |
| ↳↳↳↳`Scope_Global/Switch/case:Case_Verify/Condition/Set_variable_varStatusCode_No_Pending_OTP_Found` | `SetVariable` | — | `Append_to_string_variable_varRsponse_No_Pending_OTP_Found` | Succeeded |
| ↳↳↳↳`Scope_Global/Switch/case:Case_Verify/Condition/Append_to_string_variable_varRsponse_No_Pending_OTP_Found` | `AppendToStringVariable` | — | `Compose_OTP_Identifier_Is_Missing` | Succeeded |
| ↳↳↳↳`Scope_Global/Switch/case:Case_Verify/Condition/Compose_OTP_Identifier_Is_Missing` | `Compose` | — | _first in its scope_ | — |
| ↳↳↳↳↳`Scope_Global/Switch/case:Case_Verify/Condition/else/Condition_1` | `If` | — | `Compose_OTP_Code_Verify` | Succeeded |
| ↳↳↳↳↳↳`Scope_Global/Switch/case:Case_Verify/Condition/else/Condition_1/Update_item` | `OpenApiConnection` | — | _first in its scope_ | — |
| ↳↳↳↳↳↳`Scope_Global/Switch/case:Case_Verify/Condition/else/Condition_1/Set_variable_Verification_Successful` | `SetVariable` | — | `Update_item` | Succeeded |
| ↳↳↳↳↳↳`Scope_Global/Switch/case:Case_Verify/Condition/else/Condition_1/Append_to_string_variable_Verification_Successful` | `AppendToStringVariable` | — | `Set_variable_Verification_Successful` | Succeeded |
| ↳↳↳↳↳↳↳`Scope_Global/Switch/case:Case_Verify/Condition/else/Condition_1/else/Set_variable_OTP__OTP_Invalid_or_Expired` | `SetVariable` | — | _first in its scope_ | — |
| ↳↳↳↳↳↳↳`Scope_Global/Switch/case:Case_Verify/Condition/else/Condition_1/else/Append_to_string_variable__OTP_Invalid_or_Expired` | `AppendToStringVariable` | — | `Set_variable_OTP__OTP_Invalid_or_Expired` | Succeeded |
| ↳↳↳↳↳`Scope_Global/Switch/case:Case_Verify/Condition/else/Compose_OTP_Identifier_Verify` | `Compose` | — | _first in its scope_ | — |
| ↳↳↳↳↳`Scope_Global/Switch/case:Case_Verify/Condition/else/Compose_OTP_Code_Verify` | `Compose` | — | `Compose_OTP_Identifier_Verify` | Succeeded |
| ↳↳↳`Scope_Global/Switch/case:Case_Verify/Compose_Count_Items_Check` | `Compose` | — | `Get_items` | Succeeded |
| ↳`Scope_Global/Compose__Standard_Response_Revised` | `Compose` | — | `Set_variable_varData` | Succeeded/TimedOut/Skipped/Failed |
| ↳`Scope_Global/Response` | `Response` | `Http` | `Set_variable_varHTTPResponse` | Succeeded |
| ↳`Scope_Global/Set_variable_varData` | `SetVariable` | — | `Scope_Finalize_Response_State` | Succeeded/TimedOut/Skipped/Failed |
| ↳`Scope_Global/Compose__Standard_Response_Revised_Updated` | `Compose` | — | `Compose__Standard_Response_Revised` | Succeeded |
| ↳`Scope_Global/Set_variable_varHTTPResponse` | `SetVariable` | — | `Compose__Standard_Response_Revised_Updated` | Succeeded |
| `Initialize_variable_varFailed` | `InitializeVariable` | — | `Initialize_variable_varRequestId` | Succeeded |
| `Initialize_variable_varRequestId` | `InitializeVariable` | — | `Initialize_variable_varDurationMs` | Succeeded |
| `Initialize_variable_varReceivedAtUtc` | `InitializeVariable` | — | `Initialize_variable_varStartTicks` | Succeeded |
| `Initialize_variable_varStartTicks` | `InitializeVariable` | — | `Initialize_variable_varResults` | Succeeded |
| `Initialize_variable_varResults` | `InitializeVariable` | — | `Initialize_variable_varErrors` | Succeeded |
| `Initialize_variable_varErrors` | `InitializeVariable` | — | `Initialize_variable_varHTTPResponse` | Succeeded |
| `Initialize_variable_varData` | `InitializeVariable` | — | `Initialize_variable_varRandomOTP` | Succeeded |
| `Initialize_variable_varCompletedAtUtc` | `InitializeVariable` | — | `Initialize_variable_varReceivedAtUtc` | Succeeded |
| `Initialize_variable_varDurationMs` | `InitializeVariable` | — | `Initialize_variable_varCompletedAtUtc` | Succeeded |
| `Compose` | `Compose` | — | _first in its scope_ | — |
| `Compose_1` | `Compose` | — | `Compose` | Succeeded |
| `Initialize_variable_varHTTPResponse` | `InitializeVariable` | — | `Initialize_variable_varData` | Succeeded |

## 13. Configured values, action by action

The configured input of every action, verbatim. An action with no `inputs` key — a
`Scope`, an `If`, a `Foreach` — is documented by its control expression instead.

### `Initialize_variable_varCurrentTime`

| Field | As provisioned |
|---|---|
| Type | `InitializeVariable` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Compose_1` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "variables": [
    {
      "name": "varCurrentTime",
      "type": "string",
      "value": "@utcNow()"
    }
  ]
}
```

### `Initialize_variable_varResponse`

| Field | As provisioned |
|---|---|
| Type | `InitializeVariable` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Initialize_variable_varCurrentTime` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "variables": [
    {
      "name": "varResponse",
      "type": "string"
    }
  ]
}
```

### `Initialize_variable_varStatusCode`

| Field | As provisioned |
|---|---|
| Type | `InitializeVariable` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Initialize_variable_varResponse` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "variables": [
    {
      "name": "varStatusCode",
      "type": "integer"
    }
  ]
}
```

### `Initialize_variable_varRandomOTP`

| Field | As provisioned |
|---|---|
| Type | `InitializeVariable` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Initialize_variable_varStatusCode` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "variables": [
    {
      "name": "varRandomOTP",
      "type": "integer",
      "value": "@rand(100000, 999999)"
    }
  ]
}
```

### `Scope_Flow_Data_Capture`

| Field | As provisioned |
|---|---|
| Type | `Scope` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Scope_Global` (Succeeded/TimedOut/Skipped/Failed) |

_This action declares no `inputs`._

### `Scope_Flow_Data_Capture/Send_Telemetry_Email`

| Field | As provisioned |
|---|---|
| Type | `OpenApiConnection` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Compose_Telemetry_Attachments` (Succeeded) |
| Connector | `shared_office365-2` |
| Operation | `SendEmailV2` |

**`inputs`, verbatim:**

```json
{
  "parameters": {
    "emailMessage/To": "dgsRegistry@nitda.gov.ng",
    "emailMessage/Subject": "@{coalesce(workflow()?['tags']?['flowDisplayName'], workflow()?['name'])} — Flow Run Record — @{workflow()?['run']?['name']}",
    "emailMessage/Body": "<p class=\"editor-paragraph\">Automated flow run record capture.<br>Flow: @{coalesce(workflow()?['tags']?['flowDisplayName'], workflow()?['name'])}<br>Run: @{workflow()?['run']?['name']}<br>Outcome: @{outputs('Compose__Standard_Response_Revised')}</p>",
    "emailMessage/Attachments": "@outputs('Compose_Telemetry_Attachments')",
    "emailMessage/Importance": "Normal"
  },
  "host": {
    "apiId": "/providers/Microsoft.PowerApps/apis/shared_office365",
    "connectionName": "shared_office365-2",
    "operationId": "SendEmailV2"
  },
  "authentication": "@parameters('$authentication')"
}
```

### `Scope_Flow_Data_Capture/Compose_Telemetry_Attachments`

| Field | As provisioned |
|---|---|
| Type | `Compose` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Compose_Flow_Run_Record` (Succeeded) |

**`inputs`, verbatim:**

```json
[
  {
    "Name": "@{coalesce(workflow()?['tags']?['flowDisplayName'], workflow()?['name'], 'Flow')}__@{workflow()?['run']?['name']}__flow_run_record.json",
    "ContentBytes": "@{string(outputs('Compose_Flow_Run_Record'))}"
  },
  {
    "Name": "@{coalesce(workflow()?['tags']?['flowDisplayName'], workflow()?['name'], 'Flow')}__@{workflow()?['run']?['name']}__trigger_input_schema.json",
    "ContentBytes": "@{string(\n        coalesce(\n          outputs('Get_Flow_Definition')?['body']?['properties']?['definition']?['triggers']?['manual']?['inputs']?['schema'],\n          json('{}')\n        )\n    )}"
  },
  {
    "Name": "@{coalesce(workflow()?['tags']?['flowDisplayName'], workflow()?['name'], 'Flow')}__@{workflow()?['run']?['name']}__Compose_HTTP_Response.txt",
    "ContentBytes": "@{outputs('Compose_Flow_Run_Record')}"
  },
  {
    "Name": "@{coalesce(workflow()?['tags']?['flowDisplayName'], workflow()?['name'], 'Flow')}__@{workflow()?['run']?['name']}__varResponse.txt",
    "ContentBytes": "@{variables('varData')}"
  }
]
```

### `Scope_Flow_Data_Capture/Compose_Flow_Run_Record`

| Field | As provisioned |
|---|---|
| Type | `Compose` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Get_Flow_Definition` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "capture_metadata": {
    "telemetry_kind": "flow_run_record",
    "capture_version": "2.0.0",
    "captured_at_utc": "@{utcNow()}",
    "capture_scope": "Scope_Flow_Data_Capture"
  },
  "workflow_identity": {
    "internal_name": "@{workflow()?['name']}",
    "full_resource_id": "@{workflow()?['id']}",
    "type": "@{workflow()?['type']}",
    "location": "@{workflow()?['location']}",
    "tags": "@coalesce(workflow()?['tags'], json('{}'))",
    "run": "@coalesce(workflow()?['run'], json('{}'))"
  },
  "run_identity": {
    "run_name": "@{workflow()?['run']?['name']}",
    "run_resource_id": "@{workflow()?['run']?['id']}",
    "run_type": "@{workflow()?['run']?['type']}",
    "environment_id": "@{workflow()?['tags']?['environmentName']}",
    "run_url": "@{concat('https://make.powerautomate.com/environments/', coalesce(workflow()?['tags']?['environmentName'], ''), '/flows/', workflow()?['name'], '/runs/', coalesce(workflow()?['run']?['name'], ''))}"
  },
  "trigger_record": {
    "name": "@{trigger()?['name']}",
    "start_time": "@{trigger()?['startTime']}",
    "end_time": "@{trigger()?['endTime']}",
    "scheduled_time": "@{trigger()?['scheduledTime']}",
    "status": "@{trigger()?['status']}",
    "code": "@{trigger()?['code']}",
    "tracking_id": "@{trigger()?['trackingId']}",
    "client_tracking_id": "@{trigger()?['clientTrackingId']}",
    "origin_history_name": "@{trigger()?['originHistoryName']}",
    "correlation": "@coalesce(trigger()?['correlation'], json('{}'))"
  },
  "request": {
    "method": "@{triggerOutputs()?['method']}",
    "relative_path_parameters": "@coalesce(triggerOutputs()?['relativePathParameters'], json('{}'))",
    "body": "@triggerBody()"
  },
  "design_definition": {
    "get_flow_body": "@coalesce(outputs('Get_Flow_Definition')?['body'], json('{}'))",
    "display_name": "",
    "state": "",
    "provisioning_method": "",
    "created_time": "",
    "last_modified_time": "",
    "creator": "@coalesce(outputs('Get_Flow_Definition')?['body']?['properties']?['creator'], json('{}'))",
    "definition": "@coalesce(outputs('Get_Flow_Definition')?['body']?['properties']?['definition'], json('{}'))",
    "trigger_definition": "@coalesce(outputs('Get_Flow_Definition')?['body']?['properties']?['definition']?['triggers'], json('{}'))",
    "trigger_input_schema": "@coalesce(outputs('Get_Flow_Definition')?['body']?['properties']?['definition']?['triggers']?['manual']?['inputs']?['schema'], json('{}'))",
    "action_definitions": "@coalesce(outputs('Get_Flow_Definition')?['body']?['properties']?['definition']?['actions'], json('{}'))",
    "connection_references": "@coalesce(outputs('Get_Flow_Definition')?['body']?['properties']?['connectionReferences'], json('{}'))"
  },
  "response_record": {
    "action_status": "@{actions('Response')?['status']}",
    "action_code": "@{actions('Response')?['code']}",
    "action_start_time": "@{actions('Response')?['startTime']}",
    "action_end_time": "@{actions('Response')?['endTime']}",
    "tracking_id": "@{actions('Response')?['trackingId']}",
    "client_tracking_id": "@{actions('Response')?['clientTrackingId']}",
    "correlation": "@coalesce(actions('Response')?['correlation'], json('{}'))",
    "status_code_sent": "@coalesce(actions('Response')?['inputs']?['statusCode'], 0)",
    "headers_sent": "@coalesce(actions('Response')?['inputs']?['headers'], json('{}'))",
    "body_sent": "@coalesce(actions('Response')?['inputs']?['body'], json('{}'))",
    "raw_action_record": "@coalesce(actions('Response'), json('{}'))"
  },
  "outcome": {
    "flow_outcome": "@if(equals(actions('Scope_Global')?['status'], 'Succeeded'), 'Succeeded', 'Failed')",
    "failed_action_count": 0,
    "first_failed_action": {},
    "all_scope_action_results": "@coalesce(result('Scope_Global'), json('[]'))"
  },
  "timing": {
    "trigger_start_time": "@{trigger()?['startTime']}",
    "captured_at_utc": "@{utcNow()}",
    "total_elapsed_seconds": "@div(sub(ticks(utcNow()), ticks(trigger()?['startTime'])), 10000000)",
    "total_elapsed_ms": "@div(sub(ticks(utcNow()), ticks(trigger()?['startTime'])), 10000)",
    "response_action_duration_ms": "@if(empty(coalesce(actions('Response')?['endTime'], '')), 0, div(sub(ticks(actions('Response')?['endTime']), ticks(actions('Response')?['startTime'])), 10000))"
  }
}
```

### `Scope_Flow_Data_Capture/Get_Flow_Definition`

| Field | As provisioned |
|---|---|
| Type | `OpenApiConnection` |
| Kind | — |
| Description | _not declared_ |
| Runs after | _first in its scope_ |
| Connector | `shared_flowmanagement-1` |
| Operation | `GetFlow` |

**`inputs`, verbatim:**

```json
{
  "parameters": {
    "environmentName": "Default-ca6a4b3f-9123-49bc-bcb9-27085ebbf1a1",
    "flowName": "@workflow()?['name']"
  },
  "host": {
    "apiId": "/providers/Microsoft.PowerApps/apis/shared_flowmanagement",
    "connectionName": "shared_flowmanagement-1",
    "operationId": "GetFlow"
  },
  "authentication": "@parameters('$authentication')"
}
```

### `Scope_Global`

| Field | As provisioned |
|---|---|
| Type | `Scope` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Initialize_variable_varFailed` (Succeeded) |

_This action declares no `inputs`._

### `Scope_Global/Scope_Finalize_Response_State`

| Field | As provisioned |
|---|---|
| Type | `Scope` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Scope_Flow_Processing_Catch` (Succeeded/Failed/Skipped/TimedOut), `Switch` (Succeeded/TimedOut/Skipped/Failed) |

_This action declares no `inputs`._

### `Scope_Global/Scope_Finalize_Response_State/Set_variable__varDurationMs`

| Field | As provisioned |
|---|---|
| Type | `SetVariable` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Set_variable_varCompletedAtUtc` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "name": "varDurationMs",
  "value": "@if(\n  equals(coalesce(variables('varStartTicks'), 0), 0),\n  0,\n  max(\n    div(\n      sub(\n        ticks(\n          coalesce(\n            variables('varCompletedAtUtc'),\n            utcNow()\n          )\n        ),\n        variables('varStartTicks')\n      ),\n      10000\n    ),\n    0\n  )\n)"
}
```

### `Scope_Global/Scope_Finalize_Response_State/Set_variable_varCompletedAtUtc`

| Field | As provisioned |
|---|---|
| Type | `SetVariable` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Set_variable_varStatusCode` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "name": "varCompletedAtUtc",
  "value": "@utcNow()"
}
```

### `Scope_Global/Scope_Finalize_Response_State/Set_variable_varStatusCode`

| Field | As provisioned |
|---|---|
| Type | `SetVariable` |
| Kind | — |
| Description | _not declared_ |
| Runs after | _first in its scope_ |

**`inputs`, verbatim:**

```json
{
  "name": "varStatusCode",
  "value": "@if(or(greater(variables('varFailedCount'), 0), greater(length(coalesce(variables('varErrors'), json('[]'))), 0)), 400, 200)"
}
```

### `Scope_Global/Scope_Flow_Processing_Catch`

| Field | As provisioned |
|---|---|
| Type | `Scope` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Switch` (TimedOut/Skipped/Failed) |

_This action declares no `inputs`._

### `Scope_Global/Scope_Flow_Processing_Catch/Append_to_array_variable_varErrrors_(array)_Flow_Processing_Catch`

| Field | As provisioned |
|---|---|
| Type | `AppendToArrayVariable` |
| Kind | — |
| Description | _not declared_ |
| Runs after | _first in its scope_ |

**`inputs`, verbatim:**

```json
{
  "name": "varErrors",
  "value": {
    "scope": "",
    "stage": "task-creation",
    "status": "failed",
    "message": "Task creation scope failed or timed out.",
    "trackedAtUtc": "@utcNow()",
    "requestId": "@if(empty(trim(coalesce(triggerBody()?['requestId'], ''))), variables('varRequestId'), triggerBody()?['requestId'])",
    "action": "@if(empty(triggerBody()?['action']), 'singleassignment', triggerBody()?['action'])",
    "operation": "@if(empty(triggerBody()?['operation']), 'create', triggerBody()?['operation'])",
    "source": "@if(empty(triggerBody()?['source']), 'http', triggerBody()?['source'])",
    "details": "@result('Switch')"
  }
}
```

### `Scope_Global/Scope_Flow_Processing_Catch/Increment_variable_varFailedCount_Flow_Processing_Catch`

| Field | As provisioned |
|---|---|
| Type | `IncrementVariable` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Append_to_array_variable_varErrrors_(array)_Flow_Processing_Catch` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "name": "varFailedCount",
  "value": 1
}
```

### `Scope_Global/Switch`

| Field | As provisioned |
|---|---|
| Type | `Switch` |
| Kind | — |
| Description | _not declared_ |
| Runs after | _first in its scope_ |

_This action declares no `inputs`._

**`expression`, verbatim:**

```json
"@triggerBody()?['action']"
```

### `Scope_Global/Switch/default/Append_to_string_variable_Default`

| Field | As provisioned |
|---|---|
| Type | `AppendToStringVariable` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Set_variable_Default` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "name": "varResponse",
  "value": {
    "valid": false,
    "message": "Unknown or invalid action: @{triggerBody()}"
  }
}
```

### `Scope_Global/Switch/default/Set_variable_Default`

| Field | As provisioned |
|---|---|
| Type | `SetVariable` |
| Kind | — |
| Description | _not declared_ |
| Runs after | _first in its scope_ |

**`inputs`, verbatim:**

```json
{
  "name": "varStatusCode",
  "value": 400
}
```

### `Scope_Global/Switch/case:Case_Generate/Compose_OTP_Expiry`

| Field | As provisioned |
|---|---|
| Type | `Compose` |
| Kind | — |
| Description | _not declared_ |
| Runs after | _first in its scope_ |

**`inputs`, verbatim:**

```json
"@addMinutes(utcNow(), 5)"
```

### `Scope_Global/Switch/case:Case_Generate/Create_item_OTP_Record`

| Field | As provisioned |
|---|---|
| Type | `OpenApiConnection` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Compose_OTP_Expiry` (Succeeded) |
| Connector | `shared_sharepointonline-1` |
| Operation | `PostItem` |

**`inputs`, verbatim:**

```json
{
  "parameters": {
    "dataset": "https://nitdanigeria.sharepoint.com/sites/NITDADGO-EAAACTIVITYTRACKING",
    "table": "9421d473-8906-43b7-a41f-a213046683c1",
    "item/Title": "@triggerBody()?['identifier']",
    "item/OTP_Code": "@variables('varRandomOTP')",
    "item/Expires_At": "@outputs('Compose_OTP_Expiry')",
    "item/Is_Verified": false
  },
  "host": {
    "apiId": "/providers/Microsoft.PowerApps/apis/shared_sharepointonline",
    "connectionName": "shared_sharepointonline-1",
    "operationId": "PostItem"
  },
  "authentication": "@parameters('$authentication')"
}
```

### `Scope_Global/Switch/case:Case_Generate/Append_to_string_variable_varResponse_Generate_OTP`

| Field | As provisioned |
|---|---|
| Type | `AppendToStringVariable` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Send_an_email_(V2)` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "name": "varResponse",
  "value": {
    "status": "success",
    "message": "OTP sent",
    "expiry_minutes": "@outputs('Create_item_OTP_Record')?['body/Expires_At']"
  }
}
```

### `Scope_Global/Switch/case:Case_Generate/Set_variable_varStatusCode_Generate_OTP`

| Field | As provisioned |
|---|---|
| Type | `SetVariable` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Append_to_string_variable_varResponse_Generate_OTP` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "name": "varStatusCode",
  "value": 200
}
```

### `Scope_Global/Switch/case:Case_Generate/Send_an_email_(V2)`

| Field | As provisioned |
|---|---|
| Type | `OpenApiConnection` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Create_item_OTP_Record` (Succeeded) |
| Connector | `shared_office365` |
| Operation | `SendEmailV2` |

**`inputs`, verbatim:**

```json
{
  "parameters": {
    "emailMessage/To": "dgsregistry@nitda.gov.ng",
    "emailMessage/Subject": "Your Verification Code",
    "emailMessage/Body": "<div style=\"font-family: Arial, sans-serif; padding: 20px; border: 1px solid #e0e0e0; border-radius: 5px;\">\n    <h2 style=\"color: #008751;\">NITDA Verification</h2>\n    <p>Use the code below to complete your verification:</p>\n    <div style=\"font-size: 24px; font-weight: bold; letter-spacing: 5px; margin: 20px 0;\">\n        @{variables('varRandomOTP')}\n    </div>\n    <p style=\"color: #666; font-size: 12px;\">This code expires in 5 minutes.</p>\n</div>\n",
    "emailMessage/Importance": "Normal"
  },
  "host": {
    "apiId": "/providers/Microsoft.PowerApps/apis/shared_office365",
    "connectionName": "shared_office365",
    "operationId": "SendEmailV2"
  },
  "authentication": "@parameters('$authentication')"
}
```

### `Scope_Global/Switch/case:Case_Generate/Compose_OTP_Identifier`

| Field | As provisioned |
|---|---|
| Type | `Compose` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Set_variable_varStatusCode_Generate_OTP` (Succeeded) |

**`inputs`, verbatim:**

```json
"@outputs('Create_item_OTP_Record')?['body/OTP_Code']"
```

### `Scope_Global/Switch/case:Case_Verify/Get_items`

| Field | As provisioned |
|---|---|
| Type | `OpenApiConnection` |
| Kind | — |
| Description | _not declared_ |
| Runs after | _first in its scope_ |
| Connector | `shared_sharepointonline` |
| Operation | `GetItems` |

**`inputs`, verbatim:**

```json
{
  "parameters": {
    "dataset": "https://nitdanigeria.sharepoint.com/sites/NITDADGO-EAAACTIVITYTRACKING",
    "table": "9421d473-8906-43b7-a41f-a213046683c1",
    "$filter": "Title eq '@{triggerBody()?['identifier']}' and Is_Verified eq 0",
    "$orderby": "Created desc",
    "$top": 1
  },
  "host": {
    "apiId": "/providers/Microsoft.PowerApps/apis/shared_sharepointonline",
    "connectionName": "shared_sharepointonline",
    "operationId": "GetItems"
  },
  "authentication": "@parameters('$authentication')"
}
```

### `Scope_Global/Switch/case:Case_Verify/Condition`

| Field | As provisioned |
|---|---|
| Type | `If` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Compose_Count_Items_Check` (Succeeded) |

_This action declares no `inputs`._

**`expression`, verbatim:**

```json
{
  "and": [
    {
      "less": [
        "@outputs('Compose_Count_Items_Check')",
        1
      ]
    }
  ]
}
```

### `Scope_Global/Switch/case:Case_Verify/Condition/Set_variable_varStatusCode_No_Pending_OTP_Found`

| Field | As provisioned |
|---|---|
| Type | `SetVariable` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Append_to_string_variable_varRsponse_No_Pending_OTP_Found` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "name": "varStatusCode",
  "value": 404
}
```

### `Scope_Global/Switch/case:Case_Verify/Condition/Append_to_string_variable_varRsponse_No_Pending_OTP_Found`

| Field | As provisioned |
|---|---|
| Type | `AppendToStringVariable` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Compose_OTP_Identifier_Is_Missing` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "name": "varResponse",
  "value": "No OTP found"
}
```

### `Scope_Global/Switch/case:Case_Verify/Condition/Compose_OTP_Identifier_Is_Missing`

| Field | As provisioned |
|---|---|
| Type | `Compose` |
| Kind | — |
| Description | _not declared_ |
| Runs after | _first in its scope_ |

**`inputs`, verbatim:**

```json
"@variables('varRandomOTP')"
```

### `Scope_Global/Switch/case:Case_Verify/Condition/else/Condition_1`

| Field | As provisioned |
|---|---|
| Type | `If` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Compose_OTP_Code_Verify` (Succeeded) |

_This action declares no `inputs`._

**`expression`, verbatim:**

```json
{
  "and": [
    {
      "equals": [
        "@first(outputs('Get_items')?['body/value']?['OTP_Code'])",
        "@triggerBody()?['otp_code']"
      ]
    },
    {
      "greater": [
        "@first(outputs('Get_items')?['body/value']?['Expires_At'])",
        "@utcNow()"
      ]
    }
  ]
}
```

### `Scope_Global/Switch/case:Case_Verify/Condition/else/Condition_1/Update_item`

| Field | As provisioned |
|---|---|
| Type | `OpenApiConnection` |
| Kind | — |
| Description | _not declared_ |
| Runs after | _first in its scope_ |
| Connector | `shared_sharepointonline` |
| Operation | `PatchItem` |

**`inputs`, verbatim:**

```json
{
  "parameters": {
    "dataset": "https://nitdanigeria.sharepoint.com/sites/NITDADGO-EAAACTIVITYTRACKING",
    "table": "9421d473-8906-43b7-a41f-a213046683c1",
    "id": "@first(outputs('Get_items')?['body/value']?['ID'])",
    "item/Is_Verified": true
  },
  "host": {
    "apiId": "/providers/Microsoft.PowerApps/apis/shared_sharepointonline",
    "connectionName": "shared_sharepointonline",
    "operationId": "PatchItem"
  },
  "authentication": "@parameters('$authentication')"
}
```

### `Scope_Global/Switch/case:Case_Verify/Condition/else/Condition_1/Set_variable_Verification_Successful`

| Field | As provisioned |
|---|---|
| Type | `SetVariable` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Update_item` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "name": "varStatusCode",
  "value": 200
}
```

### `Scope_Global/Switch/case:Case_Verify/Condition/else/Condition_1/Append_to_string_variable_Verification_Successful`

| Field | As provisioned |
|---|---|
| Type | `AppendToStringVariable` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Set_variable_Verification_Successful` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "name": "varResponse",
  "value": {
    "valid": true,
    "message": "Verification successful"
  }
}
```

### `Scope_Global/Switch/case:Case_Verify/Condition/else/Condition_1/else/Set_variable_OTP__OTP_Invalid_or_Expired`

| Field | As provisioned |
|---|---|
| Type | `SetVariable` |
| Kind | — |
| Description | _not declared_ |
| Runs after | _first in its scope_ |

**`inputs`, verbatim:**

```json
{
  "name": "varStatusCode",
  "value": 400
}
```

### `Scope_Global/Switch/case:Case_Verify/Condition/else/Condition_1/else/Append_to_string_variable__OTP_Invalid_or_Expired`

| Field | As provisioned |
|---|---|
| Type | `AppendToStringVariable` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Set_variable_OTP__OTP_Invalid_or_Expired` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "name": "varResponse",
  "value": {
    "valid": false,
    "message": "Invalid code or code expired"
  }
}
```

### `Scope_Global/Switch/case:Case_Verify/Condition/else/Compose_OTP_Identifier_Verify`

| Field | As provisioned |
|---|---|
| Type | `Compose` |
| Kind | — |
| Description | _not declared_ |
| Runs after | _first in its scope_ |

**`inputs`, verbatim:**

```json
"@variables('varRandomOTP')"
```

### `Scope_Global/Switch/case:Case_Verify/Condition/else/Compose_OTP_Code_Verify`

| Field | As provisioned |
|---|---|
| Type | `Compose` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Compose_OTP_Identifier_Verify` (Succeeded) |

**`inputs`, verbatim:**

```json
"@variables('varRandomOTP')"
```

### `Scope_Global/Switch/case:Case_Verify/Compose_Count_Items_Check`

| Field | As provisioned |
|---|---|
| Type | `Compose` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Get_items` (Succeeded) |

**`inputs`, verbatim:**

```json
"@length(outputs('Get_items')?['body/value'])"
```

### `Scope_Global/Compose__Standard_Response_Revised`

| Field | As provisioned |
|---|---|
| Type | `Compose` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Set_variable_varData` (Succeeded/TimedOut/Skipped/Failed) |

**`inputs`, verbatim:**

```json
{
  "ok": "@less(coalesce(variables('varStatusCode'), 500), 400)",
  "status": {
    "http": "@coalesce(variables('varStatusCode'), 500)",
    "code": "@if(\n      less(coalesce(variables('varStatusCode'), 500), 400),\n      'OK',\n      concat('ERR', string(coalesce(variables('varStatusCode'), 500)))\n    )",
    "message": "@if(\n      greater(coalesce(variables('varFailedCount'), 0), 0),\n      'Completed with errors',\n      if(\n        greaterOrEquals(coalesce(variables('varStatusCode'), 500), 400),\n        'Failed',\n        'Completed successfully'\n      )\n    )"
  },
  "confidence": "@and(\n    less(coalesce(variables('varStatusCode'), 500), 400),\n    equals(coalesce(variables('varFailedCount'), 0), 0),\n    greater(length(coalesce(variables('varResults'), json('[]'))), 0)\n  )",
  "request": {
    "requestId": "@if(\n      empty(trim(string(coalesce(triggerBody()?['requestId'], '')))),\n      variables('varRequestId'),\n      triggerBody()?['requestId']\n    )",
    "trackingId": "@coalesce(\n      triggerBody()?['trackingId'],\n      triggerBody()?['trackingID'],\n      ''\n    )",
    "action": "@coalesce(string(triggerBody()?['action']), 'singleassignment')",
    "operation": "@coalesce(string(triggerBody()?['operation']), 'create')",
    "mode": "@if(\n      greater(length(coalesce(triggerBody()?['SelectedItems'], json('[]'))), 1),\n      'bulk',\n      'single'\n    )",
    "requestedBy": "@if(\n      empty(trim(string(coalesce(triggerBody()?['userEmail'], '')))),\n      coalesce(triggerBody()?['NewActivityTask']?['CreatedBy'], 'unknown'),\n      triggerBody()?['userEmail']\n    )",
    "source": "@coalesce(string(triggerBody()?['source']), 'http')"
  },
  "timing": {
    "receivedAtUtc": "@coalesce(\n      variables('varReceivedAtUtc'),\n      variables('varCompletedAtUtc')\n    )",
    "completedAtUtc": "@variables('varCompletedAtUtc')",
    "durationMs": "@coalesce(variables('varDurationMs'), 0)"
  },
  "data": "@variables('varData')",
  "results": "@coalesce(variables('varResults'), json('[]'))",
  "errors": "@coalesce(variables('varErrors'), json('[]'))",
  "meta": {
    "ts": "@variables('varCompletedAtUtc')",
    "runId": "@workflow()?['run']?['name']",
    "flowName": "@workflow()?['name']",
    "contractVersion": "2026-04-14.1"
  }
}
```

### `Scope_Global/Response`

| Field | As provisioned |
|---|---|
| Type | `Response` |
| Kind | `Http` |
| Description | _not declared_ |
| Runs after | `Set_variable_varHTTPResponse` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "statusCode": "@variables('varStatusCode')",
  "headers": {
    "Access-Control-Allow-Origin": "https://your-host",
    "Access-Control-Allow-Methods": "GET, POST, OPTIONS",
    "Access-Control-Allow-Headers": "Content-Type, X-DGO-Trigger, X-Correlation-ID",
    "Content-Type": "application/json"
  },
  "body": " @{variables('varHTTPResponse')}"
}
```

### `Scope_Global/Set_variable_varData`

| Field | As provisioned |
|---|---|
| Type | `SetVariable` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Scope_Finalize_Response_State` (Succeeded/TimedOut/Skipped/Failed) |

**`inputs`, verbatim:**

```json
{
  "name": "varData",
  "value": "@variables('varResponse')"
}
```

### `Scope_Global/Compose__Standard_Response_Revised_Updated`

| Field | As provisioned |
|---|---|
| Type | `Compose` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Compose__Standard_Response_Revised` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "ok": "@less(int(coalesce(variables('varStatusCode'), 500)), 400)",
  "status": {
    "http": "@int(coalesce(variables('varStatusCode'), 500))",
    "code": "@if(less(int(coalesce(variables('varStatusCode'), 500)), 400), 'OK', if(equals(int(coalesce(variables('varStatusCode'), 500)), 400), 'BAD_REQUEST', if(equals(int(coalesce(variables('varStatusCode'), 500)), 401), 'OTP_INVALID', if(equals(int(coalesce(variables('varStatusCode'), 500)), 404), 'OTP_NOT_FOUND', if(equals(int(coalesce(variables('varStatusCode'), 500)), 410), 'OTP_EXPIRED', concat('ERR', string(int(coalesce(variables('varStatusCode'), 500)))))))))",
    "message": "@if(less(int(coalesce(variables('varStatusCode'), 500)), 400), 'Completed successfully', if(equals(int(coalesce(variables('varStatusCode'), 500)), 400), 'Bad request', if(equals(int(coalesce(variables('varStatusCode'), 500)), 401), 'Invalid OTP', if(equals(int(coalesce(variables('varStatusCode'), 500)), 404), 'OTP not found', if(equals(int(coalesce(variables('varStatusCode'), 500)), 410), 'OTP expired', 'Failed')))))"
  },
  "confidence": "@and(less(int(coalesce(variables('varStatusCode'), 500)), 400), equals(int(coalesce(variables('varFailedCount'), 0)), 0), equals(length(coalesce(variables('varErrors'), json('[]'))), 0))",
  "request": {
    "requestId": "@if(empty(trim(string(coalesce(triggerBody()?['requestId'], '')))), variables('varRequestId'), trim(string(triggerBody()?['requestId'])))",
    "trackingId": "@trim(string(coalesce(triggerBody()?['trackingId'], triggerBody()?['trackingID'], '')))",
    "action": "@if(empty(trim(string(coalesce(triggerBody()?['action'], triggerBody()?['operation'], '')))), 'generate', toLower(trim(string(coalesce(triggerBody()?['action'], triggerBody()?['operation'])))))",
    "operation": "@if(empty(trim(string(coalesce(triggerBody()?['operation'], '')))), 'otp', trim(string(triggerBody()?['operation'])))",
    "mode": "@if(empty(trim(string(coalesce(triggerBody()?['mode'], '')))), 'single', trim(string(triggerBody()?['mode'])))",
    "requestedBy": "@if(empty(trim(string(coalesce(triggerBody()?['userEmail'], triggerBody()?['identifier'], triggerBody()?['id'], triggerBody()?['device']?['id'], '')))), 'unknown', trim(string(coalesce(triggerBody()?['userEmail'], triggerBody()?['identifier'], triggerBody()?['id'], triggerBody()?['device']?['id']))))",
    "source": "@if(empty(trim(string(coalesce(triggerBody()?['source'], '')))), 'http', trim(string(triggerBody()?['source'])))"
  },
  "timing": {
    "receivedAtUtc": "@coalesce(variables('varReceivedAtUtc'), variables('varCompletedAtUtc'), utcNow())",
    "completedAtUtc": "@coalesce(variables('varCompletedAtUtc'), utcNow())",
    "durationMs": "@int(coalesce(variables('varDurationMs'), 0))"
  },
  "data": "@coalesce(variables('varData'), json('{}'))",
  "results": "@coalesce(variables('varResults'), json('[]'))",
  "errors": "@coalesce(variables('varErrors'), json('[]'))",
  "meta": {
    "ts": "@coalesce(variables('varCompletedAtUtc'), utcNow())",
    "runId": "@workflow()?['run']?['name']",
    "flowName": "@workflow()?['name']",
    "contractVersion": "2026-07-15.otp.2"
  }
}
```

### `Scope_Global/Set_variable_varHTTPResponse`

| Field | As provisioned |
|---|---|
| Type | `SetVariable` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Compose__Standard_Response_Revised_Updated` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "name": "varHTTPResponse",
  "value": {
    "ok": "@less(int(coalesce(variables('varStatusCode'), 500)), 400)",
    "status": {
      "http": "@int(coalesce(variables('varStatusCode'), 500))",
      "code": "@if(less(int(coalesce(variables('varStatusCode'), 500)), 400), 'OK', if(equals(int(coalesce(variables('varStatusCode'), 500)), 400), 'BAD_REQUEST', if(equals(int(coalesce(variables('varStatusCode'), 500)), 401), 'OTP_INVALID', if(equals(int(coalesce(variables('varStatusCode'), 500)), 404), 'OTP_NOT_FOUND', if(equals(int(coalesce(variables('varStatusCode'), 500)), 410), 'OTP_EXPIRED', concat('ERR', string(int(coalesce(variables('varStatusCode'), 500)))))))))",
      "message": "@if(less(int(coalesce(variables('varStatusCode'), 500)), 400), 'Completed successfully', if(equals(int(coalesce(variables('varStatusCode'), 500)), 400), 'Bad request', if(equals(int(coalesce(variables('varStatusCode'), 500)), 401), 'Invalid OTP', if(equals(int(coalesce(variables('varStatusCode'), 500)), 404), 'OTP not found', if(equals(int(coalesce(variables('varStatusCode'), 500)), 410), 'OTP expired', 'Failed')))))"
    },
    "confidence": "@and(less(int(coalesce(variables('varStatusCode'), 500)), 400), equals(int(coalesce(variables('varFailedCount'), 0)), 0), equals(length(coalesce(variables('varErrors'), json('[]'))), 0))",
    "request": {
      "requestId": "@if(empty(trim(string(coalesce(triggerBody()?['requestId'], '')))), variables('varRequestId'), trim(string(triggerBody()?['requestId'])))",
      "trackingId": "@trim(string(coalesce(triggerBody()?['trackingId'], triggerBody()?['trackingID'], '')))",
      "action": "@if(empty(trim(string(coalesce(triggerBody()?['action'], triggerBody()?['operation'], '')))), 'generate', toLower(trim(string(coalesce(triggerBody()?['action'], triggerBody()?['operation'])))))",
      "operation": "@if(empty(trim(string(coalesce(triggerBody()?['operation'], '')))), 'otp', trim(string(triggerBody()?['operation'])))",
      "mode": "@if(empty(trim(string(coalesce(triggerBody()?['mode'], '')))), 'single', trim(string(triggerBody()?['mode'])))",
      "requestedBy": "@if(empty(trim(string(coalesce(triggerBody()?['userEmail'], triggerBody()?['identifier'], triggerBody()?['id'], triggerBody()?['device']?['id'], '')))), 'unknown', trim(string(coalesce(triggerBody()?['userEmail'], triggerBody()?['identifier'], triggerBody()?['id'], triggerBody()?['device']?['id']))))",
      "source": "@if(empty(trim(string(coalesce(triggerBody()?['source'], '')))), 'http', trim(string(triggerBody()?['source'])))"
    },
    "timing": {
      "receivedAtUtc": "@coalesce(variables('varReceivedAtUtc'), variables('varCompletedAtUtc'), utcNow())",
      "completedAtUtc": "@coalesce(variables('varCompletedAtUtc'), utcNow())",
      "durationMs": "@int(coalesce(variables('varDurationMs'), 0))"
    },
    "data": "@coalesce(variables('varData'), json('{}'))",
    "results": "@coalesce(variables('varResults'), json('[]'))",
    "errors": "@coalesce(variables('varErrors'), json('[]'))",
    "meta": {
      "ts": "@coalesce(variables('varCompletedAtUtc'), utcNow())",
      "runId": "@workflow()?['run']?['name']",
      "flowName": "@workflow()?['name']",
      "contractVersion": "2026-07-15.otp.2"
    }
  }
}
```

### `Initialize_variable_varFailed`

| Field | As provisioned |
|---|---|
| Type | `InitializeVariable` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Initialize_variable_varRequestId` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "variables": [
    {
      "name": "varFailedCount",
      "type": "integer",
      "value": 0
    }
  ]
}
```

### `Initialize_variable_varRequestId`

| Field | As provisioned |
|---|---|
| Type | `InitializeVariable` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Initialize_variable_varDurationMs` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "variables": [
    {
      "name": "varRequestId",
      "type": "string",
      "value": "@coalesce(triggerBody()?['requestId'], guid())"
    }
  ]
}
```

### `Initialize_variable_varReceivedAtUtc`

| Field | As provisioned |
|---|---|
| Type | `InitializeVariable` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Initialize_variable_varStartTicks` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "variables": [
    {
      "name": "varReceivedAtUtc",
      "type": "string",
      "value": "@utcNow()"
    }
  ]
}
```

### `Initialize_variable_varStartTicks`

| Field | As provisioned |
|---|---|
| Type | `InitializeVariable` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Initialize_variable_varResults` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "variables": [
    {
      "name": "varStartTicks",
      "type": "integer",
      "value": "@ticks(utcNow())"
    }
  ]
}
```

### `Initialize_variable_varResults`

| Field | As provisioned |
|---|---|
| Type | `InitializeVariable` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Initialize_variable_varErrors` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "variables": [
    {
      "name": "varResults",
      "type": "array",
      "value": []
    }
  ]
}
```

### `Initialize_variable_varErrors`

| Field | As provisioned |
|---|---|
| Type | `InitializeVariable` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Initialize_variable_varHTTPResponse` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "variables": [
    {
      "name": "varErrors",
      "type": "array",
      "value": []
    }
  ]
}
```

### `Initialize_variable_varData`

| Field | As provisioned |
|---|---|
| Type | `InitializeVariable` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Initialize_variable_varRandomOTP` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "variables": [
    {
      "name": "varData",
      "type": "string"
    }
  ]
}
```

### `Initialize_variable_varCompletedAtUtc`

| Field | As provisioned |
|---|---|
| Type | `InitializeVariable` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Initialize_variable_varReceivedAtUtc` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "variables": [
    {
      "name": "varCompletedAtUtc",
      "type": "string"
    }
  ]
}
```

### `Initialize_variable_varDurationMs`

| Field | As provisioned |
|---|---|
| Type | `InitializeVariable` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Initialize_variable_varCompletedAtUtc` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "variables": [
    {
      "name": "varDurationMs",
      "type": "integer"
    }
  ]
}
```

### `Compose`

| Field | As provisioned |
|---|---|
| Type | `Compose` |
| Kind | — |
| Description | _not declared_ |
| Runs after | _first in its scope_ |

**`inputs`, verbatim:**

```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "title": "Standardized Trigger Contract",
  "description": "Self-contained HTTP trigger schema. Includes the standard shared trigger fields and the flow-specific field.",
  "properties": {
    "action": {
      "type": [
        "string",
        "null"
      ],
      "minLength": 1,
      "description": "The high-level action to perform."
    },
    "operation": {
      "type": [
        "string",
        "null"
      ],
      "minLength": 1,
      "description": "The specific operation name."
    },
    "mode": {
      "type": [
        "string",
        "null"
      ],
      "description": "Execution mode for the request."
    },
    "source": {
      "type": [
        "string",
        "null"
      ],
      "description": "Source system initiating the request."
    },
    "userEmail": {
      "type": [
        "string",
        "null"
      ],
      "format": "email",
      "description": "The initiating user's email address."
    },
    "method": {
      "type": [
        "string",
        "null"
      ],
      "description": "Request or execution method."
    },
    "id": {
      "type": [
        "string",
        "integer",
        "null"
      ],
      "description": "Primary identifier associated with the request."
    },
    "device": {
      "type": [
        "object",
        "null"
      ],
      "description": "Device information related to the request.",
      "properties": {
        "id": {
          "type": [
            "string",
            "integer",
            "null"
          ],
          "description": "Device identifier."
        }
      },
      "additionalProperties": true
    },
    "otp_code": {
      "type": "string",
      "description": "Required only for verification"
    }
  },
  "required": [],
  "additionalProperties": true,
  "x-backward-compatible": true,
  "x-compatibility-note": "Shared and flow-specific trigger fields are fully built into this single schema without external references."
}
```

### `Compose_1`

| Field | As provisioned |
|---|---|
| Type | `Compose` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Compose` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "type": "object",
  "properties": {
    "action": {
      "type": "string",
      "description": "Either 'generate' or 'verify'"
    },
    "identifier": {
      "type": "string",
      "description": "Email address or Phone number"
    },
    "otp_code": {
      "type": "string",
      "description": "Required only for verification"
    }
  },
  "required": [
    "action",
    "identifier"
  ]
}
```

### `Initialize_variable_varHTTPResponse`

| Field | As provisioned |
|---|---|
| Type | `InitializeVariable` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Initialize_variable_varData` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "variables": [
    {
      "name": "varHTTPResponse",
      "type": "object"
    }
  ]
}
```

