# Web - Send Email

> **deployed export — the live tenant.** Every value below is read from
> [`docs/reference/flow-contracts/deployed/Web - Send Email__e5e2c6a2-52ea-2ba8-b0b3-60f25d43387f__full_definition.json`](../../flow-contracts/deployed/Web - Send Email__e5e2c6a2-52ea-2ba8-b0b3-60f25d43387f__full_definition.json) and printed as that package carries it.
> Generated by `scripts/build-provisioning-reference.mjs` — do not edit by hand.

## 1. Identity

| Field | As provisioned |
|---|---|
| Display name | `Web - Send Email` |
| Internal name | `e5e2c6a2-52ea-2ba8-b0b3-60f25d43387f` |
| Workflow id | _not carried by this package_ |
| Environment | `Default-ca6a4b3f-9123-49bc-bcb9-27085ebbf1a1` |
| Full resource id | `null` — the export records the field and leaves it null |
| Estate | Internal platform — no portal register names this flow |
| Package family | deployed export — the live tenant |
| Exported at (UTC) | `2026-08-19T06:08:40Z` |
| Exported by | `scripts/export-power-automate-flows.ps1` |
| Source package | `docs/reference/flow-contracts/deployed/Web - Send Email__e5e2c6a2-52ea-2ba8-b0b3-60f25d43387f__full_definition.json` |

## 2. Definition envelope

| Field | As provisioned |
|---|---|
| `$schema` | `https://schema.management.azure.com/providers/Microsoft.Logic/schemas/2016-06-01/workflowdefinition.json#` |
| `contentVersion` | `1.0.0.0` |
| `description` | _not declared_ |
| `outputs` | _not declared_ |
| Actions | 15 |

## 3. Definition parameters

| Parameter | Type | Default value |
|---|---|---|
| `$authentication` | `SecureObject` | `{}` |
| `$connections` | `Object` | `{}` |

## 4. Connections and references

**Connections the actions are bound to**, read from each action's `inputs.host`:

| Connection name | API id | Reference name | Operations used |
|---|---|---|---|
| `shared_office365` | `/providers/Microsoft.PowerApps/apis/shared_office365` | — | `SendEmailV2`×1 |
| `shared_office365-1` | `/providers/Microsoft.PowerApps/apis/shared_office365` | — | `SendEmailV2`×2 |

## 5. Trigger — the endpoint this flow exposes

| Field | As provisioned |
|---|---|
| Trigger name | `manual` |
| `type` | `Request` |
| `kind` | `Http` |
| `method` | _not declared_ |
| `relativePath` | _not declared — the trigger accepts the bare invoke path_ |
| `triggerAuthenticationType` | `Tenant` |
| `operationOptions` | _not declared_ |
| `conditions` | _not declared_ |
| `splitOn` | _not declared_ |
| `correlation` | _not declared_ |
| `recurrence` | _not declared_ |
| Declared request headers | _not declared_ |
| Declared query parameters | _not declared_ |
| `description` | _not declared_ |

### 5.1 Request payload — declared properties

_The trigger declares no schema. Any JSON body is accepted and nothing is validated at
the boundary._

## 6. Responses returned to the caller

| Action | Status code | Runs after | On statuses |
|---|---|---|---|
| `Response` | `200` | `Compose_Response` | Succeeded |
| `Response_1` | `200` | `Send_an_email_(V2)` | Succeeded |

### 6.1 `Response`

| Field | As provisioned |
|---|---|
| `statusCode` | `200` |
| `operationOptions` | _not declared_ |

| Header | Configured value |
|---|---|
| `Access-Control-Allow-Origin` | `*` |
| `Access-Control-Allow-Methods` | `GET, POST, OPTIONS` |
| `Access-Control-Allow-Headers` | `Content-Type` |
| `Content-Type` | `application/json` |

**Response body, verbatim:**

```json
"@outputs('Compose_Response')"
```

### 6.2 `Response_1`

| Field | As provisioned |
|---|---|
| `statusCode` | `200` |
| `operationOptions` | _not declared_ |

| Header | Configured value |
|---|---|
| `Access-Control-Allow-Origin` | `*` |
| `Access-Control-Allow-Methods` | `GET, POST, OPTIONS` |
| `Access-Control-Allow-Headers` | `Content-Type` |
| `Content-Type` | `application/json` |

**Response body, verbatim:**

```json
"@outputs('Compose_Response_to_HTML')"
```

## 7. Termination

| Action | `runStatus` | Error code | Error message | Runs after |
|---|---|---|---|---|
| `Terminate` | `Succeeded` | — | — | `Response_1` (Succeeded) |

## 8. Error handling and run-time configuration

_No action is wired to run on `Failed`, `Skipped` or `TimedOut`. Every dependency in
this flow is a success edge._

_No action carries a retry policy, secure-data setting, operation option, tracked
property or loop limit. Every call runs on the platform defaults._

## 9. Variables

**Declared, with the initial value each is provisioned with:**

| Variable | Type | Initial value | Declared by |
|---|---|---|---|
| `varEmailAttachments` | `array` | _no initial value_ | `Initialize_variable` |

**Every write, with the expression or literal assigned:**

| Action | Operation | Variable | Assigned value |
|---|---|---|---|
| `Condition_Check_for_Attachments/else/Apply_to_each/Append_to_array_variable_Attachments` | `AppendToArrayVariable` | `varEmailAttachments` | `@outputs('Compose_Attachments')` |

## 10. SharePoint operations

_This package performs no SharePoint operation._

## 11. Outbound HTTP calls

_This package makes no raw HTTP call._

## 12. Action dependency register

Every action in the package, in graph order, with the edge that starts it. `↳` marks
nesting depth; the path is the address used everywhere else on this page.

| Action path | Type | Kind | Runs after | On statuses |
|---|---|---|---|---|
| `Compose_Trigger_Body` | `Compose` | — | _first in its scope_ | — |
| `Response` | `Response` | `Http` | `Compose_Response` | Succeeded |
| `Compose_Response` | `Compose` | — | `Condition_Check_for_Attachments` | Succeeded |
| `Initialize_variable` | `InitializeVariable` | — | `Compose_Trigger_Body` | Succeeded |
| `Condition_Check_for_Attachments` | `If` | — | `Terminate` | Succeeded |
| ↳`Condition_Check_for_Attachments/Send_an_email_(V2)_2` | `OpenApiConnection` | — | _first in its scope_ | — |
| ↳↳`Condition_Check_for_Attachments/else/Apply_to_each` | `Foreach` | — | _first in its scope_ | — |
| ↳↳↳`Condition_Check_for_Attachments/else/Apply_to_each/Compose_Attachments` | `Compose` | — | _first in its scope_ | — |
| ↳↳↳`Condition_Check_for_Attachments/else/Apply_to_each/Append_to_array_variable_Attachments` | `AppendToArrayVariable` | — | `Compose_Attachments` | Succeeded |
| ↳↳`Condition_Check_for_Attachments/else/Send_an_email_(V2)_1` | `OpenApiConnection` | — | `Apply_to_each` | Succeeded |
| `Compose._Acknowledgement` | `Compose` | — | `Initialize_variable` | Succeeded |
| `Compose_Response_to_HTML` | `Compose` | — | `Compose._Acknowledgement` | Succeeded |
| `Send_an_email_(V2)` | `OpenApiConnection` | — | `Compose_Response_to_HTML` | Succeeded |
| `Response_1` | `Response` | `Http` | `Send_an_email_(V2)` | Succeeded |
| `Terminate` | `Terminate` | — | `Response_1` | Succeeded |

## 13. Configured values, action by action

The configured input of every action, verbatim. An action with no `inputs` key — a
`Scope`, an `If`, a `Foreach` — is documented by its control expression instead.

### `Compose_Trigger_Body`

| Field | As provisioned |
|---|---|
| Type | `Compose` |
| Kind | — |
| Description | _not declared_ |
| Runs after | _first in its scope_ |

**`inputs`, verbatim:**

```json
"@triggerBody()"
```

### `Response`

| Field | As provisioned |
|---|---|
| Type | `Response` |
| Kind | `Http` |
| Description | _not declared_ |
| Runs after | `Compose_Response` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "statusCode": 200,
  "headers": {
    "Access-Control-Allow-Origin": "*",
    "Access-Control-Allow-Methods": "GET, POST, OPTIONS",
    "Access-Control-Allow-Headers": "Content-Type",
    "Content-Type": "application/json"
  },
  "body": "@outputs('Compose_Response')"
}
```

### `Compose_Response`

| Field | As provisioned |
|---|---|
| Type | `Compose` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Condition_Check_for_Attachments` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "status": "success",
  "message": "Email sent successfully",
  "itemID": "",
  "to": "",
  "cc": "",
  "bcc": "",
  "subject": "",
  "importance": "",
  "iAttachmentsCount": ""
}
```

### `Initialize_variable`

| Field | As provisioned |
|---|---|
| Type | `InitializeVariable` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Compose_Trigger_Body` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "variables": [
    {
      "name": "varEmailAttachments",
      "type": "array"
    }
  ]
}
```

### `Condition_Check_for_Attachments`

| Field | As provisioned |
|---|---|
| Type | `If` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Terminate` (Succeeded) |

_This action declares no `inputs`._

**`expression`, verbatim:**

```json
{
  "and": [
    {
      "equals": [
        "@empty(triggerBody()?['attachments'])",
        false
      ]
    }
  ]
}
```

### `Condition_Check_for_Attachments/Send_an_email_(V2)_2`

| Field | As provisioned |
|---|---|
| Type | `OpenApiConnection` |
| Kind | — |
| Description | _not declared_ |
| Runs after | _first in its scope_ |
| Connector | `shared_office365-1` |
| Operation | `SendEmailV2` |

**`inputs`, verbatim:**

```json
{
  "parameters": {
    "emailMessage/To": "dgsRegistry@nitda.gov.ng",
    "emailMessage/Subject": "@outputs('Compose._Acknowledgement')",
    "emailMessage/Body": "<p class=\"editor-paragraph\">@{outputs('Compose_Response_to_HTML')}</p>"
  },
  "host": {
    "apiId": "/providers/Microsoft.PowerApps/apis/shared_office365",
    "connectionName": "shared_office365-1",
    "operationId": "SendEmailV2"
  },
  "authentication": "@parameters('$authentication')"
}
```

### `Condition_Check_for_Attachments/else/Apply_to_each`

| Field | As provisioned |
|---|---|
| Type | `Foreach` |
| Kind | — |
| Description | _not declared_ |
| Runs after | _first in its scope_ |

_This action declares no `inputs`._

**`foreach`, verbatim:**

```json
"@triggerBody()"
```

### `Condition_Check_for_Attachments/else/Apply_to_each/Compose_Attachments`

| Field | As provisioned |
|---|---|
| Type | `Compose` |
| Kind | — |
| Description | _not declared_ |
| Runs after | _first in its scope_ |

**`inputs`, verbatim:**

```json
{
  "fileName": "Sendemailflow.txt",
  "ContentBytes": "@outputs('Compose_Response_to_HTML')"
}
```

### `Condition_Check_for_Attachments/else/Apply_to_each/Append_to_array_variable_Attachments`

| Field | As provisioned |
|---|---|
| Type | `AppendToArrayVariable` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Compose_Attachments` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "name": "varEmailAttachments",
  "value": "@outputs('Compose_Attachments')"
}
```

### `Condition_Check_for_Attachments/else/Send_an_email_(V2)_1`

| Field | As provisioned |
|---|---|
| Type | `OpenApiConnection` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Apply_to_each` (Succeeded) |
| Connector | `shared_office365-1` |
| Operation | `SendEmailV2` |

**`inputs`, verbatim:**

```json
{
  "parameters": {
    "emailMessage/To": "dgsRegistry@nitda.gov.ng",
    "emailMessage/Subject": "@outputs('Compose._Acknowledgement')",
    "emailMessage/Body": "@outputs('Compose_Response_to_HTML')",
    "emailMessage/Attachments": "@variables('varEmailAttachments')"
  },
  "host": {
    "apiId": "/providers/Microsoft.PowerApps/apis/shared_office365",
    "connectionName": "shared_office365-1",
    "operationId": "SendEmailV2"
  },
  "authentication": "@parameters('$authentication')"
}
```

### `Compose._Acknowledgement`

| Field | As provisioned |
|---|---|
| Type | `Compose` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Initialize_variable` (Succeeded) |

**`inputs`, verbatim:**

```json
"Xxx"
```

### `Compose_Response_to_HTML`

| Field | As provisioned |
|---|---|
| Type | `Compose` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Compose._Acknowledgement` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "message": "Success"
}
```

### `Send_an_email_(V2)`

| Field | As provisioned |
|---|---|
| Type | `OpenApiConnection` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Compose_Response_to_HTML` (Succeeded) |
| Connector | `shared_office365` |
| Operation | `SendEmailV2` |

**`inputs`, verbatim:**

```json
{
  "parameters": {
    "emailMessage/To": "dgsRegistry@nitda.gov.ng",
    "emailMessage/Subject": "Acknowledgement - ",
    "emailMessage/Body": "@outputs('Compose._Acknowledgement')",
    "emailMessage/Importance": "Normal"
  },
  "host": {
    "apiId": "/providers/Microsoft.PowerApps/apis/shared_office365",
    "connectionName": "shared_office365",
    "operationId": "SendEmailV2"
  },
  "authentication": "@parameters('$authentication')"
}
```

### `Response_1`

| Field | As provisioned |
|---|---|
| Type | `Response` |
| Kind | `Http` |
| Description | _not declared_ |
| Runs after | `Send_an_email_(V2)` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "statusCode": 200,
  "headers": {
    "Access-Control-Allow-Origin": "*",
    "Access-Control-Allow-Methods": "GET, POST, OPTIONS",
    "Access-Control-Allow-Headers": "Content-Type",
    "Content-Type": "application/json"
  },
  "body": "@outputs('Compose_Response_to_HTML')"
}
```

### `Terminate`

| Field | As provisioned |
|---|---|
| Type | `Terminate` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Response_1` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "runStatus": "Succeeded"
}
```

