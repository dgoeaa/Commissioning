# Get Emails HTTP

> **deployed export — the live tenant.** Every value below is read from
> [`docs/reference/flow-contracts/deployed/Get Emails HTTP__e209e8a5-6f31-5664-f40d-85c58c43efbd__full_definition.json`](../../flow-contracts/deployed/Get Emails HTTP__e209e8a5-6f31-5664-f40d-85c58c43efbd__full_definition.json) and printed as that package carries it.
> Generated by `scripts/build-provisioning-reference.mjs` — do not edit by hand.

## 1. Identity

| Field | As provisioned |
|---|---|
| Display name | `Get Emails HTTP` |
| Internal name | `e209e8a5-6f31-5664-f40d-85c58c43efbd` |
| Workflow id | _not carried by this package_ |
| Environment | `Default-ca6a4b3f-9123-49bc-bcb9-27085ebbf1a1` |
| Full resource id | `null` — the export records the field and leaves it null |
| Estate | Internal platform — no portal register names this flow |
| Package family | deployed export — the live tenant |
| Exported at (UTC) | `2026-08-19T06:09:24Z` |
| Exported by | `scripts/export-power-automate-flows.ps1` |
| Source package | `docs/reference/flow-contracts/deployed/Get Emails HTTP__e209e8a5-6f31-5664-f40d-85c58c43efbd__full_definition.json` |

## 2. Definition envelope

| Field | As provisioned |
|---|---|
| `$schema` | `https://schema.management.azure.com/providers/Microsoft.Logic/schemas/2016-06-01/workflowdefinition.json#` |
| `contentVersion` | `1.0.0.0` |
| `description` | _not declared_ |
| `outputs` | _not declared_ |
| Actions | 12 |

## 3. Definition parameters

| Parameter | Type | Default value |
|---|---|---|
| `$connections` | `Object` | `{}` |
| `$authentication` | `SecureObject` | `{}` |

## 4. Connections and references

**Connections the actions are bound to**, read from each action's `inputs.host`:

| Connection name | API id | Reference name | Operations used |
|---|---|---|---|
| `shared_office365` | `/providers/Microsoft.PowerApps/apis/shared_office365` | — | `HttpRequest`×2 |

## 5. Trigger — the endpoint this flow exposes

| Field | As provisioned |
|---|---|
| Trigger name | `manual` |
| `type` | `Request` |
| `kind` | `PowerAppV2` |
| `method` | _not declared_ |
| `relativePath` | _not declared — the trigger accepts the bare invoke path_ |
| `triggerAuthenticationType` | _absent from the export — no posture is recorded_ |
| `operationOptions` | _not declared_ |
| `conditions` | _not declared_ |
| `splitOn` | _not declared_ |
| `correlation` | _not declared_ |
| `recurrence` | _not declared_ |
| Declared request headers | _not declared_ |
| Declared query parameters | _not declared_ |
| `description` | _not declared_ |

### 5.1 Request payload — declared properties

_The trigger declares a schema with no properties._

### 5.2 Request payload — schema as provisioned

```json
{
  "type": "object",
  "properties": {},
  "required": []
}
```

## 6. Responses returned to the caller

| Action | Status code | Runs after | On statuses |
|---|---|---|---|
| `Respond_to_a_Power_App_or_flow` | `200` | `Compose_Merge_Inbox_and_Sent_Items` | Succeeded |

### 6.1 `Respond_to_a_Power_App_or_flow`

| Field | As provisioned |
|---|---|
| `statusCode` | `200` |
| `operationOptions` | _not declared_ |

_No response headers are configured._

**Response body, verbatim:**

```json
{
  "emaildata": "@{string(outputs('Compose_JSON_Select_Output'))}"
}
```

**Declared response schema, verbatim:**

```json
{
  "type": "object",
  "properties": {
    "emaildata": {
      "title": "emaildata",
      "x-ms-dynamically-added": true,
      "type": "string"
    }
  }
}
```

## 7. Termination

_No `Terminate` action. The run ends when the action graph does._

## 8. Error handling and run-time configuration

**Actions wired to run on something other than success.** These are the flow's catch
paths — an action that runs after `Failed`, `Skipped` or `TimedOut` is error handling,
whatever it is named.

| Action | Type | Runs after | On statuses |
|---|---|---|---|
| `Compose_JSON_Select_Output` | `Compose` | `Select_Parse_JSON_Inbox`, `Select_Parse_JSON_Sent_Items` | Succeeded, Skipped, Failed, TimedOut · Succeeded, Failed, Skipped, TimedOut |

_No action carries a retry policy, secure-data setting, operation option, tracked
property or loop limit. Every call runs on the platform defaults._

## 9. Variables

_This package initialises no variables._

## 10. SharePoint operations

_This package performs no SharePoint operation._

## 11. Outbound HTTP calls

_This package makes no raw HTTP call._

## 12. Action dependency register

Every action in the package, in graph order, with the edge that starts it. `↳` marks
nesting depth; the path is the address used everywhere else on this page.

| Action path | Type | Kind | Runs after | On statuses |
|---|---|---|---|---|
| `Send_an_HTTP_request_Sent_Items` | `OpenApiConnection` | — | `Compose_URL_Query_String_Sent_Items` | Succeeded |
| `Parse_JSON_HTTP_Response_Sent_Items` | `ParseJson` | — | `Send_an_HTTP_request_Sent_Items` | Succeeded |
| `Select_Parse_JSON_Sent_Items` | `Select` | — | `Parse_JSON_HTTP_Response_Sent_Items` | Succeeded |
| `Compose_HTTP_Request_URL_2` | `Compose` | — | _first in its scope_ | — |
| `Send_an_HTTP_request_Inbox` | `OpenApiConnection` | — | `Compose_URL_Query_String_Inbox` | Succeeded |
| `Parse_JSON_HTTP_Response_Inbox` | `ParseJson` | — | `Send_an_HTTP_request_Inbox` | Succeeded |
| `Compose_URL_Query_String_Sent_Items` | `Compose` | — | `Compose_HTTP_Request_URL_2` | Succeeded |
| `Compose_URL_Query_String_Inbox` | `Compose` | — | `Compose_HTTP_Request_URL_2` | Succeeded |
| `Select_Parse_JSON_Inbox` | `Select` | — | `Parse_JSON_HTTP_Response_Inbox` | Succeeded |
| `Respond_to_a_Power_App_or_flow` | `Response` | `PowerApp` | `Compose_Merge_Inbox_and_Sent_Items` | Succeeded |
| `Compose_Merge_Inbox_and_Sent_Items` | `Compose` | — | `Compose_JSON_Select_Output` | Succeeded |
| `Compose_JSON_Select_Output` | `Compose` | — | `Select_Parse_JSON_Inbox`, `Select_Parse_JSON_Sent_Items` | Succeeded/Skipped/Failed/TimedOut · Succeeded/Failed/Skipped/TimedOut |

## 13. Configured values, action by action

The configured input of every action, verbatim. An action with no `inputs` key — a
`Scope`, an `If`, a `Foreach` — is documented by its control expression instead.

### `Send_an_HTTP_request_Sent_Items`

| Field | As provisioned |
|---|---|
| Type | `OpenApiConnection` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Compose_URL_Query_String_Sent_Items` (Succeeded) |
| Connector | `shared_office365` |
| Operation | `HttpRequest` |

**`inputs`, verbatim:**

```json
{
  "host": {
    "apiId": "/providers/Microsoft.PowerApps/apis/shared_office365",
    "connectionName": "shared_office365",
    "operationId": "HttpRequest"
  },
  "parameters": {
    "Uri": "@outputs('Compose_URL_Query_String_Sent_Items')",
    "Method": "GET",
    "ContentType": "application/json"
  },
  "authentication": {
    "value": "@json(decodeBase64(triggerOutputs().headers['X-MS-APIM-Tokens']))['$ConnectionKey']",
    "type": "Raw"
  }
}
```

### `Parse_JSON_HTTP_Response_Sent_Items`

| Field | As provisioned |
|---|---|
| Type | `ParseJson` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Send_an_HTTP_request_Sent_Items` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "content": "@body('Send_an_HTTP_request_Sent_Items')",
  "schema": {
    "type": "object",
    "properties": {
      "@@odata.context": {
        "type": "string"
      },
      "value": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "@@odata.etag": {
              "type": "string"
            },
            "id": {
              "type": "string"
            },
            "receivedDateTime": {
              "type": "string"
            },
            "hasAttachments": {
              "type": "boolean"
            },
            "subject": {
              "type": "string"
            },
            "bodyPreview": {
              "type": "string"
            },
            "importance": {
              "type": "string"
            },
            "webLink": {
              "type": "string"
            },
            "body": {
              "type": "object",
              "properties": {
                "contentType": {
                  "type": "string"
                },
                "content": {
                  "type": "string"
                }
              }
            },
            "from": {
              "type": "object",
              "properties": {
                "emailAddress": {
                  "type": "object",
                  "properties": {
                    "name": {
                      "type": "string"
                    },
                    "address": {
                      "type": "string"
                    }
                  }
                }
              }
            },
            "toRecipients": {
              "type": "array",
              "items": {
                "type": "object",
                "properties": {
                  "emailAddress": {
                    "type": "object",
                    "properties": {
                      "name": {
                        "type": "string"
                      },
                      "address": {
                        "type": "string"
                      }
                    }
                  }
                },
                "required": [
                  "emailAddress"
                ]
              }
            },
            "ccRecipients": {
              "type": "array",
              "items": {}
            },
            "bccRecipients": {
              "type": "array",
              "items": {}
            }
          },
          "required": [
            "@@odata.etag",
            "id",
            "receivedDateTime",
            "hasAttachments",
            "subject",
            "bodyPreview",
            "importance",
            "webLink",
            "body",
            "from",
            "toRecipients",
            "ccRecipients",
            "bccRecipients"
          ]
        }
      },
      "@@odata.nextLink": {
        "type": "string"
      }
    }
  }
}
```

### `Select_Parse_JSON_Sent_Items`

| Field | As provisioned |
|---|---|
| Type | `Select` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Parse_JSON_HTTP_Response_Sent_Items` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "from": "@body('Parse_JSON_HTTP_Response_Sent_Items')?['value']",
  "select": {
    "id": "@item()?['id']",
    "receivedDateTime": "@item()?['receivedDateTime']",
    "hasAttachments": "@item()?['hasAttachments']",
    "subject": "@item()?['subject']",
    "bodyPreview": "@item()?['bodyPreview']",
    "importance": "@item()?['importance']",
    "webLink": "@item()?['webLink']",
    "bodyContent": "@item()?['body']?['content']",
    "from": "@item()?['from']?['emailAddress']?['name']",
    "fromAddress": "@item()?['from']?['emailAddress']?['name']",
    "toRecipients": "\"\"",
    "ccRecipients": "\"\"",
    "bccRecipients": "\"\"",
    "attachments": "\"\"",
    "internetMessageId": "\"\"",
    "conversationId": "\"\"",
    "body": "@item()?['body']?['content']"
  }
}
```

### `Compose_HTTP_Request_URL_2`

| Field | As provisioned |
|---|---|
| Type | `Compose` |
| Kind | — |
| Description | _not declared_ |
| Runs after | _first in its scope_ |

**`inputs`, verbatim:**

```json
"https://graph.microsoft.com/v1.0/me/mailFolders/Inbox/messages?$select=subject,from,receivedDateTime,bodyPreview,body,toRecipients,ccRecipients,bccRecipients,hasAttachments,importance,webLink&$filter=isRead eq false AND receivedDateTime ge @{startOfDay(utcNow())} AND receivedDateTime lt @{addDays(startOfDay(utcNow()), 1)}&$top=1000"
```

### `Send_an_HTTP_request_Inbox`

| Field | As provisioned |
|---|---|
| Type | `OpenApiConnection` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Compose_URL_Query_String_Inbox` (Succeeded) |
| Connector | `shared_office365` |
| Operation | `HttpRequest` |

**`inputs`, verbatim:**

```json
{
  "host": {
    "apiId": "/providers/Microsoft.PowerApps/apis/shared_office365",
    "connectionName": "shared_office365",
    "operationId": "HttpRequest"
  },
  "parameters": {
    "Uri": "@outputs('Compose_URL_Query_String_Inbox')",
    "Method": "GET",
    "ContentType": "application/json"
  },
  "authentication": {
    "value": "@json(decodeBase64(triggerOutputs().headers['X-MS-APIM-Tokens']))['$ConnectionKey']",
    "type": "Raw"
  }
}
```

### `Parse_JSON_HTTP_Response_Inbox`

| Field | As provisioned |
|---|---|
| Type | `ParseJson` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Send_an_HTTP_request_Inbox` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "content": "@body('Send_an_HTTP_request_Inbox')",
  "schema": {
    "type": "object",
    "properties": {
      "@@odata.context": {
        "type": "string"
      },
      "value": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "@@odata.etag": {
              "type": "string"
            },
            "id": {
              "type": "string"
            },
            "receivedDateTime": {
              "type": "string"
            },
            "hasAttachments": {
              "type": "boolean"
            },
            "subject": {
              "type": "string"
            },
            "bodyPreview": {
              "type": "string"
            },
            "importance": {
              "type": "string"
            },
            "webLink": {
              "type": "string"
            },
            "body": {
              "type": "object",
              "properties": {
                "contentType": {
                  "type": "string"
                },
                "content": {
                  "type": "string"
                }
              }
            },
            "from": {
              "type": "object",
              "properties": {
                "emailAddress": {
                  "type": "object",
                  "properties": {
                    "name": {
                      "type": "string"
                    },
                    "address": {
                      "type": "string"
                    }
                  }
                }
              }
            },
            "toRecipients": {
              "type": "array",
              "items": {
                "type": "object",
                "properties": {
                  "emailAddress": {
                    "type": "object",
                    "properties": {
                      "name": {
                        "type": "string"
                      },
                      "address": {
                        "type": "string"
                      }
                    }
                  }
                },
                "required": [
                  "emailAddress"
                ]
              }
            },
            "ccRecipients": {
              "type": "array",
              "items": {}
            },
            "bccRecipients": {
              "type": "array",
              "items": {}
            }
          },
          "required": [
            "@@odata.etag",
            "id",
            "receivedDateTime",
            "hasAttachments",
            "subject",
            "bodyPreview",
            "importance",
            "webLink",
            "body",
            "from",
            "toRecipients",
            "ccRecipients",
            "bccRecipients"
          ]
        }
      },
      "@@odata.nextLink": {
        "type": "string"
      }
    }
  }
}
```

### `Compose_URL_Query_String_Sent_Items`

| Field | As provisioned |
|---|---|
| Type | `Compose` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Compose_HTTP_Request_URL_2` (Succeeded) |

**`inputs`, verbatim:**

```json
"https://graph.microsoft.com/v1.0/me/mailFolders/SentItems/messages?$select=subject,from,receivedDateTime,bodyPreview,body,toRecipients,ccRecipients,bccRecipients,hasAttachments,importance,webLink&$filter=receivedDateTime lt @{addDays(startOfDay(utcNow()), 1)}&$top=1000"
```

### `Compose_URL_Query_String_Inbox`

| Field | As provisioned |
|---|---|
| Type | `Compose` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Compose_HTTP_Request_URL_2` (Succeeded) |

**`inputs`, verbatim:**

```json
"https://graph.microsoft.com/v1.0/me/mailFolders/Inbox/messages?$select=subject,from,receivedDateTime,bodyPreview,body,toRecipients,ccRecipients,bccRecipients,hasAttachments,importance,webLink&$filter=receivedDateTime lt @{addDays(startOfDay(utcNow()), 1)}&$top=1000"
```

### `Select_Parse_JSON_Inbox`

| Field | As provisioned |
|---|---|
| Type | `Select` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Parse_JSON_HTTP_Response_Inbox` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "from": "@body('Parse_JSON_HTTP_Response_Inbox')?['value']",
  "select": {
    "id": "@item()?['id']",
    "receivedDateTime": "@item()?['receivedDateTime']",
    "hasAttachments": "@item()?['hasAttachments']",
    "subject": "@item()?['subject']",
    "bodyPreview": "@item()?['bodyPreview']",
    "importance": "@item()?['importance']",
    "webLink": "@item()?['webLink']",
    "bodyContent": "@item()?['body']?['content']",
    "from": "@item()?['from']?['emailAddress']?['name']",
    "fromAddress": "@item()?['from']?['emailAddress']?['name']",
    "toRecipients": "\"\"",
    "ccRecipients": "\"\"",
    "bccRecipients": "\"\"",
    "attachments": "\"\"",
    "internetMessageId": "\"\"",
    "conversationId": "\"\"",
    "body": "@item()?['body']?['content']"
  }
}
```

### `Respond_to_a_Power_App_or_flow`

| Field | As provisioned |
|---|---|
| Type | `Response` |
| Kind | `PowerApp` |
| Description | _not declared_ |
| Runs after | `Compose_Merge_Inbox_and_Sent_Items` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "statusCode": 200,
  "body": {
    "emaildata": "@{string(outputs('Compose_JSON_Select_Output'))}"
  },
  "schema": {
    "type": "object",
    "properties": {
      "emaildata": {
        "title": "emaildata",
        "x-ms-dynamically-added": true,
        "type": "string"
      }
    }
  }
}
```

### `Compose_Merge_Inbox_and_Sent_Items`

| Field | As provisioned |
|---|---|
| Type | `Compose` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Compose_JSON_Select_Output` (Succeeded) |

**`inputs`, verbatim:**

```json
"@concat(body('Select_Parse_JSON_Inbox'),body('Select_Parse_JSON_Sent_Items'))"
```

### `Compose_JSON_Select_Output`

| Field | As provisioned |
|---|---|
| Type | `Compose` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Select_Parse_JSON_Inbox` (Succeeded/Skipped/Failed/TimedOut), `Select_Parse_JSON_Sent_Items` (Succeeded/Failed/Skipped/TimedOut) |

**`inputs`, verbatim:**

```json
"@union(body('Select_Parse_JSON_Inbox'),body('Select_Parse_JSON_Sent_Items'))"
```

