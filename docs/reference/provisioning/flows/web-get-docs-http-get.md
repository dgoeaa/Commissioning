# WEB Get Docs  HTTP GET

> **deployed export — the live tenant.** Every value below is read from
> [`docs/reference/flow-contracts/deployed/WEB Get Docs  HTTP GET__d4907f18-4dba-3f06-28de-2ed24f5f7936__full_definition.json`](../../flow-contracts/deployed/WEB Get Docs  HTTP GET__d4907f18-4dba-3f06-28de-2ed24f5f7936__full_definition.json) and printed as that package carries it.
> Generated by `scripts/build-provisioning-reference.mjs` — do not edit by hand.

## 1. Identity

| Field | As provisioned |
|---|---|
| Display name | `WEB Get Docs  HTTP GET` |
| Internal name | `d4907f18-4dba-3f06-28de-2ed24f5f7936` |
| Workflow id | _not carried by this package_ |
| Environment | `Default-ca6a4b3f-9123-49bc-bcb9-27085ebbf1a1` |
| Full resource id | `null` — the export records the field and leaves it null |
| Estate | Internal platform — no portal register names this flow |
| Package family | deployed export — the live tenant |
| Exported at (UTC) | `2026-08-19T06:09:14Z` |
| Exported by | `scripts/export-power-automate-flows.ps1` |
| Source package | `docs/reference/flow-contracts/deployed/WEB Get Docs  HTTP GET__d4907f18-4dba-3f06-28de-2ed24f5f7936__full_definition.json` |

**Endpoint contract keys attributed to this flow** by
`docs/reference/internal-flow-register.json`:

| Contract key | Physical flow | Fixed action | Workflow id |
|---|---|---|---|
| `GET_DOCS` | `GET_DOCS` | `getDocs` | `818ec4053f1e4f0b87845114241d8b74` |

## 2. Definition envelope

| Field | As provisioned |
|---|---|
| `$schema` | `https://schema.management.azure.com/providers/Microsoft.Logic/schemas/2016-06-01/workflowdefinition.json#` |
| `contentVersion` | `1.0.0.0` |
| `description` | _not declared_ |
| `outputs` | _not declared_ |
| Actions | 11 |

## 3. Definition parameters

| Parameter | Type | Default value |
|---|---|---|
| `$authentication` | `SecureObject` | `{}` |
| `$connections` | `Object` | `{}` |

## 4. Connections and references

**Connections the actions are bound to**, read from each action's `inputs.host`:

| Connection name | API id | Reference name | Operations used |
|---|---|---|---|
| `shared_office365-1` | `/providers/Microsoft.PowerApps/apis/shared_office365` | — | `SendEmailV2`×1 |
| `shared_sharepointonline` | `/providers/Microsoft.PowerApps/apis/shared_sharepointonline` | — | `HttpRequest`×1 |

## 5. Trigger — the endpoint this flow exposes

| Field | As provisioned |
|---|---|
| Trigger name | `manual` |
| `type` | `Request` |
| `kind` | `Http` |
| `method` | `GET` |
| `relativePath` | _not declared — the trigger accepts the bare invoke path_ |
| `triggerAuthenticationType` | `All` |
| `operationOptions` | _not declared_ |
| `conditions` | _not declared_ |
| `splitOn` | _not declared_ |
| `correlation` | _not declared_ |
| `recurrence` | _not declared_ |
| Declared request headers | _not declared_ |
| Declared query parameters | _not declared_ |
| `description` | _not declared_ |

### 5.1 Request payload — declared properties

_The trigger declares no schema. Any JSON body is accepted and nothing is validated at
the boundary._

## 6. Responses returned to the caller

| Action | Status code | Runs after | On statuses |
|---|---|---|---|
| `Scope_Response/Response` | `200` | `Compose_Response` | Succeeded/TimedOut/Skipped/Failed |

### 6.1 `Scope_Response/Response`

| Field | As provisioned |
|---|---|
| `statusCode` | `200` |
| `operationOptions` | _not declared_ |

| Header | Configured value |
|---|---|
| `Access-Control-Allow-Origin` | `*` |
| `Access-Control-Allow-Methods` | `GET, POST, OPTIONS` |
| `Access-Control-Allow-Headers` | `Content-Type` |
| `Content-Type` | `application/json` |

**Response body, verbatim:**

```json
"@outputs('Compose_Response')"
```

## 7. Termination

_No `Terminate` action. The run ends when the action graph does._

## 8. Error handling and run-time configuration

**Actions wired to run on something other than success.** These are the flow's catch
paths — an action that runs after `Failed`, `Skipped` or `TimedOut` is error handling,
whatever it is named.

| Action | Type | Runs after | On statuses |
|---|---|---|---|
| `Scope_Response` | `Scope` | `Scope_Get_Docs_Activities` | Succeeded, Failed, Skipped, TimedOut |
| `Scope_Response/Send_an_email_(V2)` | `OpenApiConnection` | `Compose_Response` | Succeeded, TimedOut, Failed, Skipped |
| `Scope_Response/Response` | `Response` | `Compose_Response` | Succeeded, TimedOut, Skipped, Failed |

_No action carries a retry policy, secure-data setting, operation option, tracked
property or loop limit. Every call runs on the platform defaults._

## 9. Variables

_This package initialises no variables._

## 10. SharePoint operations

Each row is one connector call. `table` is printed as configured; the resolved column
names the list that GUID belongs to per `docs/reference/sharepoint-list-index.json`,
and says so plainly when the index does not carry it.

| Action | Operation | Connection | `dataset` (site) | `table` (list) | Resolves to |
|---|---|---|---|---|---|
| `Scope_Get_Docs_Activities/Send_an_HTTP_request_to_SharePoint` | `HttpRequest` | `shared_sharepointonline` | `https://nitdanigeria.sharepoint.com/sites/NITDADGO-EAAACTIVITYTRACKING` | `null` | _not in the list index_ |

## 11. Outbound HTTP calls

_This package makes no raw HTTP call._

## 12. Action dependency register

Every action in the package, in graph order, with the edge that starts it. `↳` marks
nesting depth; the path is the address used everywhere else on this page.

| Action path | Type | Kind | Runs after | On statuses |
|---|---|---|---|---|
| `Compose_Trigger_Body` | `Compose` | — | _first in its scope_ | — |
| `Scope_Response` | `Scope` | — | `Scope_Get_Docs_Activities` | Succeeded/Failed/Skipped/TimedOut |
| ↳`Scope_Response/Compose_ItemsCount` | `Compose` | — | _first in its scope_ | — |
| ↳`Scope_Response/Compose_Response` | `Compose` | — | `Compose_ItemsCount` | Succeeded |
| ↳`Scope_Response/Send_an_email_(V2)` | `OpenApiConnection` | — | `Compose_Response` | Succeeded/TimedOut/Failed/Skipped |
| ↳`Scope_Response/Response` | `Response` | `Http` | `Compose_Response` | Succeeded/TimedOut/Skipped/Failed |
| `Scope_Get_Docs_Activities` | `Scope` | — | `Compose_Trigger_Body` | Succeeded |
| ↳`Scope_Get_Docs_Activities/Compose_ODataQuery` | `Compose` | — | _first in its scope_ | — |
| ↳`Scope_Get_Docs_Activities/Send_an_HTTP_request_to_SharePoint` | `OpenApiConnection` | — | `Compose_ODataQuery_V2` | Succeeded |
| ↳`Scope_Get_Docs_Activities/Select` | `Select` | — | `Send_an_HTTP_request_to_SharePoint` | Succeeded |
| ↳`Scope_Get_Docs_Activities/Compose_ODataQuery_V2` | `Compose` | — | `Compose_ODataQuery` | Succeeded |

## 13. Configured values, action by action

The configured input of every action, verbatim. An action with no `inputs` key — a
`Scope`, an `If`, a `Foreach` — is documented by its control expression instead.

### `Compose_Trigger_Body`

| Field | As provisioned |
|---|---|
| Type | `Compose` |
| Kind | — |
| Description | _not declared_ |
| Runs after | _first in its scope_ |

**`inputs`, verbatim:**

```json
"@triggerBody()"
```

### `Scope_Response`

| Field | As provisioned |
|---|---|
| Type | `Scope` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Scope_Get_Docs_Activities` (Succeeded/Failed/Skipped/TimedOut) |

_This action declares no `inputs`._

### `Scope_Response/Compose_ItemsCount`

| Field | As provisioned |
|---|---|
| Type | `Compose` |
| Kind | — |
| Description | _not declared_ |
| Runs after | _first in its scope_ |

**`inputs`, verbatim:**

```json
"@length(body('Select'))"
```

### `Scope_Response/Compose_Response`

| Field | As provisioned |
|---|---|
| Type | `Compose` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Compose_ItemsCount` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "tasks": "@body('Select')"
}
```

### `Scope_Response/Send_an_email_(V2)`

| Field | As provisioned |
|---|---|
| Type | `OpenApiConnection` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Compose_Response` (Succeeded/TimedOut/Failed/Skipped) |
| Connector | `shared_office365-1` |
| Operation | `SendEmailV2` |

**`inputs`, verbatim:**

```json
{
  "parameters": {
    "emailMessage/To": "dgsRegistry@nitda.gov.ng",
    "emailMessage/Subject": "WEB Get Docs  HTTP GET Response Data",
    "emailMessage/Body": "<p class=\"editor-paragraph\">WEB Get Docs  HTTP GET</p>",
    "emailMessage/Attachments": [
      {
        "Name": "WEB Get Docs  HTTP GET Response.txt",
        "ContentBytes": "@outputs('Compose_Response')"
      },
      {
        "Name": "WEB Get Docs  HTTP GET select.txt",
        "ContentBytes": "@body('Select')"
      }
    ],
    "emailMessage/Importance": "Normal"
  },
  "host": {
    "apiId": "/providers/Microsoft.PowerApps/apis/shared_office365",
    "connectionName": "shared_office365-1",
    "operationId": "SendEmailV2"
  },
  "authentication": "@parameters('$authentication')"
}
```

### `Scope_Response/Response`

| Field | As provisioned |
|---|---|
| Type | `Response` |
| Kind | `Http` |
| Description | _not declared_ |
| Runs after | `Compose_Response` (Succeeded/TimedOut/Skipped/Failed) |

**`inputs`, verbatim:**

```json
{
  "statusCode": 200,
  "headers": {
    "Access-Control-Allow-Origin": "*",
    "Access-Control-Allow-Methods": "GET, POST, OPTIONS",
    "Access-Control-Allow-Headers": "Content-Type",
    "Content-Type": "application/json"
  },
  "body": "@outputs('Compose_Response')"
}
```

### `Scope_Get_Docs_Activities`

| Field | As provisioned |
|---|---|
| Type | `Scope` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Compose_Trigger_Body` (Succeeded) |

_This action declares no `inputs`._

### `Scope_Get_Docs_Activities/Compose_ODataQuery`

| Field | As provisioned |
|---|---|
| Type | `Compose` |
| Kind | — |
| Description | _not declared_ |
| Runs after | _first in its scope_ |

**`inputs`, verbatim:**

```json
"@concat(\r\n'/_api/web/lists/getbytitle(''', 'DGO DIGITAL OPS', ''')/items?',\r\n'$select=ID,Title,Created,Category0,AssignmentStatus,Status,Routed_x0020_To,AssignedTo,CC_x0027_dTo,Modified,Assigned,AttachmentLink,Description',\r\n'&$orderby=Created desc',\r\n'&$top=',\r\nstring(coalesce(triggerOutputs()?['queries']?['top'], 50)),\r\nif(empty(triggerOutputs()?['queries']?['status']), '', concat('&$filter=Status eq ''', triggerOutputs()?['queries']?['status'], ''''))\r\n)"
```

### `Scope_Get_Docs_Activities/Send_an_HTTP_request_to_SharePoint`

| Field | As provisioned |
|---|---|
| Type | `OpenApiConnection` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Compose_ODataQuery_V2` (Succeeded) |
| Connector | `shared_sharepointonline` |
| Operation | `HttpRequest` |

**`inputs`, verbatim:**

```json
{
  "parameters": {
    "dataset": "https://nitdanigeria.sharepoint.com/sites/NITDADGO-EAAACTIVITYTRACKING",
    "parameters/method": "GET",
    "parameters/uri": "@outputs('Compose_ODataQuery')",
    "parameters/headers": {
      "Accept": "application/json;odata=nometadata"
    }
  },
  "host": {
    "apiId": "/providers/Microsoft.PowerApps/apis/shared_sharepointonline",
    "connectionName": "shared_sharepointonline",
    "operationId": "HttpRequest"
  },
  "authentication": "@parameters('$authentication')"
}
```

### `Scope_Get_Docs_Activities/Select`

| Field | As provisioned |
|---|---|
| Type | `Select` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Send_an_HTTP_request_to_SharePoint` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "from": "@body('Send_an_HTTP_request_to_SharePoint')?['value']",
  "select": {
    "ID": "@item()?['ID']",
    "Title": "@coalesce(item()?['Title'], '')",
    "Created": "@item()?['Created']",
    "AssignedTo": "@coalesce(item()?['AssignedTo'], '')",
    "Category": "@coalesce(item()?['Category0'], '')",
    "AssignmentStatus": "@coalesce(item()?['AssignmentStatus'], '')",
    "Status": "@coalesce(item()?['Status'], '')",
    "Routed_x0020_To": "@coalesce(item()?['Routed_x0020_To'], '')",
    "Description": "@coalesce(item()?['Description'], '')",
    "CC_x0027_dTo": "@coalesce(item()?['CC_x0027_dTo'], '')",
    "Assigned": "@coalesce(item()?['Assigned'], '')",
    "AttachmentLink": "@coalesce(item()?['AttachmentLink'], '')"
  }
}
```

### `Scope_Get_Docs_Activities/Compose_ODataQuery_V2`

| Field | As provisioned |
|---|---|
| Type | `Compose` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Compose_ODataQuery` (Succeeded) |

**`inputs`, verbatim:**

```json
"_api/web/lists/getbytitle('DGO DIGITAL OPS')/items?$select=ID,Title,Created,Category0,AssignmentStatus,Status,Routed_x0020_To,RefIDD,CC_x0027_dTo,Modified,Assigned,AttachmentLink&$orderby=Created desc&$top=3000"
```

