# Get Selected Activities and Tasks

> **deployed export — the live tenant.** Every value below is read from
> [`docs/reference/flow-contracts/deployed/Get Selected Activities and Tasks__912f4b9f-d1e3-4d1e-a789-8128eb581045__full_definition.json`](../../flow-contracts/deployed/Get Selected Activities and Tasks__912f4b9f-d1e3-4d1e-a789-8128eb581045__full_definition.json) and printed as that package carries it.
> Generated by `scripts/build-provisioning-reference.mjs` — do not edit by hand.

## 1. Identity

| Field | As provisioned |
|---|---|
| Display name | `Get Selected Activities and Tasks` |
| Internal name | `912f4b9f-d1e3-4d1e-a789-8128eb581045` |
| Workflow id | _not carried by this package_ |
| Environment | `Default-ca6a4b3f-9123-49bc-bcb9-27085ebbf1a1` |
| Full resource id | `null` — the export records the field and leaves it null |
| Estate | Internal platform — no portal register names this flow |
| Package family | deployed export — the live tenant |
| Exported at (UTC) | `2026-08-19T06:09:30Z` |
| Exported by | `scripts/export-power-automate-flows.ps1` |
| Source package | `docs/reference/flow-contracts/deployed/Get Selected Activities and Tasks__912f4b9f-d1e3-4d1e-a789-8128eb581045__full_definition.json` |

## 2. Definition envelope

| Field | As provisioned |
|---|---|
| `$schema` | `https://schema.management.azure.com/providers/Microsoft.Logic/schemas/2016-06-01/workflowdefinition.json#` |
| `contentVersion` | `1.0.0.0` |
| `description` | _not declared_ |
| `outputs` | `{}` — declared and empty |
| Actions | 5 |

## 3. Definition parameters

| Parameter | Type | Default value |
|---|---|---|
| `$connections` | `Object` | `{}` |
| `$authentication` | `SecureObject` | `{}` |

## 4. Connections and references

**Connections the actions are bound to**, read from each action's `inputs.host`:

| Connection name | API id | Reference name | Operations used |
|---|---|---|---|
| `shared_sharepointonline` | `/providers/Microsoft.PowerApps/apis/shared_sharepointonline` | — | `GetItems`×2 |

## 5. Trigger — the endpoint this flow exposes

| Field | As provisioned |
|---|---|
| Trigger name | `manual` |
| `type` | `Request` |
| `kind` | `PowerAppV2` |
| `method` | _not declared_ |
| `relativePath` | _not declared — the trigger accepts the bare invoke path_ |
| `triggerAuthenticationType` | _absent from the export — no posture is recorded_ |
| `operationOptions` | _not declared_ |
| `conditions` | _not declared_ |
| `splitOn` | _not declared_ |
| `correlation` | _not declared_ |
| `recurrence` | _not declared_ |
| Declared request headers | _not declared_ |
| Declared query parameters | _not declared_ |
| `description` | _not declared_ |

### 5.1 Request payload — declared properties

| Property | Type | Required | Format | Enum | Default | Constraints | Description |
|---|---|---|---|---|---|---|---|
| `number` | `number` | **yes** | — | — | — | — | Please enter a number |
| `text` | `string` | **yes** | — | — | — | — | Please enter your input |
| `text_1` | `string` | **yes** | — | — | — | — | Please enter your input |

Root `required`: `number`, `text`, `text_1`. Root `additionalProperties`: _not declared_.

### 5.2 Request payload — schema as provisioned

```json
{
  "type": "object",
  "properties": {
    "number": {
      "title": "Top Count",
      "type": "number",
      "x-ms-dynamically-added": true,
      "description": "Please enter a number",
      "x-ms-content-hint": "NUMBER"
    },
    "text": {
      "title": "Activities Filter Query",
      "type": "string",
      "x-ms-dynamically-added": true,
      "description": "Please enter your input",
      "x-ms-content-hint": "TEXT"
    },
    "text_1": {
      "title": "Tasks Filter Query",
      "type": "string",
      "x-ms-dynamically-added": true,
      "description": "Please enter your input",
      "x-ms-content-hint": "TEXT"
    }
  },
  "required": [
    "number",
    "text",
    "text_1"
  ]
}
```

## 6. Responses returned to the caller

| Action | Status code | Runs after | On statuses |
|---|---|---|---|
| `Respond_to_a_Power_App_or_flow` | `200` | `Tasks` | Succeeded |

### 6.1 `Respond_to_a_Power_App_or_flow`

| Field | As provisioned |
|---|---|
| `statusCode` | `200` |
| `operationOptions` | _not declared_ |

_No response headers are configured._

**Response body, verbatim:**

```json
{
  "activities": "@{body('Activities')}",
  "tasks": "@{body('Tasks')}"
}
```

**Declared response schema, verbatim:**

```json
{
  "type": "object",
  "properties": {
    "activities": {
      "title": "Activities",
      "x-ms-dynamically-added": true,
      "type": "string"
    },
    "tasks": {
      "title": "Tasks",
      "x-ms-dynamically-added": true,
      "type": "string"
    }
  }
}
```

## 7. Termination

_No `Terminate` action. The run ends when the action graph does._

## 8. Error handling and run-time configuration

_No action is wired to run on `Failed`, `Skipped` or `TimedOut`. Every dependency in
this flow is a success edge._

_No action carries a retry policy, secure-data setting, operation option, tracked
property or loop limit. Every call runs on the platform defaults._

## 9. Variables

_This package initialises no variables._

## 10. SharePoint operations

Each row is one connector call. `table` is printed as configured; the resolved column
names the list that GUID belongs to per `docs/reference/sharepoint-list-index.json`,
and says so plainly when the index does not carry it.

| Action | Operation | Connection | `dataset` (site) | `table` (list) | Resolves to |
|---|---|---|---|---|---|
| `Get_Activities` | `GetItems` | `shared_sharepointonline` | `https://nitdanigeria.sharepoint.com/sites/NITDADGO-EAAACTIVITYTRACKING` | `1f1cb303-3fd2-43c8-8f24-c409b6c2fde3` | DGO DIGITAL OPS — NITDADGO-EAAACTIVITYTRACKING |
| `Get_Tasks` | `GetItems` | `shared_sharepointonline` | `https://nitdanigeria.sharepoint.com/sites/NITDADGO-EAAACTIVITYTRACKING` | `ee82725a-c408-45e2-a8e8-facf7a092047` | Global Tracking Queue — NITDADGO-EAAACTIVITYTRACKING |

## 11. Outbound HTTP calls

_This package makes no raw HTTP call._

## 12. Action dependency register

Every action in the package, in graph order, with the edge that starts it. `↳` marks
nesting depth; the path is the address used everywhere else on this page.

| Action path | Type | Kind | Runs after | On statuses |
|---|---|---|---|---|
| `Activities` | `Select` | — | `Get_Tasks` | Succeeded |
| `Get_Activities` | `OpenApiConnection` | — | _first in its scope_ | — |
| `Respond_to_a_Power_App_or_flow` | `Response` | `PowerApp` | `Tasks` | Succeeded |
| `Get_Tasks` | `OpenApiConnection` | — | `Get_Activities` | Succeeded |
| `Tasks` | `Select` | — | `Activities` | Succeeded |

## 13. Configured values, action by action

The configured input of every action, verbatim. An action with no `inputs` key — a
`Scope`, an `If`, a `Foreach` — is documented by its control expression instead.

### `Activities`

| Field | As provisioned |
|---|---|
| Type | `Select` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Get_Tasks` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "from": "@outputs('Get_Activities')?['body/value']",
  "select": {
    "ID": "@item()?['ID']",
    "Title": "@item()?['Title']",
    "Created": "@item()?['Created']",
    "Category": "@item()?['Category0']",
    "Assignment Status": "@item()?['AssignmentStatus']",
    "Status": "@item()?['Status']",
    "Assigned To": "@item()?['Routed_x0020_To']",
    "RefIDD": "@item()?['RefIDD']",
    "CC'd To": "@item()?['CC_x0027_dTo']",
    "Modified": "@item()?['Modified']"
  }
}
```

### `Get_Activities`

| Field | As provisioned |
|---|---|
| Type | `OpenApiConnection` |
| Kind | — |
| Description | _not declared_ |
| Runs after | _first in its scope_ |
| Connector | `shared_sharepointonline` |
| Operation | `GetItems` |

**`inputs`, verbatim:**

```json
{
  "host": {
    "apiId": "/providers/Microsoft.PowerApps/apis/shared_sharepointonline",
    "connectionName": "shared_sharepointonline",
    "operationId": "GetItems"
  },
  "parameters": {
    "dataset": "https://nitdanigeria.sharepoint.com/sites/NITDADGO-EAAACTIVITYTRACKING",
    "table": "1f1cb303-3fd2-43c8-8f24-c409b6c2fde3",
    "$filter": "Created ge '@{formatDateTime(addDays(utcNow(), -180), 'yyyy-MM-ddTHH:mm:ssZ')}' and Created le '@{formatDateTime(utcNow(), 'yyyy-MM-ddTHH:mm:ssZ')}'",
    "$orderby": "ID desc",
    "$top": "@triggerBody()['number']",
    "view": "d85457ae-9ea2-4fd9-bf8f-9646f55de3d7"
  },
  "authentication": {
    "value": "@json(decodeBase64(triggerOutputs().headers['X-MS-APIM-Tokens']))['$ConnectionKey']",
    "type": "Raw"
  }
}
```

### `Respond_to_a_Power_App_or_flow`

| Field | As provisioned |
|---|---|
| Type | `Response` |
| Kind | `PowerApp` |
| Description | _not declared_ |
| Runs after | `Tasks` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "statusCode": 200,
  "body": {
    "activities": "@{body('Activities')}",
    "tasks": "@{body('Tasks')}"
  },
  "schema": {
    "type": "object",
    "properties": {
      "activities": {
        "title": "Activities",
        "x-ms-dynamically-added": true,
        "type": "string"
      },
      "tasks": {
        "title": "Tasks",
        "x-ms-dynamically-added": true,
        "type": "string"
      }
    }
  }
}
```

### `Get_Tasks`

| Field | As provisioned |
|---|---|
| Type | `OpenApiConnection` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Get_Activities` (Succeeded) |
| Connector | `shared_sharepointonline` |
| Operation | `GetItems` |

**`inputs`, verbatim:**

```json
{
  "host": {
    "apiId": "/providers/Microsoft.PowerApps/apis/shared_sharepointonline",
    "connectionName": "shared_sharepointonline",
    "operationId": "GetItems"
  },
  "parameters": {
    "dataset": "https://nitdanigeria.sharepoint.com/sites/NITDADGO-EAAACTIVITYTRACKING",
    "table": "ee82725a-c408-45e2-a8e8-facf7a092047",
    "$filter": "Created ge '@{formatDateTime(addDays(utcNow(), -180), 'yyyy-MM-ddTHH:mm:ssZ')}' and Created le '@{formatDateTime(utcNow(), 'yyyy-MM-ddTHH:mm:ssZ')}'",
    "$orderby": "ID desc",
    "$top": "@triggerBody()['number']",
    "view": "c597306b-a456-47c7-8cd8-83a3d24b7866"
  },
  "authentication": {
    "value": "@json(decodeBase64(triggerOutputs().headers['X-MS-APIM-Tokens']))['$ConnectionKey']",
    "type": "Raw"
  }
}
```

### `Tasks`

| Field | As provisioned |
|---|---|
| Type | `Select` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Activities` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "from": "@outputs('Get_Tasks')?['body/value']",
  "select": {
    "RefID": "@item()?['RefID']",
    "Priority": "@item()?['Priority']",
    "Status": "@item()?['Progress']",
    "ID": "@item()?['ID']",
    "Title": "@item()?['Title']",
    "Created": "@item()?['Created']",
    "Activity Tracking ID": "@item()?['ActivityTrackingID']",
    "RefIDD": "@item()?['RefIDD']",
    "DueDate": "@item()?['DueDate']",
    "Category": "@item()?['Classification']",
    "AssignedTo": "@item()?['AssignedTo']",
    "Co-Assignees": "@item()?['Assigned']",
    "CCTo": "@item()?['OData__x0033_rdAssigned']",
    "Comments": "@item()?['Comments']",
    "Modified": "@item()?['Modified']",
    "StartDate": "@item()?['StartDate']",
    "AttachmentLink": "@item()?['AttachmentLink']",
    "RefIDDN": "@item()?['RefIDDN']",
    "Reference_ID": "@item()?['Reference_ID']"
  }
}
```

