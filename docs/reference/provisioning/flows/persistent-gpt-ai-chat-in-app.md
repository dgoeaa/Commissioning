# Persistent GPT AI Chat in App

> **deployed export — the live tenant.** Every value below is read from
> [`docs/reference/flow-contracts/deployed/Persistent GPT AI Chat in App__033e5d70-ea07-4632-8e9a-f32590913637__full_definition.json`](../../flow-contracts/deployed/Persistent GPT AI Chat in App__033e5d70-ea07-4632-8e9a-f32590913637__full_definition.json) and printed as that package carries it.
> Generated by `scripts/build-provisioning-reference.mjs` — do not edit by hand.

## 1. Identity

| Field | As provisioned |
|---|---|
| Display name | `Persistent GPT AI Chat in App` |
| Internal name | `033e5d70-ea07-4632-8e9a-f32590913637` |
| Workflow id | _not carried by this package_ |
| Environment | `Default-ca6a4b3f-9123-49bc-bcb9-27085ebbf1a1` |
| Full resource id | `null` — the export records the field and leaves it null |
| Estate | Internal platform — no portal register names this flow |
| Package family | deployed export — the live tenant |
| Exported at (UTC) | `2026-08-19T06:09:27Z` |
| Exported by | `scripts/export-power-automate-flows.ps1` |
| Source package | `docs/reference/flow-contracts/deployed/Persistent GPT AI Chat in App__033e5d70-ea07-4632-8e9a-f32590913637__full_definition.json` |

**Endpoint contract keys attributed to this flow** by
`docs/reference/internal-flow-register.json`:

| Contract key | Physical flow | Fixed action | Workflow id |
|---|---|---|---|
| `AI_CHAT` | `AI_CHAT` | `aiChat` | `a13c8b577bd44f8787c50d095ea3faf9` |

## 2. Definition envelope

| Field | As provisioned |
|---|---|
| `$schema` | `https://schema.management.azure.com/providers/Microsoft.Logic/schemas/2016-06-01/workflowdefinition.json#` |
| `contentVersion` | `undefined` |
| `description` | _not declared_ |
| `outputs` | _not declared_ |
| Actions | 14 |

## 3. Definition parameters

| Parameter | Type | Default value |
|---|---|---|
| `$authentication` | `SecureObject` | `{}` |

## 4. Connections and references

_No action in this package binds to a connector._

## 5. Trigger — the endpoint this flow exposes

| Field | As provisioned |
|---|---|
| Trigger name | `manual` |
| `type` | `Request` |
| `kind` | `PowerAppV2` |
| `method` | _not declared_ |
| `relativePath` | _not declared — the trigger accepts the bare invoke path_ |
| `triggerAuthenticationType` | _absent from the export — no posture is recorded_ |
| `operationOptions` | _not declared_ |
| `conditions` | _not declared_ |
| `splitOn` | _not declared_ |
| `correlation` | _not declared_ |
| `recurrence` | _not declared_ |
| Declared request headers | _not declared_ |
| Declared query parameters | _not declared_ |
| `description` | _not declared_ |

### 5.1 Request payload — declared properties

| Property | Type | Required | Format | Enum | Default | Constraints | Description |
|---|---|---|---|---|---|---|---|
| `text` | `string` | **yes** | — | — | — | — | Please enter your prompt |

Root `required`: `text`. Root `additionalProperties`: _not declared_.

### 5.2 Request payload — schema as provisioned

```json
{
  "type": "object",
  "properties": {
    "text": {
      "description": "Please enter your prompt",
      "title": "varConversationJson",
      "type": "string",
      "x-ms-content-hint": "TEXT",
      "x-ms-dynamically-added": true
    }
  },
  "required": [
    "text"
  ]
}
```

## 6. Responses returned to the caller

| Action | Status code | Runs after | On statuses |
|---|---|---|---|
| `Respond_to_a_Power_App_or_flow` | `200` | `Append_to_string_variable_varAssistantReply` | Succeeded |

### 6.1 `Respond_to_a_Power_App_or_flow`

| Field | As provisioned |
|---|---|
| `statusCode` | `200` |
| `operationOptions` | _not declared_ |

_No response headers are configured._

**Response body, verbatim:**

```json
{
  "hf_response": "@{outputs('Compose_Response')}"
}
```

**Declared response schema, verbatim:**

```json
{
  "type": "object",
  "properties": {
    "hf_response": {
      "title": "HF_Response",
      "x-ms-dynamically-added": true,
      "type": "string"
    }
  }
}
```

## 7. Termination

_No `Terminate` action. The run ends when the action graph does._

## 8. Error handling and run-time configuration

_No action is wired to run on `Failed`, `Skipped` or `TimedOut`. Every dependency in
this flow is a success edge._

**Run-time configuration set on individual actions** — retry policy, secure data,
operation options, tracked properties and loop limits, as configured:

| Action | Type | `runtimeConfiguration` | `operationOptions` | `trackedProperties` | `limit` |
|---|---|---|---|---|---|
| `HTTP` | `Http` | `{"contentTransfer":{"transferMode":"Chunked"}}` | — | — | — |

## 9. Variables

**Declared, with the initial value each is provisioned with:**

| Variable | Type | Initial value | Declared by |
|---|---|---|---|
| `varConversationJson` | `string` | _no initial value_ | `Initialize_variable_varConversationJson` |
| `varConversation` | `array` | `@if(equals(variables('varConversationJson'), ''), json('[]'), json(variables('varConversationJson')))↵` | `Initialize_variable_varConversation` |
| `varTruncatedConversation` | `array` | `@if(greater(length(variables('varConversation')), 12), skip(variables('varConversation'), sub(length(variables('varConversation')), 12)), variables('varConversation'))↵` | `Initialize_variable_varTruncatedConversation` |
| `varMessages` | `array` | `[{"role":"system","content":"You are a helpful assistant. Keep answers concise and explicit. If asked to return HTML or code, return only the requested block. Do not include extra explanation."}]` | `Initialize_variable_varMessages` |
| `varAssistantReply` | `string` | _no initial value_ | `Initialize_variable_varAssistantReply` |

**Every write, with the expression or literal assigned:**

| Action | Operation | Variable | Assigned value |
|---|---|---|---|
| `Apply_to_each_varTruncatedConversation/Append_to_array_variable_varMessages` | `AppendToArrayVariable` | `varMessages` | `@items('Apply_to_each_varTruncatedConversation')` |
| `Append_to_string_variable_varAssistantReply` | `AppendToStringVariable` | `varAssistantReply` | `@first(body('Parse_JSON')?['choices'])?['message']?['content']↵` |

## 10. SharePoint operations

_This package performs no SharePoint operation._

## 11. Outbound HTTP calls

### `HTTP`

| Field | As provisioned |
|---|---|
| `method` | `POST` |
| `uri` | `https://router.huggingface.co/v1/chat/completions` |
| `authentication` | _not declared_ |
| `retryPolicy` | _not declared — platform default_ |

| Request header | Configured value |
|---|---|
| `Authorization` | `Bearer REDACTED-HUGGINGFACE-TOKEN` |
| `Content-Type` | `application/json` |

**Request body, verbatim:**

```json
{
  "model": "openai/gpt-oss-120b:fireworks-ai",
  "messages": "@variables('varMessages')",
  "max_tokens": 1200,
  "temperature": 0.2
}
```

## 12. Action dependency register

Every action in the package, in graph order, with the edge that starts it. `↳` marks
nesting depth; the path is the address used everywhere else on this page.

| Action path | Type | Kind | Runs after | On statuses |
|---|---|---|---|---|
| `Compose_Prompt` | `Compose` | — | `Apply_to_each_varTruncatedConversation` | Succeeded |
| `HTTP` | `Http` | — | `Compose_Prompt` | Succeeded |
| `Parse_JSON` | `ParseJson` | — | `HTTP` | Succeeded |
| `Compose_Response` | `Compose` | — | `Parse_JSON` | Succeeded |
| `Respond_to_a_Power_App_or_flow` | `Response` | `PowerApp` | `Append_to_string_variable_varAssistantReply` | Succeeded |
| `Initialize_variable_varConversationJson` | `InitializeVariable` | — | _first in its scope_ | — |
| `Initialize_variable_varConversation` | `InitializeVariable` | — | `Initialize_variable_varConversationJson` | Succeeded |
| `Initialize_variable_varTruncatedConversation` | `InitializeVariable` | — | `Initialize_variable_varConversation` | Succeeded |
| `Initialize_variable_varMessages` | `InitializeVariable` | — | `Initialize_variable_varTruncatedConversation` | Succeeded |
| `Apply_to_each_varTruncatedConversation` | `Foreach` | — | `Initialize_variable_varAssistantReply` | Succeeded |
| ↳`Apply_to_each_varTruncatedConversation/Append_to_array_variable_varMessages` | `AppendToArrayVariable` | — | _first in its scope_ | — |
| `Initialize_variable_varAssistantReply` | `InitializeVariable` | — | `Initialize_variable_varMessages` | Succeeded |
| `Append_to_string_variable_varAssistantReply` | `AppendToStringVariable` | — | `Compose_Response` | Succeeded |
| `Compose_Conversation_Persistence` | `Compose` | — | `Respond_to_a_Power_App_or_flow` | Succeeded |

## 13. Configured values, action by action

The configured input of every action, verbatim. An action with no `inputs` key — a
`Scope`, an `If`, a `Foreach` — is documented by its control expression instead.

### `Compose_Prompt`

| Field | As provisioned |
|---|---|
| Type | `Compose` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Apply_to_each_varTruncatedConversation` (Succeeded) |

**`inputs`, verbatim:**

```json
"@triggerBody()?['text']"
```

### `HTTP`

| Field | As provisioned |
|---|---|
| Type | `Http` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Compose_Prompt` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "method": "POST",
  "uri": "https://router.huggingface.co/v1/chat/completions",
  "headers": {
    "Authorization": "Bearer REDACTED-HUGGINGFACE-TOKEN",
    "Content-Type": "application/json"
  },
  "body": {
    "model": "openai/gpt-oss-120b:fireworks-ai",
    "messages": "@variables('varMessages')",
    "max_tokens": 1200,
    "temperature": 0.2
  }
}
```

**`runtimeConfiguration`, verbatim:**

```json
{
  "contentTransfer": {
    "transferMode": "Chunked"
  }
}
```

### `Parse_JSON`

| Field | As provisioned |
|---|---|
| Type | `ParseJson` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `HTTP` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "content": "@body('HTTP')",
  "schema": {
    "type": "object",
    "properties": {
      "choices": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "message": {
              "type": "object",
              "properties": {
                "role": {
                  "type": "string"
                },
                "content": {
                  "type": "string"
                }
              }
            }
          }
        }
      }
    }
  }
}
```

### `Compose_Response`

| Field | As provisioned |
|---|---|
| Type | `Compose` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Parse_JSON` (Succeeded) |

**`inputs`, verbatim:**

```json
"@first(body('Parse_JSON')?['choices'])?['message']?['content']"
```

### `Respond_to_a_Power_App_or_flow`

| Field | As provisioned |
|---|---|
| Type | `Response` |
| Kind | `PowerApp` |
| Description | _not declared_ |
| Runs after | `Append_to_string_variable_varAssistantReply` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "statusCode": 200,
  "body": {
    "hf_response": "@{outputs('Compose_Response')}"
  },
  "schema": {
    "type": "object",
    "properties": {
      "hf_response": {
        "title": "HF_Response",
        "x-ms-dynamically-added": true,
        "type": "string"
      }
    }
  }
}
```

### `Initialize_variable_varConversationJson`

| Field | As provisioned |
|---|---|
| Type | `InitializeVariable` |
| Kind | — |
| Description | _not declared_ |
| Runs after | _first in its scope_ |

**`inputs`, verbatim:**

```json
{
  "variables": [
    {
      "name": "varConversationJson",
      "type": "string"
    }
  ]
}
```

### `Initialize_variable_varConversation`

| Field | As provisioned |
|---|---|
| Type | `InitializeVariable` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Initialize_variable_varConversationJson` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "variables": [
    {
      "name": "varConversation",
      "type": "array",
      "value": "@if(equals(variables('varConversationJson'), ''), json('[]'), json(variables('varConversationJson')))\r\n"
    }
  ]
}
```

### `Initialize_variable_varTruncatedConversation`

| Field | As provisioned |
|---|---|
| Type | `InitializeVariable` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Initialize_variable_varConversation` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "variables": [
    {
      "name": "varTruncatedConversation",
      "type": "array",
      "value": "@if(greater(length(variables('varConversation')), 12), skip(variables('varConversation'), sub(length(variables('varConversation')), 12)), variables('varConversation'))\r\n"
    }
  ]
}
```

### `Initialize_variable_varMessages`

| Field | As provisioned |
|---|---|
| Type | `InitializeVariable` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Initialize_variable_varTruncatedConversation` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "variables": [
    {
      "name": "varMessages",
      "type": "array",
      "value": [
        {
          "role": "system",
          "content": "You are a helpful assistant. Keep answers concise and explicit. If asked to return HTML or code, return only the requested block. Do not include extra explanation."
        }
      ]
    }
  ]
}
```

### `Apply_to_each_varTruncatedConversation`

| Field | As provisioned |
|---|---|
| Type | `Foreach` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Initialize_variable_varAssistantReply` (Succeeded) |

_This action declares no `inputs`._

**`foreach`, verbatim:**

```json
"@variables('varTruncatedConversation')"
```

### `Apply_to_each_varTruncatedConversation/Append_to_array_variable_varMessages`

| Field | As provisioned |
|---|---|
| Type | `AppendToArrayVariable` |
| Kind | — |
| Description | _not declared_ |
| Runs after | _first in its scope_ |

**`inputs`, verbatim:**

```json
{
  "name": "varMessages",
  "value": "@items('Apply_to_each_varTruncatedConversation')"
}
```

### `Initialize_variable_varAssistantReply`

| Field | As provisioned |
|---|---|
| Type | `InitializeVariable` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Initialize_variable_varMessages` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "variables": [
    {
      "name": "varAssistantReply",
      "type": "string"
    }
  ]
}
```

### `Append_to_string_variable_varAssistantReply`

| Field | As provisioned |
|---|---|
| Type | `AppendToStringVariable` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Compose_Response` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "name": "varAssistantReply",
  "value": "@first(body('Parse_JSON')?['choices'])?['message']?['content']\r\n"
}
```

### `Compose_Conversation_Persistence`

| Field | As provisioned |
|---|---|
| Type | `Compose` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Respond_to_a_Power_App_or_flow` (Succeeded) |

**`inputs`, verbatim:**

```json
"@ JSON( concat( variables('varConversation'), variables('varAssistantReply') ) )"
```

