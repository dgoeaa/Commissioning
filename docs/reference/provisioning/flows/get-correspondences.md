# Get Correspondences

> **deployed export — the live tenant.** Every value below is read from
> [`docs/reference/flow-contracts/deployed/Get Correspondences__4d1c9a59-1d81-47ed-9c7c-38172edd5584__full_definition.json`](../../flow-contracts/deployed/Get Correspondences__4d1c9a59-1d81-47ed-9c7c-38172edd5584__full_definition.json) and printed as that package carries it.
> Generated by `scripts/build-provisioning-reference.mjs` — do not edit by hand.

## 1. Identity

| Field | As provisioned |
|---|---|
| Display name | `Get Correspondences` |
| Internal name | `4d1c9a59-1d81-47ed-9c7c-38172edd5584` |
| Workflow id | _not carried by this package_ |
| Environment | `Default-ca6a4b3f-9123-49bc-bcb9-27085ebbf1a1` |
| Full resource id | `null` — the export records the field and leaves it null |
| Estate | Internal platform — no portal register names this flow |
| Package family | deployed export — the live tenant |
| Exported at (UTC) | `2026-08-19T06:09:19Z` |
| Exported by | `scripts/export-power-automate-flows.ps1` |
| Source package | `docs/reference/flow-contracts/deployed/Get Correspondences__4d1c9a59-1d81-47ed-9c7c-38172edd5584__full_definition.json` |

## 2. Definition envelope

| Field | As provisioned |
|---|---|
| `$schema` | `https://schema.management.azure.com/providers/Microsoft.Logic/schemas/2016-06-01/workflowdefinition.json#` |
| `contentVersion` | `1.0.0.0` |
| `description` | _not declared_ |
| `outputs` | `{}` — declared and empty |
| Actions | 10 |

## 3. Definition parameters

| Parameter | Type | Default value |
|---|---|---|
| `$authentication` | `SecureObject` | `{}` |
| `$connections` | `Object` | `{}` |

## 4. Connections and references

**Connections the actions are bound to**, read from each action's `inputs.host`:

| Connection name | API id | Reference name | Operations used |
|---|---|---|---|
| `shared_office365` | `/providers/Microsoft.PowerApps/apis/shared_office365` | — | `SendEmailV2`×1 |
| `shared_sharepointonline` | `/providers/Microsoft.PowerApps/apis/shared_sharepointonline` | — | `GetItems`×1 |

## 5. Trigger — the endpoint this flow exposes

| Field | As provisioned |
|---|---|
| Trigger name | `manual` |
| `type` | `Request` |
| `kind` | `Http` |
| `method` | `POST` |
| `relativePath` | _not declared — the trigger accepts the bare invoke path_ |
| `triggerAuthenticationType` | `All` |
| `operationOptions` | _not declared_ |
| `conditions` | _not declared_ |
| `splitOn` | _not declared_ |
| `correlation` | _not declared_ |
| `recurrence` | _not declared_ |
| Declared request headers | _not declared_ |
| Declared query parameters | _not declared_ |
| `description` | _not declared_ |

### 5.1 Request payload — declared properties

_The trigger declares no schema. Any JSON body is accepted and nothing is validated at
the boundary._

## 6. Responses returned to the caller

| Action | Status code | Runs after | On statuses |
|---|---|---|---|
| `Response` | `200` | `Select_Extended` | Succeeded |

### 6.1 `Response`

| Field | As provisioned |
|---|---|
| `statusCode` | `200` |
| `operationOptions` | _not declared_ |

| Header | Configured value |
|---|---|
| `Access-Control-Allow-Origin` | `*` |
| `Access-Control-Allow-Methods` | `GET, POST, OPTIONS` |
| `Access-Control-Allow-Headers` | `Content-Type` |
| `Content-Type` | `application/json` |

**Response body, verbatim:**

```json
"@body('Select_Extended')"
```

## 7. Termination

_No `Terminate` action. The run ends when the action graph does._

## 8. Error handling and run-time configuration

_No action is wired to run on `Failed`, `Skipped` or `TimedOut`. Every dependency in
this flow is a success edge._

_No action carries a retry policy, secure-data setting, operation option, tracked
property or loop limit. Every call runs on the platform defaults._

## 9. Variables

**Declared, with the initial value each is provisioned with:**

| Variable | Type | Initial value | Declared by |
|---|---|---|---|
| `varResponse` | `string` | _no initial value_ | `Initialize_variable_varResponse` |
| `varstatuscode` | `integer` | _no initial value_ | `Initialize_variable_varstatuscode` |
| `varstatus` | `string` | _no initial value_ | `Initialize_variable_varStatus` |
| `vardocId` | `integer` | _no initial value_ | `Initialize_variable_vardocId` |
| `varmessage` | `string` | _no initial value_ | `Initialize_variable_varmessage` |

## 10. SharePoint operations

Each row is one connector call. `table` is printed as configured; the resolved column
names the list that GUID belongs to per `docs/reference/sharepoint-list-index.json`,
and says so plainly when the index does not carry it.

| Action | Operation | Connection | `dataset` (site) | `table` (list) | Resolves to |
|---|---|---|---|---|---|
| `Get_items_1` | `GetItems` | `shared_sharepointonline` | `https://nitdanigeria.sharepoint.com/sites/NITDADGO-EAAACTIVITYTRACKING` | `1f1cb303-3fd2-43c8-8f24-c409b6c2fde3` | DGO DIGITAL OPS — NITDADGO-EAAACTIVITYTRACKING |

## 11. Outbound HTTP calls

_This package makes no raw HTTP call._

## 12. Action dependency register

Every action in the package, in graph order, with the edge that starts it. `↳` marks
nesting depth; the path is the address used everywhere else on this page.

| Action path | Type | Kind | Runs after | On statuses |
|---|---|---|---|---|
| `Response` | `Response` | `Http` | `Select_Extended` | Succeeded |
| `Initialize_variable_varResponse` | `InitializeVariable` | — | _first in its scope_ | — |
| `Initialize_variable_varstatuscode` | `InitializeVariable` | — | `Initialize_variable_varResponse` | Succeeded |
| `Initialize_variable_varStatus` | `InitializeVariable` | — | `Initialize_variable_varstatuscode` | Succeeded |
| `Initialize_variable_vardocId` | `InitializeVariable` | — | `Initialize_variable_varStatus` | Succeeded |
| `Initialize_variable_varmessage` | `InitializeVariable` | — | `Initialize_variable_vardocId` | Succeeded |
| `Send_an_email_(V2)` | `OpenApiConnection` | — | `Response` | Succeeded |
| `Get_items_1` | `OpenApiConnection` | — | `Initialize_variable_varmessage` | Succeeded |
| `Select_Lean` | `Select` | — | `Get_items_1` | Succeeded |
| `Select_Extended` | `Select` | — | `Select_Lean` | Succeeded |

## 13. Configured values, action by action

The configured input of every action, verbatim. An action with no `inputs` key — a
`Scope`, an `If`, a `Foreach` — is documented by its control expression instead.

### `Response`

| Field | As provisioned |
|---|---|
| Type | `Response` |
| Kind | `Http` |
| Description | _not declared_ |
| Runs after | `Select_Extended` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "statusCode": 200,
  "headers": {
    "Access-Control-Allow-Origin": "*",
    "Access-Control-Allow-Methods": "GET, POST, OPTIONS",
    "Access-Control-Allow-Headers": "Content-Type",
    "Content-Type": "application/json"
  },
  "body": "@body('Select_Extended')"
}
```

### `Initialize_variable_varResponse`

| Field | As provisioned |
|---|---|
| Type | `InitializeVariable` |
| Kind | — |
| Description | _not declared_ |
| Runs after | _first in its scope_ |

**`inputs`, verbatim:**

```json
{
  "variables": [
    {
      "name": "varResponse",
      "type": "string"
    }
  ]
}
```

### `Initialize_variable_varstatuscode`

| Field | As provisioned |
|---|---|
| Type | `InitializeVariable` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Initialize_variable_varResponse` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "variables": [
    {
      "name": "varstatuscode",
      "type": "integer"
    }
  ]
}
```

### `Initialize_variable_varStatus`

| Field | As provisioned |
|---|---|
| Type | `InitializeVariable` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Initialize_variable_varstatuscode` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "variables": [
    {
      "name": "varstatus",
      "type": "string"
    }
  ]
}
```

### `Initialize_variable_vardocId`

| Field | As provisioned |
|---|---|
| Type | `InitializeVariable` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Initialize_variable_varStatus` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "variables": [
    {
      "name": "vardocId",
      "type": "integer"
    }
  ]
}
```

### `Initialize_variable_varmessage`

| Field | As provisioned |
|---|---|
| Type | `InitializeVariable` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Initialize_variable_vardocId` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "variables": [
    {
      "name": "varmessage",
      "type": "string"
    }
  ]
}
```

### `Send_an_email_(V2)`

| Field | As provisioned |
|---|---|
| Type | `OpenApiConnection` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Response` (Succeeded) |
| Connector | `shared_office365` |
| Operation | `SendEmailV2` |

**`inputs`, verbatim:**

```json
{
  "parameters": {
    "emailMessage/To": "hkani@nitda.gov.ng",
    "emailMessage/Subject": "NITDA_Get_Documents Glass Pane Portal submission",
    "emailMessage/Body": "<br><br><p class=\"editor-paragraph\"></p>",
    "emailMessage/Attachments": [
      {
        "Name": "Get Correspondence Extended Output.txt",
        "ContentBytes": "@body('Select_Extended')"
      },
      {
        "Name": "Ge tCorrespondence Lean Output.txt",
        "ContentBytes": "@body('Select_Lean')"
      }
    ],
    "emailMessage/Importance": "Normal"
  },
  "host": {
    "apiId": "/providers/Microsoft.PowerApps/apis/shared_office365",
    "connectionName": "shared_office365",
    "operationId": "SendEmailV2"
  },
  "authentication": "@parameters('$authentication')"
}
```

### `Get_items_1`

| Field | As provisioned |
|---|---|
| Type | `OpenApiConnection` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Initialize_variable_varmessage` (Succeeded) |
| Connector | `shared_sharepointonline` |
| Operation | `GetItems` |

**`inputs`, verbatim:**

```json
{
  "parameters": {
    "dataset": "https://nitdanigeria.sharepoint.com/sites/NITDADGO-EAAACTIVITYTRACKING",
    "table": "1f1cb303-3fd2-43c8-8f24-c409b6c2fde3",
    "$orderby": "ID desc",
    "$top": 100,
    "view": "d486128d-ed61-48f9-9507-7bb1a33cbba1"
  },
  "host": {
    "apiId": "/providers/Microsoft.PowerApps/apis/shared_sharepointonline",
    "connectionName": "shared_sharepointonline",
    "operationId": "GetItems"
  },
  "authentication": "@parameters('$authentication')"
}
```

### `Select_Lean`

| Field | As provisioned |
|---|---|
| Type | `Select` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Get_items_1` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "from": "@outputs('Get_items_1')?['body/value']",
  "select": {
    "ID": "@item()?['ID']",
    "Subject": "@item()?['Title']",
    "Department": "@item()?['Category0']",
    "Status": "@item()?['Status']",
    "Date": "@item()?['Created']",
    "Deadline": "@item()?['Modified']"
  }
}
```

### `Select_Extended`

| Field | As provisioned |
|---|---|
| Type | `Select` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Select_Lean` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "from": "@outputs('Get_items_1')?['body/value']",
  "select": {
    "ID": "@coalesce(item()?['ID'], 0)",
    "Title": "@coalesce(item()?['Title'], 'Untitled')",
    "Description": "@coalesce(item()?['Description'], 'No description provided')",
    "Status": "@coalesce(item()?['Status']?['Value'], 'Unknown')",
    "AssignmentStatuss": "@coalesce(item()?['AssignmentStatus']?['Value'], 'Undefined')",
    "Category": "@coalesce(item()?['Category0'], item()?['Category0'], 'Uncategorized')",
    "Created": "@coalesce(item()?['Created'], utcNow())",
    "AssignedTo": "@coalesce(item()?['AssignedTo'], 'Unassigned')",
    "Attachments": "",
    "AtttachmentsLink": "@item()?['AttachmentLink']"
  }
}
```

