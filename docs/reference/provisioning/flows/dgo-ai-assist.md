# DGO_AI_Assist

> **deployed export — the live tenant.** Every value below is read from
> [`docs/reference/flow-contracts/deployed/DGO_AI_Assist__89925df0-276f-da78-1cba-5847cf24c7d9__full_definition.json`](../../flow-contracts/deployed/DGO_AI_Assist__89925df0-276f-da78-1cba-5847cf24c7d9__full_definition.json) and printed as that package carries it.
> Generated by `scripts/build-provisioning-reference.mjs` — do not edit by hand.

## 1. Identity

| Field | As provisioned |
|---|---|
| Display name | `DGO_AI_Assist` |
| Internal name | `89925df0-276f-da78-1cba-5847cf24c7d9` |
| Workflow id | _not carried by this package_ |
| Environment | `Default-ca6a4b3f-9123-49bc-bcb9-27085ebbf1a1` |
| Full resource id | `null` — the export records the field and leaves it null |
| Estate | Internal platform — no portal register names this flow |
| Package family | deployed export — the live tenant |
| Exported at (UTC) | `2026-08-19T06:09:23Z` |
| Exported by | `scripts/export-power-automate-flows.ps1` |
| Source package | `docs/reference/flow-contracts/deployed/DGO_AI_Assist__89925df0-276f-da78-1cba-5847cf24c7d9__full_definition.json` |

**Endpoint contract keys attributed to this flow** by
`docs/reference/internal-flow-register.json`:

| Contract key | Physical flow | Fixed action | Workflow id |
|---|---|---|---|
| `AI_DOC_ANALYSIS` | `AI_DOC_ANALYSIS` | `aiAnalyseEventDocs` | `3f018fea1031490fb73dff6a8d6341f2` |

## 2. Definition envelope

| Field | As provisioned |
|---|---|
| `$schema` | `https://schema.management.azure.com/providers/Microsoft.Logic/schemas/2016-06-01/workflowdefinition.json#` |
| `contentVersion` | `1.0.0.0` |
| `description` | _not declared_ |
| `outputs` | _not declared_ |
| Actions | 8 |

## 3. Definition parameters

| Parameter | Type | Default value |
|---|---|---|
| `$authentication` | `SecureObject` | `{}` |

## 4. Connections and references

_No action in this package binds to a connector._

## 5. Trigger — the endpoint this flow exposes

| Field | As provisioned |
|---|---|
| Trigger name | `manual` |
| `type` | `Request` |
| `kind` | `PowerAppV2` |
| `method` | _not declared_ |
| `relativePath` | _not declared — the trigger accepts the bare invoke path_ |
| `triggerAuthenticationType` | _absent from the export — no posture is recorded_ |
| `operationOptions` | _not declared_ |
| `conditions` | _not declared_ |
| `splitOn` | _not declared_ |
| `correlation` | _not declared_ |
| `recurrence` | _not declared_ |
| Declared request headers | _not declared_ |
| Declared query parameters | _not declared_ |
| `description` | _not declared_ |

### 5.1 Request payload — declared properties

| Property | Type | Required | Format | Enum | Default | Constraints | Description |
|---|---|---|---|---|---|---|---|
| `text` | `string` | **yes** | — | — | — | — | Please enter your input |
| `text_1` | `string` | **yes** | — | — | — | — | Please enter your input |
| `text_2` | `string` | **yes** | — | — | — | — | Please enter your input |

Root `required`: `text`, `text_1`, `text_2`. Root `additionalProperties`: _not declared_.

### 5.2 Request payload — schema as provisioned

```json
{
  "type": "object",
  "properties": {
    "text": {
      "title": "prompt",
      "type": "string",
      "x-ms-dynamically-added": true,
      "description": "Please enter your input",
      "x-ms-content-hint": "TEXT"
    },
    "text_1": {
      "title": "emailSubject",
      "type": "string",
      "x-ms-dynamically-added": true,
      "description": "Please enter your input",
      "x-ms-content-hint": "TEXT"
    },
    "text_2": {
      "title": "emailBody",
      "type": "string",
      "x-ms-dynamically-added": true,
      "description": "Please enter your input",
      "x-ms-content-hint": "TEXT"
    }
  },
  "required": [
    "text",
    "text_1",
    "text_2"
  ]
}
```

## 6. Responses returned to the caller

| Action | Status code | Runs after | On statuses |
|---|---|---|---|
| `Respond_to_a_Power_App_or_flow` | `200` | `ai_response` | Succeeded |

### 6.1 `Respond_to_a_Power_App_or_flow`

| Field | As provisioned |
|---|---|
| `statusCode` | `200` |
| `operationOptions` | _not declared_ |

_No response headers are configured._

**Response body, verbatim:**

```json
{
  "ai_response": "@{outputs('ai_response')}"
}
```

**Declared response schema, verbatim:**

```json
{
  "type": "object",
  "properties": {
    "ai_response": {
      "title": "ai response",
      "x-ms-dynamically-added": true,
      "type": "string"
    }
  }
}
```

## 7. Termination

_No `Terminate` action. The run ends when the action graph does._

## 8. Error handling and run-time configuration

_No action is wired to run on `Failed`, `Skipped` or `TimedOut`. Every dependency in
this flow is a success edge._

_No action carries a retry policy, secure-data setting, operation option, tracked
property or loop limit. Every call runs on the platform defaults._

## 9. Variables

**Declared, with the initial value each is provisioned with:**

| Variable | Type | Initial value | Declared by |
|---|---|---|---|
| `varApiKey` | `string` | _no initial value_ | `Initialize_variable_varApiKey` |
| `varApiUrl` | `string` | _no initial value_ | `Initialize_variable_varApiUrl` |

## 10. SharePoint operations

_This package performs no SharePoint operation._

## 11. Outbound HTTP calls

_This package makes no raw HTTP call._

## 12. Action dependency register

Every action in the package, in graph order, with the edge that starts it. `↳` marks
nesting depth; the path is the address used everywhere else on this page.

| Action path | Type | Kind | Runs after | On statuses |
|---|---|---|---|---|
| `Compose_Full_Prompt` | `Compose` | — | `Parse_JSON` | Succeeded |
| `Initialize_variable_varApiKey` | `InitializeVariable` | — | _first in its scope_ | — |
| `Initialize_variable_varApiUrl` | `InitializeVariable` | — | `Initialize_variable_varApiKey` | Succeeded |
| `Compose_Full_Prompt_2` | `Compose` | — | `Compose_Full_Prompt` | Succeeded |
| `Compose_JSON_Request` | `Compose` | — | `Initialize_variable_varApiUrl` | Succeeded |
| `Parse_JSON` | `Compose` | — | `Compose_JSON_Request` | Succeeded |
| `Respond_to_a_Power_App_or_flow` | `Response` | `PowerApp` | `ai_response` | Succeeded |
| `ai_response` | `Compose` | — | `Compose_Full_Prompt_2` | Succeeded |

## 13. Configured values, action by action

The configured input of every action, verbatim. An action with no `inputs` key — a
`Scope`, an `If`, a `Foreach` — is documented by its control expression instead.

### `Compose_Full_Prompt`

| Field | As provisioned |
|---|---|
| Type | `Compose` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Parse_JSON` (Succeeded) |

**`inputs`, verbatim:**

```json
"Compose_Full_Prompt\n\nhttps://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-preview-09-2025:generateContent?key=@{variables('varApiKey')}\n\n\nFlow Name: DGO_AI_Assist Flow Type: Instant cloud flow Trigger: PowerApps (V2) 1. Trigger Action: PowerApps (V2) Add three (3) Inputs to the trigger: Name: prompt (Type: Text) Name: emailSubject (Type: Text) Name: emailBody (Type: Text) 2. Initialize Gemini API Key Action: Initialize variable Name: varApiKey Type: String Value: [PASTE_YOUR_GEMINI_API_KEY_HERE] 3. Initialize Gemini API URL Action: Initialize variable Name: varApiUrl Type: String Value: https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-preview-09-2025:generateContent?key=@{variables('varApiKey')} 4. Compose Full Prompt Action: Compose Name: Compose_Full_Prompt Inputs: (Use expressions to get the trigger inputs) --- TASK --- @{triggerBody()['text']} --- EMAIL CONTEXT --- Subject: @{triggerBody()['text_1']} Body: @{triggerBody()['text_2']} 5. Compose JSON Request Body Action: Compose Name: Compose_JSON_Request Inputs: { \"contents\": [ { \"parts\": [ { \"text\": \"\" } ] } ] } 6. Call Gemini API Action: HTTP Method: POST URI: @{variables('varApiUrl')} Headers: Content-Type | application/json Body: @{outputs('Compose_JSON_Request')} 7. Parse JSON Response Action: Parse JSON Content: Body (from the \"Call Gemini API\" step) Schema: (Click \"Generate from sample\" and paste this in) { \"candidates\": [ { \"content\": { \"parts\": [ { \"text\": \"sample text\" } ], \"role\": \"model\" } } ] } 8. Respond to Power App Action: Respond to a PowerApp or flow Add one (1) Output: Type: Text Name: ai_response Value: (Use an expression to get the text from the parsed JSON) @{body('Parse_JSON')?['candidates']?[0]?['content']?['parts']?[0]?['text']} 9. (Optional) Add Error Handling Add a \"Configure run after\" setting to the Respond to Power App step. Make sure it runs even if the Parse JSON or HTTP step \"has failed\". In the ai_response output, set the value to: if(empty(body('Parse_JSON')?['candidates']), \"Error: The AI response was empty or failed.\", body('Parse_JSON')?['candidates']?[0]?['content']?['parts']?[0]?['text'])"
```

### `Initialize_variable_varApiKey`

| Field | As provisioned |
|---|---|
| Type | `InitializeVariable` |
| Kind | — |
| Description | _not declared_ |
| Runs after | _first in its scope_ |

**`inputs`, verbatim:**

```json
{
  "variables": [
    {
      "name": "varApiKey",
      "type": "string"
    }
  ]
}
```

### `Initialize_variable_varApiUrl`

| Field | As provisioned |
|---|---|
| Type | `InitializeVariable` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Initialize_variable_varApiKey` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "variables": [
    {
      "name": "varApiUrl",
      "type": "string"
    }
  ]
}
```

### `Compose_Full_Prompt_2`

| Field | As provisioned |
|---|---|
| Type | `Compose` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Compose_Full_Prompt` (Succeeded) |

**`inputs`, verbatim:**

```json
"Compose_Full_Prompt\n\nhttps://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-preview-09-2025:generateContent?key=@{variables('varApiKey')}\n\n\nFlow Name: DGO_AI_Assist Flow Type: Instant cloud flow Trigger: PowerApps (V2) 1. Trigger Action: PowerApps (V2) Add three (3) Inputs to the trigger: Name: prompt (Type: Text) Name: emailSubject (Type: Text) Name: emailBody (Type: Text) 2. Initialize Gemini API Key Action: Initialize variable Name: varApiKey Type: String Value: [PASTE_YOUR_GEMINI_API_KEY_HERE] 3. Initialize Gemini API URL Action: Initialize variable Name: varApiUrl Type: String Value: https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-preview-09-2025:generateContent?key=@{variables('varApiKey')} 4. Compose Full Prompt Action: Compose Name: Compose_Full_Prompt Inputs: (Use expressions to get the trigger inputs) --- TASK --- @{triggerBody()['text']} --- EMAIL CONTEXT --- Subject: @{triggerBody()['text_1']} Body: @{triggerBody()['text_2']} 5. Compose JSON Request Body Action: Compose Name: Compose_JSON_Request Inputs: { \"contents\": [ { \"parts\": [ { \"text\": \"@{outputs('Compose_Full_Prompt')}\" } ] } ] } 6. Call Gemini API Action: HTTP Method: POST URI: @{variables('varApiUrl')} Headers: Content-Type | application/json Body: @{outputs('Compose_JSON_Request')} 7. Parse JSON Response Action: Parse JSON Content: Body (from the \"Call Gemini API\" step) Schema: (Click \"Generate from sample\" and paste this in) { \"candidates\": [ { \"content\": { \"parts\": [ { \"text\": \"sample text\" } ], \"role\": \"model\" } } ] } 8. Respond to Power App Action: Respond to a PowerApp or flow Add one (1) Output: Type: Text Name: ai_response Value: (Use an expression to get the text from the parsed JSON) @{body('Parse_JSON')?['candidates']?[0]?['content']?['parts']?[0]?['text']} 9. (Optional) Add Error Handling Add a \"Configure run after\" setting to the Respond to Power App step. Make sure it runs even if the Parse JSON or HTTP step \"has failed\". In the ai_response output, set the value to: if(empty(body('Parse_JSON')?['candidates']), \"Error: The AI response was empty or failed.\", body('Parse_JSON')?['candidates']?[0]?['content']?['parts']?[0]?['text'])"
```

### `Compose_JSON_Request`

| Field | As provisioned |
|---|---|
| Type | `Compose` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Initialize_variable_varApiUrl` (Succeeded) |

**`inputs`, verbatim:**

```json
"xxxx"
```

### `Parse_JSON`

| Field | As provisioned |
|---|---|
| Type | `Compose` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Compose_JSON_Request` (Succeeded) |

**`inputs`, verbatim:**

```json
"xxxx"
```

### `Respond_to_a_Power_App_or_flow`

| Field | As provisioned |
|---|---|
| Type | `Response` |
| Kind | `PowerApp` |
| Description | _not declared_ |
| Runs after | `ai_response` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "statusCode": 200,
  "body": {
    "ai_response": "@{outputs('ai_response')}"
  },
  "schema": {
    "type": "object",
    "properties": {
      "ai_response": {
        "title": "ai response",
        "x-ms-dynamically-added": true,
        "type": "string"
      }
    }
  }
}
```

### `ai_response`

| Field | As provisioned |
|---|---|
| Type | `Compose` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Compose_Full_Prompt_2` (Succeeded) |

**`inputs`, verbatim:**

```json
"xxxx"
```

