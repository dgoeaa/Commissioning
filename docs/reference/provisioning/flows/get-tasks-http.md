# Get Tasks HTTP

> **deployed export — the live tenant.** Every value below is read from
> [`docs/reference/flow-contracts/deployed/Get Tasks HTTP__2b146c0c-a7d2-c676-5447-76807c98e6b9__full_definition.json`](../../flow-contracts/deployed/Get Tasks HTTP__2b146c0c-a7d2-c676-5447-76807c98e6b9__full_definition.json) and printed as that package carries it.
> Generated by `scripts/build-provisioning-reference.mjs` — do not edit by hand.

## 1. Identity

| Field | As provisioned |
|---|---|
| Display name | `Get Tasks HTTP` |
| Internal name | `2b146c0c-a7d2-c676-5447-76807c98e6b9` |
| Workflow id | _not carried by this package_ |
| Environment | `Default-ca6a4b3f-9123-49bc-bcb9-27085ebbf1a1` |
| Full resource id | `null` — the export records the field and leaves it null |
| Estate | Internal platform — no portal register names this flow |
| Package family | deployed export — the live tenant |
| Exported at (UTC) | `2026-08-19T06:09:20Z` |
| Exported by | `scripts/export-power-automate-flows.ps1` |
| Source package | `docs/reference/flow-contracts/deployed/Get Tasks HTTP__2b146c0c-a7d2-c676-5447-76807c98e6b9__full_definition.json` |

## 2. Definition envelope

| Field | As provisioned |
|---|---|
| `$schema` | `https://schema.management.azure.com/providers/Microsoft.Logic/schemas/2016-06-01/workflowdefinition.json#` |
| `contentVersion` | `1.0.0.0` |
| `description` | _not declared_ |
| `outputs` | _not declared_ |
| Actions | 6 |

## 3. Definition parameters

| Parameter | Type | Default value |
|---|---|---|
| `$connections` | `Object` | `{}` |
| `$authentication` | `SecureObject` | `{}` |

## 4. Connections and references

**Connections the actions are bound to**, read from each action's `inputs.host`:

| Connection name | API id | Reference name | Operations used |
|---|---|---|---|
| `shared_sharepointonline` | `/providers/Microsoft.PowerApps/apis/shared_sharepointonline` | — | `HttpRequest`×1 |

## 5. Trigger — the endpoint this flow exposes

| Field | As provisioned |
|---|---|
| Trigger name | `manual` |
| `type` | `Request` |
| `kind` | `PowerAppV2` |
| `method` | _not declared_ |
| `relativePath` | _not declared — the trigger accepts the bare invoke path_ |
| `triggerAuthenticationType` | _absent from the export — no posture is recorded_ |
| `operationOptions` | _not declared_ |
| `conditions` | _not declared_ |
| `splitOn` | _not declared_ |
| `correlation` | _not declared_ |
| `recurrence` | _not declared_ |
| Declared request headers | _not declared_ |
| Declared query parameters | _not declared_ |
| `description` | _not declared_ |

### 5.1 Request payload — declared properties

| Property | Type | Required | Format | Enum | Default | Constraints | Description |
|---|---|---|---|---|---|---|---|
| `text` | `string` | **yes** | — | — | — | — | Add relevant columns to retrieve |
| `text_1` | `string` | **yes** | — | — | — | — | Add Complex Columns to breakdown |
| `text_2` | `string` | **yes** | — | — | — | — | Provide Filter Parameters |

Root `required`: `text`, `text_1`, `text_2`. Root `additionalProperties`: _not declared_.

### 5.2 Request payload — schema as provisioned

```json
{
  "type": "object",
  "properties": {
    "text": {
      "title": "SelectColumns",
      "type": "string",
      "x-ms-dynamically-added": true,
      "description": "Add relevant columns to retrieve",
      "x-ms-content-hint": "TEXT"
    },
    "text_1": {
      "title": "ExpandColumns",
      "type": "string",
      "x-ms-dynamically-added": true,
      "description": "Add Complex Columns to breakdown",
      "x-ms-content-hint": "TEXT"
    },
    "text_2": {
      "title": "FilterQuery",
      "type": "string",
      "x-ms-dynamically-added": true,
      "description": "Provide Filter Parameters",
      "x-ms-content-hint": "TEXT"
    }
  },
  "required": [
    "text",
    "text_1",
    "text_2"
  ]
}
```

## 6. Responses returned to the caller

| Action | Status code | Runs after | On statuses |
|---|---|---|---|
| `Respond_to_a_Power_App_or_flow` | `200` | `Compose_HTTP_Data` | Succeeded |

### 6.1 `Respond_to_a_Power_App_or_flow`

| Field | As provisioned |
|---|---|
| `statusCode` | `200` |
| `operationOptions` | _not declared_ |

_No response headers are configured._

**Response body, verbatim:**

```json
{
  "gbltasksflowresponse": "@{body('Select_1')}"
}
```

**Declared response schema, verbatim:**

```json
{
  "type": "object",
  "properties": {
    "gbltasksflowresponse": {
      "title": "gbltasksflowresponse",
      "x-ms-dynamically-added": true,
      "type": "string"
    }
  }
}
```

## 7. Termination

_No `Terminate` action. The run ends when the action graph does._

## 8. Error handling and run-time configuration

_No action is wired to run on `Failed`, `Skipped` or `TimedOut`. Every dependency in
this flow is a success edge._

_No action carries a retry policy, secure-data setting, operation option, tracked
property or loop limit. Every call runs on the platform defaults._

## 9. Variables

_This package initialises no variables._

## 10. SharePoint operations

Each row is one connector call. `table` is printed as configured; the resolved column
names the list that GUID belongs to per `docs/reference/sharepoint-list-index.json`,
and says so plainly when the index does not carry it.

| Action | Operation | Connection | `dataset` (site) | `table` (list) | Resolves to |
|---|---|---|---|---|---|
| `Send_an_HTTP_request_to_SharePoint` | `HttpRequest` | `shared_sharepointonline` | `https://nitdanigeria.sharepoint.com/sites/NITDADGO-EAAACTIVITYTRACKING` | `null` | _not in the list index_ |

## 11. Outbound HTTP calls

_This package makes no raw HTTP call._

## 12. Action dependency register

Every action in the package, in graph order, with the edge that starts it. `↳` marks
nesting depth; the path is the address used everywhere else on this page.

| Action path | Type | Kind | Runs after | On statuses |
|---|---|---|---|---|
| `Compose_ODataQuery` | `Compose` | — | _first in its scope_ | — |
| `Compose_HTTP_Data` | `Compose` | — | `Select_1` | Succeeded |
| `Respond_to_a_Power_App_or_flow` | `Response` | `PowerApp` | `Compose_HTTP_Data` | Succeeded |
| `Send_an_HTTP_request_to_SharePoint` | `OpenApiConnection` | — | `Compose_ODataQuery` | Succeeded |
| `Parse_JSON_2` | `ParseJson` | — | `Send_an_HTTP_request_to_SharePoint` | Succeeded |
| `Select_1` | `Select` | — | `Parse_JSON_2` | Succeeded |

## 13. Configured values, action by action

The configured input of every action, verbatim. An action with no `inputs` key — a
`Scope`, an `If`, a `Foreach` — is documented by its control expression instead.

### `Compose_ODataQuery`

| Field | As provisioned |
|---|---|
| Type | `Compose` |
| Kind | — |
| Description | _not declared_ |
| Runs after | _first in its scope_ |

**`inputs`, verbatim:**

```json
"@concat(\n  '?$select=', triggerBody()['text'],\n  if(not(empty(triggerBody()['text_1'])), concat('&$expand=', triggerBody()['text_1']), ''),\n  if(not(empty(triggerBody()['text_2'])), concat('&$filter=', triggerBody()['text_2']), ''),\n  '&$orderby=Created desc',\n  '&$top=2000'\n)"
```

### `Compose_HTTP_Data`

| Field | As provisioned |
|---|---|
| Type | `Compose` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Select_1` (Succeeded) |

**`inputs`, verbatim:**

```json
"@string(outputs('Send_an_HTTP_request_to_SharePoint')?['body'])"
```

### `Respond_to_a_Power_App_or_flow`

| Field | As provisioned |
|---|---|
| Type | `Response` |
| Kind | `PowerApp` |
| Description | _not declared_ |
| Runs after | `Compose_HTTP_Data` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "statusCode": 200,
  "body": {
    "gbltasksflowresponse": "@{body('Select_1')}"
  },
  "schema": {
    "type": "object",
    "properties": {
      "gbltasksflowresponse": {
        "title": "gbltasksflowresponse",
        "x-ms-dynamically-added": true,
        "type": "string"
      }
    }
  }
}
```

### `Send_an_HTTP_request_to_SharePoint`

| Field | As provisioned |
|---|---|
| Type | `OpenApiConnection` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Compose_ODataQuery` (Succeeded) |
| Connector | `shared_sharepointonline` |
| Operation | `HttpRequest` |

**`inputs`, verbatim:**

```json
{
  "parameters": {
    "dataset": "https://nitdanigeria.sharepoint.com/sites/NITDADGO-EAAACTIVITYTRACKING",
    "parameters/method": "GET",
    "parameters/uri": "_api/web/lists/getbytitle('Global Tracking Queue')/items@{outputs('Compose_ODataQuery')}",
    "parameters/headers": {
      "Accept": "application/json;odata=nometadata"
    }
  },
  "host": {
    "apiId": "/providers/Microsoft.PowerApps/apis/shared_sharepointonline",
    "connectionName": "shared_sharepointonline",
    "operationId": "HttpRequest"
  },
  "authentication": {
    "value": "@json(decodeBase64(triggerOutputs().headers['X-MS-APIM-Tokens']))['$ConnectionKey']",
    "type": "Raw"
  }
}
```

### `Parse_JSON_2`

| Field | As provisioned |
|---|---|
| Type | `ParseJson` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Send_an_HTTP_request_to_SharePoint` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "content": "@body('Send_an_HTTP_request_to_SharePoint')",
  "schema": {
    "type": "object",
    "properties": {
      "statusCode": {
        "type": "integer"
      },
      "headers": {
        "type": "object",
        "properties": {
          "Cache-Control": {
            "type": "string"
          },
          "Transfer-Encoding": {
            "type": "string"
          },
          "Vary": {
            "type": "string"
          },
          "Set-Cookie": {
            "type": "string"
          },
          "Strict-Transport-Security": {
            "type": "string"
          },
          "X-NetworkStatistics": {
            "type": "string"
          },
          "X-SharePointHealthScore": {
            "type": "string"
          },
          "X-SP-SERVERSTATE": {
            "type": "string"
          },
          "DATASERVICEVERSION": {
            "type": "string"
          },
          "SPClientServiceRequestDuration": {
            "type": "string"
          },
          "IsOCDI": {
            "type": "string"
          },
          "X-DataBoundary": {
            "type": "string"
          },
          "X-1DSCollectorUrl": {
            "type": "string"
          },
          "X-AriaCollectorURL": {
            "type": "string"
          },
          "SPRequestGuid": {
            "type": "string"
          },
          "Request-Id": {
            "type": "string"
          },
          "MS-CV": {
            "type": "string"
          },
          "SPLogId": {
            "type": "string"
          },
          "X-Frame-Options": {
            "type": "string"
          },
          "Content-Security-Policy": {
            "type": "string"
          },
          "MicrosoftSharePointTeamServices": {
            "type": "string"
          },
          "X-Content-Type-Options": {
            "type": "string"
          },
          "X-MS-InvokeApp": {
            "type": "string"
          },
          "P3P": {
            "type": "string"
          },
          "X-AspNet-Version": {
            "type": "string"
          },
          "X-Powered-By": {
            "type": "string"
          },
          "x-ms-request-id": {
            "type": "string"
          },
          "x-ms-environment-id": {
            "type": "string"
          },
          "x-ms-tenant-id": {
            "type": "string"
          },
          "x-ms-dlp-re": {
            "type": "string"
          },
          "x-ms-dlp-gu": {
            "type": "string"
          },
          "x-ms-dlp-ef": {
            "type": "string"
          },
          "Timing-Allow-Origin": {
            "type": "string"
          },
          "x-ms-apihub-cached-response": {
            "type": "string"
          },
          "x-ms-apihub-obo": {
            "type": "string"
          },
          "Date": {
            "type": "string"
          },
          "Content-Type": {
            "type": "string"
          },
          "Expires": {
            "type": "string"
          },
          "Last-Modified": {
            "type": "string"
          },
          "Content-Length": {
            "type": "string"
          }
        }
      },
      "body": {
        "type": "object",
        "properties": {
          "odata.nextLink": {
            "type": "string"
          },
          "value": {
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "RefID": {
                  "type": "array"
                },
                "Editor": {
                  "type": "object",
                  "properties": {
                    "EMail": {
                      "type": "string"
                    }
                  }
                },
                "GBDLookUp": {
                  "type": "array"
                },
                "Author": {
                  "type": "object",
                  "properties": {
                    "Title": {
                      "type": "string"
                    }
                  }
                },
                "Id": {
                  "type": "integer"
                },
                "Title": {
                  "type": "string"
                },
                "Description": {
                  "type": "string"
                },
                "Progress": {
                  "type": "string"
                },
                "Priority": {
                  "type": "string"
                },
                "StartDate": {
                  "type": "string"
                },
                "DueDate": {
                  "type": "string"
                },
                "GDSUROUT": {
                  "type": "string"
                },
                "AssignedTo": {
                  "type": "string"
                },
                "Created": {
                  "type": "string"
                },
                "ID": {
                  "type": "integer"
                }
              },
              "required": [
                "RefID",
                "Editor",
                "GBDLookUp",
                "Author",
                "Id",
                "Title",
                "Description",
                "Progress",
                "Priority",
                "StartDate",
                "DueDate",
                "GDSUROUT",
                "AssignedTo",
                "Created",
                "ID"
              ]
            }
          }
        }
      }
    }
  }
}
```

### `Select_1`

| Field | As provisioned |
|---|---|
| Type | `Select` |
| Kind | — |
| Description | _not declared_ |
| Runs after | `Parse_JSON_2` (Succeeded) |

**`inputs`, verbatim:**

```json
{
  "from": "@body('Parse_JSON_2')?['value']",
  "select": {
    "Title": "@{coalesce(item()?['Author']?['Title'], 'No Title')}",
    "AssignedTo": "@{coalesce(item()?['AssignedTo'], 'Unassigned')}",
    "Created": "@{coalesce(item()?['Created'], 'No Date')}",
    "Priority": "@{coalesce(item()?['Priority'], 'Normal')}",
    "Progress": "@{coalesce(item()?['Progress'], 'Not Started')}",
    "DueDate": "@{coalesce(item()?['DueDate'], 'No Due Date')}",
    "StartDate": "@{coalesce(item()?['StartDate'], 'No Start Date')}",
    "Description": "@{coalesce(item()?['Description'], 'No Description')}",
    "Editor": "@{coalesce(item()?['Editor'], 'No Editor')}",
    "Author": "@{coalesce(item()?['Author'], 'No Author')}",
    "RefID": "@{coalesce(item()?['RefID'], 'No RefID')}",
    "GBDLookUp": "@{coalesce(item()?['GBDLookUp'], 'No Lookup')}",
    "GDSUROUT": "@{coalesce(item()?['GDSUROUT'], 'No Route')}",
    "ID": "@{coalesce(item()?['ID'], 'No ID')}"
  }
}
```

