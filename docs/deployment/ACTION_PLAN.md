# Action plan — the 22 open items

> **This document carries steps, and owns none of them.** 24 **Do** blocks, one per open item, with its closing condition,
> every one generated from [`PRODUCTION_READINESS_REGISTER.json`](./PRODUCTION_READINESS_REGISTER.json)
> by `scripts/build-action-plan.mjs`. Change the register, not this file — a `--check` fails the build on drift.
>
> Console steps are elsewhere: [`PORTAL-TENANT-RUNBOOK.md`](./PORTAL-TENANT-RUNBOOK.md) and [`GOVERNANCE-TENANT-RUNBOOK.md`](./governance/GOVERNANCE-TENANT-RUNBOOK.md).

Generated by `npm run actions` from [`PRODUCTION_READINESS_REGISTER.json`](./PRODUCTION_READINESS_REGISTER.json).
Do not edit by hand — `npm run test:actions` fails on drift.

48 items in the register. 26 closed.
**22 open**: 7 APPLIED_UNVERIFIED, 4 BLOCKED, 4 DECISION_REQUIRED, 2 EVIDENCE_INCOMPLETE, 2 READY, 3 SPECIFIED_NOT_APPLIED.

Ordered by what unblocks the most. Items 1–7 come first because the rest depend on
them; the agency decisions are last only because nothing else waits on them being done in order.

Nothing here is closable from this repository. Each item names the party that acts.

---

### 1. ITEM-2 — The tenant write path has never been executed

**Status** BLOCKED  ·  **Acts** operator

**Do**
   1. Apply one flow by the route you intend to use. Console route: open make.powerautomate.com, open any flow you own so the page holds a token, then paste one file from docs/deployment/internal/flows/console-apply/ into the browser devtools console — it carries DRY_RUN = true and changes nothing on the first paste. PowerShell route: the dry run documented in docs/deployment/sharepoint/remediation/patched/README.md. (Corrected 2026-09-15: this step cited remediation/patched/README.md, a path that does not exist in this repository — the file is under docs/deployment/sharepoint/. The evidence entry on this item already carried the full path; only the step text was wrong, so an operator following the step hit a dead end while the citation beside it resolved.)
   2. Confirm it reports the flow name and action count, and that a fresh export carries the change.
   3. Record the result; if it fails, raise it before any delivery-track item proceeds.

**Closes when** One flow carries a change that was applied from outside the Power Automate designer, and a fresh export of that flow contains it. The dry run is the mandatory first pass and is not on its own sufficient: it reads and reports, and this item exists to prove that a WRITE reaches the tenant.

---

### 2. MANUAL-2 — Test records must be cleared before real correspondence arrives

**Status** READY  ·  **Acts** operator
**Blocked by** CFG-1, CFG-2, all commissioning verification

**Do**
   1. Complete commissioning verification. 2. Identify every test row. 3. Remove them. 4. Decide and record the sequence-counter treatment.

**Closes when** No test row remains, and the sequence counter's starting value is recorded.

---

### 3. ITEM-43 — The portal still calls the defective OTP flow — the new one is deployed but not in service

**Status** READY  ·  **Acts** operator
**Blocked by** ITEM-38 — the new flow exists and is verified. Met 2026-09-04.

**Do**
   1. Put the new flow's trigger URL into the deployed configuration as OTP_VERIFY.
   2. Exercise the four tests in BUILD.md against it: verify as another caller must fail; verify as the right caller must succeed; verify with no valid code must send no mail; six wrong guesses must return 429.
   3. Turn off 3e201620-f1e8-4c17-a90a-4d95b94a24c2. Not delete — off, so it can be re-enabled if step 2 finds something.
   4. Re-run npm run test:triggerauth and npm run wiring, and record the outcome as an evidence file.

**Closes when** The deployed configuration resolves OTP_VERIFY to the flow the tenant register names (b372d45e4b2a47d88b8e8b032da67fcd, serving OTP_GENERATE and OTP_VERIFY), the OTP tests pass against it, and no configuration resolves OTP_VERIFY to a flow absent from the register. Re-based: the register contains neither c5e314c7-68b5-4bdc-8935-0954ef6a256d nor the defective 3e201620-f1e8-4c17-a90a-4d95b94a24c2, so the original criteria cannot be evaluated.

---

### 4. ITEM-56 — The OTP allow-list row is set — applied by the operator, not verified here

**Status** APPLIED_UNVERIFIED  ·  **Acts** operator
**Blocked by** The portal's deployed origin must be known, ITEM-55 (the flow must read the allow-list at all) — met, ITEM-2 — the resolution criterion is a live OTP verify call, and whyOpen states ITEM-2 remains the first such proof. Recorded here 2026-09-15 for the same reason as ITEM-12: stated in prose, absent from this field, so it read as released.

**Do**
   Open the configuration list the OTP flows read via Get_Allowed_Origins_Otp.
   Confirm a ConfigValue row carries the portal's deployed origin, exactly as the browser sends it in the Origin header — scheme and host, no trailing slash.
   Make the first live OTP call (ITEM-43 / R-3) and confirm the response carries that origin back.

**Closes when** A live OTP verify call from the portal returns Access-Control-Allow-Origin equal to the portal's origin.

---

### 5. ITEM-44 — The client spoke a protocol neither OTP flow reads — reconciled on the client side

**Status** APPLIED_UNVERIFIED  ·  **Acts** operator (the live proof); the direction is settled

**Do**
   1. Done — the client sends action generate/verify with identifier and otp_code at the top level.
   2. Done — npm run test:otpprotocol asserts the seam against the deployed definitions.
   3. Outstanding — the recipient decision: Case_Generate mails the code to the literal dgsregistry@nitda.gov.ng rather than to the requester. That is a flow change and an agency call.
   4. Outstanding — run the four cutover tests through the platform (ITEM-43).

**Closes when** One request from the platform reaches a matched Switch case, and one code issued through the platform verifies through the platform. Until both are observed against the tenant, this is unresolved regardless of what the code reads like.

---

### 6. G-04 — No server-side enforcement is running anywhere

**Status** BLOCKED  ·  **Acts** Power Automate (implementation); agency (posture decision)
**Blocked by** Each flow must be reachable and editable, which depends on ITEM-2 and ITEM-35.

**Do**
   1. Decide the posture (pilot vs enforced) — agency.
   2. If enforced: implement token validation, role derivation and per-action authorisation in each flow.
   3. Set auth.enabled and supply tenant configuration.
   4. Verify a viewer cannot reach a privileged action by editing local storage.

**Closes when** A flow called directly with a forged or absent identity refuses the action.

---

### 7. ITEM-11 — An unknown reference is answered 200 and rendered as found

**Status** APPLIED_UNVERIFIED  ·  **Acts** operator
**Blocked by** ITEM-2 (the patch route must be proven) or the paste route via ITEM-35., ITEM-23 — the filter reads the indexed ReferenceId.

**Do**
   1. Apply the artifact (patch or paste). 2. Test an unknown reference. 3. Test a valid reference with the wrong email. 4. Compare the two responses byte for byte.

**Closes when** An unknown reference returns 404, a wrong-email pairing returns a materially identical 404, and track.js shows the denial.

---

### 8. ITEM-12 — A submission reports delivered while its attachments are never sent

**Status** APPLIED_UNVERIFIED  ·  **Acts** operator
**Blocked by** ITEM-35 (SUBMISSION and UPLOAD targets are among the five resolved) — met., ITEM-2 — whyOpen states it outright: 'Verification is blocked on ITEM-2 and on nothing else.' Recorded here 2026-09-15 because it was stated in prose and not in this field, so every instrument reading dependencies structurally read this item as released when it is not.

**Do**
   1. Paste Portal_SUBMISSION_ECM_DOCS onto de9ef13b. 2. Paste Portal_UPLOAD_ECM_DOCS onto df7ddff1. 3. Submit with an attachment end to end.

**Closes when** A submission with an attachment produces a row in Portal Attachments.

---

### 9. ITEM-23 — Five index targets unset on hot-path lists

**Status** APPLIED_UNVERIFIED  ·  **Acts** operator

**Do**
   1. Run §2 of docs/deployment/PORTAL-TENANT-RUNBOOK.md against the sites. 2. Confirm the index pass reports 5 already indexed on a second run.

**Closes when** The provisioner's index pass reports 5 already indexed. A single assignment against a known reference returns 200; a flag write against a known reference returns 200.

---

### 10. ITEM-25 — 15 of 19 DYNAMIC_ACTIONS operations answer 501

**Status** EVIDENCE_INCOMPLETE  ·  **Acts** operator + author

**Do**
   1. Read the Global Tracking Queue column set in the tenant and check for Status, NVERSE and Category.
   2. Confirm or correct the category -> Category0 pairing on DGO DIGITAL OPS.
   3. Decide the dispatch and archive question; if they persist, name the list and capture its columns.
   4. Record every confirmed column in internal-field-evidence.json with its evidence class.
   5. Build the write bodies; npm run test:listcolumns must stay green.

**Closes when** npm run designerpaste builds the writes and no operation answers 501.

---

### 11. ITEM-30 — Portal Flow Telemetry.RunRecordJson is not provisioned, so no run record is retained

**Status** APPLIED_UNVERIFIED  ·  **Acts** operator + author
**Blocked by** Same provisioner run as ITEM-23 and ITEM-8.

**Do**
   1. Run §2 of docs/deployment/PORTAL-TENANT-RUNBOOK.md.
   2. Flip RunRecordJson out of PENDING in portal-field-spec.json.
   3. Run npm run build:internalpaste && npm run build:designerpaste.
   4. Confirm the write reappears in the packages, then re-paste.

**Closes when** A telemetry row carries a run record.

---

### 12. ITEM-40 — Four endpoint triggers answer every verb and five carry no request schema

**Status** EVIDENCE_INCOMPLETE  ·  **Acts** operator

**Do**
   1. Set Method = POST on IP_Get_Docs_Endpoint. 2. Export each of the other thirteen and run npm run compare:export, which reports the trigger. 3. Set Method = POST wherever it is unset.

**Closes when** Every endpoint trigger is recorded as method POST with a request schema, read from an export taken after the change. Currently answeringEveryVerb: 4 and withNoRequestSchema: 5 against a target of 0 and 0; unread is already 0.

---

### 13. ITEM-48 — The portal promises citizens four emails the estate does not send

**Status** SPECIFIED_NOT_APPLIED  ·  **Acts** Platform technical owner for the sends; the agency for any decision to withdraw a promise instead.
**Blocked by** ITEM-39 — the decision and action-required mails need a status-change or scheduled carrier, which does not exist

**Do**
   Decide build or withdraw, per promise.
   For the confirmation: add the send and the outbox receipt to the SUBMISSION package, regenerate, and re-run npm run designerpaste.
   For the outcome mails: land ITEM-39 first, then key the sends off the status write.
   Re-run npm run notifications -- --check; RN-001 and RN-005 must derive PROVISIONED.

**Closes when** npm run notifications -- --check derives PROVISIONED for RN-001 and RN-005, and either PROVISIONED for RN-003 and RN-004 or the portal copy no longer makes the promise, which the verifier's portal-promise checks will detect and fail on until the matrix is updated.

---

### 14. ITEM-50 — Verification codes, correspondence, reports and assignment notices go to a fixed mailbox

**Status** SPECIFIED_NOT_APPLIED  ·  **Acts** Platform technical owner
**Blocked by** ITEM-4 — the internal packages are pasted as part of the same visit

**Do**
   Paste Portal_VERIFY_ECM_DOCS and Portal_VERIFY_CONFIRM_ECM_DOCS per Session 4 — this closes RN-006.
   Paste DGO_SEND_EMAIL into the flow the configured EMAIL URL resolves to — it addresses every send from the payload, gates on DGO_UserDirectory, and writes a Portal Outbox Receipts row per send. The paste target is not settled in this repository: read the workflows/<32-hex> segment off the EMAIL entry in config/config.local.js first (EXECUTION_GUIDE §6.7).
   Alternatively, if the deployed Web - Send Email body is to be kept, apply docs/deployment/sharepoint/remediation/patched/Web_Send_Email.definition.json — but correct its recipient expression first: it reads triggerBody()?['to'], and every one of the three desks nests the recipient under payload.
   Patch Web - OTP Generate to use the same dynamic form its sibling Web - OTP Verify already carries.
   Paste DGO_OTP, DGO_SINGLE_ASSIGNMENT and DGO_BULK_ASSIGNMENT per the walkthrough.
   Re-address the new-submission notice and add it to the SUBMISSION package.
   Re-run npm run notifications -- --check.

**Closes when** npm run notifications -- --check derives PROVISIONED for RN-006, RN-016, RN-021, RN-024 and RN-037, and the fixed-recipient census shows no verification, correspondence, report or assignment send hard-coding an address.

---

### 15. ITEM-51 — Officers are never told of assignments, approvals, escalations, delegations or reminders

**Status** SPECIFIED_NOT_APPLIED  ·  **Acts** Platform technical owner, with the correspondence lifecycle process owner for the copy-to decision
**Blocked by** GAP-001 must be closed for notify-owner before its audit trail can be trusted

**Do**
   Port the assignee recipient expression from Deployed - Create Task into DGO_SINGLE_ASSIGNMENT — this is a regression to prevent, not a feature to design.
   Add 'notify-owner' to config/action-ownership.config.js with an owner, service, audit event and mandatory backend.
   Change remind-assignee's backend from DYNAMIC_ACTIONS.optional to mandatory, or change the operator copy so it does not claim the assignee was reminded.
   Add the assignment and approval sends to the packages.
   Re-run npm run notifications -- --check and npm run test:governance.

**Closes when** npm run notifications -- --check derives PROVISIONED for RN-009, RN-011, RN-012, RN-015, RN-017, RN-018, RN-019, RN-034, RN-036 and RN-038, and no runtime action reports a notification as queued that has no carrier.

---

### 16. ITEM-6 — Visit 4 — ECM_DOCS_INTAKE write-back

**Status** APPLIED_UNVERIFIED  ·  **Acts** operator
**Blocked by** ITEM-2

**Do**
   1. Patch. 2. Verify. 3. Apply. 4. Re-export and sweep.

**Closes when** Circle C7 closes: a citizen's status can change.

---

### 17. ITEM-9 — 39 of 77 flows have no stated purpose

**Status** BLOCKED  ·  **Acts** someone who knows the estate

**Do**
   1. Identify who knows the estate's history. 2. Walk the 27 unattributed flows with them. 3. Record one sentence each into the contract register. 4. Regenerate the catalogue.

**Closes when** The catalogue reports 0 with no stated purpose.

---

### 18. MANUAL-3 — The browser suite must run against the deployed build

**Status** BLOCKED  ·  **Acts** operator
**Blocked by** CFG-1, CFG-2

**Do**
   1. Deploy. 2. Point baseURL at the deployed hostname. 3. Run all ten spec files. 4. Record the result.

**Closes when** All ten spec files pass against the deployed hostname.

---

### 19. ITEM-37 — The sanctioned crossing does not exist in the live flow — restore it, or amend D1 and D6

**Status** DECISION_REQUIRED  ·  **Acts** agency
**Blocked by** ITEM-35 established that this id is one flow renamed in place; that finding is the input to this one., The UPLOAD paste (EXECUTION_GUIDE 5.2) targets this same workflow and must not proceed until this is settled.

**Do**
   1. Read item37_settled in the 2026-09-01 paste-state evidence. 2. Decide whether the portal must feed the internal registry at all. 3. Record the decision, and amend D1 and D6 if it is not to be restored.

**Closes when** The agency records one of two things: that D1 and D6 stand as written, given the crossing measures present — which closes this item with no work — or that the crossing should sit somewhere other than where it does, which makes it a change request rather than a restoration.

---

### 20. ITEM-41 — Content approval is still ON for Global Tracking Queue — 12,079 of 15,936 rows are invisible to readers

**Status** DECISION_REQUIRED  ·  **Acts** agency

**Do**
   1. Decide whether this list is to carry content approval. 2. If not, run the approval script with APPLY set and confirm by read-back.

**Closes when** A recorded decision, and if it is to be off, a read-back confirming EnableModeration is false.

---

### 21. ITEM-52 — Read endpoints and AI flows email record payloads to a shared mailbox

**Status** DECISION_REQUIRED  ·  **Acts** The agency for SN-001, alongside MANUAL-4; the platform technical owner for SN-002 to SN-004.
**Blocked by** MANUAL-4 — the posture decision on citizens' personal data covers the read-endpoint payload sends, ITEM-9 — 39 of 77 flows have no stated purpose, which is what makes 'is this still wanted' unanswerable from the repository (corrected 2026-09-15 from '27 of 58', the count this citation carried after ITEM-9's own text had been corrected against docs/reference/FLOW_CATALOGUE.md)

**Do**
   Review SN-001 alongside MANUAL-4 — they are the same posture decision.
   Confirm with estate knowledge whether SN-002's AI sends are still wanted in production.
   Retire SN-004's audit mails in favour of the audit lists, or record why the mail copy is needed.
   Remove or complete the SN-003 empty send.
   Update SurplusNotifications and re-run npm run notifications -- --check.

**Closes when** Each of the four surplus patterns carries a recorded decision, and any send decided against is gone from the exported estate.

---

### 22. ITEM-57 — The archived design system is unreconciled against the live one, and nobody has said which is authoritative

**Status** DECISION_REQUIRED  ·  **Acts** the agency, with whoever owns the platform's visual design

**Do**
   Read docs/archive/dgo-ops/design-system/docs/01-tokens.md against document-portal/ds/colors_and_type.css.
   Either mark the archived set superseded in its README, or raise a scoped item for the parts worth promoting.
   If superseded, nothing further is required — it stays as a record.

**Closes when** The archived design system carries an explicit statement of its standing relative to document-portal/ds/, decided rather than inferred.
