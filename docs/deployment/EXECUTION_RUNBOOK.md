# Execution runbook

> **This document carries steps, and owns none of them.** 48 **Steps** blocks, one per open item, in dependency order,
> every one generated from [`PRODUCTION_READINESS_REGISTER.json`](./PRODUCTION_READINESS_REGISTER.json)
> by `scripts/build-execution-runbook.mjs`. Change the register, not this file — a `--check` fails the build on drift.
>
> Console steps are elsewhere: [`PORTAL-TENANT-RUNBOOK.md`](./PORTAL-TENANT-RUNBOOK.md) and [`GOVERNANCE-TENANT-RUNBOOK.md`](./governance/GOVERNANCE-TENANT-RUNBOOK.md).
>
> The command-line commissioning path is
> [`CLEAR-THE-LAST-BLOCKER.md`](./CLEAR-THE-LAST-BLOCKER.md) — on a phone,
> [`CLEAR-THE-LAST-BLOCKER-TERMUX.md`](./CLEAR-THE-LAST-BLOCKER-TERMUX.md).

**Generated by `node scripts/build-execution-runbook.mjs` from
[`PRODUCTION_READINESS_REGISTER.json`](./PRODUCTION_READINESS_REGISTER.json).** Do not edit by
hand — the order below is a topological sort over the register's own `dependencies`, so editing
this file makes it disagree with the data it came from. Change the register and regenerate.

Register version 1.2.5, compiled against commit `4608832`
on 2026-09-15. 48 items in 4 waves.

## What a wave means

A wave is the set of items whose blockers have all cleared in earlier waves. Nothing in a wave
waits on anything else in the same wave, so its items may be worked in any order, or at once, by
whoever owns them. **A wave is not a schedule.** No item in this register carries a target date —
none is established anywhere in the estate — and no item has ever been executed against the
tenant, so nothing here can say how long a wave takes.

## What is not modelled

Some dependencies are conditions rather than items — a licensing confirmation, a decision, a
deployed build to test against. The sort cannot order those, so they are carried into each item
as **Also waits on** and must be read before starting it.


---

## Wave 1 — 28 item(s)

**Owners needed:** Operational owner · Platform technical owner · Platform technical owner, with the correspondence lifecycle process owner for the copy-to decision · Power Automate (implementation); agency (posture decision) · agency · agency + author · author · operator · operator (the live proof); the direction is settled · operator + author · security authority · someone who knows the estate · the agency, with whoever owns the platform's visual design

### ITEM-22 — Rotate the disclosed trigger tokens — done; the disclosed set is superseded

| | |
|---|---|
| **Status** | RESOLVED |
| **Category** | BLOCKING |
| **Owner** | security authority |
| **Target date** | _none established_ |
| **Waits on (in register)** | _nothing_ |
| **Also waits on** | Redaction must be applied to the four leaking flows BEFORE rotation, or the regenerated tokens leak by the same route. |

55 live sig= trigger tokens are disclosed across 39 workflows, in 193 occurrences across 28 tracked files under docs/reference/foundational/, and in git history. Separately, four deployed flows capture x-ms-igw-external-uri and x-ms-igw-raw-target, which carry the inbound URL including its token, into a run record emailed on every run.

**Why it closed.** Closed. It was open because rotation can only be performed in Power Automate and no repository action could close it; that rotation has since been performed and reconciled against the tenant endpoint register.

**Constraints**

- A sig= token is a bearer credential and must not be pasted into a ticket, an email or a chat message (EXECUTION_GUIDE Appendix B).

**Decisions required first**

- 2026-09-08: closed on measurement. The 43 signatures under docs/reference/foundational/ remain committed and remain in git history; they are burned rather than removed. D5 keeps the corpus verbatim as the record of the pre-rotation estate.

**Steps**

- Work the order in docs/deployment/rotation/ROTATION.md. It is six steps, and the first (rotate the 24 workflows behind no endpoint key) shrinks the disclosed surface without touching either runtime.

**Done when.** A scan of the tenant flow URLs shares no token with this repository. Met: docs/reference/endpoint-register.json is the tenant export and shares none of the 43 disclosed signatures.

**Validated by.** Tenant-side scan of every flow trigger URL, compared against the 55 signatures npm run commission reports. Repository-side, npm run designerpaste check 9b confirms the generated packages continue to blank both headers.

### ITEM-2 — The tenant write path has never been executed

| | |
|---|---|
| **Status** | BLOCKED |
| **Category** | BLOCKING |
| **Owner** | operator |
| **Target date** | _none established_ |
| **Waits on (in register)** | _nothing_ |

scripts/update-flow-definition.ps1 PATCHes a flow definition back to the tenant, which is the same call the designer makes on Save. It has never been run. The patch track is no longer the only delivery route: PowerShell was dropped from the automation on 2026-09-08 because PnP.PowerShell needs an Entra app registration the estate had already ruled out, and the paste track — docs/deployment/internal/flows/console-apply/, one snippet per flow package, generated by npm run console:apply — needs no PowerShell, no app registration and no tenant admin. What is still unproven is that ANY write reaches the tenant from outside the designer, which is what this item is for; prove it by whichever route you intend to deliver by.

**Why it is still open.** Requires tenant credentials and a Power Automate session. Nothing in this repository can execute it. (Criterion corrected 2026-09-15. It previously read "The dry run prints the flow name and action count without error" — which this item's own steps contradicted, since step 2 asks for a fresh export carrying the change. The steps were right and the criterion was wrong: a dry run does not write, and the title of this item is that the WRITE path has never been executed. Ten items name ITEM-2 as a prerequisite and every one of them needs its endpoint exercised, which a rehearsal cannot deliver. Closing this on a dry run would have been the applied-versus-observed collapse this register exists to prevent, committed on the item that gates the most.)

**Constraints**

- The dry run must be used first; it sends nothing.

**Steps**

- Apply one flow by the route you intend to use. Console route: open make.powerautomate.com, open any flow you own so the page holds a token, then paste one file from docs/deployment/internal/flows/console-apply/ into the browser devtools console — it carries DRY_RUN = true and changes nothing on the first paste. PowerShell route: the dry run documented in docs/deployment/sharepoint/remediation/patched/README.md. (Corrected 2026-09-15: this step cited remediation/patched/README.md, a path that does not exist in this repository — the file is under docs/deployment/sharepoint/. The evidence entry on this item already carried the full path; only the step text was wrong, so an operator following the step hit a dead end while the citation beside it resolved.)
- Confirm it reports the flow name and action count, and that a fresh export carries the change.
- Record the result; if it fails, raise it before any delivery-track item proceeds.

**Done when.** One flow carries a change that was applied from outside the Power Automate designer, and a fresh export of that flow contains it. The dry run is the mandatory first pass and is not on its own sufficient: it reads and reports, and this item exists to prove that a WRITE reaches the tenant.

**Validated by.** Two artefacts, recorded against the flow they were taken on: the dry-run console output, and a fresh export of the same flow showing the applied change. Without the second, nothing distinguishes a reachable API from a successful write.

### G-04 — No server-side enforcement is running anywhere

| | |
|---|---|
| **Status** | BLOCKED |
| **Category** | BLOCKING |
| **Owner** | Power Automate (implementation); agency (posture decision) |
| **Target date** | _none established_ |
| **Waits on (in register)** | _nothing_ |
| **Also waits on** | Each flow must be reachable and editable, which depends on ITEM-2 and ITEM-35. |

The authenticating proxy that would have validated tokens, derived roles and authorised actions was built and then removed rather than deployed. Every obligation it carried now belongs to each flow individually: token validation, role derivation, per-action authorisation, rate limiting, reference minting, upload ticketing and the filename policy. Caller identity is a client-asserted userEmail read from localStorage.

**Why it is still open.** The work is in Power Automate, not in this repository. No code here can close it.

**Constraints**

- The generated packages already carry rate limiting, reference minting and upload ticketing. Token validation, role derivation and per-action authorisation are not in them.

**Decisions required first**

- Whether to go live in the pilot posture, or hold until enforcement runs. This is an agency risk decision, not a technical one.

**Steps**

- Decide the posture (pilot vs enforced) — agency.
- If enforced: implement token validation, role derivation and per-action authorisation in each flow.
- Set auth.enabled and supply tenant configuration.
- Verify a viewer cannot reach a privileged action by editing local storage.

**Done when.** A flow called directly with a forged or absent identity refuses the action.

**Validated by.** A direct call to each endpoint bypassing the client, asserting refusal. npm run commission ceases to report the pilot-posture warning.

### ITEM-23 — Five index targets unset on hot-path lists

| | |
|---|---|
| **Status** | APPLIED_UNVERIFIED |
| **Category** | PROVISIONING |
| **Owner** | operator |
| **Target date** | _none established_ |
| **Waits on (in register)** | _nothing_ |

Portal Rate Limits is filtered on every request to every endpoint; Portal Verification Proofs on every proof presentation; Portal Support Cases on every case; Global Tracking Queue.RefIDD by every assignment (15,804 items at capture); DGO DIGITAL OPS.RefIDD by the flag, status-transition and document-read paths (21,249 items at capture).

**Why it is still open.** The index requirement is met and verified: the apply pass set three Title indexes and the verify pass re-read all five targets and found every one indexed. What remains is the other half of the criterion — one assignment and one flag write against a known reference returning 200 — which is live traffic nobody has exercised. Separately, the run corrected the register: OPEN_ITEMS items 31 and 33 asserted these two columns were unindexed, and no file in this repository was ever a source for that.

**Constraints**

- SharePoint's 5,000-item view threshold makes an unindexed equality filter FAIL, not merely slow.

**Steps**

- Run §2 of docs/deployment/PORTAL-TENANT-RUNBOOK.md against the sites. 2. Confirm the index pass reports 5 already indexed on a second run.

**Done when.** The provisioner's index pass reports 5 already indexed. A single assignment against a known reference returns 200; a flag write against a known reference returns 200.

**Validated by.** Re-run the provisioner and read the index pass count; then exercise one assignment and one flag write end to end.

### ITEM-34 — The list holding the allowed origins is unowned and unaccounted for

| | |
|---|---|
| **Status** | RESOLVED |
| **Category** | PROVISIONING |
| **Owner** | operator + author |
| **Target date** | _none established_ |
| **Waits on (in register)** | _nothing_ |

Flow Configuration was created 2025-10-30, ten months before the estate capture; it carries no description, holds one item unchanged in over nine months, and none of the 58 exported flows names it. It is dormant and unowned.

**Why it closed.** Resolved 2026-08-31. The agency decided to keep Flow Configuration as the list the allowed origins are read from, and D15 records it with the tenant reading it was taken on. Moving the control would have been a rebuild of fourteen packages to buy provenance for a value that is public by nature — an allowed origin is a scheme and a host that every response already announces in its own header. The list remains unowned; what D15 settles is that this is accepted for this value and no other.

**Decisions required first**

- Keep Flow Configuration and provision ConfigValue on it; or move the control to DGO_EndpointRegistry, which is adopted and specified; or create DGO_PlatformConfig.

**Steps**

- Read the list's settings page for its column set. 2. Open the single row and record what it holds. 3. Decide keep / move / replace. 4. Record the decision in DECISIONS.md.

**Done when.** The origin is read from a list the estate can account for, and the decision is recorded.

**Validated by.** The decision appears in docs/deployment/sharepoint/DECISIONS.md, and ITEM-8's validation passes against the chosen list.

### ITEM-30 — Portal Flow Telemetry.RunRecordJson is not provisioned, so no run record is retained

| | |
|---|---|
| **Status** | APPLIED_UNVERIFIED |
| **Category** | PROVISIONING |
| **Owner** | operator + author |
| **Target date** | _none established_ |
| **Waits on (in register)** | _nothing_ |
| **Also waits on** | Same provisioner run as ITEM-23 and ITEM-8. |

Every generated package composes Compose_Flow_Run_Record. Nothing writes it, because the designer validates item/<Column> against the list at save time and would refuse the flow outright with WorkflowOperationParametersExtraParameter. The generator strips any write naming a column the spec marks PENDING, so the packages save and the compose wires itself the moment the column exists.

**Why it is still open.** The column exists in the tenant and all fourteen packages carry the write again — the builder and the schema checker now gate on PENDING and unprovisioned rather than on PENDING alone, so capturedState stays as the record of the 2026-08-14 capture and provisionedBy carries the current state. What remains is not repository work: one live run has to produce a Portal Flow Telemetry row carrying a run record, and nothing here can do that.

**Constraints**

- The order matters: provision the column, THEN flip PENDING, THEN rebuild. Flipping first produces packages that cannot be saved.

**Steps**

- Run §2 of docs/deployment/PORTAL-TENANT-RUNBOOK.md.
- Flip RunRecordJson out of PENDING in portal-field-spec.json.
- Run npm run build:internalpaste && npm run build:designerpaste.
- Confirm the write reappears in the packages, then re-paste.

**Done when.** A telemetry row carries a run record.

**Validated by.** npm run designerpaste after the rebuild shows the item/RunRecordJson write present; then one live run produces a Portal Flow Telemetry row carrying it.

### ITEM-7 — Third-party API keys rotated

| | |
|---|---|
| **Status** | RESOLVED |
| **Category** | PROVISIONING |
| **Owner** | operator |
| **Target date** | _none established_ |
| **Waits on (in register)** | _nothing_ |

Google x4, OpenRouter, OpenAI, Hugging Face. Redacted in the repository; redaction is not rotation.

**Why it closed.** Closed. The agency confirmed all rotations complete on 2026-09-08.

**Constraints**

- Revoke the old key LAST, after the replacement is in place, or the dependent service breaks.

**Decisions required first**

- 2026-09-08: agency confirmed all rotations complete. Recorded on the agencys statement; a 401 read-back against the old keys is the operators standing verification.

**Steps**

- Check each provider console for misuse. 2. Mint a replacement. 3. Move the value into a Power Automate environment variable or Key Vault reference. 4. Revoke the old key last.

**Done when.** The old keys return 401. Met: the agency confirmed on 2026-09-08 that all rotations are complete.

**Validated by.** A request with each old key returns 401 at its provider.

### ITEM-35 — Two paste targets unconfirmed; five resolved

| | |
|---|---|
| **Status** | RESOLVED |
| **Category** | APPLY |
| **Owner** | operator |
| **Target date** | _none established_ |
| **Waits on (in register)** | _nothing_ |

The EXECUTION_GUIDE named a paste target per package. Five were resolved on 2026-08-31 from the export set: the CG_* names are renames, but where the guide's id and the run record's id differ they are distinct flows, and the guide named the wrong one three times. Two remain unconfirmed because their live flows have no export here.

**Why it closed.** Resolved 2026-08-31. Both ids were exported from the tenant and resolve to exactly the flow the 2026-08-27 run records name. Neither id appears elsewhere in deployed/, so both are flows never previously captured rather than renames. All seven packages now name a target id confirmed present, and npm run wiring moved from 7/59 to 24/59 because the evidence arrived, not because anything was fixed.

**Constraints**

- Pasting on an unconfirmed id risks a correct body in a flow nothing calls: every repository check passes, the paste succeeds, and the endpoint keeps serving the old body.

**Steps**

- Locate 5e13db77 and 21d4bfd3 in the environment. 2. Export both. 3. Confirm all seven packages name a target id present in the export set.

**Done when.** All seven packages name a target id confirmed present in the environment.

**Validated by.** Both exports present in docs/reference/flow-contracts/deployed/, and npm run wiring can then see those endpoints.

### ITEM-18 — The upload SHA-256 cannot be computed on this tenant — the digest is recorded, not enforced

| | |
|---|---|
| **Status** | ACCEPTED |
| **Category** | UNSPECIFIED |
| **Owner** | author |
| **Target date** | _none established_ |
| **Waits on (in register)** | _nothing_ |
| **Also waits on** | Confirmation that the environment's licensing permits Execute JavaScript Code. |

INT-002 requires the upload to verify size and SHA-256 against the arriving bytes. Size is met: the package compares DeclaredSizeBytes to Content-Length and answers 422 on a mismatch. The digest cannot be. Workflow Definition Language has no hash function and no bitwise operators, so the only route inside Power Automate is an inline-code action, and the operator confirmed on 2026-09-03 that this tenant cannot run one. That is not an unwritten action or a porting job — it is a platform constraint, and no implementation closes it.

**Why it closed.** Accepted 2026-09-03, not closed by work. The requirement stands and cannot be met on this tenant. What is in place instead is disclosed above and is deliberately not described as an integrity control.

**Constraints**

- Until closed, no document may claim the digest is verified. npm run test:claims enforces this.

**Decisions required first**

- The digest is RECORDED, not ENFORCED. A client-computed SHA-256 is stored as DeclaredSha256 and supports out-of-band verification later; nothing verifies it at intake.
- The design example is not to be ported under any circumstance. It is typed Javascript, which does not exist, and compares against an uninterpolated literal, so a correct digest would be refused.

**Steps**

- Nothing to execute. If the tenant's Power Automate plan or region later provides an inline-code action, revisit this record: the action must then be written from scratch and demonstrated with two live uploads, one correct and one corrupted.

**Done when.** Accepted rather than resolved. The criterion is that the constraint stays recorded and unclaimed: npm run test:claims continues to pass, and the acceptance record remains cited by this item.

**Validated by.** node tests/standing-claims.test.mjs — the digest-verified claim is absent from every scanned document and fails when mutated in.

### ITEM-25 — 15 of 19 DYNAMIC_ACTIONS operations answer 501

| | |
|---|---|
| **Status** | EVIDENCE_INCOMPLETE |
| **Category** | UNSPECIFIED |
| **Owner** | operator + author |
| **Target date** | _none established_ |
| **Waits on (in register)** | _nothing_ |

The operations are recognised, authorised and audited, then explicitly refused. The client sees 501 NOT_IMPLEMENTED with applied: false, which is the truth; it previously saw 200 with applied: true. The column evidence was captured on 2026-08-31 and splits the fifteen into three different problems.

**Why it is still open.** Three distinct blockers. Status, NVERSE and Category are not on the Global Tracking Queue and NVERSE appears in no exported definition anywhere. The category -> Category0 pairing on DGO DIGITAL OPS is reasoned, not observed. And dispatch, dispatchoutbound and archivereference have no SharePoint target at all.

**Constraints**

- The designer validates item/<Column> at save time, so writing an absent column produces a package that cannot be saved. Twelve correct columns and three wrong ones is not a partial write.

**Decisions required first**

- Whether dispatch and archive should persist server-side at all, and to which list.

**Steps**

- Read the Global Tracking Queue column set in the tenant and check for Status, NVERSE and Category.
- Confirm or correct the category -> Category0 pairing on DGO DIGITAL OPS.
- Decide the dispatch and archive question; if they persist, name the list and capture its columns.
- Record every confirmed column in internal-field-evidence.json with its evidence class.
- Build the write bodies; npm run test:listcolumns must stay green.

**Done when.** npm run designerpaste builds the writes and no operation answers 501.

**Validated by.** npm run test:listcolumns and npm run designerpaste both green after the evidence is added; then each formerly-501 operation exercised end to end.

### ITEM-9 — 39 of 77 flows have no stated purpose

| | |
|---|---|
| **Status** | BLOCKED |
| **Category** | UNSPECIFIED |
| **Owner** | someone who knows the estate |
| **Target date** | _none established_ |
| **Waits on (in register)** | _nothing_ |

The generated catalogue reports 77 definitions catalogued, 38 attributed to a contract key, and 39 unattributed. The catalogue records what each flow does — trigger, parameters, lists, structure — but not why it exists. This item read '27 of 58' from 2026-09-05 until 2026-09-14, a figure that never matched the catalogue it cited and had since moved further: the gap is wider, not narrower. Eight GOV definitions exported on 2026-09-09 landed unattributed, and attribution has not moved at all — 38 then, 38 now.

**Why it is still open.** Purpose is the one thing a definition cannot state. The catalogue's own rule is that an unattributed flow is marked 'no stated purpose' rather than given an invented one, because an invented purpose reads as knowledge. Deriving 39 sentences from the definitions would be inventing them. It needs someone who knows what the estate was for.

**Constraints**

- Must come from knowledge of the estate, not from the definition.

**Steps**

- Identify who knows the estate's history. 2. Walk the 27 unattributed flows with them. 3. Record one sentence each into the contract register. 4. Regenerate the catalogue.

**Done when.** The catalogue reports 0 with no stated purpose.

**Validated by.** npm run catalogue regenerates and the summary line reads 0.

### MANUAL-1 — Routing table approved by the agency

| | |
|---|---|
| **Status** | RESOLVED |
| **Category** | MANUAL_GATE |
| **Owner** | agency |
| **Target date** | _none established_ |
| **Waits on (in register)** | _nothing_ |

docs/deployment/MINIMAL-PILOT.md section 8 decides which desk each kind of correspondence lands on. It has not been approved.

**Why it closed.** Closed. The agency approved and supplied the matrix on 2026-09-08.

**Decisions required first**

- Approve the routing table as written, or amend it.
- 2026-09-08: approved. The provisional eleven-kind table is superseded; routing is Category + Subcategory per the approved matrix.

**Steps**

- Circulate section 8 to the accountable desk owners. 2. Record the approval or the amendments.

**Done when.** A recorded approval exists. Met: the agency supplied the approved category matrix and departments list on 2026-09-08, now committed at docs/reference/approved/ and generating the routing configuration.

**Validated by.** The approval is recorded in the repository or in the agency's own record, and npm run commission's manual item is marked done by the operator.

### G-08 — The overrides cascade debt is measured and ratcheted — 89 contended pairs, and it can no longer grow

| | |
|---|---|
| **Status** | RESOLVED |
| **Category** | DEBT |
| **Owner** | author |
| **Target date** | _none established_ |
| **Waits on (in register)** | _nothing_ |

styles/index.css explained correctly why platform-authority.css and app.css cannot be split into sub-layers — layers are compared before specificity and their order is inverted for !important, so the winner is decided per rule and no single layer order reproduces that mix. What was not true was everything it said about the MEASUREMENT. It cited tests/tools/cascade-snapshot.mjs, tests/static/css-contract.mjs and tests/baseline.json, and an override-overlap budget those files were said to ratchet down. None of the three exists in this repository. The element-digest figures quoted from them could not be reproduced, and the one figure that could be checked was wrong: the important counts were given as 51 and 4 against a measured 92 and 28. tests/references.test.mjs could not catch it, because those were paths inside a CSS comment rather than links.

**Why it closed.** Closed 2026-09-03. The finding was that the cascade producing the asserted appearance was not measured. It is measured now, and the measurement is a ratchet in the gate rather than a number in a comment.

**Decisions required first**

- Whether to measure it at all, given appearance is now asserted directly.

**Steps**

- Reduce the overlap by resolving each contended declaration in one sheet rather than both, lowering OVERLAP_BUDGET as it falls. At 0, the sub-layer split becomes safe to make.

**Done when.** A committed, executable measurement of the overrides layer that runs in npm test and refuses to let the debt grow. Met: override-overlap 89, budgeted, with both directions negative-controlled — adding three genuinely new contended pairs takes it to 92 and fails, and re-citing a missing tool above the correction note fails.

**Validated by.** node tests/css-cascade-contract.mjs — 10 passed, 0 failed.

### ITEM-36 — A public portal endpoint reads the internal operations estate, against the estate's own principle

| | |
|---|---|
| **Status** | RESOLVED |
| **Category** | BLOCKING |
| **Owner** | agency + author |
| **Target date** | _none established_ |
| **Waits on (in register)** | _nothing_ |
| **Also waits on** | D15 settled the list's provenance and explicitly does not settle this |

portal-wiring.json states its principle without qualification: "No portal flow may read or write the internal operations estate or any DGO_* governance list." The deployed CG_Writeback_Endpoint, invoked by anonymous callers, reads Flow Configuration on NITDADGO-EAAACTIVITYTRACKING for its allowed CORS origins. All seven portal designer-paste packages read the same list for the same value, so applying them extends this from one deployed endpoint to all seven.

**Why it closed.** Resolved 2026-08-31. The agency accepted the crossing and D16 records it, for all seven portal endpoints since all seven packages read the same list for the same value. The principle is not amended; this is its one named exception. Bounded to one list, one operation, one column — any other list, any write, or any read whose target a caller can influence is not covered. What D16 does not settle, and this register does not claim it does: whether the portal flows' SharePoint connection should be scoped to the portal estate rather than the whole site.

**Constraints**

- Fourteen packages read this list by GUID, so moving the control is a rebuild, not an edit.
- The read is chained on all statuses and fails closed; no portal response returns content from it.
- The deployed endpoint already does this in production — not applying the packages does not undo it.

**Decisions required first**

- Accept the crossing and record why, or move the allowed origins to a portal-estate list and rebuild the fourteen packages.

**Steps**

- Decide between accepting and removing the crossing.
- If accepted: record the decision, set declaredCrossings[0].state to ACCEPTED, and cite it from D15.
- If removed: choose a portal-estate list, move the ALLOWED_ORIGIN rows, rebuild all fourteen packages, re-export CG_Writeback_Endpoint and confirm the sweep reports 0 crossings.

**Done when.** declaredCrossings[0].state is ACCEPTED with a decision record, or REMOVED with npm run wiring reporting 0 boundary crossings.

**Validated by.** npm run wiring and npm run test:wiringspec.

### ITEM-39 — OTP_Transactions.Attempts is provisioned, and the list is inside the provisioning specification

| | |
|---|---|
| **Status** | RESOLVED |
| **Category** | PROVISIONING |
| **Owner** | operator |
| **Target date** | _none established_ |
| **Waits on (in register)** | _nothing_ |

The attempt cap in ITEM-38 counts a guess before the code is checked, which needs a numeric Attempts column on OTP_Transactions (9421d473-8906-43b7-a41f-a213046683c1, NITDADGO-EAAACTIVITYTRACKING). Until 1.1.0 that list appeared nowhere in portal-field-spec.json, so the column was outside the specification, outside its 99/99 provisioning ledger and outside the 38 assertions of tests/sharepoint-field-spec.test.mjs; it was to be created by a bespoke one-off browser script instead. The list and the column are now declared in the specification, so the sanctioned provisioner creates them on its next run and the ledger accounts for them like every other column.

**Why it closed.** Resolved 2026-09-03. The operator re-ran the provisioner as a dry run, which agreed with 2026-09-02 that exactly one column was missing, then ran it with DRY_RUN = false. It created Attempts and skipped the other 102. The specification names the run record in provisionedBy, so the column is accounted for by the same ledger machinery as every other column in the estate. Two independent readings support it: a tenant read on 2026-09-02 establishing exactly one column missing, and an apply on 2026-09-03 creating exactly one, with SharePoint's own acceptance of the create. What is NOT in hand is a read taken after the write; the run record says so rather than leaving it to be inferred, and it arrives with ITEM-38 at no extra cost — a flow that writes Attempts cannot save if the column is absent.

**Constraints**

- It must be created BEFORE the OTP flow is saved, not after.

**Decisions required first**

- Folded into portal-field-spec.json rather than kept as a one-off script, so one provisioning run closes it and the existing suite guards it. The bespoke script is deleted.

**Steps**

- Nothing to execute. Both steps were carried out on 2026-09-03 and are recorded above.

**Done when.** A provisioning run record accounts for OTP_Transactions.Attempts, and the specification carries provisionedBy naming it.

**Validated by.** Two readings agree that the estate held 102 of 103 columns and that Attempts was the missing one; the apply run created exactly one column, with 0 failed. node tests/sharepoint-field-spec.test.mjs holds the run record to the specification: the column claiming provisioning names a run record that exists, that record has a ledger row matching it on site, list, field and type, it claims no column outside the PENDING set, and — because no verify pass followed the apply — it declares that absence outstanding. Saving the ITEM-38 definition supplies the post-write reading.

### ITEM-40 — Four endpoint triggers answer every verb and five carry no request schema

| | |
|---|---|
| **Status** | EVIDENCE_INCOMPLETE |
| **Category** | UNSPECIFIED |
| **Owner** | operator |
| **Target date** | _none established_ |
| **Waits on (in register)** | _nothing_ |

Every trigger has now been READ — npm run triggerauth reports unread: 0 across the contract keys, each resolved from a dated export. What the reading found is worse than the title this item carried until 2026-09-14 ('thirteen of fourteen unverified, and the fourteenth answers any verb'): FOUR keys answer every verb, not one — GET_DOCS, FETCH_EMAIL_ATTACHMENTS, AI_DOC_ANALYSIS and AI_CHAT — and FIVE carry no request schema, including CG_Upload_Endpoint and IP_SCAN_INTAKE. The measurement half of this item is closed; the posture half is not, and it was understated.

**Why it is still open.** Method and request schema are designer fields on the deployed flow, not definition changes, so no paste package can carry them and nothing in this repository can set them. Reading them is done; changing them is a tenant visit per flow.

**Constraints**

- Changing the trigger URL is forbidden — setting Method on an existing trigger does not change it, replacing the trigger does.

**Decisions required first**

- Recorded as a single item across all fourteen rather than fourteen items, because one export sweep answers all of them.

**Steps**

- Set Method = POST on IP_Get_Docs_Endpoint. 2. Export each of the other thirteen and run npm run compare:export, which reports the trigger. 3. Set Method = POST wherever it is unset.

**Done when.** Every endpoint trigger is recorded as method POST with a request schema, read from an export taken after the change. Currently answeringEveryVerb: 4 and withNoRequestSchema: 5 against a target of 0 and 0; unread is already 0.

**Validated by.** A GET against a trigger URL is refused by the platform rather than answered.

### ITEM-41 — Content approval is still ON for Global Tracking Queue — 12,079 of 15,936 rows are invisible to readers

| | |
|---|---|
| **Status** | DECISION_REQUIRED |
| **Category** | MANUAL_GATE |
| **Owner** | agency |
| **Target date** | _none established_ |
| **Waits on (in register)** | _nothing_ |

Measured 2026-09-01 by a complete tally: Global Tracking Queue holds 15,936 items of which 12,079 are Pending and therefore not visible to readers. Four of the seven internal flows write to this list, so rows they create land Pending and invisible. DGO DIGITAL OPS carried the same setting with 14,882 of 21,532 hidden and was turned off on agency direction, confirmed by read-back. Global Tracking Queue was deliberately not changed: it carries its own decision and the script will not make it by implication.

**Why it is still open.** This is an agency decision about a list the agency has given no direction on. It is not a technical task.

**Constraints**

- $filter on _ModerationStatus returns HTTP 500 on both lists — the column is unindexed and both are over the 5,000-item view threshold — so any count must page every row.

**Decisions required first**

- DGO DIGITAL OPS: off, 2026-09-01, on agency direction. Global Tracking Queue: outstanding.

**Steps**

- Decide whether this list is to carry content approval. 2. If not, run the approval script with APPLY set and confirm by read-back.

**Done when.** A recorded decision, and if it is to be off, a read-back confirming EnableModeration is false.

**Validated by.** A reader-facing query returns the list's full ItemCount.

### ITEM-44 — The client spoke a protocol neither OTP flow reads — reconciled on the client side

| | |
|---|---|
| **Status** | APPLIED_UNVERIFIED |
| **Category** | BLOCKING |
| **Owner** | operator (the live proof); the direction is settled |
| **Target date** | _none established_ |
| **Waits on (in register)** | _nothing_ |

core/data-client.js sends action "otpGenerate"/"otpVerify" from config/endpoints.config.js and nests everything under payload; both OTP flows switch on action "generate"/"verify" and read identifier and otp_code at the top level. Neither the dispatch value nor the field placement matches, so the Switch matches no case and neither OTP path runs. This is not caused by the ITEM-43 cutover — the old flow has the same case values — and is consistent with ITEM-2: nothing has ever exercised it. It fails loudly: varStatusCode starts at 500 and the Response returns it, so an unmatched request answers 500 rather than a misleading 200. Two further findings sit alongside it: Case_Generate mails the code to the literal dgsregistry@nitda.gov.ng rather than to the requester, and writes item/Title from identifier with no coalesce while the verify lookup binds on Title through a four-way one — so a caller sending userEmail writes an empty Title that verify can never match.

**Why it is still open.** The direction is decided and implemented, and the seam is now asserted on every build. What is NOT done is the only thing that can settle it: no request has ever been made. This item stays open until one is, because a protocol that reads correctly and has never been exercised is exactly what produced the defect.

**Decisions required first**

- DIRECTION SETTLED 2026-09-04: reconcile on the CLIENT, not the flows. The two flows already agree with each other — Web - OTP Generate and IP_OTP_VERIFY both read identifier and otp_code at the top level and switch on generate/verify — so the client was the single outlier and one change fixes both. Changing the flows instead would mean a second designer paste into a flow certified hours earlier, plus a change to a second flow nobody has reviewed, to reach the same place.
- The mechanism is the client's own: flatPayload already existed for flows that read their fields where they were sent, and modules/single-assignment.js already uses it. No new mechanism was introduced.
- Blast radius is nil: each endpoint's action is an independent frozen entry and flatPayload is per-call, so no other endpoint's wire format changed. The options passthrough added to invokeObsidianAction is additive — every existing caller passes two arguments and gets {}.
- The estate has two conventions and both are correct: the portal posts flat bodies with no action because one flow serves one endpoint; the internal runtime dispatches {action, payload} because DYNAMIC_ACTIONS and EMAIL share a flow. The OTP flows are switch-dispatched, so they need an action — the disagreement was vocabulary, not architecture.

**Steps**

- Done — the client sends action generate/verify with identifier and otp_code at the top level.
- Done — npm run test:otpprotocol asserts the seam against the deployed definitions.
- Outstanding — the recipient decision: Case_Generate mails the code to the literal dgsregistry@nitda.gov.ng rather than to the requester. That is a flow change and an agency call.
- Outstanding — run the four cutover tests through the platform (ITEM-43).

**Done when.** One request from the platform reaches a matched Switch case, and one code issued through the platform verifies through the platform. Until both are observed against the tenant, this is unresolved regardless of what the code reads like.

**Validated by.** Read from the tenant, not from the repository: a run history entry on IP_OTP_VERIFY showing Case_Verify entered, and an OTP_Transactions row whose Title equals the identity the platform sent.

### ITEM-45 — The scheduled sweep completes: the list view threshold failure is closed

| | |
|---|---|
| **Status** | RESOLVED |
| **Category** | PROVISIONING |
| **Owner** | operator |
| **Target date** | _none established_ |
| **Waits on (in register)** | _nothing_ |

AUTO_SCHEDULED_SWEEP's first live run returned status 400, 'The attempted operation is prohibited because it exceeds the list view threshold', twice — once for Get_Ack_Overdue and once for Get_Due_Breached. Global Tracking Queue held 15,804 items at capture, three times SharePoint's 5,000 threshold, and both queries filtered AND sorted on a date column carrying no index, so neither could be resolved. The threshold applies twice over and both halves had to be answered: the query must be resolvable through an index, AND the clause it resolves must return under 5,000 rows. 'DueDate lt now' on a task list is every task ever due, which no index makes small. The flow now asks for a 30-day window ordered newest-first, which is the code half; the columns still need indexes, which is this item. Three further targets are latent rather than live: DGO_FETCH_ALL sorts both big lists on Modified and DGO_GET_DOCS sorts DGO DIGITAL OPS on it with no filter at all when no reference is supplied — past the threshold SharePoint refuses to SORT on an unindexed column exactly as it refuses to filter on one, so the boot call fails the same way the moment that package is pasted.

**Why it closed.** Resolved 2026-09-05. Run 08584130061891154567402797399CU18 completed in 9m50s with every reported scope Succeeded and an empty error list. Get_Due_Breached and Get_Ack_Overdue — the two actions that returned 400 on the first live run — sit inside scopes that succeeded. Both halves were needed: indexing DueDate and Acknowledgement_x0020_Due_x0020_ (ITEM-47), and bounding the queries to 30 days so the indexed clause returns under the 5,000-row threshold. This is an observed run, not an expectation.

**Constraints**

- SharePoint allows at most 20 indexed columns per list; both lists are far below that.
- Indexing a column on a list already past the threshold can be refused. The provisioner reports the refusal rather than swallowing it. If Global Tracking Queue refuses, the fallback is a filtered view or an index added during a maintenance window.

**Steps**

- Apply the index pass — §2 of docs/deployment/PORTAL-TENANT-RUNBOOK.md, which carries the step; it sets all four and skips any already indexed. 2. Record the run under docs/deployment/sharepoint/evidence/ with an indexRun ledger, as the 2026-08-31 run does. 3. Re-import DGO_SCHEDULED_SWEEP.zip, or re-paste the scope, so the flow carries the bounded window. 4. Watch one hourly run and confirm no 400.

**Done when.** One hourly run of AUTO_SCHEDULED_SWEEP completes with no 400 on Get_Due_Breached or Get_Ack_Overdue. Met.

**Validated by.** npm run test:sharepoint — every index target declared before the last run has a recorded outcome

### ITEM-46 — Two detectors for the threshold read only equality filters, which is why this shipped

| | |
|---|---|
| **Status** | RESOLVED |
| **Category** | DEBT |
| **Owner** | Platform technical owner |
| **Target date** | _none established_ |
| **Waits on (in register)** | _nothing_ |

The preflight generator and tests/index-targets.test.mjs both extracted filtered columns with the pattern '<Col> eq ', and neither looked at $orderby at all. The scheduled sweep filtered with 'lt' and sorted on the same column, so both detectors saw nothing, the preflight printed READY TO PASTE, and the flow was imported. A check written to catch exactly this class of failure could not see the instance of it that reached production. Both now read eq/ne/lt/le/gt/ge plus startswith and substringof, and both read $orderby. The preflight's mustIndex also stopped being an intersection with a curated list — a column could previously only be reported as needing an index if someone had already written it down, which makes the check incapable of finding anything new. It is now driven by the size of the list, which is the property the threshold actually depends on.

**Why it closed.** Resolved 2026-09-02. Recorded because the fix to ITEM-45 is worth nothing if the detector that missed it stays as it was.

**Steps**

- Done.

**Done when.** npm run test:indextargets fails on an undeclared filtered OR sorted column, whatever the operator.

**Validated by.** npm run test:indextargets && npm run test:preflight

### ITEM-47 — The four index targets ITEM-45 declared are applied and recorded

| | |
|---|---|
| **Status** | RESOLVED |
| **Category** | BLOCKING |
| **Owner** | operator |
| **Target date** | _none established_ |
| **Waits on (in register)** | _nothing_ |
| **Also waits on** | A tenant session with permission to index columns on Global Tracking Queue and DGO DIGITAL OPS |

ITEM-45 declared four index targets on 2026-09-02 after the live sweep hit the list view threshold: Global Tracking Queue.DueDate, Global Tracking Queue.Acknowledgement_x0020_Due_x0020_, Global Tracking Queue.Modified and DGO DIGITAL OPS.Modified. The 2026-09-03 provisioning apply run carried an index pass, and that pass indexed five columns — Portal Rate Limits.Title, Portal Verification Proofs.Title, Portal Support Cases.Title, Global Tracking Queue.RefIDD and DGO DIGITAL OPS.RefIDD — none of them these four. The 2026-08-31 run indexed the same five. Those are the only two index runs recorded anywhere in this repository, on any branch. Neither branch could see this alone: the branch that declared the targets does not carry the 2026-09-03 run, and the branch that carries the run does not declare the targets. Consolidating the two is what made a target declared before the last run, and not reached by it, visible. tests/sharepoint-field-spec.test.mjs distinguishes a target declared AFTER the newest run (outstanding, a queue) from one declared before it (skipped, a fault) precisely so this could not be reported as either a fabricated outcome or a deleted declaration. It reports these four as the second kind.

**Why it closed.** Resolved 2026-09-05 by the operator. DueDate and Acknowledgement_x0020_Due_x0020_ were indexed on Global Tracking Queue; Modified read back already indexed on both Global Tracking Queue and DGO DIGITAL OPS, which is an observed outcome rather than an assumption — the provisioner reads each field's state before deciding. Nine declared targets, nine outcomes, none refused. SharePoint can refuse an index on a list already over the view threshold and did not.

**Constraints**

- An unindexed filtered or sorted column on a list past the view threshold fails the query rather than slowing it — the estate treats it as a correctness fault, not a performance one
- Both named lists are already past the threshold: that is what raised ITEM-45

**Steps**

- Run §2 of docs/deployment/PORTAL-TENANT-RUNBOOK.md against the tenant — it is the only document that carries the step.
- Run the index pass for Global Tracking Queue (DueDate, Acknowledgement_x0020_Due_x0020_, Modified) and DGO DIGITAL OPS (Modified).
- Save the run record to docs/deployment/sharepoint/evidence/<date>-provisioning-index-run.json with its indexRun ledger.
- npm run test:sharepoint — the assertion naming these four must pass.

**Done when.** npm run test:sharepoint passes 'every index target declared before the last run has a recorded outcome', with the outcome read from a committed run record. Met.

**Validated by.** npm run test:sharepoint && npm run test:indextargets

### ITEM-49 — The estate can fire on a schedule — AUTO_SCHEDULED_SWEEP is deployed

| | |
|---|---|
| **Status** | RESOLVED |
| **Category** | ARCHITECTURE |
| **Owner** | Operational owner |
| **Target date** | _none established_ |
| **Waits on (in register)** | _nothing_ |

59 of the 60 exported flow definitions carry a Request trigger and the 60th carries a OneDrive file trigger. There is no Recurrence trigger and no SharePoint item-created or item-modified trigger anywhere in the environment. Every flow waits to be called. Eight required notifications are about something changing or a clock expiring, and none of them can exist until one scheduled or event-driven flow does.

**Why it closed.** Resolved 2026-09-05 by the operator. The export carries a Recurrence trigger, hourly, W. Central Africa Standard Time. This was the estate's largest structural gap: every deferred, breach-driven and retry notification needed a mechanism that did not exist. It exists.

**Constraints**

- One scheduled flow can carry all eight rows: a sweep of Portal Registry, Portal Support Cases, the assignment clocks and Portal Outbox Receipts.

**Decisions required first**

- The sweep interval, and whether the estate prefers one scheduled sweep or per-list change triggers.

**Steps**

- Create the scheduled flow in the environment.
- Point it at the acknowledgement and due clocks first — RN-013 and RN-014 close with no new list.
- Add the Portal Outbox Receipts sweep for RN-026 and RN-027.
- Add the status-change sweep for RN-002, RN-003, RN-004 and RN-008.
- Update config/notification-matrix.config.js to name the carrier, then npm run notifications -- --emit.

**Done when.** A Recurrence trigger exists in the exported estate, the matrix names it as carrier for the eight gated rows, and npm run notifications -- --check derives PROVISIONED for each.

**Validated by.** npm run notifications -- --check

### ITEM-51 — Officers are never told of assignments, approvals, escalations, delegations or reminders

| | |
|---|---|
| **Status** | SPECIFIED_NOT_APPLIED |
| **Category** | CORRECTNESS |
| **Owner** | Platform technical owner, with the correspondence lifecycle process owner for the copy-to decision |
| **Target date** | _none established_ |
| **Waits on (in register)** | _nothing_ |
| **Also waits on** | GAP-001 must be closed for notify-owner before its audit trail can be trusted |

The assignment cascade requires an assignee and a due date on every assignment and resolves a supporting unit, a supporting assignee and a copy-to list besides. Neither DGO_SINGLE_ASSIGNMENT nor DGO_BULK_ASSIGNMENT carries any mail action, so none of those audiences is written to. pending_review can only be left by a second person's decision and nothing tells that person it is waiting. The acknowledgment module's remind-assignee and FastTrack's notify-owner both report to the operator that a notice was queued; both write only to browser storage, and notify-owner has no action-ownership spec at all, so it runs with no declared backend or audit vocabulary. The FastTrack escalation action writes an escalation record with status Open that reaches nobody, escalate-non-ack is a charter capability with no implementation at all, the orchestrator's set-reminder records a dueAt that nothing reads at that time, and executive-escalate delegates a record to a person who is never told it is theirs. Separately, Deployed - Create Task DOES notify the assignee from the task's AssignedTo field — and DGO_SINGLE_ASSIGNMENT, the package that replaces it, carries no mail action, so pasting it removes the one working assignment notice in the estate.

**Why it is still open.** Two clocks start on every assignment and the person they run against is not told they have started. The two actions that claim to notify someone are the more serious half: an operator who reads 'Owner notification queued' has been told a message is on its way, and nothing is queued anywhere but that browser.

**Constraints**

- RN-020 is in-app only and is bounded by the notification centre being device-local by design.

**Decisions required first**

- Whether supporting and copy-to parties receive a message or only the primary assignee.

**Steps**

- Port the assignee recipient expression from Deployed - Create Task into DGO_SINGLE_ASSIGNMENT — this is a regression to prevent, not a feature to design.
- Add 'notify-owner' to config/action-ownership.config.js with an owner, service, audit event and mandatory backend.
- Change remind-assignee's backend from DYNAMIC_ACTIONS.optional to mandatory, or change the operator copy so it does not claim the assignee was reminded.
- Add the assignment and approval sends to the packages.
- Re-run npm run notifications -- --check and npm run test:governance.

**Done when.** npm run notifications -- --check derives PROVISIONED for RN-009, RN-011, RN-012, RN-015, RN-017, RN-018, RN-019, RN-034, RN-036 and RN-038, and no runtime action reports a notification as queued that has no carrier.

**Validated by.** npm run notifications -- --check

### ITEM-53 — CG_SEND_EMAIL no longer accepts anonymous callers

| | |
|---|---|
| **Status** | RESOLVED |
| **Category** | BLOCKING |
| **Owner** | operator |
| **Target date** | _none established_ |
| **Waits on (in register)** | _nothing_ |
| **Also waits on** | PRE-5 — the re-pointing this must precede |

CG_SEND_EMAIL, created in the tenant on 2026-09-02 from DGO_SEND_EMAIL, carries triggerAuthenticationType 'All' — anyone holding the signed URL can invoke it. Web - Send Email, the flow it replaces, is one of only five Request triggers in the whole estate set to 'Tenant'. Re-pointing EMAIL at the new flow without changing this takes an internal officer endpoint from tenant-restricted to open-with-URL. It matters more here than on a read endpoint: the flow's authorisation gate resolves the caller from DGO_UserDirectory using the userEmail in the REQUEST BODY, which an anonymous caller supplies. So with an 'All' trigger, anyone with the URL can name any active officer and have the registry mailbox send mail to any address of their choosing — the open relay the 401 gate exists to prevent, defeated one layer below it. The gate is sound for a tenant-authenticated trigger, where the platform has already established who is calling.

**Why it closed.** Resolved 2026-09-05 by the operator. triggerAuthenticationType is Tenant, matching the flow it replaces.

**Constraints**

- Changing the trigger authentication does not change the trigger URL, so nothing needs re-configuring afterwards.

**Steps**

- Power Automate → CG_SEND_EMAIL → edit → the 'When an HTTP request is received' trigger → Who can trigger the flow? → 'Any user in my tenant'. 2. Save. 3. Re-export and confirm triggerAuthenticationType is Tenant. 4. Only then re-point EMAIL (PRE-5).

**Done when.** The next export of CG_SEND_EMAIL carries triggerAuthenticationType 'Tenant'.

**Validated by.** node scripts/audit-trigger-auth.mjs --check, and the recorded posture in docs/reference/flow-trigger-auth.json

### ITEM-54 — IP_OTP_Endpoint no longer returns officer claims with a wildcard origin

| | |
|---|---|
| **Status** | RESOLVED |
| **Category** | CORRECTNESS |
| **Owner** | operator |
| **Target date** | _none established_ |
| **Waits on (in register)** | _nothing_ |

The live IP_OTP_Endpoint answers with 'Access-Control-Allow-Origin: *' where DGO_OTP resolves the caller's Origin against the ALLOWED_ORIGIN rows in Flow Configuration and fails closed. On a successful verification the response body carries the officer's claims — email, full name, roles, persona, access scope — so the wildcard lets any page on the internet read them cross-origin from a browser that has the URL. The same export is also missing item/RunRecordJson on its telemetry write. Both are signs the flow was pasted from a DGO_OTP built before those two changes landed, rather than deliberate edits: the export is dated 2026-09-01 and every other action matches the current package exactly.

**Why it closed.** Resolved 2026-09-05 by the operator. Access-Control-Allow-Origin is now @outputs('Compose_Allowed_Origin_Otp'), not *.

**Constraints**

- Re-pasting the current DGO_OTP package fixes both in one action; a hand-edit of the two headers fixes only the disclosure.

**Steps**

- Re-paste DGO_OTP.designer-paste.json over Scope_Otp_Flow in IP_OTP_Endpoint (the variables are already at the top level and do not need re-creating). 2. Confirm Response_Otp reads @outputs('Compose_Allowed_Origin_Otp'). 3. Confirm the ALLOWED_ORIGIN rows exist in Flow Configuration first — with none, the header is empty and the browser refuses, which is the intended fail-closed behaviour but will look like a regression if it is unexpected. 4. Re-export.

**Done when.** The next export of IP_OTP_Endpoint resolves its CORS origin from Flow Configuration and writes item/RunRecordJson.

**Validated by.** Diff the export against docs/deployment/internal/flows/designer-paste/DGO_OTP.designer-paste.json

### ITEM-55 — IP_OTP_VERIFY's placeholder origin is replaced by the allow-list its sibling uses

| | |
|---|---|
| **Status** | RESOLVED |
| **Category** | BLOCKING |
| **Owner** | operator |
| **Target date** | _none established_ |
| **Waits on (in register)** | _nothing_ |
| **Also waits on** | The portal's deployed origin must be known before the value can be set |

Every Response action in IP_OTP_VERIFY (c5e314c7-68b5-4bdc-8935-0954ef6a256d) sets Access-Control-Allow-Origin to the literal string 'https://your-host'. That is the template placeholder, not an origin. No browser origin matches it, so every browser call to OTP verify fails CORS and the response is discarded before the portal can read it. IP_OTP_Endpoint, corrected in the same round, uses @outputs('Compose_Allowed_Origin_Otp'); this flow did not get the same treatment. The wildcard this replaced was a disclosure risk that still worked; the placeholder is not a disclosure risk and does not work.

**Why it closed.** Resolved 2026-09-05 by the operator, 36 minutes after the 06:17:06 export that introduced it. Access-Control-Allow-Origin is now @{outputs('Compose_Allowed_Origin_Otp')}, and that Compose exists in this flow: it echoes the request Origin when it appears in the allow-list and otherwise falls back to the first entry. No placeholder remains anywhere in the definition.

**Constraints**

- OTP verify is called from the browser, so the response header is load-bearing rather than advisory

**Steps**

- Open IP_OTP_VERIFY in Power Automate.
- Replace 'https://your-host' in every Response action's headers with the portal origin, or with the Compose_Allowed_Origin pattern the sibling flow uses.
- Re-export and replace the committed definition.
- npm run test:otpdeployed

**Done when.** No Response action in the committed IP_OTP_VERIFY definition carries a placeholder origin. Met.

**Validated by.** npm run test:otpdeployed && npm run test:otpprotocol

### ITEM-57 — The archived design system is unreconciled against the live one, and nobody has said which is authoritative

| | |
|---|---|
| **Status** | DECISION_REQUIRED |
| **Category** | DEBT |
| **Owner** | the agency, with whoever owns the platform's visual design |
| **Target date** | _none established_ |
| **Waits on (in register)** | _nothing_ |

docs/archive/dgo-ops/design-system/ is a 105-file design system documentation set dated 2026-07-30 — foundations, tokens, colour, typography, spacing, iconography, motion, elevation, accessibility, i18n/RTL, 27 component pages, styles and a legacy-v1. The platform's live design system is document-portal/ds/ (ds.css, colors_and_type.css, fonts, icons, logo), gated by npm run test:tokens and npm run test:breakpoints. The two were built separately and neither defers to the other. dgoeaa/BESPOKE_DSIGN_SYSTEM is not a third source: it holds one file, an unextracted zip. Nothing depends on resolving this — the archived set is not wired to anything and not served by any page — but it is the only copy of a substantial body of design work, and leaving it unlabelled invites someone to treat it as current.

**Why it is still open.** It is a judgement about which body of design work the agency intends to carry forward, not a defect. Nothing fails while it stands.

**Constraints**

- The archived set is dated before main (63d79ee, 2026-08-16), so it predates the current platform

**Steps**

- Read docs/archive/dgo-ops/design-system/docs/01-tokens.md against document-portal/ds/colors_and_type.css.
- Either mark the archived set superseded in its README, or raise a scoped item for the parts worth promoting.
- If superseded, nothing further is required — it stays as a record.

**Done when.** The archived design system carries an explicit statement of its standing relative to document-portal/ds/, decided rather than inferred.

**Validated by.** none — this is a decision, and no gate can hold it

### ITEM-58 — Unresolved DSU keys in the approved matrix — closed by agency direction

| | |
|---|---|
| **Status** | RESOLVED |
| **Category** | UNSPECIFIED |
| **Owner** | agency |
| **Target date** | _none established_ |
| **Waits on (in register)** | _nothing_ |

Eleven rows of the approved matrix named a primary responsible unit the approved departments list did not define, and two rows named none at all. All are now resolved by agency direction of 2026-09-08, applied through docs/reference/approved/dsu-key-corrections.csv: five unit-key adoptions (HRA to HRM, PATODGCEO and PADGCEO to PA, ODG to DGO, PRS to CCMR), one transcription correction (Education lal Institutions to Educational Institutions, collapsing a duplicate row), and two row assignments to DGO with supporting and informed units left blank.

**Why it closed.** Closed by agency direction of 2026-09-08.

**Decisions required first**

- 2026-09-08: raised on implementing the approved matrix. Not guessed.
- 2026-09-08: the agency adopted the five resemblance values and the spelling correction. Applied. The remaining question is narrowed to the two rows with no unit.
- 2026-09-08: DGO assigned as responsible unit for Unclassifieds and for Sponsorship And Collaborations / PROPOSALS, with supporting and informed units left blank on instruction. PROPOSALS loses the DGO that stood in INFORMDSU1: DGO now owns the row, and informing a unit of its own work is noise.

**Steps**

- Decide whether Unclassifieds should have a responsible unit or remain deliberately unassigned, and record which.
- Name the responsible unit for Sponsorship And Collaborations / PROPOSALS, or record that it has none by design.
- Add any decision to docs/reference/approved/dsu-key-corrections.csv; npm run orgrouting regenerates the configuration.

**Done when.** npm run test:categories reports no unresolved DSU keys and no duplicate category/subcategory pair. Met: 24 passed, 0 failed; 0 rows carry no responsible unit.

**Validated by.** The generator resolves every approved row to a role mailbox.


---

## Wave 2 — 13 item(s)

**Owners needed:** Platform technical owner for the sends; the agency for any decision to withdraw a promise instead. · agency · operator · operator + author

### CFG-1 — Internal runtime is not configured

| | |
|---|---|
| **Status** | DISCHARGED_UNTRACKABLE |
| **Category** | BLOCKING |
| **Owner** | operator |
| **Target date** | _none established_ |
| **Waits on (in register)** | ITEM-22 |

config/config.local.js does not exist, so every endpoint in the internal runtime resolves to an empty string. The platform boots and renders, and transmits nothing.

**Why it closed.** Closed 2026-09-13 by a live run, and unverifiable here forever. The operator harvested all 25 signatures and ran setup: 18 of 18 internal keys wired, 0 automated commissioning blockers. The values file was then shredded and config/config.local.js is git-ignored — both correct, both meaning no command run in this repository can ever confirm it. npm run commission WILL report 'Internal runtime: not configured' in every clone; that is the absence of a credential, not the absence of the work. It was open because the live endpoint URLs did not exist; ITEM-22 produced them and the tenant register now carries a complete template for all 25 keys.

**Constraints**

- config.local.js is untracked by design; npm run commission verifies deploy-time config is untracked, and that check currently passes.

**Steps**

- Only needed on a NEW host. npm run values:template ~/dgo-values.txt — 25 keys, every URL complete but for its signature.
- For each of the 20 flows, copy the 43 characters after sig= from its trigger card and npm run values:sign -- ~/dgo-values.txt <KEY>. Do not press Regenerate; the rotation is behind you.
- npm run check:values -- ~/dgo-values.txt, then npm run setup -- --values ~/dgo-values.txt --force, then npm run check:config.
- shred -u ~/dgo-values.txt. The proof does not survive, and is not supposed to.

**Done when.** Met on 2026-09-13: npm run commission reported 0 automated blockers on the operator's host. It cannot be re-met in this repository — see the status. Re-running the steps on a new host is a deployment, not a reopening.

**Validated by.** npm run commission, and npm run verify:endpoints against the configured endpoints.

### ITEM-8 — The allowed origin is listed but has never been confirmed against the platform

| | |
|---|---|
| **Status** | RESOLVED |
| **Category** | PROVISIONING |
| **Owner** | operator + author |
| **Target date** | _none established_ |
| **Waits on (in register)** | ITEM-34 |

Every response echoes the caller's Origin only when a Flow Configuration row names it. It fails closed: no rows, an unreadable list, or an unlisted origin yields an empty header, which a browser rejects. Flow Configuration.ConfigValue carries capturedState PENDING and is not provisioned.

**Why it closed.** The row was added on 2026-09-01. Nothing in this repository can verify that https://activityweb.page.gd is the origin the platform actually serves from, and a mismatch of scheme or a trailing slash fails the browser CORS check exactly as an absent row does. Nothing blocks this any more. The column exists, and D15 settles which list it sits on. What is left is adding one row per origin — Title = ALLOWED_ORIGIN_1, ConfigValue = the portal's scheme and host — which is a browser session against the tenant, not repository work. Until a row exists every response carries an empty Access-Control-Allow-Origin header, which a browser rejects; that is the fail-closed behaviour working, not a fault.

**Constraints**

- The read is chained on every status and coalesces, so a missing or renamed list costs the CORS header, not the answer.

**Decisions required first**

- ITEM-34: which list owns the allowed-origin control.

**Steps**

- Decide ITEM-34. 2. Provision ConfigValue on the chosen list. 3. Add one row per origin. 4. Load the portal in a browser and confirm it reads a response.

**Done when.** The portal reads a response in a browser.

**Validated by.** A browser round-trip against the deployed portal, with the network panel showing an Access-Control-Allow-Origin header matching the portal's origin.

### ITEM-17 — WRITEBACK — client and specification done, flow and endpoint key outstanding

| | |
|---|---|
| **Status** | RESOLVED |
| **Category** | APPLY |
| **Owner** | operator |
| **Target date** | _none established_ |
| **Waits on (in register)** | ITEM-35 |

Two of three parts are complete: the client is built and browser-verified, and the flow is specified across all ten operations. The flow itself is not built and the seventh endpoint key is not provisioned. The UI stays dormant until it is set.

**Why it closed.** Closed. It was open because WRITEBACK had a client and a specification but no wired flow; the reconciliation wired it.

**Constraints**

- Do not create a second flow: the 2026-08-27 run records show CG_Writeback_Endpoint already serving the endpoint. Creating one mints a second trigger URL and leaves the live one untouched.

**Decisions required first**

- 2026-09-08: re-based against docs/reference/endpoint-register.json. The original criteria were written before the estate was rotated and reconciled, and named a state the reconciled estate no longer has.

**Steps**

- Confirm 21d4bfd3 exists and export it (ITEM-35). 2. Paste the package. 3. Provision the seventh endpoint key. 4. Publish its URL.

**Done when.** npm run wiring reads every required operation in place with WRITEBACK among them. Met: 58/58 required operations, 7/7 endpoints fully wired, WRITEBACK 10/10 reading CG_Writeback_Endpoint. The original 59 was the pre-reconciliation total.

**Validated by.** npm run wiring after re-export; then a citizen write-back action produces a Portal Audit Events row.

### ITEM-12 — A submission reports delivered while its attachments are never sent

| | |
|---|---|
| **Status** | APPLIED_UNVERIFIED |
| **Category** | APPLY |
| **Owner** | operator |
| **Target date** | _none established_ |
| **Waits on (in register)** | ITEM-35, ITEM-2 |

The original trigger expected one inline base64 file; the client sends an attachments[] array with no bytes. Both halves are now specified: field names reconciled, and cardinality carried by the two-phase upload — SUBMISSION issues one ticket per declared attachment and returns uploads[]; UPLOAD redeems a ticket, writes the bytes and creates the Portal Attachments row.

**Why it is still open.** Applied to the tenant and verified byte-for-byte against its package on 2026-09-01 — the upload and submission packages are applied. It is NOT closed, because every completion criterion below requires a live call and ITEM-2 records that no flow in this estate has ever been executed. Verification is blocked on ITEM-2 and on nothing else. Closes on pasting both packages, which depends on the paste targets. (Corrected 2026-09-15: the blanket premise these items rested on — that no flow in this estate has ever been executed — is no longer true. The governance provisioner ran three times against DGO_ECM_GOVERNANCE on 2026-09-09 — dry run, apply, and two verification re-runs, recorded in docs/reference/governance-estate-position.json — and the endpoint harvest ran live on 2026-09-13: 25 values harvested, 25 of 25 carrying a complete signature, 0 automated commissioning blockers, recorded in evidence/email-related-task-workflow-remediation.md. (This sentence previously dated the harvest to 2026-09-12 and credited it with resolving 21 of 25 keys through ListCallbackUrl. Neither was evidenced: 2026-09-12 is the export timestamp of two flow packages in docs/reference/flow-exports/, and no source in this repository states 21 of 25. Corrected 2026-09-15.) What is still true, and is what actually holds this item open, is that THIS endpoint has not been exercised: a live call through a configured runtime. ITEM-2 remains the first such proof.)

**Steps**

- Paste Portal_SUBMISSION_ECM_DOCS onto de9ef13b. 2. Paste Portal_UPLOAD_ECM_DOCS onto df7ddff1. 3. Submit with an attachment end to end.

**Done when.** A submission with an attachment produces a row in Portal Attachments.

**Validated by.** End-to-end submission with one attachment; assert the Portal Attachments row exists and carries StoredName.

### ITEM-11 — An unknown reference is answered 200 and rendered as found

| | |
|---|---|
| **Status** | APPLIED_UNVERIFIED |
| **Category** | APPLY |
| **Owner** | operator |
| **Target date** | _none established_ |
| **Waits on (in register)** | ITEM-2, ITEM-23 |

The denial path fails open: any reference reads as a match, and the uniform denial that stops a stranger distinguishing 'no such reference' from 'not yours' never fires. Now specified — the filter matches the indexed ReferenceId and the owning SenderEmail in one query, so a miss and a mismatch are the same empty result and answer the same 404.

**Why it is still open.** Applied to the tenant and verified byte-for-byte against its package on 2026-09-01 — the status package is applied. It is NOT closed, because every completion criterion below requires a live call and ITEM-2 records that no flow in this estate has ever been executed. Verification is blocked on ITEM-2 and on nothing else. Requires patching and applying to the tenant. (Corrected 2026-09-15: the blanket premise these items rested on — that no flow in this estate has ever been executed — is no longer true. The governance provisioner ran three times against DGO_ECM_GOVERNANCE on 2026-09-09 — dry run, apply, and two verification re-runs, recorded in docs/reference/governance-estate-position.json — and the endpoint harvest ran live on 2026-09-13: 25 values harvested, 25 of 25 carrying a complete signature, 0 automated commissioning blockers, recorded in evidence/email-related-task-workflow-remediation.md. (This sentence previously dated the harvest to 2026-09-12 and credited it with resolving 21 of 25 keys through ListCallbackUrl. Neither was evidenced: 2026-09-12 is the export timestamp of two flow packages in docs/reference/flow-exports/, and no source in this repository states 21 of 25. Corrected 2026-09-15.) What is still true, and is what actually holds this item open, is that THIS endpoint has not been exercised: a live call through a configured runtime. ITEM-2 remains the first such proof.)

**Constraints**

- INT-006 requires a byte-identical 404 for a miss and a mismatch.

**Steps**

- Apply the artifact (patch or paste). 2. Test an unknown reference. 3. Test a valid reference with the wrong email. 4. Compare the two responses byte for byte.

**Done when.** An unknown reference returns 404, a wrong-email pairing returns a materially identical 404, and track.js shows the denial.

**Validated by.** Two live requests, responses compared byte for byte.

### ITEM-3 — Portal_Verify_Confirm is patched and verified on disk but never applied

| | |
|---|---|
| **Status** | RESOLVED |
| **Category** | APPLY |
| **Owner** | operator |
| **Target date** | _none established_ |
| **Waits on (in register)** | ITEM-2, ITEM-35 |

The patched definition is in the repository and verified at 7/7 on disk. It has not been applied to the tenant.

**Why it closed.** Closed. It was open because the patch existed on disk with no evidence it had been applied; the reconciled wiring is that evidence.

**Decisions required first**

- 2026-09-08: re-based against docs/reference/endpoint-register.json. The original criteria were written before the estate was rotated and reconciled, and named a state the reconciled estate no longer has.

**Steps**

- Confirm the target id (ITEM-35). 2. Run with -Apply. 3. Re-export. 4. Run npm run wiring.

**Done when.** npm run wiring shows Portal_Verify_Confirm applied. Met and exceeded: VERIFY_CONFIRM reads 7/7 operations against CG_Verification_Confirmation_Endpoint, within 58/58 overall and 7/7 endpoints. The original 15/49, 2/6, crossings 12 described the interim state expected at the time of writing.

**Validated by.** npm run wiring after re-export.

### ITEM-4 — Visit 2 — the two submission flows

| | |
|---|---|
| **Status** | RESOLVED |
| **Category** | APPLY |
| **Owner** | operator |
| **Target date** | _none established_ |
| **Waits on (in register)** | ITEM-2 |

The bodies do the right job; this is insertion. The author half is not outstanding: 02-submission-registry-write.json carries 13 actions, names both submission flow ids in appliesTo, and passes npm run test:remediation.

**Why it closed.** Closed. The route was chosen and applied; the reconciled wiring reflects it.

**Constraints**

- The paste track supersedes this artifact for SUBMISSION, since Portal_SUBMISSION_ECM_DOCS.designer-paste.json exists. Apply whichever route is taken, not both.

**Decisions required first**

- Which route: patch this artifact, or paste the package onto de9ef13b.

**Steps**

- Choose the route. 2. Apply. 3. Re-export and sweep.

**Done when.** The chosen route is applied and npm run wiring reflects it. Met: SUBMISSION reads 12/12 operations against CG_Submission_Endpoint.

**Validated by.** npm run wiring after re-export.

### ITEM-6 — Visit 4 — ECM_DOCS_INTAKE write-back

| | |
|---|---|
| **Status** | APPLIED_UNVERIFIED |
| **Category** | APPLY |
| **Owner** | operator |
| **Target date** | _none established_ |
| **Waits on (in register)** | ITEM-2 |

Placements are done and the flow's shape is clean. Closes circle C7 — the first point at which a citizen's status can change.

**Why it is still open.** Applied to the tenant and verified byte-for-byte against its package on 2026-09-01 — the write-back is applied. It is NOT closed, because every completion criterion below requires a live call and ITEM-2 records that no flow in this estate has ever been executed. Verification is blocked on ITEM-2 and on nothing else. Requires patching and applying to the tenant. (Corrected 2026-09-15: the blanket premise these items rested on — that no flow in this estate has ever been executed — is no longer true. The governance provisioner ran three times against DGO_ECM_GOVERNANCE on 2026-09-09 — dry run, apply, and two verification re-runs, recorded in docs/reference/governance-estate-position.json — and the endpoint harvest ran live on 2026-09-13: 25 values harvested, 25 of 25 carrying a complete signature, 0 automated commissioning blockers, recorded in evidence/email-related-task-workflow-remediation.md. (This sentence previously dated the harvest to 2026-09-12 and credited it with resolving 21 of 25 keys through ListCallbackUrl. Neither was evidenced: 2026-09-12 is the export timestamp of two flow packages in docs/reference/flow-exports/, and no source in this repository states 21 of 25. Corrected 2026-09-15.) What is still true, and is what actually holds this item open, is that THIS endpoint has not been exercised: a live call through a configured runtime. ITEM-2 remains the first such proof.)

**Steps**

- Patch. 2. Verify. 3. Apply. 4. Re-export and sweep.

**Done when.** Circle C7 closes: a citizen's status can change.

**Validated by.** npm run wiring after re-export, then one status change observed end to end.

### MANUAL-4 — Personal data disposition decided: identity lives in DGO_UserDirectory, not in the repository

| | |
|---|---|
| **Status** | RESOLVED |
| **Category** | MANUAL_GATE |
| **Owner** | agency |
| **Target date** | _none established_ |
| **Waits on (in register)** | G-04 |

Making the repository private closed the exposure. Live usage puts that data through a channel whose enforcement posture is being chosen at G-04. The pilot posture does not protect it.

**Why it closed.** Closed by the agency decision of 2026-09-08.

**Decisions required first**

- Accept the pilot posture for live citizen data, or hold live operation until G-04's enforcement runs.
- 2026-09-08: agency direction — personal data is to be held in the DGO_UserDirectory SharePoint list. 40 personal addresses excluded from the repository; the exclusion is enforced by a gate rather than by convention.

**Steps**

- Put the posture choice to the accountable authority with G-04's evidence. 2. Record the decision and its date. 3. If accepted, record the compensating controls relied on.

**Done when.** A recorded decision exists, naming who made it. Met: the agency directed on 2026-09-08 that personal data is held in the DGO_UserDirectory SharePoint list. 40 personal addresses were removed from the approved departments export before it entered the repository, and npm run test:orgrouting refuses any address that identifies a person.

**Validated by.** The recorded decision, referenced from STATUS_REPORT.md.

### ITEM-56 — The OTP allow-list row is set — applied by the operator, not verified here

| | |
|---|---|
| **Status** | APPLIED_UNVERIFIED |
| **Category** | BLOCKING |
| **Owner** | operator |
| **Target date** | _none established_ |
| **Waits on (in register)** | ITEM-55, ITEM-2 |
| **Also waits on** | The portal's deployed origin must be known |

Both OTP flows now resolve Access-Control-Allow-Origin from Select_Allowed_Origins_Otp, which reads ConfigValue items from a SharePoint configuration list via Get_Allowed_Origins_Otp. This is the right design — the origins are tenant-configured rather than compiled in — but it moves the value out of the flow and into data. If that list does not contain the portal's deployed origin, Compose_Allowed_Origin_Otp falls back to the first entry, or to an empty string when the list is empty, and the browser rejects the response exactly as it did under the placeholder. Nothing in this repository can read that list.

**Why it is still open.** Applied 2026-09-05: the operator states the ALLOWED_ORIGIN row carries the portal's deployed origin. Nothing in this repository can read the Flow Configuration list, so this is APPLIED_UNVERIFIED rather than RESOLVED — the distinction the register keeps between an action taken and an outcome observed. It closes on the first live OTP verify call, whose response must carry that origin back. No such call has ever been made (R-3). (Corrected 2026-09-15: the blanket premise these items rested on — that no flow in this estate has ever been executed — is no longer true. The governance provisioner ran three times against DGO_ECM_GOVERNANCE on 2026-09-09 — dry run, apply, and two verification re-runs, recorded in docs/reference/governance-estate-position.json — and the endpoint harvest ran live on 2026-09-13: 25 values harvested, 25 of 25 carrying a complete signature, 0 automated commissioning blockers, recorded in evidence/email-related-task-workflow-remediation.md. (This sentence previously dated the harvest to 2026-09-12 and credited it with resolving 21 of 25 keys through ListCallbackUrl. Neither was evidenced: 2026-09-12 is the export timestamp of two flow packages in docs/reference/flow-exports/, and no source in this repository states 21 of 25. Corrected 2026-09-15.) What is still true, and is what actually holds this item open, is that THIS endpoint has not been exercised: a live call through a configured runtime. ITEM-2 remains the first such proof.)

**Constraints**

- An empty or wrong allow-list fails identically to the placeholder, and just as silently, because no live OTP call has been made

**Steps**

- Open the configuration list the OTP flows read via Get_Allowed_Origins_Otp.
- Confirm a ConfigValue row carries the portal's deployed origin, exactly as the browser sends it in the Origin header — scheme and host, no trailing slash.
- Make the first live OTP call (ITEM-43 / R-3) and confirm the response carries that origin back.

**Done when.** A live OTP verify call from the portal returns Access-Control-Allow-Origin equal to the portal's origin.

**Validated by.** the first live OTP call — no repository gate can establish this

### ITEM-38 — The OTP verify hardening is deployed — built fresh as IP_OTP_VERIFY on 2026-09-04

| | |
|---|---|
| **Status** | RESOLVED |
| **Category** | BLOCKING |
| **Owner** | operator |
| **Target date** | _none established_ |
| **Waits on (in register)** | ITEM-39 |
| **Also waits on** | None outstanding. Recorded here because the browser route needs no PowerShell module, no Add-PowerAppsAccount and no git checkout — only a signed-in make.powerautomate.com session with edit rights on the flow. |

Web - OTP Verify (3e201620-f1e8-4c17-a90a-4d95b94a24c2) carries five defects closed in this repository at 010f752 and applied nowhere: (1) the code lookup ignored the caller identity, so a code issued to one person verified anyone — an authentication bypass; (2) caller input was interpolated raw into the OData filter, so a crafted code rewrites the query and strips fix 1 back off; (3) the identity was computed after the lookup that needs it, so fix 1 would have failed at runtime on every call; (4) no attempt cap against a six-digit code; (5) Scope_VERIFY_Complete_No_Trigger ran on every request, an open mail relay on the agency Office 365 connection. 65 actions before, 59 after, trigger byte-identical so the flow keeps its URL, id, owners, connections and run history.

**Why it closed.** Resolved 2026-09-04. The hardening no longer exists only on disk: it is deployed, in a flow built by hand from the designer-paste kit, and the operator's export proves it action by action. Two things this does NOT mean, and ITEM-43 carries both: it is a NEW flow, so the portal still calls the defective one; and no live call has been made, so the behaviour is established by reading the definition rather than by exercising it.

**Constraints**

- The trigger must not be replaced. It is byte-identical in the patched definition for that reason: a new trigger is a new URL, and the current one is published to the portal.

**Decisions required first**

- Delivered as a complete definition rather than a patch, so the applied state does not depend on what the flow contained beforehand.

**Steps**

- Nothing to execute here. The build is done and recorded. The cutover is ITEM-43.

**Done when.** A fresh export of 3e201620 compared with npm run compare:export reports zero differences against the patched definition, and a wrong code from a caller the code was not issued to is refused.

**Validated by.** node tests/otp-verify-deployed.test.mjs, against the operator's own export. Two independent readings support it: the export's action graph matches the kit's 36 actions by name, and each of the nine defects is asserted individually by the property that closes it rather than by the diff being empty.

### ITEM-37 — The sanctioned crossing does not exist in the live flow — restore it, or amend D1 and D6

| | |
|---|---|
| **Status** | DECISION_REQUIRED |
| **Category** | BLOCKING |
| **Owner** | agency |
| **Target date** | _none established_ |
| **Waits on (in register)** | ITEM-35 |
| **Also waits on** | The UPLOAD paste (EXECUTION_GUIDE 5.2) targets this same workflow and must not proceed until this is settled. |

Workflow df7ddff1-9275-4f23-acf6-e169525f4e2f was in docs/reference/flow-contracts/deployed/ TWICE: as ECM_DOCS_INTAKE, captured 2026-08-19 with 2 actions and a Portal Registry GetItems, and as CG_Upload_Endpoint, captured 2026-08-24 with 9 actions and no Portal Registry operation. That was read as one flow renamed and rebuilt in place, destroying the single sanctioned crossing D1 and D6 rest on. IT WAS NOT. The second file was CG_Upload_Endpoint's definition stamped with ECM_DOCS_INTAKE's workflow id — one corrupted export, not two states of one flow. It was removed on 2026-09-03 and the finding withdrawn.

**Why it is still open.** THE MEASUREMENT IS WITHDRAWN AND THE CROSSING IS IN PLACE. npm run wiring, run against the current corpus, reads 73 deployed definitions and reports 'BRIDGE ECM_DOCS_INTAKE reads Portal Registry — the one designed crossing is in place', with 58/58 required operations and 7/7 endpoints wired. This item's own cited evidence quoted that same command saying 'bridge present:false readsPortalRegistry:false; 59 definitions read' — a reading taken 2026-09-01, before the corrupted export was removed, and left here after it was. WHAT IS ACTUALLY OPEN, if anything, is for the agency to say: nothing needs restoring, because nothing was destroyed. If the agency confirms D1 and D6 stand as written, this item closes with no work. It is left DECISION_REQUIRED rather than closed here because the register does not close an agency item on an author's reading of a command.

**Constraints**

- Record workflow ids only. A trigger URL carries a bearer credential and must not be pasted into a ticket, an email or a chat message.

**Decisions required first**

- D1 — the write-back extends this crossing rather than adding a second.
- D6 — this flow is the single sanctioned crossing, tenant-authenticated and read-only.

**Steps**

- Read item37_settled in the 2026-09-01 paste-state evidence. 2. Decide whether the portal must feed the internal registry at all. 3. Record the decision, and amend D1 and D6 if it is not to be restored.

**Done when.** The agency records one of two things: that D1 and D6 stand as written, given the crossing measures present — which closes this item with no work — or that the crossing should sit somewhere other than where it does, which makes it a change request rather than a restoration.

**Validated by.** npm run test:wiringspec — the bridge state recorded in the specification must equal what the sweep measures, in both directions.

### ITEM-48 — The portal promises citizens four emails the estate does not send

| | |
|---|---|
| **Status** | SPECIFIED_NOT_APPLIED |
| **Category** | CORRECTNESS |
| **Owner** | Platform technical owner for the sends; the agency for any decision to withdraw a promise instead. |
| **Target date** | _none established_ |
| **Waits on (in register)** | ITEM-39 |

The public portal states, at the moment of submission and in three other places, that a confirmation carrying the tracking ID is on its way, and that the decision outcome is emailed. No submission-confirmation and no decision-outcome mail action exists in any flow export or paste package. The submission screen additionally instructs the citizen to contact the helpdesk if no confirmation arrives within one working day, which every submitter will satisfy.

**Why it is still open.** The SUBMISSION and WRITEBACK packages carry no SendEmailV2 action, and nothing in the estate fires on a status change, so neither the confirmation nor the outcome has a carrier. Two responses are possible and they are not equivalent: build the sends, or withdraw the promises. Only the second can be done without ITEM-39.

**Constraints**

- The submission confirmation itself does not need ITEM-39: CG_Submission_Endpoint already holds the sender address and can send inline.

**Decisions required first**

- Whether to build the four sends or withdraw the four promises. Withdrawing them is a service decision, not a technical one.

**Steps**

- Decide build or withdraw, per promise.
- For the confirmation: add the send and the outbox receipt to the SUBMISSION package, regenerate, and re-run npm run designerpaste.
- For the outcome mails: land ITEM-39 first, then key the sends off the status write.
- Re-run npm run notifications -- --check; RN-001 and RN-005 must derive PROVISIONED.

**Done when.** npm run notifications -- --check derives PROVISIONED for RN-001 and RN-005, and either PROVISIONED for RN-003 and RN-004 or the portal copy no longer makes the promise, which the verifier's portal-promise checks will detect and fail on until the matrix is updated.

**Validated by.** npm run notifications -- --check


---

## Wave 3 — 5 item(s)

**Owners needed:** Platform technical owner · The agency for SN-001, alongside MANUAL-4; the platform technical owner for SN-002 to SN-004. · operator

### CFG-2 — Public portal is not configured

| | |
|---|---|
| **Status** | DISCHARGED_UNTRACKABLE |
| **Category** | BLOCKING |
| **Owner** | operator |
| **Target date** | _none established_ |
| **Waits on (in register)** | ITEM-22, ITEM-8 |

document-portal/config.local.js does not exist, so every portal endpoint resolves to an empty string. The portal boots and renders, and transmits nothing.

**Why it closed.** Closed 2026-09-13 by the same live run: 7 of 7 portal keys wired, 0 automated commissioning blockers. document-portal/config.local.js is git-ignored, so this is unverifiable here for the same reason as CFG-1. The reason recorded before — that SUBMISSION, SUPPORT and UPLOAD 'have no workflow id recorded anywhere in this repository' — was true when written and is now false: docs/reference/endpoint-workflow-ids.json is schema v2, exported from the tenant post-rotation, and carries 1041ed37ce924e3c886d891f23e8142c, 052013da80724713a4285edee72ccb4a and 62fe121e5a95416bb91275e43dd0e37e respectively. ACTION_PLAN.md carried that stale sentence as its second action, instructing an operator to do work already done.

**Steps**

- Only needed on a NEW host, and it is the same values file as CFG-1 — one template covers all 25 keys across both runtimes.
- npm run check:values -- ~/dgo-values.txt, then npm run setup -- --values ~/dgo-values.txt --force.
- npm run check:config:portal, then npm run commission.

**Done when.** Met on 2026-09-13: 7 of 7 portal keys wired and npm run commission reported 0 automated blockers on the operator's host. Not re-meetable in this repository — see the status.

**Validated by.** npm run commission, then a browser round-trip against the deployed portal.

### ITEM-42 — The portal sitemap and robots.txt need the deployed origin before the portal is indexable

| | |
|---|---|
| **Status** | RESOLVED |
| **Category** | PROVISIONING |
| **Owner** | operator |
| **Target date** | _none established_ |
| **Waits on (in register)** | ITEM-8 |

Both use relative URLs. The sitemaps.org 0.9 protocol requires every <loc> to begin with the protocol, and a Sitemap: directive in robots.txt must be fully qualified — a relative one is ignored. As shipped the public portal is not indexable. The correct value is the deployed origin, which this repository does not hold and must not invent; both files now carry the requirement, the reason and the exact edit at the point of use.

**Why it closed.** Closed. The deployed origin was set and both files now name it absolutely.

**Decisions required first**

- Not templated with a placeholder host: a placeholder that looks like a hostname is indistinguishable from a real one at review.

**Steps**

- Done — both files are absolute against the confirmed origin.
- Done — the deploy-time instructions that would now cause double-prefixing are removed, and npm run test:portalseo fails if they return.
- Redeploy the portal so the published files are these ones.
- Point a sitemap validator at https://activityweb.page.gd/sitemap.xml and record the result.

**Done when.** The sitemap validates and robots.txt names it absolutely. Met: both are absolute against https://activityweb.page.gd and npm run test:portalseo passes.

**Validated by.** A sitemap validator accepts the file from the deployed origin.

### ITEM-43 — The portal still calls the defective OTP flow — the new one is deployed but not in service

| | |
|---|---|
| **Status** | READY |
| **Category** | BLOCKING |
| **Owner** | operator |
| **Target date** | _none established_ |
| **Waits on (in register)** | ITEM-38 |

IP_OTP_VERIFY (c5e314c7-68b5-4bdc-8935-0954ef6a256d) was built on 2026-09-04 and carries the hardening. It is a NEW flow, so it has a NEW trigger URL. config/endpoints.config.js resolves OTP_VERIFY from the deployed configuration, which still names 3e201620-f1e8-4c17-a90a-4d95b94a24c2 — the flow that carries all five defects and is still running. Until the configuration is repointed and the old flow disabled, the hardening is deployed and unused: a code issued to one person still verifies anyone, and the agency's Office 365 connection is still an open relay, for anyone holding the old URL.

**Why it is still open.** Both remaining steps need an authenticated session against the tenant and neither can be performed from this repository. Raised the moment the build landed rather than left implicit, because a half-done cutover is the one outcome worse than not having started: two flows for one job, and the insecure one still serving.

**Constraints**

- The new trigger URL carries a sig= bearer token. It goes into the deployed configuration and nowhere else — not into this repository, a ticket, an email or a chat message.

**Decisions required first**

- Built fresh rather than patched in place, because the flow-management API proved unreachable from the operator's session across three attempts. The new URL is the accepted cost of that route.
- 2026-09-08: re-based against docs/reference/endpoint-register.json. The original criteria were written before the estate was rotated and reconciled, and named a state the reconciled estate no longer has. The defective flow is no longer referenced anywhere in the configuration; what remains outstanding is the live proof against the tenant.

**Steps**

- Put the new flow's trigger URL into the deployed configuration as OTP_VERIFY.
- Exercise the four tests in BUILD.md against it: verify as another caller must fail; verify as the right caller must succeed; verify with no valid code must send no mail; six wrong guesses must return 429.
- Turn off 3e201620-f1e8-4c17-a90a-4d95b94a24c2. Not delete — off, so it can be re-enabled if step 2 finds something.
- Re-run npm run test:triggerauth and npm run wiring, and record the outcome as an evidence file.

**Done when.** The deployed configuration resolves OTP_VERIFY to the flow the tenant register names (b372d45e4b2a47d88b8e8b032da67fcd, serving OTP_GENERATE and OTP_VERIFY), the OTP tests pass against it, and no configuration resolves OTP_VERIFY to a flow absent from the register. Re-based: the register contains neither c5e314c7-68b5-4bdc-8935-0954ef6a256d nor the defective 3e201620-f1e8-4c17-a90a-4d95b94a24c2, so the original criteria cannot be evaluated.

**Validated by.** The four live calls in step 2, and a tenant read showing the old flow stopped.

### ITEM-50 — Verification codes, correspondence, reports and assignment notices go to a fixed mailbox

| | |
|---|---|
| **Status** | SPECIFIED_NOT_APPLIED |
| **Category** | CORRECTNESS |
| **Owner** | Platform technical owner |
| **Target date** | _none established_ |
| **Waits on (in register)** | ITEM-4 |

Of 76 live mail actions across 41 flows, four are addressed solely to the person concerned. The live VERIFY endpoint (Portal_Verify, 86897b2f) sends every citizen's verification code to the literal address dgsregistry@nitda.gov.ng, so a citizen cannot complete verification unless someone forwards it from a shared mailbox. Web - OTP Generate does the same for every officer's sign-in code. Web - Send Email, the flow behind the internal EMAIL endpoint, hard-codes To = dgsRegistry@nitda.gov.ng on all three of its sends with no Cc, no Bcc and no dynamic recipient, so every outward official correspondence, report and statistics email an officer composes is delivered to the registry mailbox instead of the addressee while the sent register displays the intended recipient. The bulk assignment flows send nine notices to one named individual, and the three flows that announce a new portal submission all address that same individual rather than a monitored registry address.

**Why it is still open.** Reporting delivery to a correspondent who was never written to is worse than a visible failure: the officer has no reason to follow up. DGO_OTP fixes the sign-in code and DGO_BULK_ASSIGNMENT would fix the assignment notices, but neither has been pasted, so until Sessions 4-6 run the deployed behaviour is what the estate does.

**Constraints**

- The payload already carries the recipient in every case; the carrier discards it. No new contract is needed, only the expression.

**Steps**

- Paste Portal_VERIFY_ECM_DOCS and Portal_VERIFY_CONFIRM_ECM_DOCS per Session 4 — this closes RN-006.
- Paste DGO_SEND_EMAIL into the flow the configured EMAIL URL resolves to — it addresses every send from the payload, gates on DGO_UserDirectory, and writes a Portal Outbox Receipts row per send. The paste target is not settled in this repository: read the workflows/<32-hex> segment off the EMAIL entry in config/config.local.js first (EXECUTION_GUIDE §6.7).
- Alternatively, if the deployed Web - Send Email body is to be kept, apply docs/deployment/sharepoint/remediation/patched/Web_Send_Email.definition.json — but correct its recipient expression first: it reads triggerBody()?['to'], and every one of the three desks nests the recipient under payload.
- Patch Web - OTP Generate to use the same dynamic form its sibling Web - OTP Verify already carries.
- Paste DGO_OTP, DGO_SINGLE_ASSIGNMENT and DGO_BULK_ASSIGNMENT per the walkthrough.
- Re-address the new-submission notice and add it to the SUBMISSION package.
- Re-run npm run notifications -- --check.

**Done when.** npm run notifications -- --check derives PROVISIONED for RN-006, RN-016, RN-021, RN-024 and RN-037, and the fixed-recipient census shows no verification, correspondence, report or assignment send hard-coding an address.

**Validated by.** npm run notifications -- --check

### ITEM-52 — Read endpoints and AI flows email record payloads to a shared mailbox

| | |
|---|---|
| **Status** | DECISION_REQUIRED |
| **Category** | CORRECTNESS |
| **Owner** | The agency for SN-001, alongside MANUAL-4; the platform technical owner for SN-002 to SN-004. |
| **Target date** | _none established_ |
| **Waits on (in register)** | MANUAL-4, ITEM-9 |

Thirteen flows send mail that nothing in the estate requires. Eight are read endpoints that email their response payload — the records they just returned — to dgsRegistry@nitda.gov.ng or hkani@nitda.gov.ng on every call. Two are AI chat and preprocessing flows that mail their results out with subjects reading 'Result--Hugging Face --' and 'HTTP CHAT FLOW'. Two mail an audit line to one individual that the audit and Portal Audit Events lists already hold. One, in Web - Email To Task Processing, carries an empty recipient and an empty subject and cannot succeed on any run that reaches it.

**Why it is still open.** These are not missing messages but surplus ones, and the decision they need is not technical. MANUAL-4 puts roughly 785 individuals in scope for the data these read endpoints return; mailing that data to a shared mailbox on every call is the same posture decision as MANUAL-4, and it has not been taken. The AI and audit sends look like development instrumentation left in deployed flows rather than designed notifications, which is a judgement only someone with estate knowledge can confirm — the same knowledge ITEM-9 is waiting on.

**Constraints**

- Removing a send is a flow edit and needs the same paste or patch path as any other change.

**Decisions required first**

- Keep, narrow, or remove each of the four surplus patterns.

**Steps**

- Review SN-001 alongside MANUAL-4 — they are the same posture decision.
- Confirm with estate knowledge whether SN-002's AI sends are still wanted in production.
- Retire SN-004's audit mails in favour of the audit lists, or record why the mail copy is needed.
- Remove or complete the SN-003 empty send.
- Update SurplusNotifications and re-run npm run notifications -- --check.

**Done when.** Each of the four surplus patterns carries a recorded decision, and any send decided against is gone from the exported estate.

**Validated by.** npm run notifications -- --check


---

## Wave 4 — 2 item(s)

**Owners needed:** operator

### MANUAL-2 — Test records must be cleared before real correspondence arrives

| | |
|---|---|
| **Status** | READY |
| **Category** | MANUAL_GATE |
| **Owner** | operator |
| **Target date** | _none established_ |
| **Waits on (in register)** | CFG-1, CFG-2 |
| **Also waits on** | all commissioning verification |

Commissioning verification writes real rows into the Correspondence list, and a reference sequence that has issued test numbers keeps issuing from there.

**Why it is still open.** Cannot be done until commissioning verification has actually run, which depends on the runtimes being configured.

**Constraints**

- The reference sequence continues from wherever the test numbers left it. Clearing rows does not reset the counter.

**Decisions required first**

- Whether to reset the sequence counter or accept a starting offset and record why.

**Steps**

- Complete commissioning verification. 2. Identify every test row. 3. Remove them. 4. Decide and record the sequence-counter treatment.

**Done when.** No test row remains, and the sequence counter's starting value is recorded.

**Validated by.** A read of the Correspondence list showing no test rows, plus the recorded counter value.

### MANUAL-3 — The browser suite must run against the deployed build

| | |
|---|---|
| **Status** | BLOCKED |
| **Category** | MANUAL_GATE |
| **Owner** | operator |
| **Target date** | _none established_ |
| **Waits on (in register)** | CFG-1, CFG-2 |

The suite covers boot, accessibility, all 29 routes, themes and the portal — but against a local server. Deployment is exactly where config.local.js presence differs.

**Why it is still open.** There is no deployed build to run against until CFG-1 and CFG-2 land.

**Constraints**

- In this container the suite needs DGO_CHROME_PATH set, because the pinned Playwright build and the pre-installed browser differ.

**Steps**

- Deploy. 2. Point baseURL at the deployed hostname. 3. Run all ten spec files. 4. Record the result.

**Done when.** All ten spec files pass against the deployed hostname.

**Validated by.** The Playwright run output against the deployed hostname, retained.


---

## Coverage

| Wave | Items |
|---|---|
| 1 | ITEM-22, ITEM-2, G-04, ITEM-23, ITEM-34, ITEM-30, ITEM-7, ITEM-35, ITEM-18, ITEM-25, ITEM-9, MANUAL-1, G-08, ITEM-36, ITEM-39, ITEM-40, ITEM-41, ITEM-44, ITEM-45, ITEM-46, ITEM-47, ITEM-49, ITEM-51, ITEM-53, ITEM-54, ITEM-55, ITEM-57, ITEM-58 |
| 2 | CFG-1, ITEM-8, ITEM-17, ITEM-12, ITEM-11, ITEM-3, ITEM-4, ITEM-6, MANUAL-4, ITEM-56, ITEM-38, ITEM-37, ITEM-48 |
| 3 | CFG-2, ITEM-42, ITEM-43, ITEM-50, ITEM-52 |
| 4 | MANUAL-2, MANUAL-3 |

Every one of the 48 register items appears exactly once above.
