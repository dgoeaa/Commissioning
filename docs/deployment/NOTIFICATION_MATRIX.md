# The required-notification matrix

> **Generated.** Produced by `scripts/verify-notification-matrix.mjs --emit` from
> `config/notification-matrix.config.js`, measured against the four artifact sets named below.
> Do not edit this file: edit the matrix, or the script that measures it, and regenerate.
> Compiled 2026-09-01 against `aeaa7e0`.

**Purpose.** The messages this estate is *required* to send — not the messages it happens to send.
`docs/process/18-NOTIFICATION-AND-ESCALATION-CATALOGUE.md` is the inventory; this is the requirement.
An inventory cannot report a message that was never built.

---

## Where it stands

39 required notifications. **16 provisioned.** 9 absent, 6 misaddressed, 2 regression, 4 local_only, 2 unimplemented.

Of the estate's 106 live mail actions across 54 flows, **22 are addressed solely to the person concerned.** 81 hard-code a fixed mailbox, and a further 3 resolve the recipient at run time but append a fixed address alongside it.

| Severity of what is outstanding | Rows |
|---|---|
| CRITICAL | 7 |
| HIGH | 9 |
| MEDIUM | 5 |
| LOW | 2 |

## Preconditions

Five things that are not notifications but that block or distort a whole class of them.

### PRE-1 — The sending mailbox is decided, and the tenant has now confirmed the connection — RESOLVED 2026-09-02

DECIDED 2026-09-01: the mailbox citizens and officers see as the sender is dgsregistry@nitda.gov.ng. That settled the service-identity half of the execution guide's section 4.2 — the address the public reads.

CONFIRMED 2026-09-02, and it needed confirming. An Outlook connection sends as the account that authorised it, and a connection id carries no account: the run records named c0b9e7a5b0854c39a435fd8ce92f48ad only as "Office 365 Outlook", so nothing here could say whether the connection every package binds was the registry mailbox or an individual officer's. If it had been personal, every citizen verification mail would have gone out under one person's name and stopped working the day they left.

The three flows exported back from the environment on 1-2 September answer it. Each package manifest NAMES its connection resources, joined through connectionsMap.json, and across all three flows and both APIs every one of the six resources is named dgsregistry@nitda.gov.ng. That is the tenant stating what the connection is rather than this repository inferring it, and npm run notifications now asserts it on every run.

Note what this decision is still not: it fixes the FROM address, not the TO. RN-006, RN-016 and RN-021 are about recipients being hard-coded and are untouched by it — the fact that dgsregistry is also the mailbox currently receiving those misdirected messages is a coincidence of this estate, not a resolution of them.

**Owner:** Agency — service identity (mailbox); operator (connection binding). Both settled.  
**Resolved when:** Done. No tenant check remains: the exported packages name the connection, which is the check that was outstanding. Re-confirm only if a connection is re-authorised by a different account, which would change the name in the next export.

Evidence:
- `docs/reference/flow-contracts/deployed/CG_SEND_EMAIL__63a7f2d7-d137-467a-9578-8099c52407dc__full_definition.json — packageProvenance.connections: both APIs named dgsregistry@nitda.gov.ng`
- `docs/reference/flow-contracts/deployed/IP_OTP_Endpoint__5d0d0a72-1467-4949-a808-61b58cdea0ce__full_definition.json — the same two names`
- `docs/reference/flow-contracts/deployed/AUTO_SCHEDULED_SWEEP__1cb48715-b1e4-4b9b-8740-fb3ee8c4d6f1__full_definition.json — the same two names`
- `docs/deployment/EXECUTION_GUIDE.md — section 4.2, the decision and section 13, the sign-off row`
- `npm run notifications — asserts every connection resource in the exported packages is that mailbox`

### PRE-2 — The scheduled carrier is live in the tenant — RESOLVED 2026-09-02

RESOLVED 2026-09-02. The estate had no clock at all: 59 Request triggers and one OneDrive file trigger across the whole exported set, no Recurrence and no SharePoint item trigger, so nothing could fire on time passing. Eight rows of this matrix depended on that one fact.

AUTO_SCHEDULED_SWEEP now runs in environment Default-ca6a4b3f-9123-49bc-bcb9-27085ebbf1a1 on an hourly Recurrence in W. Central Africa Standard Time. It was imported from DGO_SCHEDULED_SWEEP.zip and exported back on 2026-09-02, and the export was diffed against the package it came from: the same 78 actions, the same six scopes, no drift beyond the designer normalising a single-predicate condition to {and:[…]} and the operator chaining the first scope after the variable initialisation, which is correct and which the package could not do because a clipboard scope has no variables above it.

The verifier now asserts the POSITIVE — a scheduled trigger is present — so a sweep that is deleted or turned off fails the check instead of leaving eight rows green on a clock that has stopped.

**Owner:** Operational owner  
**Resolved when:** Done. Imported and confirmed by export. What remains is operational rather than structural: watch the first few hourly runs and confirm one notice per changed record, no re-send on the next cycle, and a Portal Outbox Receipts row per send.

Evidence:
- `docs/reference/flow-contracts/deployed/AUTO_SCHEDULED_SWEEP__1cb48715-b1e4-4b9b-8740-fb3ee8c4d6f1__full_definition.json — exported from the tenant 2026-09-02, Recurrence Hour/1, W. Central Africa Standard Time`
- `docs/deployment/internal/flows/import-package/DGO_SCHEDULED_SWEEP — the package it was imported from`
- `tests/scheduled-sweep.test.mjs — structure, idempotency, and that every column it names is already deployed`

### PRE-3 — The generated notification catalogue does not read the packages being deployed

Document 18 is generated from the deployed exports and from a superseded fragment set under designer-paste/correspondence-gateway. The scan path is hard-coded. The fourteen packages the operator walkthrough actually pastes are not read by it, so the estate's own notification catalogue has never described the state the walkthrough produces. This matrix reads all four artifact sets, which is why its counts and document 18's differ and both are correct about what they looked at.

**Owner:** Platform technical owner  
**Resolved when:** The discovery scan reads the deployed package set. Not done here: widening it regenerates all 28 process documents and renumbers every NOTIF id, which is a change to make deliberately rather than as a side effect of adding this matrix.

Evidence:
- `scripts/process-discovery.mjs:339 — pasteDir is designer-paste/correspondence-gateway`
- `docs/reference/process-inventory.json — 0 sources under the top-level designer-paste directories`

### PRE-4 — The readiness register carried no notification item

All 27 entries in the production-readiness register concerned tokens, columns, origins, indexes and endpoints. Nothing in it tracked a message to a human, so no amount of working the register would have surfaced any row in this matrix. Closed by this change: ITEM-38 through ITEM-42 now carry the five classes.

**Owner:** Platform technical owner  
**Resolved when:** Done.

Evidence:
- `docs/deployment/PRODUCTION_READINESS_REGISTER.json — items ITEM-38 through ITEM-42`

### PRE-5 — The rebuilt carriers are live beside the flows they replace, and the URL decides which runs

CG_SEND_EMAIL and IP_OTP_Endpoint were created in the tenant on 2 and 1 September from DGO_SEND_EMAIL and DGO_OTP, and both address their mail from the payload. Neither replaced the flow it was built to replace: Web - Send Email and Web - OTP Generate are both still deployed and both still hard-code dgsregistry@nitda.gov.ng.

Creating a new flow rather than pasting over the old one is the safe choice — the old flow keeps working while the new one is proved — but it means the fix is DEPLOYED AND NOT YET WIRED. Which flow an officer's correspondence actually reaches is decided by the URL under EMAIL, OTP_GENERATE and OTP_VERIFY in config.local.js, and that file is git-ignored because it holds signed trigger URLs. No check in this repository can see it.

So the three rows this qualifies stay MISADDRESSED rather than turning green on the existence of a correct flow. That is the conservative reading and the one that keeps the re-pointing step visible; a matrix that called this fixed would be describing a tenant nobody has configured yet. The rows turn PROVISIONED when the old carrier stops being deployed, or stops hard-coding its recipient.

**Owner:** operator  
**Resolved when:** For each of EMAIL, OTP_GENERATE and OTP_VERIFY: open the new flow in Power Automate, read the workflows/<32-hex>/ segment of its trigger URL, put the URL in config.local.js under that key, exercise the endpoint once, then TURN OFF the old flow rather than deleting it. Record the id only, never the URL — it carries a sig= bearer token. Re-export the estate and re-run npm run notifications -- --check; the three rows turn PROVISIONED when the old carrier leaves the deployed set.

Evidence:
- `docs/reference/flow-contracts/deployed/CG_SEND_EMAIL__63a7f2d7-d137-467a-9578-8099c52407dc__full_definition.json — every send addressed from the payload`
- `docs/reference/flow-contracts/deployed/IP_OTP_Endpoint__5d0d0a72-1467-4949-a808-61b58cdea0ce__full_definition.json — the code addressed to the requesting officer`
- `docs/reference/flow-contracts/deployed/Web - Send Email__e5e2c6a2-52ea-2ba8-b0b3-60f25d43387f__full_definition.json — three sends, all fixed, still deployed`
- `npm run notifications — the PRE-5 line names each pair and the endpoint key that decides it`

## The matrix

| ID | Event | Platform | Audience | Channel | State | Severity | Carrier |
|---|---|---|---|---|---|---|---|
| RN-001 | A submission is accepted and a reference is minted | portal | citizen | email | ABSENT | CRITICAL | docs/deployment/sharepoint/flows/designer-paste/Portal_SUBMISSION_ECM_DOCS.designer-paste.json |
| RN-002 | A record moves to action-required | portal | citizen | email | ✅ PROVISIONED | CRITICAL | docs/deployment/internal/flows/import-package/DGO_SCHEDULED_SWEEP (legacy import package, Recurrence trigger) |
| RN-003 | A decision is issued — the record moves to approved | portal | citizen | email | ✅ PROVISIONED | CRITICAL | docs/deployment/internal/flows/import-package/DGO_SCHEDULED_SWEEP (legacy import package, Recurrence trigger) |
| RN-004 | A decision is issued — the record moves to declined | portal | citizen | email | ✅ PROVISIONED | CRITICAL | docs/deployment/internal/flows/import-package/DGO_SCHEDULED_SWEEP (legacy import package, Recurrence trigger) |
| RN-005 | A submitter withdraws their own submission | portal | citizen | email | ABSENT | HIGH | docs/deployment/sharepoint/flows/designer-paste/Portal_WRITEBACK_ECM_DOCS.designer-paste.json |
| RN-006 | A citizen requests a verification code | portal | citizen | email | MISADDRESSED | CRITICAL | Portal_VERIFY_ECM_DOCS and Portal_VERIFY_CONFIRM_ECM_DOCS, action Send_Otp_Email |
| RN-007 | A support case is opened from the portal | portal | citizen | email | ✅ PROVISIONED | MEDIUM | Portal_SUPPORT_ECM_DOCS, action Send_Support_Acknowledgement |
| RN-008 | A support case is answered or closed | portal | citizen | email | ✅ PROVISIONED | HIGH | docs/deployment/internal/flows/import-package/DGO_SCHEDULED_SWEEP (legacy import package, Recurrence trigger) |
| RN-009 | A record is assigned to one officer | internal | assignee | email | REGRESSION | CRITICAL | docs/deployment/internal/flows/designer-paste/DGO_SINGLE_ASSIGNMENT.designer-paste.json |
| RN-010 | Records are assigned in bulk | internal | assignee | email | MISADDRESSED | CRITICAL | docs/deployment/internal/flows/designer-paste/DGO_BULK_ASSIGNMENT.designer-paste.json |
| RN-011 | A record is assigned with supporting and copy-to parties | internal | supporting | email | ABSENT | MEDIUM | DGO_SINGLE_ASSIGNMENT and DGO_BULK_ASSIGNMENT |
| RN-012 | An officer is reminded to acknowledge an assignment | internal | assignee | email | LOCAL_ONLY | HIGH | core/acknowledgement-service.js via SUBSIDIARY_ACTIONS |
| RN-013 | An acknowledgement clock expires unacknowledged | internal | owner | email | ✅ PROVISIONED | HIGH | docs/deployment/internal/flows/import-package/DGO_SCHEDULED_SWEEP (legacy import package, Recurrence trigger) |
| RN-014 | A due date is breached | internal | owner | email | ✅ PROVISIONED | HIGH | docs/deployment/internal/flows/import-package/DGO_SCHEDULED_SWEEP (legacy import package, Recurrence trigger) |
| RN-015 | An operator notifies the owner of a fast-tracked record | internal | owner | email | LOCAL_ONLY | HIGH | No artifact. The action has no declared backend at all. |
| RN-016 | An officer requests a sign-in code | internal | assignee | email | MISADDRESSED | CRITICAL | docs/deployment/internal/flows/designer-paste/DGO_OTP.designer-paste.json, action Send_Otp_Mail |
| RN-017 | A record enters pending_review and awaits an approval | internal | approver | email | ABSENT | HIGH | No artifact. |
| RN-018 | An approval is decided, returned or rejected | internal | originator | email | ABSENT | HIGH | No artifact. |
| RN-019 | A record is reassigned from one officer to another | internal | assignee | email | ABSENT | MEDIUM | DGO_SINGLE_ASSIGNMENT |
| RN-020 | A comment is added to a reference someone else is working | internal | assignee | in-app | ABSENT | LOW | core/notification-center.js, and a cross-device carrier it does not yet have |
| RN-021 | An officer sends an outward official correspondence | internal | correspondent | email | MISADDRESSED | CRITICAL | The flow behind the EMAIL endpoint — deployed as Web - Send Email |
| RN-022 | An approved response is dispatched to its recipients | internal | correspondent | email | UNIMPLEMENTED | CRITICAL | DGO_DYNAMIC_GLOBAL_ACTIONS, operation dispatchoutbound |
| RN-023 | A dispatch fails | internal | originator | in-app | ✅ PROVISIONED | MEDIUM | core/action-authority.js failure toast and core/notification-center.js |
| RN-024 | An officer sends a report or statistics email | internal | originator | email | MISADDRESSED | HIGH | The flow behind the EMAIL endpoint — deployed as Web - Send Email |
| RN-025 | A flow run completes and its run record is captured | flow-estate | registry-fixed | email | REGRESSION | MEDIUM | Send_Telemetry_Email, in the deployed flows |
| RN-026 | An outbound message to a citizen fails to send | flow-estate | administrator | email | ✅ PROVISIONED | HIGH | docs/deployment/internal/flows/import-package/DGO_SCHEDULED_SWEEP (legacy import package, Recurrence trigger) |
| RN-027 | A failed outbound message is retried | flow-estate | citizen | ledger | ✅ PROVISIONED | HIGH | docs/deployment/internal/flows/import-package/DGO_SCHEDULED_SWEEP (legacy import package, Recurrence trigger) |
| RN-028 | A provisioning or gap-closure run completes | flow-estate | administrator | email | ✅ PROVISIONED | LOW | Global_Gap_Remediation_Provisioning, Send an email notification V2 |
| RN-029 | A scanned document is deposited by the intake pipeline | flow-estate | registry-fixed | email | ✅ PROVISIONED | LOW | DGSO INCOMING AI PROCESSING, Send an email (V2) |
| RN-030 | A governed action fails | internal | operator | in-app | ✅ PROVISIONED | LOW | core/action-authority.js and core/notification-center.js |
| RN-031 | An operator action succeeds and must remain findable afterwards | internal | operator | in-app | ✅ PROVISIONED | LOW | core/notification-center.js |
| RN-032 | A write cannot reach the registry and is held on the device | internal | operator | in-app | ✅ PROVISIONED | LOW | core/pending-queue.js and the module toast at each call site |
| RN-033 | A verification code is requested for delivery by SMS | internal | assignee | sms | UNIMPLEMENTED | MEDIUM | No artifact. No SMS connector exists anywhere in the estate. |
| RN-034 | An escalation is raised on a record | internal | owner | email | LOCAL_ONLY | HIGH | No artifact. Neither action has a declared backend. |
| RN-035 | A reminder set on a task falls due | internal | assignee | email | LOCAL_ONLY | MEDIUM | No artifact. A scheduled carrier must be created — see PRE-2. |
| RN-036 | The executive authority delegates a record to someone else | internal | assignee | email | ABSENT | HIGH | No artifact. |
| RN-037 | A new portal submission arrives and the registry must pick it up | flow-estate | administrator | email | MISADDRESSED | HIGH | Portal_SUBMISSION_ECM_DOCS, or the monitored registry mailbox |
| RN-038 | A record is assigned and the cascade resolves a copy-to and inform list | internal | copyTo | email | ABSENT | LOW | DGO_SINGLE_ASSIGNMENT and DGO_BULK_ASSIGNMENT |
| RN-039 | An HTTP flow registry exception is open and assigned to someone | flow-estate | assignee | email | ✅ PROVISIONED | MEDIUM | Send_Exception_Email, inside Notify_Each_Exception, in 06 - GOV - Registry Exception |

## Every row in full

### RN-001 — A submission is accepted and a reference is minted

**ABSENT** · CRITICAL · portal → citizen · email

**Trigger.** CG_Submission_Endpoint, after the Portal Registry row is written

**Carrier.** `docs/deployment/sharepoint/flows/designer-paste/Portal_SUBMISSION_ECM_DOCS.designer-paste.json`

**Must say.** The tracking ID, the submission type, the time, the attachment count, and how to track.

**On failure.** A Portal Outbox Receipts row with MessageType submission-confirmation and the send status.

**Why it is required.** The portal tells every submitter a confirmation is coming, and tells them to call the helpdesk if it does not arrive within one working day. The FAQ states tracking IDs are emailed. The tracking page instructs the citizen to take the ID from that email.

- `document-portal/js/submit.js:604`
- `document-portal/js/submit.js:610`
- `document-portal/js/data.js:150`
- `document-portal/js/track.js:25`

**What the estate does today.**

- Portal_SUBMISSION_ECM_DOCS.designer-paste.json carries no SendEmailV2 action
- SUBMISSION_ECM_DOCS_PORTAL and Portal_UBMISSION_ECM_DOCS carry none either

**Owner.** Platform technical owner

**Remediation.** Add a Send_Submission_Confirmation action and a Create_Submission_Outbox_Receipt catch to the SUBMISSION package, addressed to the senderEmail already written to Portal Registry.

---

### RN-002 — A record moves to action-required

**PROVISIONED** · CRITICAL · portal → citizen · email

**Trigger.** The status write that sets Portal Registry.ActionRequired

**Carrier.** `docs/deployment/internal/flows/import-package/DGO_SCHEDULED_SWEEP (legacy import package, Recurrence trigger)`

**Must say.** What is needed, the reference, and the link to the respond form on the tracking page.

**On failure.** A Portal Outbox Receipts row with MessageType action-required.

**Why it is required.** The state exists to ask the submitter for something — "Something is needed from the submitter before this can continue" — and the tracking page carries a respond form for it. A request nobody is told about is a request that will not be answered.

- `config/status-vocabulary.config.js:29`
- `document-portal/js/data.js:57`
- `document-portal/js/track.js:424`
- `docs/deployment/sharepoint/LISTS.md — Portal Registry.ActionRequired`

**What the estate does today.**

- AUTO_SCHEDULED_SWEEP, live on an hourly Recurrence since 2026-09-02, is the carrier — Scope_Status
- Scope_Status/Foreach_Status_ActionRequired in DGO_SCHEDULED_SWEEP sends action-required to Portal Registry.SenderEmail, deduped on the outbox Title
- The carrier is built and validated; it reaches the tenant by import, which is PRE-2.

**Owner.** Process owner, correspondence lifecycle

**Remediation.** Requires PRE-2. Either a scheduled sweep of Portal Registry for rows whose status changed, or a send added to whichever write sets the status.

---

### RN-003 — A decision is issued — the record moves to approved

**PROVISIONED** · CRITICAL · portal → citizen · email

**Trigger.** The status write that sets Portal Registry.Status to approved

**Carrier.** `docs/deployment/internal/flows/import-package/DGO_SCHEDULED_SWEEP (legacy import package, Recurrence trigger)`

**Must say.** The outcome, the reference, and the printable record.

**On failure.** A Portal Outbox Receipts row with MessageType decision-outcome.

**Why it is required.** Both the governed status vocabulary and the portal say the outcome is sent to the submitter, and the portal home page states the decision is emailed.

- `config/status-vocabulary.config.js:30`
- `document-portal/js/data.js:58`
- `document-portal/index.html:111`

**What the estate does today.**

- AUTO_SCHEDULED_SWEEP · Scope_Status carries the decision outcome
- Scope_Status/Foreach_Status_Approved in DGO_SCHEDULED_SWEEP sends decision-outcome, deduped on the outbox Title
- The carrier is built and validated; it reaches the tenant by import, which is PRE-2.

**Owner.** Process owner, correspondence lifecycle

**Remediation.** Requires PRE-2. Same carrier as RN-002, keyed on the approved status.

---

### RN-004 — A decision is issued — the record moves to declined

**PROVISIONED** · CRITICAL · portal → citizen · email

**Trigger.** The status write that sets Portal Registry.Status to declined

**Carrier.** `docs/deployment/internal/flows/import-package/DGO_SCHEDULED_SWEEP (legacy import package, Recurrence trigger)`

**Must say.** The outcome, the reference, and the reasons held in the notes.

**On failure.** A Portal Outbox Receipts row with MessageType decision-outcome.

**Why it is required.** Declined is a stage-4 terminal decision in the governed vocabulary, and the portal home page promises the outcome is emailed without distinguishing which outcome.

- `config/status-vocabulary.config.js:31`
- `document-portal/js/data.js:59`
- `document-portal/index.html:111`

**What the estate does today.**

- AUTO_SCHEDULED_SWEEP · Scope_Status carries the decision outcome
- Scope_Status/Foreach_Status_Declined in DGO_SCHEDULED_SWEEP sends decision-outcome, deduped on the outbox Title
- The carrier is built and validated; it reaches the tenant by import, which is PRE-2.

**Owner.** Process owner, correspondence lifecycle

**Remediation.** Requires PRE-2. Same carrier as RN-002, keyed on the declined status.

---

### RN-005 — A submitter withdraws their own submission

**ABSENT** · HIGH · portal → citizen · email

**Trigger.** CG_Writeback_Endpoint, on the withdraw action

**Carrier.** `docs/deployment/sharepoint/flows/designer-paste/Portal_WRITEBACK_ECM_DOCS.designer-paste.json`

**Must say.** Confirmation that the reference is closed at their request, and the time it was closed.

**On failure.** A Portal Outbox Receipts row with MessageType withdrawal-receipt.

**Why it is required.** The writeback package closes a record on the citizen's instruction and sets StatusLabel "Withdrawn by the submitter". A closure taken on someone's instruction needs a receipt to that person; nothing else in the estate records that they asked.

- `docs/deployment/sharepoint/flows/designer-paste/Portal_WRITEBACK_ECM_DOCS.designer-paste.json — item/StatusLabel "Withdrawn by the submitter"`
- `config/status-vocabulary.config.js:32`

**What the estate does today.**

- Portal_WRITEBACK_ECM_DOCS.designer-paste.json carries no SendEmailV2 action

**Owner.** Process owner, correspondence lifecycle

**Remediation.** Add a send plus outbox receipt to the withdraw branch of the WRITEBACK package.

---

### RN-006 — A citizen requests a verification code

**MISADDRESSED** · CRITICAL · portal → citizen · email

**Trigger.** Case_Generate in the verify switch, after Create Otp Record

**Carrier.** `Portal_VERIFY_ECM_DOCS and Portal_VERIFY_CONFIRM_ECM_DOCS, action Send_Otp_Email`

**Must say.** The six-digit code and its expiry.

**On failure.** Create_Verify_Outbox_Receipt runs on Succeeded, Failed, TimedOut and Skipped.

**Why it is required.** Verification cannot complete without the code reaching the person verifying.

- `docs/deployment/EXECUTION_GUIDE.md:646`

**What the estate does today.**

- Both packages address Send_Otp_Email to @outputs('Compose_Verify_Email') and write Create_Verify_Outbox_Receipt on Succeeded, Failed, TimedOut and Skipped — the packages are correct
- The live VERIFY endpoint is not. Portal_Verify (86897b2f, CG_Verification_Endpoint) sends "Your Verification Code" to the literal address dgsregistry@nitda.gov.ng, so every citizen verification code issued today arrives in the registry mailbox rather than to the citizen who asked for it
- CG_Verification_Confirmation_Endpoint (5e13db77) does address the citizen dynamically, so the two halves of one verification journey behave differently
- A third flow, Portal_Verify_Confirm (3b69aa71), also sends "Your Verification Code" — once to a fixed mailbox and once dynamically. It is neither of the walkthrough's two verify paste targets, and it is not on the walkthrough's list of flows not to paste on, so it will survive Session 4 unretired and unmentioned, still issuing codes

**Owner.** Platform technical owner

**Remediation.** Session 4 fixes this by pasting the package over 86897b2f. Until it is pasted, a citizen cannot complete verification unless someone forwards the code from a shared mailbox. Subject to PRE-1, the sending mailbox decision.

---

### RN-007 — A support case is opened from the portal

**PROVISIONED** · MEDIUM · portal → citizen · email

**Trigger.** Portal_SUPPORT_ECM_DOCS, after the Portal Support Cases row is written

**Carrier.** `Portal_SUPPORT_ECM_DOCS, action Send_Support_Acknowledgement`

**Must say.** The case reference and the reply undertaking.

**On failure.** Create_Support_Outbox_Receipt runs on Succeeded, Failed, TimedOut and Skipped.

**Why it is required.** The acknowledgement carries the case reference, which the citizen has no other way to obtain.

- `docs/deployment/PORTAL_INTEGRATION_FACTSHEET.md:384`

**What the estate does today.**

- Send_Support_Acknowledgement addressed to @outputs('Compose_Support_Email')
- Create_Support_Outbox_Receipt records sent or failed with LastError

**Owner.** Platform technical owner

**Remediation.** None. Subject to PRE-1, the sending mailbox decision.

---

### RN-008 — A support case is answered or closed

**PROVISIONED** · HIGH · portal → citizen · email

**Trigger.** The write that changes Portal Support Cases.Status

**Carrier.** `docs/deployment/internal/flows/import-package/DGO_SCHEDULED_SWEEP (legacy import package, Recurrence trigger)`

**Must say.** The reply, or the reason the case was closed, quoting the case reference.

**On failure.** A Portal Outbox Receipts row with MessageType support-reply.

**Why it is required.** The acknowledgement the estate already sends undertakes that "The helpdesk replies within one working day", and Portal Support Cases carries a Status column to close on. The undertaking is made by a message that exists; the reply it promises is not.

- `docs/deployment/sharepoint/flows/designer-paste/Portal_SUPPORT_ECM_DOCS.designer-paste.json — Send_Support_Acknowledgement body`
- `docs/deployment/sharepoint/LISTS.md — Portal Support Cases.Status`

**What the estate does today.**

- AUTO_SCHEDULED_SWEEP · Scope_Support carries the support reply
- Scope_Support in DGO_SCHEDULED_SWEEP sends support-reply when Portal Support Cases.Status is closed
- The carrier is built and validated; it reaches the tenant by import, which is PRE-2.

**Owner.** Registry helpdesk owner

**Remediation.** Requires PRE-2, or a helpdesk action that sends on reply.

---

### RN-009 — A record is assigned to one officer

**REGRESSION** · CRITICAL · internal → assignee · email

**Trigger.** DGO_SINGLE_ASSIGNMENT, after the assignment is written

**Carrier.** `docs/deployment/internal/flows/designer-paste/DGO_SINGLE_ASSIGNMENT.designer-paste.json`

**Must say.** The reference, the instruction, the acknowledgement date and the due date.

**On failure.** An outbox row, or the assignment is not reported as delivered.

**Why it is required.** The cascade requires an assignee and a due date on every assignment, and derives an acknowledgement date from ackDays. Both clocks start when the assignment is written, and the person they run against is not told they have started.

- `config/assignment-cascade.config.js — validation.requireAssignedTo, requireDue`
- `config/assignment-cascade.config.js — defaultAckDays, defaultDueDays, dueByPriority`

**What the estate does today.**

- Deployed - Create Task, one of the four flows DECISIONS.md names as creating in the internal estate, DOES notify the assignee: Compose_Email_To_2 resolves Get_item_Task/AssignedTo and falls back to Assigned. It is the only assignment-shaped send in the estate addressed to the person it concerns
- DGO_SINGLE_ASSIGNMENT.designer-paste.json, the package that replaces this path, carries no SendEmailV2 action at all — so pasting it removes the one working assignment notice
- Deployed Bulk Task Assignment_Create Task addresses hkani@nitda.gov.ng, not the assignee
- Web - Email Task Created carries five sends, every one to hkani@nitda.gov.ng

**Owner.** Platform technical owner

**Remediation.** Port the Compose_Email_To_2 recipient expression from Deployed - Create Task into DGO_SINGLE_ASSIGNMENT before Session 4 pastes it. The cascade already computes the same address through categoryFieldAliases.assignedTo, so no new contract is needed.

---

### RN-010 — Records are assigned in bulk

**MISADDRESSED** · CRITICAL · internal → assignee · email

**Trigger.** DGO_BULK_ASSIGNMENT, once per distinct assignee

**Carrier.** `docs/deployment/internal/flows/designer-paste/DGO_BULK_ASSIGNMENT.designer-paste.json`

**Must say.** The references assigned, the instruction, and the earliest acknowledgement and due dates.

**On failure.** An outbox row per assignee, or the batch is not reported as delivered.

**Why it is required.** Same obligation as RN-009. Bulk changes the volume, not the duty.

- `config/assignment-cascade.config.js — validation.requireAssignedTo, requireDue`
- `config/app.config.js — maxBulkAssign`

**What the estate does today.**

- DGO_BULK_ASSIGNMENT.designer-paste.json carries no SendEmailV2 action
- Bulk Assign Direct carries six SendEmailV2 actions, all six addressed to hkani@nitda.gov.ng
- optimized Bulk Assign Direct carries three more, all to the same address

**Owner.** Platform technical owner

**Remediation.** Re-address the nine existing sends to the resolved assignee, or add the send to the replacement package and retire the deployed flows.

---

### RN-011 — A record is assigned with supporting and copy-to parties

**ABSENT** · MEDIUM · internal → supporting · email

**Trigger.** The same write as RN-009 and RN-010

**Carrier.** `DGO_SINGLE_ASSIGNMENT and DGO_BULK_ASSIGNMENT`

**Must say.** The reference and the instruction, marked as information rather than action.

**On failure.** An outbox row. A failure here does not fail the assignment.

**Why it is required.** The cascade resolves a supporting DSU, a supporting assignee, and a copy-to and inform-DSU list on every row of the routing matrix. Resolving an audience the system never writes to is work with no effect.

- `config/assignment-cascade.config.js — categoryFieldAliases.supportDsuKey, supportingAssignee, copyTo, infoDsu`
- `config/assignment-cascade.config.js — fallbackMatrix, six rows each naming a supportDsuKey`

**What the estate does today.**

- Neither assignment package carries any SendEmailV2 action

**Owner.** Process owner, correspondence lifecycle

**Remediation.** Add a second send, addressed to the resolved supporting and copy-to lists.

---

### RN-012 — An officer is reminded to acknowledge an assignment

**LOCAL_ONLY** · HIGH · internal → assignee · email

**Trigger.** The acknowledgment module's remind-assignee action

**Carrier.** `core/acknowledgement-service.js via SUBSIDIARY_ACTIONS`

**Must say.** The reference, the original instruction, and the acknowledgement date now passed or approaching.

**On failure.** The reminder is queued in PendingQueue and retried, not reported as sent.

**Why it is required.** The action exists, is confirmed by the operator, and reports "Reminder recorded for <assignee>". A reminder that reaches nobody is not a reminder.

- `modules/acknowledgment.js:54`
- `config/action-ownership.config.js:62 — remind-assignee, backend DYNAMIC_ACTIONS.optional`

**What the estate does today.**

- modules/acknowledgment.js:54 calls State.patch and pushes onto State.notifications
- State.notifications is browser state; no endpoint carries it off the device
- The action's declared backend is DYNAMIC_ACTIONS.optional — optional, so absent is not an error

**Owner.** Platform technical owner

**Remediation.** Make the backend contract mandatory and give it a carrier, or change the operator copy so it does not claim the assignee was reminded.

---

### RN-013 — An acknowledgement clock expires unacknowledged

**PROVISIONED** · HIGH · internal → owner · email

**Trigger.** A scheduled sweep of assignments whose ack date has passed

**Carrier.** `docs/deployment/internal/flows/import-package/DGO_SCHEDULED_SWEEP (legacy import package, Recurrence trigger)`

**Must say.** The references unacknowledged, their assignees, and how long each has been overdue.

**On failure.** The sweep run record; a failed sweep must be visible, not silent.

**Why it is required.** Every routing row carries an ackDays count that becomes a dated field on the assignment. A clock whose expiry raises nothing is a measurement, not a control.

- `config/assignment-cascade.config.js — ackDays on all six fallbackMatrix rows`

**What the estate does today.**

- AUTO_SCHEDULED_SWEEP · Scope_Ack_Overdue carries this, hourly
- Scope_Ack_Overdue in DGO_SCHEDULED_SWEEP sends acknowledgement-overdue to the assignee on Global Tracking Queue
- The carrier is built and validated; it reaches the tenant by import, which is PRE-2.

**Owner.** Operational owner

**Remediation.** Requires PRE-2. One scheduled flow closes RN-013, RN-014 and RN-027 together.

---

### RN-014 — A due date is breached

**PROVISIONED** · HIGH · internal → owner · email

**Trigger.** A scheduled sweep of assignments whose due date has passed

**Carrier.** `docs/deployment/internal/flows/import-package/DGO_SCHEDULED_SWEEP (legacy import package, Recurrence trigger)`

**Must say.** The breached references, their assignees, and the escalation level raised.

**On failure.** The sweep run record.

**Why it is required.** The cascade sets a due date by priority on every assignment, and FastTrack already classifies every tracked item Breached, Due soon or Unassigned against it. The detection exists and the response exists; only the connection is missing.

- `config/assignment-cascade.config.js — dueByPriority, defaultDueDays`

**What the estate does today.**

- AUTO_SCHEDULED_SWEEP · Scope_Due_Breached carries this, hourly
- Scope_Due_Breached in DGO_SCHEDULED_SWEEP sends due-breached to the assignee on Global Tracking Queue
- The carrier is built and validated; it reaches the tenant by import, which is PRE-2.

**Owner.** Operational owner

**Remediation.** Requires PRE-2. Same scheduled carrier as RN-013.

---

### RN-015 — An operator notifies the owner of a fast-tracked record

**LOCAL_ONLY** · HIGH · internal → owner · email

**Trigger.** The fasttrack module's notify-owner action

**Carrier.** `No artifact. The action has no declared backend at all.`

**Must say.** The reference, the fast-track notice, and the escalation level if one was raised.

**On failure.** Queued in PendingQueue and retried.

**Why it is required.** The action is confirmed by the operator and reports "Owner notification queued". Nothing is queued anywhere but browser storage.

- `modules/fasttrack.js:24 — data-notify handler`
- `config/module-boundaries.config.js:17 — fasttrack owns notify-owner`

**What the estate does today.**

- notify-owner has no entry in config/action-ownership.config.js, so it runs with no declared service, audit vocabulary or backend
- The handler calls State.patch and pushes onto State.notifications

**Owner.** Platform technical owner

**Remediation.** Give notify-owner an action-ownership spec naming a mandatory backend, then a carrier. Until it has one, its audit trail cannot be trusted.

---

### RN-016 — An officer requests a sign-in code

**MISADDRESSED** · CRITICAL · internal → assignee · email

**Trigger.** DGO_OTP, after the OTP record is created

**Carrier.** `docs/deployment/internal/flows/designer-paste/DGO_OTP.designer-paste.json, action Send_Otp_Mail`

**Must say.** The six-digit code and its expiry.

**On failure.** None defined. This row is provisioned on delivery and unprovisioned on failure.

**Why it is required.** Sign-in cannot complete without the code reaching the officer signing in.

- `docs/deployment/EXECUTION_GUIDE.md:648`

**What the estate does today.**

- DEPLOYED BESIDE IT, 2026-09-01: IP_OTP_Endpoint was created from DGO_OTP and addresses the code to the requesting officer, but Web - OTP Generate is still deployed and still fixed. OTP_GENERATE decides which answers. See PRE-5
- DGO_OTP.designer-paste.json addresses @outputs('Compose_Otp_Email') — correct
- The deployed flow it replaces, Web - OTP Generate, addresses dgsregistry@nitda.gov.ng for the "Your Verification Code" send — the code goes to the registry mailbox, not the officer
- Web - OTP Verify carries both forms: one fixed, one dynamic
- DGO_OTP writes no outbox receipt, so a failed officer code is unrecorded

**Owner.** Platform technical owner

**Remediation.** The package fixes the address. Until it is pasted, the deployed flow sends every officer's sign-in code to one shared mailbox. Add an outbox receipt to the package.

---

### RN-017 — A record enters pending_review and awaits an approval

**ABSENT** · HIGH · internal → approver · email

**Trigger.** The approvals module's create-approval action

**Carrier.** `No artifact.`

**Must say.** The reference, what is being approved, and who raised it.

**On failure.** Queued in PendingQueue and retried.

**Why it is required.** pending_review is a governed lifecycle state whose only exits are decisions by a second person — approved, approved_with_edit, returned, escalated or rejected. A state that can only be left by someone who is never told about it stalls by construction.

- `core/lifecycle.js:2 — pending_review transitions`
- `modules/approvals.js — create-approval`

**What the estate does today.**

- No flow in any set sends on an approval request

**Owner.** Process owner, correspondence lifecycle

**Remediation.** Add a send to the approval write, addressed to the approver the record names.

---

### RN-018 — An approval is decided, returned or rejected

**ABSENT** · HIGH · internal → originator · email

**Trigger.** The write that records the approval decision

**Carrier.** `No artifact.`

**Must say.** The decision, the reference, and any edit or reason recorded with it.

**On failure.** Queued in PendingQueue and retried.

**Why it is required.** returned sends the record back to in_progress — back to the originator, who must act on it. Nothing tells them it came back.

- `core/lifecycle.js:2 — returned:['in_progress']`

**What the estate does today.**

- No flow in any set sends on an approval decision

**Owner.** Process owner, correspondence lifecycle

**Remediation.** Add a send to the decision write, addressed to the originator.

---

### RN-019 — A record is reassigned from one officer to another

**ABSENT** · MEDIUM · internal → assignee · email

**Trigger.** The reassign-task route into single-assignment

**Carrier.** `DGO_SINGLE_ASSIGNMENT`

**Must say.** To the new assignee, the assignment. To the previous assignee, that it has moved and to whom.

**On failure.** An outbox row.

**Why it is required.** reassign_requested is a governed exit from assigned, and the routing table carries a reassign-task action. A clock transfers with the record; the officer it transfers away from is still carrying it until told.

- `core/lifecycle.js:2 — assigned:['acknowledged','in_progress','reassign_requested']`
- `config/action-routing.config.js — reassign-task`

**What the estate does today.**

- Neither assignment package carries any SendEmailV2 action

**Owner.** Process owner, correspondence lifecycle

**Remediation.** Two sends on the reassignment write.

---

### RN-020 — A comment is added to a reference someone else is working

**ABSENT** · LOW · internal → assignee · in-app

**Trigger.** The comments module's add-comment action

**Carrier.** `core/notification-center.js, and a cross-device carrier it does not yet have`

**Must say.** Who commented, on which reference, and the first line of the comment.

**On failure.** None required for an in-app notice.

**Why it is required.** Comments are addressed to the people working a reference. The notification centre is the estate's declared durable feedback channel, and it is device-local by design, so a comment raised on one device is invisible on every other.

- `modules/comments.js — add-comment`
- `core/notification-center.js — storage is this module's own localStorage key`

**What the estate does today.**

- add-comment writes a comment entity; no notification is raised to anyone but the actor

**Owner.** Platform technical owner

**Remediation.** Raise a NotificationCenter entry for the assignee on the actor's device today, and carry it cross-device when a server-side feed exists.

---

### RN-021 — An officer sends an outward official correspondence

**MISADDRESSED** · CRITICAL · internal → correspondent · email

**Trigger.** core/correspondence-email-service.js, on the send-correspondence-email action

**Carrier.** `The flow behind the EMAIL endpoint — deployed as Web - Send Email`

**Must say.** The composed correspondence, addressed to the recipient the officer entered.

**On failure.** Marked queued with lastError, enqueued in PendingQueue, and retried.

**Why it is required.** All six correspondence templates require recipientEmail. The desk states its own purpose: "This desk is for actual outward correspondences via email." The register tab displays a Recipient column for everything it claims to have sent.

- `config/correspondence-email-templates.config.js — required:['recipientEmail', ...] on all six templates`
- `core/correspondence-email-service.js:68`
- `modules/correspondence-email.js:28 — the sent register displays x.recipientEmail`

**What the estate does today.**

- DEPLOYED BESIDE IT, 2026-09-02: CG_SEND_EMAIL was created from DGO_SEND_EMAIL and addresses every send from the payload — but Web - Send Email is still deployed and still fixed, so which one an officer reaches is decided by the EMAIL url in config.local.js, which this repository cannot read. See PRE-5
- Web - Send Email carries three SendEmailV2 actions, all three hard-coding To = dgsRegistry@nitda.gov.ng with no Cc, no Bcc and no dynamic recipient
- The payload's recipient is never read by any of the three
- markSent then records the row as sent, and the register displays the intended recipient beside a message that never went to them

**Owner.** Platform technical owner

**Remediation.** BUILT, NOT APPLIED, TWO ROUTES. The one to use is the clipboard package docs/deployment/internal/flows/designer-paste/DGO_SEND_EMAIL.designer-paste.json (60 actions, generated by scripts/build-internal-designer-paste.mjs): it addresses every send payload-first, refuses an unaddressed request with 400 rather than sending it anywhere, gates on DGO_UserDirectory, and writes a Portal Outbox Receipts row per send so DGO_SCHEDULED_SWEEP can retry a failure. Pasting preserves the trigger, so no client URL changes. The paste target is NOT settled here — read it off the configured EMAIL URL; EXECUTION_GUIDE 6.7 says how. The older route, docs/deployment/sharepoint/remediation/patched/Web_Send_Email.definition.json applied with update-flow-definition.ps1 -Apply, keeps the deployed body and rewrites only the three recipients; its expression reads triggerBody()?['to'] and must be corrected to read payload-first before use, because core/data-client.js nests every desk field under payload. Either way the row stays MISADDRESSED until the tenant runs the new body: this matrix measures the estate, not the repository.

---

### RN-022 — An approved response is dispatched to its recipients

**UNIMPLEMENTED** · CRITICAL · internal → correspondent · email

**Trigger.** core/dispatch-service.js dispatchOutbound, via the DISPATCH_OUTBOUND operation

**Carrier.** `DGO_DYNAMIC_GLOBAL_ACTIONS, operation dispatchoutbound`

**Must say.** The approved response and its attachments, to the recipients named on the dispatch.

**On failure.** markDispatchFailed, the dispatch_failed state, and the retry-dispatch action.

**Why it is required.** Dispatch is the estate's outbound stage. dispatchOutbound refuses to run without at least one recipient, transitions the record to dispatch_in_flight and then dispatched, and records a receipt — all of it predicated on a carrier that delivers.

- `core/dispatch-service.js:9`
- `docs/deployment/internal/dynamic-operations.json — dispatchoutbound is a required operation`
- `core/lifecycle.js:2 — dispatch_pending → dispatch_in_flight → dispatched`

**What the estate does today.**

- DGO_DYNAMIC_GLOBAL_ACTIONS.designer-paste.json implements four switch cases — Dynamic_Audit, Dynamic_Transition, Dynamic_Update_Flag, Dynamic_Update_Task
- dispatchoutbound is not among them and falls to the 501 default
- This is the substance of ITEM-25: 15 of 19 DYNAMIC_ACTIONS operations answer 501

**Owner.** Platform technical owner

**Remediation.** Implement the dispatchoutbound case. Tracked as ITEM-25; this matrix records what the missing operation costs in notification terms rather than restating the item.

---

### RN-023 — A dispatch fails

**PROVISIONED** · MEDIUM · internal → originator · in-app

**Trigger.** markDispatchFailed

**Carrier.** `core/action-authority.js failure toast and core/notification-center.js`

**Must say.** That it has not gone out and the record still shows it as waiting to be sent.

**On failure.** None required for an in-app notice.

**Why it is required.** DISPATCH_FAILED carries its own operator sentence in the failure vocabulary, and the dispatch_failed state has a retry route. The operator must know to take it.

- `core/action-authority.js — FAILURE_OUTCOME[ErrorClass.DISPATCH_FAILED]`
- `core/lifecycle.js:2 — dispatch_failed:['dispatch_pending','no_dispatch']`

**What the estate does today.**

- executeOwnedAction toasts actionFailureMessage on every failed governed action unless the call site opts out, and the notification centre retains warn and error unread

**Owner.** Platform technical owner

**Remediation.** None. Device-local by design, which is acceptable for a notice to the actor.

---

### RN-024 — An officer sends a report or statistics email

**MISADDRESSED** · HIGH · internal → originator · email

**Trigger.** The reports and statistics modules, via the EMAIL endpoint

**Carrier.** `The flow behind the EMAIL endpoint — deployed as Web - Send Email`

**Must say.** The rendered report, to the recipients the officer confirmed.

**On failure.** Held on the device and sent when the connection returns.

**Why it is required.** Reports states its destination to the officer before sending — "It goes to <email>" — and statistics refuses to send without at least one recipient and passes them as `to`.

- `modules/reports.js:49 — 'Sent to': s.profile.email`
- `modules/statistics.js:48 — payload {subject, to: recipients, ...}`

**What the estate does today.**

- CG_SEND_EMAIL, live since 2026-09-02, carries the report and statistics traffic correctly the moment EMAIL is re-pointed at it; until then this is Web - Send Email. See PRE-5
- The same three hard-coded sends as RN-021 carry this traffic
- statistics.js validates a recipient list that the carrier then ignores

**Owner.** Platform technical owner

**Remediation.** Same carrier and the same fix as RN-021: DGO_SEND_EMAIL. The shapes differ and one expression serves all three — statistics sends payload.to as an ARRAY, correspondence sends payload.email.to as a STRING, and reports sends no recipient at all, relying on the envelope userEmail it shows the officer as 'Sent to'. join(array(x),';') handles the array and the string without a type test, and the userEmail fallback is what makes the reports desk work. Note the level: all three are nested under payload by core/data-client.js, so an expression reading triggerBody()?['to'] finds the recipient on none of them.

---

### RN-025 — A flow run completes and its run record is captured

**REGRESSION** · MEDIUM · flow-estate → registry-fixed · email

**Trigger.** Compose Telemetry Attachments succeeding, in each instrumented flow

**Carrier.** `Send_Telemetry_Email, in the deployed flows`

**Must say.** The flow, the run id, the outcome and the run record as an attachment.

**On failure.** None. No catch path follows the send, so a delivery failure fails the step and, unless a container run-after absorbs it, the run.

**Why it is required.** The telemetry mail is the estate's run-record capture, and Portal Flow Telemetry exists to hold what it carries. A fixed registry mailbox is the correct audience here — this is the one row where it is.

- `docs/deployment/sharepoint/LISTS.md — Portal Flow Telemetry.RunRecordJson`
- `docs/process/18-NOTIFICATION-AND-ESCALATION-CATALOGUE.md — NOTIF-140 and its siblings`

**What the estate does today.**

- Deployed CG_Upload_Endpoint and CG_Writeback_Endpoint each carry a Send_Telemetry_Email to dgsRegistry@nitda.gov.ng
- Portal_UPLOAD_ECM_DOCS and Portal_WRITEBACK_ECM_DOCS carry none, so Session 4 removes run-record capture from both endpoints
- The operator walkthrough does not mention the removal

**Owner.** Platform technical owner

**Remediation.** Either restore the telemetry send in the two packages, or record the removal as deliberate in the walkthrough so an operator is not surprised by silence.

---

### RN-026 — An outbound message to a citizen fails to send

**PROVISIONED** · HIGH · flow-estate → administrator · email

**Trigger.** A Portal Outbox Receipts row written with Status failed

**Carrier.** `docs/deployment/internal/flows/import-package/DGO_SCHEDULED_SWEEP (legacy import package, Recurrence trigger)`

**Must say.** The message type, the recipient, the reference and the LastError.

**On failure.** The sweep run record.

**Why it is required.** The list carries Status and LastError precisely so a failure is recoverable. Writing a failure nobody reads is a record, not a control.

- `docs/deployment/sharepoint/LISTS.md — Portal Outbox Receipts.Status, .LastError`
- `scripts/build-designer-paste.mjs:317 and :391 — the only writers`

**What the estate does today.**

- AUTO_SCHEDULED_SWEEP · Scope_Outbox raises the exhaustion alert
- Scope_Outbox in DGO_SCHEDULED_SWEEP reads the outbox and alerts on receipts that exhausted their retries
- The carrier is built and validated; it reaches the tenant by import, which is PRE-2.

**Owner.** Operational owner

**Remediation.** Requires PRE-2. Same scheduled carrier as RN-013 and RN-014.

---

### RN-027 — A failed outbound message is retried

**PROVISIONED** · HIGH · flow-estate → citizen · ledger

**Trigger.** A scheduled sweep of Portal Outbox Receipts rows whose Status is failed

**Carrier.** `docs/deployment/internal/flows/import-package/DGO_SCHEDULED_SWEEP (legacy import package, Recurrence trigger)`

**Must say.** The original message, resent, with Attempts incremented.

**On failure.** Attempts incremented and LastError replaced; a ceiling raises RN-026.

**Why it is required.** The list provisions an Attempts column and every writer hard-codes it to 1. A retry counter that never advances is a column that was designed for a mechanism nobody built.

- `docs/deployment/sharepoint/LISTS.md — Portal Outbox Receipts.Attempts`
- `scripts/build-designer-paste.mjs — item/Attempts is the literal 1 in every writer`

**What the estate does today.**

- AUTO_SCHEDULED_SWEEP · Scope_Outbox retries and advances Attempts
- Scope_Outbox/Foreach_Failed in DGO_SCHEDULED_SWEEP resends failed receipts and advances Attempts
- The carrier is built and validated; it reaches the tenant by import, which is PRE-2.

**Owner.** Operational owner

**Remediation.** Requires PRE-2. One scheduled flow closes RN-013, RN-014, RN-026 and RN-027.

---

### RN-028 — A provisioning or gap-closure run completes

**PROVISIONED** · LOW · flow-estate → administrator · email

**Trigger.** Global_Gap_Remediation_Provisioning, on both the success and error compose paths

**Carrier.** `Global_Gap_Remediation_Provisioning, Send an email notification V2`

**Must say.** The run summary, marked [DRY RUN], [ACTION REQUIRED] or [OK], with the report attached.

**On failure.** None. No catch path follows the send.

**Why it is required.** A provisioning run that changes the tenant must report what it changed to whoever asked for it.

- `docs/process/18-NOTIFICATION-AND-ESCALATION-CATALOGUE.md — NOTIF-173, NOTIF-174`

**What the estate does today.**

- Addressed to @{triggerBody()?['AdminEmail']};hkani@nitda.gov.ng — the caller's address, plus a fixed one appended

**Owner.** Platform technical owner

**Remediation.** None required. The appended fixed address is worth reviewing when PRE-1 is decided, since it sends every provisioning report to one named individual as well as the caller.

---

### RN-029 — A scanned document is deposited by the intake pipeline

**PROVISIONED** · LOW · flow-estate → registry-fixed · email

**Trigger.** DGSO INCOMING AI PROCESSING, on the OneDrive file trigger

**Carrier.** `DGSO INCOMING AI PROCESSING, Send an email (V2)`

**Must say.** The new file name and the intake outcome.

**On failure.** None. No catch path follows the send.

**Why it is required.** Scan intake is the one non-HTTP entry point in the estate, so nothing else in a citizen or officer journey reveals that a document arrived by it.

- `docs/process/18-NOTIFICATION-AND-ESCALATION-CATALOGUE.md — NOTIF-159`
- `modules/scan-intake.js — scan-deposit`

**What the estate does today.**

- Addressed to dgs@nitda.gov.ng, which config/support-routing.config.js names as the support mailbox — a fixed address that is the correct audience for this row

**Owner.** Registry owner

**Remediation.** None.

---

### RN-030 — A governed action fails

**PROVISIONED** · LOW · internal → operator · in-app

**Trigger.** executeOwnedAction catching any failure

**Carrier.** `core/action-authority.js and core/notification-center.js`

**Must say.** What was being attempted and what it means for the record — never the raw error.

**On failure.** Not applicable. This is itself the failure path.

**Why it is required.** The governance layer is the only place that sees every failed governed action, so it owns the sentence the operator reads. Fourteen error classes each have their own.

- `core/action-authority.js — FAILURE_OUTCOME, actionFailureMessage`
- `core/action-authority.js:67 — executeOwnedAction`

**What the estate does today.**

- One failure, one toast, with an explicit notify:false opt-out for call sites that can say something more useful
- warn and error arrive unread in the notification centre so they survive a route change

**Owner.** Platform technical owner

**Remediation.** None.

---

### RN-031 — An operator action succeeds and must remain findable afterwards

**PROVISIONED** · LOW · internal → operator · in-app

**Trigger.** Any module raising a toast through the shell

**Carrier.** `core/notification-center.js`

**Must say.** The outcome, its tone, the module and the reference.

**On failure.** Persist failures are logged; the feed degrades to in-memory.

**Why it is required.** Before the notification centre existed, a toast was the entire feedback surface and nothing survived a route change, so an approval or a failed sync the user did not happen to be looking at was unrecoverable from the UI.

- `core/notification-center.js — header comment and CAPACITY`

**What the estate does today.**

- 200-entry capacity, dismissal, unread counts, and a badge on the shell
- Device-local by design and correctly documented as such — it is a UI history, not a delivery channel, and this matrix does not count it as one

**Owner.** Platform technical owner

**Remediation.** None.

---

### RN-032 — A write cannot reach the registry and is held on the device

**PROVISIONED** · LOW · internal → operator · in-app

**Trigger.** PendingQueue.enqueue, on any write that fails while offline

**Carrier.** `core/pending-queue.js and the module toast at each call site`

**Must say.** That the work is saved here and goes to the registry when the connection returns.

**On failure.** Not applicable.

**Why it is required.** An operator who is told a write succeeded when it is sitting in a local queue will not come back to it. Every enqueue site in the runtime pairs the queue with a toast that says where the work actually is.

- `modules/lookup.js:153`
- `core/correspondence-email-service.js:69`
- `modules/statistics.js:48`

**What the estate does today.**

- Enqueue and operator notice are paired at every call site checked

**Owner.** Platform technical owner

**Remediation.** None.

---

### RN-033 — A verification code is requested for delivery by SMS

**UNIMPLEMENTED** · MEDIUM · internal → assignee · sms

**Trigger.** An OTP request carrying channel sms

**Carrier.** `No artifact. No SMS connector exists anywhere in the estate.`

**Must say.** The six-digit code.

**On failure.** An outbox row, or a fall back to email that the caller is told about.

**Why it is required.** The OTP trigger contract advertises the channel — "Optional: delivery method, e.g. 'sms' or 'email'" — and the internal sign-in screen offers a Phone (SMS) tab that relabels the field to a phone number. A contract that accepts a channel it cannot serve will silently do something other than what the caller asked for.

- `docs/reference/flow-contracts/deployed/Web - OTP Verify — trigger schema description`
- `docs/reference/flow-contracts/deployed/Portal_Verify_Confirm — trigger schema description`
- `core/welcome-experience.js:46 — the Phone (SMS) tab`
- `core/welcome-experience.js:54 — setChannel relabels the field for sms`

**What the estate does today.**

- No SMS, Twilio or text-message connector appears in any of the 60 exported definitions or any of the 20 paste packages
- The welcome experience is demo-mode and sends nothing, so the tab is presentational — but the trigger contract is not, and it is what an integrator reads

**Owner.** Platform technical owner

**Remediation.** Either remove sms from the trigger schema and the sign-in tab, or provision a carrier. Advertising it and ignoring it is the one option that should not survive.

---

### RN-034 — An escalation is raised on a record

**LOCAL_ONLY** · HIGH · internal → owner · email

**Trigger.** The fasttrack escalate-priority action, and the acknowledgment escalate-non-ack capability

**Carrier.** `No artifact. Neither action has a declared backend.`

**Must say.** The reference, the escalation level, the reason, and who raised it.

**On failure.** Queued in PendingQueue and retried.

**Why it is required.** escalate-priority writes an escalation record with status Open and forces priority to urgent. An escalation is by definition a request for someone above the current holder to act; a record marked Open that reaches nobody is a counter, not an escalation.

- `modules/fasttrack.js:24 — the data-escalate handler writes an escalations entry with status Open`
- `config/module-boundaries.config.js:17 — fasttrack owns escalate-priority`
- `config/module-boundaries.config.js:18 — acknowledgment owns escalate-non-ack`

**What the estate does today.**

- escalate-priority calls State.patch and writes only to browser state
- Neither escalate-priority nor escalate-non-ack appears in config/action-ownership.config.js, so both run with no declared service, audit vocabulary or backend
- escalate-non-ack has no handler in modules/acknowledgment.js at all; it is a charter capability with no implementation

**Owner.** Platform technical owner

**Remediation.** Give both actions an action-ownership spec naming a mandatory backend, then a carrier. escalate-non-ack additionally needs an implementation or removal from the charter.

---

### RN-035 — A reminder set on a task falls due

**LOCAL_ONLY** · MEDIUM · internal → assignee · email

**Trigger.** The dueAt recorded by the orchestrator set-reminder action

**Carrier.** `No artifact. A scheduled carrier must be created — see PRE-2.`

**Must say.** The reference, the task title, and the reminder the operator set.

**On failure.** The sweep run record.

**Why it is required.** set-reminder is a governed action with a mandatory DYNAMIC_ACTIONS backend and its own audit vocabulary, and it records a dueAt against a named assignee. A reminder carrying a due time that nothing reads at that time is a note, not a reminder.

- `modules/orchestrator.js:66 — writes {taskId, dueAt, assignedTo} onto State.notifications`
- `config/action-ownership.config.js:69 — set-reminder, backend DYNAMIC_ACTIONS`
- `config/action-routing.config.js:309 — set-reminder, backendContract DYNAMIC_ACTIONS`

**What the estate does today.**

- The handler writes State.notifications and nothing carries it off the device
- Even with a carrier, nothing could fire at dueAt: there is no scheduled trigger in the estate (PRE-2)
- Unlike remind-assignee, this action declares a mandatory backend — so the contract is stated and unmet, rather than optional and absent

**Owner.** Platform technical owner

**Remediation.** Requires PRE-2 for the firing. The write itself can be carried immediately, since the action already declares DYNAMIC_ACTIONS as mandatory.

---

### RN-036 — The executive authority delegates a record to someone else

**ABSENT** · HIGH · internal → assignee · email

**Trigger.** The executive-escalate action, which sets the record to Delegated

**Carrier.** `No artifact.`

**Must say.** The reference, that it has been delegated by the executive authority, and what is expected.

**On failure.** Queued in PendingQueue and retried.

**Why it is required.** Delegation moves work to a person who was not previously holding it, and Delegated maps onto the public "Under review" status — so the record continues to progress publicly while the person it now depends on has not been told it is theirs.

- `modules/executive.js:30 — executive-escalate sets status Delegated`
- `config/action-ownership.config.js:58 — executive-escalate, backend DYNAMIC_ACTIONS.optional`
- `config/status-vocabulary.config.js — InternalStatusToGoverned maps Delegated to review`

**What the estate does today.**

- No mail action in any set fires on a delegation
- The action's declared backend is optional, so its absence is not treated as an error

**Owner.** Process owner, correspondence lifecycle

**Remediation.** Add a send on the delegation write, addressed to the delegate.

---

### RN-037 — A new portal submission arrives and the registry must pick it up

**MISADDRESSED** · HIGH · flow-estate → administrator · email

**Trigger.** CG_Submission_Endpoint, after the Portal Registry row is written

**Carrier.** `Portal_SUBMISSION_ECM_DOCS, or the monitored registry mailbox`

**Must say.** The reference, the submission type, the sender and the attachment count.

**On failure.** A Portal Outbox Receipts row, or the submission is not reported as delivered.

**Why it is required.** DECISIONS.md records that a portal submission has no internal row and that no flow creates one — the portal estate and the internal estate are joined only by a live read feed. With no scheduled sweep either, a submission that nobody is told about waits in a list until somebody opens it. Three deployed flows already try to send this notice, which is the estate itself recording that it is needed.

- `docs/deployment/sharepoint/DECISIONS.md:20 — no flow creates an internal row from portal data`
- `docs/deployment/sharepoint/LISTS.md — Portal Registry, written by CG_Submission_Endpoint`

**What the estate does today.**

- UPLOAD_ECM_DOCS_PORTAL, Web - Task Update and Get Correspondences each send a "Glass Pane Portal submission" notice, and all three address hkani@nitda.gov.ng
- The notice therefore depends on one named individual reading their mail, not on a monitored registry address
- Portal_SUBMISSION_ECM_DOCS, the package that replaces the submission path, sends nothing at all

**Owner.** Registry owner

**Remediation.** Re-address the notice to a monitored registry mailbox and add it to the SUBMISSION package, so it survives Session 4 rather than depending on flows the walkthrough retires.

---

### RN-038 — A record is assigned and the cascade resolves a copy-to and inform list

**ABSENT** · LOW · internal → copyTo · email

**Trigger.** The same write as RN-009 and RN-010

**Carrier.** `DGO_SINGLE_ASSIGNMENT and DGO_BULK_ASSIGNMENT`

**Must say.** The reference and the instruction, for information, with no action implied.

**On failure.** An outbox row. A failure here does not fail the assignment.

**Why it is required.** The cascade resolves copyTo and up to three inform-DSU fields separately from the supporting assignee, because they are a different audience with a different duty — told, not tasked. Resolving a list the system never writes to is work with no effect.

- `config/assignment-cascade.config.js — categoryFieldAliases.copyTo, with CC and ccRecipients aliases`
- `config/assignment-cascade.config.js — categoryFieldAliases.infoDsu, INFORMDSU1 through INFORMDSU3`

**What the estate does today.**

- Neither assignment package carries any SendEmailV2 action

**Owner.** Process owner, correspondence lifecycle

**Remediation.** Carry the resolved copy-to and inform lists on the RN-011 send as Cc, rather than as a separate message.

---

### RN-039 — An HTTP flow registry exception is open and assigned to someone

**PROVISIONED** · MEDIUM · flow-estate → assignee · email

**Trigger.** An hourly Recurrence; Get_Open_Exceptions reads Status eq 'Open' from DGO_HTTPFlowRegistryExceptions

**Carrier.** `Send_Exception_Email, inside Notify_Each_Exception, in 06 - GOV - Registry Exception`

**Must say.** Severity, exception type and exception key as the subject, the exception detail as the body. Acknowledge_Exception then writes the notification back to the row.

**On failure.** Bounded and visible. Acknowledge_Exception runs only after Send_Exception_Email succeeds, so an undelivered notice leaves the row unacknowledged rather than silently marked sent; and If_Notification_Failed terminates the run when the final report is not Succeeded, so a failed cycle is a failed run rather than a quiet one. This is the only row in this matrix whose carrier both records its own delivery and fails loudly.

**Why it is required.** The registry provisions the address of the officer answerable for each exception, and a view of the open ones that carries that address. It also provisions an enabled configuration seed whose only stated purpose is exception notification. Resolving an accountable person and never writing to them is the defect this matrix already records against the assignment cascade in RN-011 and RN-038.

- `docs/reference/http-flow-registry-spec.json — fields: DGO_HTTPFlowRegistryExceptions.AssignedToEmail`
- `docs/reference/http-flow-registry-spec.json — views: Open Exceptions, Status eq 'Open', AssignedToEmail in viewFieldsInOrder`
- `docs/reference/http-flow-registry-spec.json — configSeeds: GovernanceNotificationEmail, isEnabled Yes`
- `docs/deployment/sharepoint/LISTS.md — DGO_HTTPFlowRegistryExceptions, Open Exceptions`

**What the estate does today.**

- 06 - GOV - Registry Exception is deployed on an hourly Recurrence, exported from the tenant 2026-09-08
- Send_Exception_Email addresses the assignee and copies the registry: where AssignedToEmail is set and is not already the registry mailbox it sends to concat(AssignedToEmail, ';dgsRegistry@nitda.gov.ng'), and where it is empty or already the registry it sends to the registry alone. The recipient census counts it as mixed rather than sole for that reason
- Live delivery is unverified, as for every row here: the tenant evidence pack is NOT_CAPTURED

**Owner.** Platform technical owner

**Remediation.** None outstanding for the carrier. Two things are: this row needs a tenant result like every other, and the standing copy to the registry mailbox on every exception notice is a choice worth confirming rather than inheriting — it is the same decision SN-006 puts to the governance owner about the same flow.

---

## Surplus — messages the estate sends that nothing requires

The complement of the matrix. Each of these is a mail action somebody has to keep working,
and several carry record payloads to a shared mailbox. They are listed rather than made rows
because a row asserts an obligation, and asserting one for these would be inventing it.

### SN-001 — Read endpoints email their response payload to a fixed mailbox

**Flows.** `FETCH_DOCS_V2_POST`, `Fetch_Emails_HTTP_POST`, `Fetch_Emails_POST`, `OPS_REFERENCES_AND_LOOKUPS`, `WEB Get Docs  HTTP GET`, `Web - Get Tasks GET SWITCH`, `Web - Subsidiary Doc Actions`, `Get Correspondences`

**Recipients.** dgsRegistry@nitda.gov.ng, hkani@nitda.gov.ng

These are per-flow diagnostic captures, distinct from the standardised Flow Run Record telemetry of RN-025. They mail response data — the records a read endpoint just returned — to a shared mailbox on every call. MANUAL-4 puts roughly 785 individuals in scope for that data. Whether this is acceptable is the same decision as MANUAL-4's posture, and it has not been taken.

**Decision needed.** Keep, narrow, or remove. Owner: the agency, alongside MANUAL-4.

### SN-002 — AI chat and preprocessing flows email their results to a fixed mailbox

**Flows.** `Instant OpenRouter AI Chat`, `Web - Preprocess user message`

**Recipients.** dgsRegistry@nitda.gov.ng

Chat content and preprocessed user messages are mailed out on each run. Subjects are "Result--Hugging Face --" and "HTTP CHAT FLOW", which suggests development instrumentation left in a deployed flow rather than a designed notification.

**Decision needed.** Confirm whether these are still wanted in a production environment.

### SN-003 — A send on a second mail connector, with an empty body

**Flows.** `Web - Email To Task Processing`

**Recipients.** dgsRegistry@nitda.gov.ng

Send_an_email_notification_(V3) is the estate's only SendEmailV3, and its only send on the Mail connector (shared_sendmail) rather than Office 365 Outlook. It has a recipient and a subject — "Email AI Assist Response- " plus the source subject — and a body of "<p class='editor-paragraph'></p>", which is an empty paragraph. So it sends, and what arrives is blank.

Two things follow. The message is useless to whoever opens it, and the send sits outside every connection control this matrix applies: the sign-off assertion and the PRE-1 naming check both read shared_office365 connections, so the identity this one authenticates as is asserted nowhere.

CORRECTED 2026-09-09. This entry previously read "a send with no recipient and no subject ... it cannot succeed". That was the measurement, not the estate: the extractor read only the emailMessage/* parameters of SendEmailV2 and this action names its parameters request/to, request/subject and request/text. It has both, and it does succeed. The extractor now reads both schemas.

**Decision needed.** Give the message a body or remove the action, and decide whether a second mail connector is intended at all — if it is, the connection controls have to cover it.

### SN-004 — Audit-trail emails duplicating what the audit list already holds

**Flows.** `Web - Email Task Created`, `Deployed Bulk Task Assignment_Create Task`

**Recipients.** hkani@nitda.gov.ng

Sends whose subjects begin "NITDA AUDIT |" mail an audit line to one individual. The estate has an audit list and a Portal Audit Events list for exactly this. Three of the five sends in Web - Email Task Created are audit or diagnostics rather than notification.

**Decision needed.** Retire in favour of the audit lists, or state why the mail copy is needed.

### SN-005 — A real acknowledgement that has no row to assert it

**Flows.** `CG_Support_Endpoint`, `IP_OTP_VERIFY`

**Recipients.** each requester's own address — @outputs('Compose_Support_Email') for the support case, Create_item_OTP_Record's Title (triggerBody()?['identifier']) for the verification code

Send_Support_Acknowledgement mails "NITDA support case <ref>" to the address the requester supplied, read from docs/reference/flow-contracts/deployed/CG_Support_Endpoint__1b2c2e53-6c07-46a3-80b2-c43be1ef69db__full_definition.json. Unlike SN-001 to SN-005 this is a legitimate notification addressed to the right person. It is recorded here only because the matrix has no row for it, and an unlisted mail action is indistinguishable from an unnoticed one. Listing it is not a claim that it is surplus. IP_OTP_VERIFY joins it after the 2026-09-05 re-export: Send_an_email_(V2) had addressed the literal dgsregistry@nitda.gov.ng and now addresses the requester, which moves it out of the surplus case and into this one — a required notification, correctly addressed, with no row.

**Decision needed.** Give each a matrix row asserting the obligation, and remove them from this register.

### SN-006 — Every governance flow emails its own execution report to the registry mailbox

**Flows.** `01 - GOV - Provision HTTP Flow Registry`, `02 - GOV - Register HTTP Flow Truth`, `03 - GOV - Record HTTP Flow Execution`, `04 - GOV - Audit HTTP Flow Registry Flow Update`, `05 - GOV - Retire HTTP Flow`, `06 - GOV - Registry Exception`, `07 - GOV - Consumer HTTP Self-Registration Template`, `08 - GOV - Discover and Register HTTP Consumers`

**Recipients.** dgsRegistry@nitda.gov.ng

Each of the eight ends by composing a JSON and an HTML execution report and mailing both to the registry mailbox, with the JSON attached. Unlike SN-001 these carry no correspondence data and no personal data — they report what the flow did to the governance lists — so the MANUAL-4 exposure does not apply. They are recorded here for the other reason this register exists: eight mail actions somebody has to keep working, on a mailbox that already receives the SN-001 to SN-005 traffic. Four of the eight are HTTP-triggered and anonymous (see GOV-04 and GOV-05 in docs/reference/governance-estate-position.json), so the volume is set by whoever holds the URL, not by the estate. That is the part worth deciding before the registry lists are provisioned.

**Decision needed.** Keep as the audit trail for a governance system that has no other observer, or route to a governance mailbox distinct from the registry one. Owner: the governance owner.

## Vocabulary

| Audience | Meaning |
|---|---|
| `citizen` | The external submitter, at the address they used. |
| `assignee` | The officer the record is assigned to. |
| `supporting` | The supporting department or unit named by the assignment cascade. |
| `copyTo` | The copy-to and inform-DSU list named by the assignment cascade. |
| `approver` | The officer whose approval the record is waiting on. |
| `originator` | The officer who raised the action being reported on. |
| `owner` | The accountable owner of an overdue or escalated record. |
| `correspondent` | The external party an outward correspondence or dispatch is addressed to. |
| `operator` | Whoever is working the platform at the time, in the UI. |
| `administrator` | The platform or registry administrator. |
| `registry-fixed` | A fixed internal mailbox, regardless of who the message concerns. |

| Channel | Meaning |
|---|---|
| `email` | Office 365 Outlook, via a SendEmailV2 action in a Power Automate flow. |
| `in-app` | A toast plus a durable entry in core/notification-center.js. Device-local. |
| `ledger` | A row in a SharePoint list that records delivery — Portal Outbox Receipts. |
| `sms` | Text message. Advertised by the OTP trigger contract; no carrier exists in the estate. |

| State | Meaning |
|---|---|
| `PROVISIONED` | Sent to the correct audience by a named artifact, with its failure behaviour defined. |
| `MISADDRESSED` | Sent, but to a hard-coded mailbox rather than the audience this row names. |
| `ABSENT` | No artifact in any of the four sets sends this. |
| `LOCAL_ONLY` | Recorded in browser storage by the runtime. Nothing leaves the device. |
| `UNIMPLEMENTED` | The carrier is named but does not implement it, or the channel has no connector. |
| `REGRESSION` | A deployed flow sends it; the package that replaces that flow does not. |

| Severity | Meaning |
|---|---|
| `CRITICAL` | A person is told something untrue, or a legally significant message never arrives. |
| `HIGH` | Work stalls unseen, or a message reaches the wrong person. |
| `MEDIUM` | A recipient must go looking for something that should have reached them. |
| `LOW` | Operational visibility only. |
