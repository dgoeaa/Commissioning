# Portal data contract

> Generated by `scripts/build-estate-docs.mjs` from
> [`portal-data-contract.json`](./portal-data-contract.json). Do not edit by hand.
> `npm test` fails if it drifts, or if any target names a column that was never provisioned.

**Version 1.0.0 · AUTHORITATIVE**

The data the portal sends, the data it reads back, and where each field must be persisted. This is the specification flows conform to — not a description of what any flow currently does. A flow that does not satisfy it is wrong; the contract is not adjusted to match a flow.

> **Precedence.** Where this disagrees with a deployed flow, the flow is wrong. Where it disagrees with the functional documentation, the disagreement is a defect in one of them and is recorded rather than resolved silently.

## Derived from

- document-portal/js/core.js — the request bodies actually sent and the response fields actually read
- document-portal/js/submit.js — the submission record and the declared-attachment shape
- document-portal/js/track.js — the status projection consumed by fromRegistry()
- docs/reference/flow-contracts/DOCUMENT_PORTAL_FLOWS.html — live-tenant probe, 2026-08-08
- NITDA_Document_Portal_Functional_Documentation v3.0 — INT-001..007
- docs/deployment/sharepoint/portal-field-spec.json — the 97 provisioned columns

## Conventions

- **required** — Required means the portal always sends it, or the portal cannot function without reading it back. Not a SharePoint column constraint.
- **persistence** — list/column names a provisioned target. `null` means the contract requires the value but no provisioned column holds it — a gap, reported by scripts/verify-portal-data-contract.mjs.
- **timestamps** — Every *AtUtc column is ISO 8601 with a Z suffix. The portal sends ISO strings; flows must not reformat them.
- **reference** — referenceId is minted by the flow, never by the portal. The portal's localId is a client-generated correlation id and is not a reference.

---

## SUBMISSION

Register a correspondence, mint its reference, issue one upload ticket per declared attachment.

`POST` · json

### Request

| Field | Type | Required | Persisted to | Notes |
|---|---|---|---|---|
| `localId` | string | **yes** | `Portal Registry/LocalId` | Client correlation id. Lets a retry be recognised as the same submission. |
| `channel` | string | **yes** | `Portal Registry/Channel` | const `Portal` |
| `correspondenceType` | string | **yes** | `Portal Registry/CorrespondenceType` | const `Incoming` |
| `subject` | string | **yes** | `Portal Registry/Subject` |  |
| `category` | string | **yes** | `Portal Registry/Category` | One of the eight correspondence types. The flow must reject a value outside the set rather than store it. |
| `sender.name` | string | **yes** | `Portal Registry/SenderName` |  |
| `sender.organisation` | string | no | `Portal Registry/SenderOrganisation` |  |
| `sender.organisationType` | string | no | `Portal Registry/SenderOrganisationType` |  |
| `senderEmail` | string | **yes** | `Portal Registry/SenderEmail` | Stored lowercased and trimmed. STATUS matches on it, so a stored variant that differs in case makes the record unreachable. |
| `senderPhone` | string | no | `Portal Registry/SenderPhone` |  |
| `eventDate` | string | no | `Portal Registry/EventDate` | Empty string unless the correspondence type carries one. |
| `description` | string | **yes** | `Portal Registry/Description` |  |
| `submittedAt` | iso8601 | **yes** | `Portal Registry/SubmittedAtUtc` |  |
| `attachments[]` | array | no | `Portal Registry/AttachmentCount` | Count only on the registry row; each element becomes a ticket. |
| `attachments[].name` | string | **yes** | `Portal Upload Tickets/DeclaredName` |  |
| `attachments[].size` | number | **yes** | `Portal Upload Tickets/DeclaredSizeBytes` |  |
| `attachments[].sha256` | string | **yes** | `Portal Upload Tickets/DeclaredSha256` | INT-002 requires UPLOAD to verify the received bytes against this. Declared here so it can be. |
| `verification` | string | no | `Portal Verification Proofs/Consumed` | The single-use proof from VERIFY_CONFIRM, present only when the wizard holds one. It is not stored on the registry row — the flow redeems it by setting Consumed on the proof it matches, and records the outcome as Portal Registry/VerifiedSubmission. A proof that is already consumed, or expired, must be refused. |

### Written by the flow

| Value | Type | Required | Persisted to | Notes |
|---|---|---|---|---|
| `referenceId` | — | — | `Portal Registry/ReferenceId` | Minted by the flow from Portal Sequence Counters. Must be unique and monotonic within its year. |
| `status` | — | — | `Portal Registry/Status` | Always `received` at creation. No other value may be set by this endpoint. |
| `verifiedSubmission` | — | — | `Portal Registry/VerifiedSubmission` | True when a proof was presented and consumed. |
| `sourceIp` | — | — | `Portal Registry/SourceIp` | From X-Forwarded-For. |
| `initial timeline event` | — | — | `Portal Status Timeline/AtUtc` | One row at creation, status `received`, actor `Registry`. |
| `ticket per attachment` | — | **yes** | `Portal Upload Tickets/Title` | Opaque single-use token. One row per declared attachment. |
| `ticket.submissionRef` | — | **yes** | `Portal Upload Tickets/SubmissionRef` |  |
| `ticket.storedName` | — | **yes** | `Portal Upload Tickets/StoredName` | Reserved at ticket creation, after the Universal Filename Policy. |
| `ticket.expiresAtUtc` | — | **yes** | `Portal Upload Tickets/ExpiresAtUtc` | Short-lived. An expired ticket must be refused. |
| `ticket.createdAtUtc` | — | **yes** | `Portal Upload Tickets/CreatedAtUtc` |  |
| `ticket.redeemed` | — | **yes** | `Portal Upload Tickets/Redeemed` | false at creation; UPLOAD sets it true. Single use. |
| `ticket.status` | — | **yes** | `Portal Upload Tickets/Status` | issued | redeemed | expired |
| `timeline.submissionRef` | — | **yes** | `Portal Status Timeline/SubmissionRef` |  |
| `timeline.actor` | — | **yes** | `Portal Status Timeline/Actor` | `Registry` for anything the flow writes. |

### Response

| Field | Type | Required | Persisted to | Notes |
|---|---|---|---|---|
| `referenceId` | string | **yes** | — | Without it the citizen has no way to track the submission. |
| `uploads[]` | array | when attachments were declared | — | One element per declared attachment. **An empty array when attachments were declared means the attachments are silently never sent.** |
| `uploads[].ticket` | string | **yes** | `Portal Upload Tickets/Title` |  |
| `uploads[].name` | string | **yes** | — |  |
| `error` | string | no | — | `verification_required`, with HTTP 403, when the address has not been verified. Any other 4xx/5xx queues the submission. |

### Status codes

| Code | Meaning |
|---|---|
| `200` | accepted |
| `403 + error=verification_required` | the wizard asks for a code |
| `other 4xx/5xx` | queued for retry |

---

## UPLOAD

Redeem one single-use ticket with the raw file.

`PUT` · raw bytes

### Request

| Field | Type | Required | Persisted to | Notes |
|---|---|---|---|---|
| `Content-Type` | header | **yes** | — | const `application/octet-stream` |
| `X-Upload-Ticket` | header | **yes** | `Portal Upload Tickets/Title` | The ticket returned by SUBMISSION. Single use: the flow must set Redeemed and refuse a second presentation. |
| `body` | bytes | **yes** | `Portal Attachments/AttachmentLink` | Raw, never base64. INT-002 requires the flow to verify size and SHA-256 against the ticket before storing. |

### Written by the flow

| Value | Type | Required | Persisted to | Notes |
|---|---|---|---|---|
| `submissionRef` | — | — | `Portal Attachments/SubmissionRef` |  |
| `declaredName` | — | — | `Portal Attachments/DeclaredName` |  |
| `storedName` | — | — | `Portal Attachments/StoredName` | After the Universal Filename Policy is applied by the flow, not the client. |
| `declaredSha256` | — | — | `Portal Attachments/DeclaredSha256` |  |
| `createdAtUtc` | — | — | `Portal Attachments/CreatedAtUtc` |  |
| `declaredSizeBytes` | — | **yes** | `Portal Attachments/DeclaredSizeBytes` | Compared to the received byte count before storing, per INT-002. |
| `status` | — | **yes** | `Portal Attachments/Status` | stored | refused |

### Response

| Field | Type | Required | Persisted to | Notes |
|---|---|---|---|---|
| `stored` | boolean | **yes** | — | **Must be truthful.** `200 {stored:false}` means the bytes were not filed. |
| `attachmentLink` | string | when stored | — |  |
| `reason` | string | when not stored | — | core.js upload() also accepts `error` as a fallback key for this value; a flow may send either, but `reason` is the name this contract specifies. |

### Status codes

| Code | Meaning |
|---|---|
| `200` | stored, or answered with stored:false |
| `401/403` | ticket refused |
| `4xx` | oversize or digest mismatch |

---

## SUPPORT

Raise a helpdesk case. Never enters the registry.

`POST` · json

### Request

| Field | Type | Required | Persisted to | Notes |
|---|---|---|---|---|
| `name` | string | **yes** | `Portal Support Cases/Name` |  |
| `email` | string | **yes** | `Portal Support Cases/Email` |  |
| `topic` | string | **yes** | `Portal Support Cases/Topic` | The topic's label, not an internal key. |
| `aboutReference` | string | no | `Portal Support Cases/AboutReference` | A tracking id, unverified. Empty string when blank. |
| `message` | string | **yes** | `Portal Support Cases/Message` |  |

### Written by the flow

| Value | Type | Required | Persisted to | Notes |
|---|---|---|---|---|
| `submittedAtUtc` | — | — | `Portal Support Cases/SubmittedAtUtc` |  |
| `status` | — | — | `Portal Support Cases/Status` | `open` at creation. |
| `sourceIp` | — | — | `Portal Support Cases/SourceIp` |  |
| `acknowledgement` | — | — | `Portal Outbox Receipts/MessageType` | One receipt row per acknowledgement sent. Never the message body. |

### Response

| Field | Type | Required | Persisted to | Notes |
|---|---|---|---|---|
| `caseRef` | string | **yes** | — | **The reference the citizen is given must be this one.** A locally generated reference exists in no system of record. |

### Status codes

| Code | Meaning |
|---|---|
| `200` | case created |
| `4xx/5xx` | queued |

---

## VERIFY

Mail a one-time code to an address.

`POST` · json

### Request

| Field | Type | Required | Persisted to | Notes |
|---|---|---|---|---|
| `email` | string | **yes** | `Portal OTP Codes/Email` | Stored lowercased and trimmed. VERIFY_CONFIRM matches on it. |

### Written by the flow

| Value | Type | Required | Persisted to | Notes |
|---|---|---|---|---|
| `code` | — | — | `Portal OTP Codes/OTP_Code` | Plaintext, per SC-003 — WDL has no bitwise operators, so no digest can be computed. Protected by lifecycle, not by storage. |
| `expiresAt` | — | — | `Portal OTP Codes/Expires_At` |  |
| `attempts` | — | — | `Portal OTP Codes/Attempts` | 0 at creation. The cap is enforced by VERIFY_CONFIRM. |
| `consumed` | — | — | `Portal OTP Codes/Consumed` | false at creation. |
| `outbox receipt` | — | — | `Portal Outbox Receipts/MessageType` | `verification-code`. Records that a code was sent, **never the code**. |
| `sourceIp` | — | **yes** | `Portal OTP Codes/SourceIp` |  |
| `createdAtUtc` | — | **yes** | `Portal OTP Codes/CreatedAtUtc` |  |

### Response

| Field | Type | Required | Persisted to | Notes |
|---|---|---|---|---|
| `sent` | boolean | **yes** | — | **Must be present and truthful.** The client tests `=== true`, so an absent field is read as failure and the citizen is told the code could not be sent. |
| `expiresAt` | iso8601 | **yes** | — |  |

### Status codes

| Code | Meaning |
|---|---|
| `200` | answered |
| `429` | rate limited |
| `other` | refused |

---

## VERIFY_CONFIRM

Exchange a code for a single-use proof.

`POST` · json

### Request

| Field | Type | Required | Persisted to | Notes |
|---|---|---|---|---|
| `email` | string | **yes** | — | Must match the address VERIFY was called with. The lookup is by email; the code is compared inside the flow. |
| `code` | string | **yes** | — | Six digits. **The flow must compare it to the stored code** — the lookup filter is not a comparison. |

### Written by the flow

| Value | Type | Required | Persisted to | Notes |
|---|---|---|---|---|
| `proof` | — | — | `Portal Verification Proofs/Title` | Opaque token. Two concatenated GUIDs with hyphens stripped. |
| `email` | — | — | `Portal Verification Proofs/Email` |  |
| `purpose` | — | — | `Portal Verification Proofs/Purpose` | `portal-verification`. |
| `expiresAtUtc` | — | — | `Portal Verification Proofs/ExpiresAtUtc` | 15 minutes. |
| `consumed` | — | — | `Portal Verification Proofs/Consumed` | false at mint; set true when SUBMISSION or STATUS redeems it. |
| `challenge consumed` | — | — | `Portal OTP Codes/Consumed` | Set true on a match, and on the fifth failed attempt. |
| `createdAtUtc` | — | **yes** | `Portal Verification Proofs/CreatedAtUtc` |  |

### Response

| Field | Type | Required | Persisted to | Notes |
|---|---|---|---|---|
| `verification` | string | **yes** | — | **The proof. Absent means the citizen is told a correct code was refused.** |
| `expiresAt` | iso8601 | **yes** | — |  |

### Status codes

| Code | Meaning |
|---|---|
| `200` | verified, proof returned |
| `4xx` | invalid or expired |

---

## STATUS

Read one submission back, in the public projection only.

`POST` · json

### Request

| Field | Type | Required | Persisted to | Notes |
|---|---|---|---|---|
| `referenceId` | string | **yes** | — | Trimmed and uppercased before matching. |
| `email` | string | when no proof is held | — | Structurally absent from the body when a proof is held — omitted, not blank. |
| `verification` | string | when a proof is held | — | Redeemed and consumed by this call. |

### Response

| Field | Type | Required | Persisted to | Notes |
|---|---|---|---|---|
| `record` | object | on a match | — | **Null or absent must be answered 404, not 200.** A 200 with no record renders as a successful match. |
| `record.referenceId` | string | **yes** | `Portal Registry/ReferenceId` |  |
| `record.status` | string | **yes** | `Portal Registry/Status` |  |
| `record.statusLabel` | string | no | `Portal Registry/StatusLabel` |  |
| `record.category` | string | no | `Portal Registry/Category` |  |
| `record.subject` | string | no | `Portal Registry/Subject` |  |
| `record.receivedAt` | iso8601 | no | `Portal Registry/SubmittedAtUtc` |  |
| `record.acknowledgedAt` | iso8601 | no | `Portal Registry/AcknowledgedAtUtc` |  |
| `record.updatedAt` | iso8601 | no | `Portal Registry/UpdatedAtUtc` |  |
| `record.closedAt` | iso8601 | no | `Portal Registry/ClosedAtUtc` |  |
| `record.actionRequired` | boolean | no | `Portal Registry/ActionRequired` |  |
| `record.timeline[]` | array | no | `Portal Status Timeline/AtUtc` |  |
| `record.timeline[].at` | iso8601 | **yes** | `Portal Status Timeline/AtUtc` |  |
| `record.timeline[].status` | string | **yes** | `Portal Status Timeline/Status` |  |
| `record.timeline[].label` | string | no | `Portal Status Timeline/Label` |  |
| `record.timeline[].note` | string | no | `Portal Status Timeline/Note` |  |

### Status codes

| Code | Meaning |
|---|---|
| `200` | record returned |
| `404` | **byte-identical denial** — the same response whether the reference does not exist or is not yours |
| `429` | rate limited |
| `403 + error=verification_required` | the email check is demanded |

### Never in the projection

- description
- attachments
- assigned officer
- handling unit
- internal notes
- SenderName, SenderPhone, SenderOrganisation and every other sender field
- SourceIp
- LocalId
- VerifiedSubmission

A field absent from this list is not thereby permitted — the projection is an allow-list.

---

## WRITEBACK

**IN SCOPE — specified, client built, flow deployed**
Specified by NITDA_Document_Portal_Functional_Documentation v3.0, INT-007.
Implemented by: document-portal/ — PF.intake.writeback() in core.js, called from track.js's respond/note/withdraw actions on a registry-sourced record. Per D12 the closure package, the build that used to carry submitWb/writeback, is discarded and is not what this was ported from — it is a clean implementation against this contract.
Configured by: config.example.js declares the WRITEBACK key as an empty slot, same as every other endpoint. No deployment has a live URL in it yet — the flow exists, but its trigger URL is a credential and is not recorded in this repository, so a deployment has to be given one by hand.

A verified citizen responds, adds a note, or withdraws. Appends to the timeline; only withdrawal changes status.

`POST` · json

### Request

| Field | Type | Required | Persisted to | Notes |
|---|---|---|---|---|
| `referenceId` | string | **yes** | — |  |
| `verification` | string | **yes** | — | A proof identifies the submitter. There is no other authentication. |
| `action` | string | **yes** | — | one of `respond`, `note`, `withdraw` |
| `body` | string | **yes** | — |  |

### Written by the flow

| Value | Type | Required | Persisted to | Notes |
|---|---|---|---|---|
| `timeline event` | — | — | `Portal Status Timeline/AtUtc` | Actor `Submitter`, not `Registry`. |
| `status change` | — | — | `Portal Registry/Status` | Only when action is `withdraw`. |
| `audit row` | — | — | `Portal Audit Events/EventType` | One row per call, unconditionally — see D13. Not a public-endpoint append D2 reserved against: WRITEBACK requires a consumed verification proof, so no anonymous caller reaches this write. |
| `audit.flow` | — | **yes** | `Portal Audit Events/Flow` |  |
| `audit.atUtc` | — | **yes** | `Portal Audit Events/AtUtc` |  |
| `audit.reference` | — | **yes** | `Portal Audit Events/Reference` |  |
| `audit.sourceIp` | — | **yes** | `Portal Audit Events/SourceIp` |  |
| `audit.detail` | — | **yes** | `Portal Audit Events/Detail` | The action taken and the body's length — never the body. |
| `timeline.submissionRef` | — | **yes** | `Portal Status Timeline/SubmissionRef` |  |
| `timeline.actor` | — | **yes** | `Portal Status Timeline/Actor` | `Submitter`. |
| `timeline.label` | — | **yes** | `Portal Status Timeline/Label` |  |
| `timeline.note` | — | **yes** | `Portal Status Timeline/Note` |  |

### Response

| Field | Type | Required | Persisted to | Notes |
|---|---|---|---|---|
| `ok` | boolean | **yes** | — |  |
| `queued` | boolean | no | — | The flow's own signal that it accepted the write but has not finished applying it — not a client-side retry queue. WRITEBACK is never queued in document-portal/'s outbox the way SUBMISSION and SUPPORT are: the proof is single-use, so a queued retry would carry one already spent or expired. A failed call is reported to the citizen immediately instead, and a fresh attempt mints a fresh proof. |
| `reason` | string | when not ok | — |  |

### Status codes

| Code | Meaning |
|---|---|
| `200` | accepted |
| `404` | byte-identical denial, matching STATUS's INT-006 — the reference is unknown or the proof does not own it |
| `429` | rate limited |
| `401/403` | the proof is missing, invalid, expired, or already consumed |
| `other 4xx/5xx` | refused |

---

## _RATE_LIMIT — applies to every endpoint

Specified by D8.

A fixed 60-minute window counter per endpoint per source. Read, then update or create, before the work.

### Written by the flow

| Value | Type | Required | Persisted to | Notes |
|---|---|---|---|---|
| `bucket key` | — | **yes** | `Portal Rate Limits/Title` | `<ENDPOINT>_IP:<X-Forwarded-For>`. A caller with no X-Forwarded-For buckets as `unknown`, which throttles harder, never softer. |
| `windowStartUtc` | — | **yes** | `Portal Rate Limits/WindowStartUtc` | Reset in place when the window has expired. Never create a second row for one bucket. |
| `requestCount` | — | **yes** | `Portal Rate Limits/RequestCount` | Incremented within the window, reset to 1 on rollover. |
| `updatedAtUtc` | — | **yes** | `Portal Rate Limits/UpdatedAtUtc` |  |

### Response

| Field | Type | Required | Persisted to | Notes |
|---|---|---|---|---|
| `429` | status | **yes** | — | Returned through the standard envelope, not by an early terminate — the refusal must still be recorded in telemetry. |

### Limits

| Endpoint | Per hour per source |
|---|---|
| VERIFY | 5 |
| VERIFY_CONFIRM | 10 |
| SUBMISSION | per D8 |
| UPLOAD | per D8 |
| SUPPORT | per D8 |
| STATUS | per D8 |
| WRITEBACK | per D8 |

---

## _TELEMETRY — applies to every endpoint

Specified by the house build standard.

One row per run, written inside the capture scope and never on the request path.

### Written by the flow

| Value | Type | Required | Persisted to | Notes |
|---|---|---|---|---|
| `flow` | — | **yes** | `Portal Flow Telemetry/Flow` |  |
| `title` | — | **yes** | `Portal Flow Telemetry/Title` | The flow's display name. |
| `runId` | — | **yes** | `Portal Flow Telemetry/RunId` |  |
| `startedAtUtc` | — | **yes** | `Portal Flow Telemetry/StartedAtUtc` |  |
| `completedAtUtc` | — | **yes** | `Portal Flow Telemetry/CompletedAtUtc` |  |
| `outcome` | — | **yes** | `Portal Flow Telemetry/Outcome` | Succeeded | Failed, from the final status code. |
| `durationMs` | — | **yes** | `Portal Flow Telemetry/DurationMs` |  |
| `errorMessage` | — | **yes** | `Portal Flow Telemetry/ErrorMessage` | The error array, stringified. Headers and query string are redacted before capture. |
| `runRecord` | — | **yes** | `Portal Flow Telemetry/RunRecordJson` | The whole Compose_Flow_Run_Record composition, stringified — the run's own record kept beside the summary fields, so a failure can be read back without the other seven being reverse-engineered into an explanation. Provisioned 2026-08-31; until then the generator stripped this write, because the designer refuses at SAVE time to store a flow naming a column the list lacks. |

---

## _SEQUENCE — applies to SUBMISSION

Specified by D6.

Mint a unique, monotonic reference per year. Replaces the last-three-digits-of-ticks scheme, which yields 1000 values a year and collides.

### Written by the flow

| Value | Type | Required | Persisted to | Notes |
|---|---|---|---|---|
| `year` | — | **yes** | `Portal Sequence Counters/Year` | One row per year per prefix. |
| `prefix` | — | **yes** | `Portal Sequence Counters/Prefix` | `NITDA`. |
| `currentSequence` | — | **yes** | `Portal Sequence Counters/CurrentSequence` | Incremented under the lock token. |
| `lockToken` | — | **yes** | `Portal Sequence Counters/LockToken` | Written, then re-read to confirm ownership before incrementing. Two flows racing must not both mint. |
| `modifiedByFlowRun` | — | **yes** | `Portal Sequence Counters/ModifiedByFlowRun` | The run id that last held the lock. |
| `lastIssuedAt` | — | **yes** | `Portal Sequence Counters/LastIssuedAt` |  |
| `lastReferenceId` | — | **yes** | `Portal Sequence Counters/LastReferenceId` | The reference actually issued, for reconciliation. |

---

## _OUTBOX — applies to VERIFY, SUPPORT

Specified by D2.

A receipt that a message was sent. Never the message body, and never a one-time code.

### Written by the flow

| Value | Type | Required | Persisted to | Notes |
|---|---|---|---|---|
| `title` | — | **yes** | `Portal Outbox Receipts/Title` |  |
| `messageType` | — | **yes** | `Portal Outbox Receipts/MessageType` | `verification-code` | `support-acknowledgement` |
| `recipientEmail` | — | **yes** | `Portal Outbox Receipts/RecipientEmail` |  |
| `reference` | — | **yes** | `Portal Outbox Receipts/Reference` | Empty for a verification code, which has none. |
| `sentAtUtc` | — | **yes** | `Portal Outbox Receipts/SentAtUtc` |  |
| `status` | — | **yes** | `Portal Outbox Receipts/Status` | sent | failed |
| `attempts` | — | **yes** | `Portal Outbox Receipts/Attempts` |  |
| `lastError` | — | **yes** | `Portal Outbox Receipts/LastError` |  |

---

## Cross-cutting

- **rateLimit** — Every endpoint reads, updates and creates a bucket in Portal Rate Limits keyed `<ENDPOINT>_IP:<X-Forwarded-For>`. 60-minute fixed window. See D8 for the per-endpoint limits.
- **telemetry** — Every endpoint writes one row to Portal Flow Telemetry inside its capture scope, never on the request path.
- **envelope** — Every response is the standard envelope, and the HTTP status is the envelope's own status code. A branch that sets a failure code must have that code survive to the response.
- **cors** — Access-Control-Allow-Origin must name the real portal origin. It is currently the literal `https://your-host` on every endpoint.
- **filenamePolicy** — The Universal Filename Policy is applied by the UPLOAD flow, not the client. document-portal/ performs no filename normalisation.

