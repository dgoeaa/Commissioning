# Implementation walkthrough — every open item, in execution order

> **This document carries no commands.** The steps for portal and endpoint commissioning live in
> [`PORTAL-TENANT-RUNBOOK.md`](./PORTAL-TENANT-RUNBOOK.md), which is the only document that carries them.
> This document is status and traceability for the portal domain — restates no step. If you are here to execute, go there.
>
> The command-line commissioning path is
> [`CLEAR-THE-LAST-BLOCKER.md`](./CLEAR-THE-LAST-BLOCKER.md) — on a phone,
> [`CLEAR-THE-LAST-BLOCKER-TERMUX.md`](./CLEAR-THE-LAST-BLOCKER-TERMUX.md).

Generated by `npm run walkthrough` from the readiness register, the tenant endpoint register,
the index targets, the notification matrix and the SharePoint list index. Do not edit by hand —
`npm run test:walkthrough` fails on drift.

**22 open items.** 48 in the register, 26 closed.
Every value below is read from the estate. There are no placeholders: where a value is not yet
known to this repository, the step names the party who holds it rather than inventing one.

---

## 1. Environment

**Purpose.** A working tree that can run every gate.

**Prerequisites.** Node 22 or later, npm 10 or later, and git. Verified on Linux x86_64 with
Node v22.22.2 and npm 10.9.7.

**Steps**

```bash
git clone https://github.com/dgoeaa/ECM_DOCS_DEV.git
cd ECM_DOCS_DEV
git checkout claude/system-remediation-gaps-ahpmsy
npm install
```

`npm install` is required only for `npm run test:smoke`, which needs Playwright. Every other
gate runs with no dependencies installed.

**You must work from a git clone, not an unpacked archive.** 15 scripts and suites read
`git ls-files`. Without `.git`, `npm run test:node` aborts at its second stage, and
`npm run commission` reports a third blocker that is the absence of git rather than a secret, and
states that rotation could not be verified. `npm run export` produces a clone for this reason;
an external party was handed an unpacked archive on 2026-09-15 and hit all three.

**Verification**

```bash
npm run test:node      # expect: exit 0, all 138 stages green
npm run readiness      # expect: 10 passed, 0 failed
npm run commission     # expect: NOT CLEARED, 2 blockers (CFG-1, CFG-2)
```

**If verification fails.** A `test:node` failure on a clean clone is a defect in the tree, not
in your environment; capture the failing stage name and its output before going further.

**Completion criteria.** `test:node` exit 0, `readiness` 10/10, and `commission` reporting
exactly the two configuration blockers.

---

## 2. Tooling for the automated path

**Purpose.** 10 console scripts and 2 recorded artifacts replace the manual passes that dominate this work.

**Constraint this obeys: no PowerShell, no Entra app registration, no tenant admin.** Every tool
below runs either as Node in this repository or from a devtools console using the session you are
already signed into. The browser provisioner states the reasoning the estate settled on: the
PowerShell runner "needs PnP.PowerShell installed and, since PnP 2.x, an Entra app registration to
sign in interactively. This file needs neither." The step itself is
**§2 of [`PORTAL-TENANT-RUNBOOK.md`](./PORTAL-TENANT-RUNBOOK.md)**.

**Prerequisites**

| Tool | Needs | Install |
|---|---|---|
| `prove:tenant` | Node 22 | already present |
| the console pastes | a signed-in browser | none |
| `test:smoke` (MANUAL-3) | Playwright Chromium | `npx playwright install chromium` |

**Permissions.** Flow work uses the portal's own user-scoped API, so you must OWN or CO-OWN each
flow; a flow owned solely by a colleague is invisible until they add you as co-owner. SharePoint
work needs **Manage Lists** on each site — Site Owner is enough. **Tenant admin is not needed
anywhere**, and no admin API is called.

**Every console artifact of this domain, in the order the runbook runs them**

Nothing in this table is typed: the artifacts come from `docs/reference/document-ownership.json`,
the section from the owning runbook's own headings, the verdict from
`PRODUCTION_READINESS_REGISTER.json`. The governance domain has had this view since
`governance/GOVERNANCE-STATUS.md`; the portal domain's equivalent was a join three files wide
that each reader had to make by hand.

| Artifact | Runbook section | What the register says | Replaces |
|---|---|---|---|
| `harvest-flow-metadata.browser.js` | §0.6 — Capture what is inside each flow, for the audit record | no item names it | reading 39 flow definitions one designer tab at a time |
| `provision-sharepoint-fields.browser.js` | §2.2 — Provision columns | `ITEM-23` APPLIED_UNVERIFIED · `ITEM-39` RESOLVED | a PnP PowerShell session and its app registration |
| `preflight-internal-flows.browser.js` | §2.4 — Preflight | `ITEM-46` RESOLVED | — |
| `harvest-trigger-urls.browser.js` | §3.2 — Get the worklist | no item names it | opening 20 flows and copying 25 URLs by hand |
| `acceptance-internal-endpoints.browser.js` | §6.3 — The browser acceptance run | no item names it | — |
| `diagnose-deployed-config.browser.js` | §7.4 — Confirm the deployed config is served | no item names it | — |
| `verify-deployed-package.browser.js` | §8.1 — Verify the deployed package is the package that was built | no item names it | — |
| `apply-otp-verify-patch.browser.js` | §9.0 — The OTP verify patch, and the cutover it belongs to | `ITEM-38` RESOLVED | — |
| `otp-cutover-tests.browser.js` | §9.0 — The OTP verify patch, and the cutover it belongs to | no item names it | — |
| `set-content-approval.browser.js` | §10 — Content approval — item 6.6 | `ITEM-41` DECISION_REQUIRED | — |

**Read that third column exactly.** It is the status of the items an artifact serves — never
proof the artifact was run. `APPLIED_UNVERIFIED` is the register's own term for *executed
against the tenant, with a run record naming the evidence*, and deliberately not the same as
resolved. `no item names it` is what a verifier looks like: it closes no item, it tells you
whether one closed.

**Artifacts recorded rather than owned**

| Artifact | Instructed by | What the register says |
|---|---|---|
| `scripts/harvest-trigger-urls.bookmarklet.txt` | [`CLEAR-THE-LAST-BLOCKER-TERMUX.md`](./CLEAR-THE-LAST-BLOCKER-TERMUX.md) | `CFG-1` DISCHARGED_UNTRACKABLE · `CFG-2` DISCHARGED_UNTRACKABLE |
| `docs/deployment/internal/flows/console-apply/*.console-apply.js` | **nothing instructs it** | `ITEM-48` SPECIFIED_NOT_APPLIED · `ITEM-50` SPECIFIED_NOT_APPLIED · `ITEM-51` SPECIFIED_NOT_APPLIED |

`npm run prove:tenant` is Node in this repository rather than a console artifact. It replaces
six separate manual round-trips, for ITEM-11, ITEM-12, ITEM-6, ITEM-44 and ITEM-56.

Regenerate the generated sets when the register changes:

```bash
npm run harvest:console      # rebuilds the console harvester from the endpoint register
npm run harvest:bookmarklet  # rebuilds the phone harvester from the same register
npm run console:apply        # rebuilds one console snippet per flow package
```

**Every console script reports before it changes anything.** Each opens with `DRY_RUN = true`;
you read the report, set it to false, and paste again. All are safe to run twice.

**Verification**

```bash
npm run test:harvestconsole      # the harvester matches the endpoint register
npm run test:harvestbookmarklet  # the phone harvester matches it, and still behaves
npm run test:consoleapply        # the snippets match their packages
```

**If verification fails.** A stale message means the register or a package changed — regenerate
with the command it names. In a console, "No Flow access token in this page" means the tab has
not acquired one: open a flow you own at `https://make.powerautomate.com` and paste again on
that tab.

**Completion criteria.** Both checks green, and each console script's dry run printing its target
list without error.

---

## 3. The endpoint register — the authority for every URL

`docs/reference/endpoint-register.json` is the post-rotation tenant export. Where it and any
other artifact disagree, it is correct. It names, for all 25 contract keys, the workflow each
calls and that workflow's complete trigger URL with only the signature removed.

| Key | Workflow id | Flow |
|---|---|---|
| `DGO_ENDPOINT_FETCH_ACTIVITIES` | `c4c26f93ba1e4d7db5247536c30cdc11` | IP_Get_Docs_Endpoint |
| `DGO_ENDPOINT_SUBSIDIARY_ACTIONS` | `d5e4b3da41e34819b3f953d2acbc2dd7` | IP_Dynamic_Global_Actions_Endpoint |
| `DGO_ENDPOINT_FETCH_ALL` | `aa662769f13a4666bfadf3039cd8d247` | FETCH_ALL |
| `DGO_ENDPOINT_REFERENCE_DATA` | `885fad5ec91f460490e8c772d5d10cb8` | IP_Reference_Data_Endpoint |
| `DGO_ENDPOINT_GET_DOCS` | `c4c26f93ba1e4d7db5247536c30cdc11` | IP_Get_Docs_Endpoint |
| `DGO_ENDPOINT_FETCH_EMAIL_ATTACHMENTS` | `d2c773332f6f4fbf97ee4a5baa01f70b` | IP_Retrieve_Email_Attachment_Endpoint |
| `DGO_ENDPOINT_SINGLE_ASSIGNMENT` | `72acfc6a694f414f9fe39c939ead7f08` | Single_Task_Assignment |
| `DGO_ENDPOINT_BULK_ASSIGNMENT` | `a705e453f922457c95ca19f32731e4d7` | BULK OPS DATA RETRIEVAL HTTP |
| `DGO_ENDPOINT_BULK_ASSIGNMENT_DIRECT` | `8f6a40a682bb4e79acd2ac5d42f15705` | IP_Bulk_Assign_Endpoint |
| `DGO_ENDPOINT_DYNAMIC_ACTIONS` | `d5e4b3da41e34819b3f953d2acbc2dd7` | IP_Dynamic_Global_Actions_Endpoint |
| `DGO_ENDPOINT_EMAIL` | `378b491ec39a4beca886ddec250e6961` | IP_SEND_EMAIL |
| `DGO_ENDPOINT_EMAIL_RELATED_TASK` | `a942d230337c4ddfa9a386e92bbd048b` | IP_Create_Email_Assignment_Endpoint |
| `DGO_ENDPOINT_AI_EMAIL_ANALYSIS` | `3f018fea1031490fb73dff6a8d6341f2` | AI_Document_Processing |
| `DGO_ENDPOINT_AI_DOC_ANALYSIS` | `3f018fea1031490fb73dff6a8d6341f2` | AI_Document_Processing |
| `DGO_ENDPOINT_AI_CHAT` | `3f018fea1031490fb73dff6a8d6341f2` | AI_Document_Processing |
| `DGO_ENDPOINT_OTP_GENERATE` | `b372d45e4b2a47d88b8e8b032da67fcd` | IP_OTP_VERIFY |
| `DGO_ENDPOINT_OTP_VERIFY` | `b372d45e4b2a47d88b8e8b032da67fcd` | IP_OTP_VERIFY |
| `DGO_ENDPOINT_SCAN_INTAKE` | `88401ce1f44148539c0da53c9491f8e6` | IP_SCAN_INTAKE |
| `PF_ENDPOINT_SUBMISSION` | `1041ed37ce924e3c886d891f23e8142c` | CG_Submission_Endpoint |
| `PF_ENDPOINT_UPLOAD` | `62fe121e5a95416bb91275e43dd0e37e` | CG_Upload_Endpoint |
| `PF_ENDPOINT_SUPPORT` | `052013da80724713a4285edee72ccb4a` | CG_Portal_Support_Endpoint |
| `PF_ENDPOINT_VERIFY` | `c0d58004c53348539d0d5aeaf49dcded` | CG_Portal_Verification_Endpoint |
| `PF_ENDPOINT_VERIFY_CONFIRM` | `b270894ff410499ebbc090e32ffe899b` | CG_Portal_Verification_Confirmation_Endpoint |
| `PF_ENDPOINT_STATUS` | `34dd28b4a5664927b66e581c74a0ab94` | CG_Portal_Status_Check_Endpoint |
| `PF_ENDPOINT_WRITEBACK` | `abf3a3ca53e64ba58c9ce5933e4e97e3` | CG_Portal_Writeback_Endpoint |

**The signature is the one thing it may never carry.** A `sig=` value is a bearer credential:
43 base64url characters, an HMAC-SHA256, and possession alone authorises invoking the flow.
Never write one into any file except `config.local.js` and `~/dgo-values.txt`, both of which
are git-ignored. Never into a commit, a comment, an issue, a chat, or a log.

---

## 4. ITEM-2 — The tenant write path has never been executed

**Status** BLOCKED  ·  **Executed by** operator

### Purpose and expected outcome

scripts/update-flow-definition.ps1 PATCHes a flow definition back to the tenant, which is the same call the designer makes on Save. It has never been run. The patch track is no longer the only delivery route: PowerShell was dropped from the automation on 2026-09-08 because PnP.PowerShell needs an Entra app registration the estate had already ruled out, and the paste track — docs/deployment/internal/flows/console-apply/, one snippet per flow package, generated by npm run console:apply — needs no PowerShell, no app registration and no tenant admin. What is still unproven is that ANY write reaches the tenant from outside the designer, which is what this item is for; prove it by whichever route you intend to deliver by.

**Expected outcome.** One flow carries a change that was applied from outside the Power Automate designer, and a fresh export of that flow contains it. The dry run is the mandatory first pass and is not on its own sufficient: it reads and reports, and this item exists to prove that a WRITE reaches the tenant.

### Prerequisites and dependencies

- None beyond the environment in §1 and, for anything that transmits, CFG-1 and CFG-2.

**Constraints**

- The dry run must be used first; it sends nothing.

### Execution steps

1. Apply one flow by the route you intend to use. Console route: open make.powerautomate.com, open any flow you own so the page holds a token, then paste one file from docs/deployment/internal/flows/console-apply/ into the browser devtools console — it carries DRY_RUN = true and changes nothing on the first paste. PowerShell route: the dry run documented in docs/deployment/sharepoint/remediation/patched/README.md. (Corrected 2026-09-15: this step cited remediation/patched/README.md, a path that does not exist in this repository — the file is under docs/deployment/sharepoint/. The evidence entry on this item already carried the full path; only the step text was wrong, so an operator following the step hit a dead end while the citation beside it resolved.)
2. Confirm it reports the flow name and action count, and that a fresh export carries the change.
3. Record the result; if it fails, raise it before any delivery-track item proceeds.

**Execution detail**

**Automated path — generated, and not yet owned**

A console snippet for this package exists:
`docs/deployment/internal/flows/console-apply/DGO_FETCH_ALL.console-apply.js`, from
`npm run console:apply`. Left at `DRY_RUN = true` it reads the flow over the same API a write
would use and reports what it would change without writing — reaching the definition and
reporting a diff **is** the write path proven, the only step it does not take being the PATCH.

**No runbook carries the step that applies it, so this document does not either.**
`docs/reference/document-ownership.json` records all 16 snippets as unowned, with the condition
that would give them an owner.

### Verification

Two artefacts, recorded against the flow they were taken on: the dry-run console output, and a fresh export of the same flow showing the applied change. Without the second, nothing distinguishes a reachable API from a successful write.

### If verification fails

- If the script does not work, the patch track is unavailable and every patch-track item (3, 4, 6, 11) must be re-planned onto the paste track.
- Never mark the item closed on an expectation. `APPLIED_UNVERIFIED` is an open status: an
  action performed without its effect observed is not complete.

### Completion criteria

One flow carries a change that was applied from outside the Power Automate designer, and a fresh export of that flow contains it. The dry run is the mandatory first pass and is not on its own sufficient: it reads and reports, and this item exists to prove that a WRITE reaches the tenant.

---

## 5. ITEM-23 — Five index targets unset on hot-path lists

**Status** APPLIED_UNVERIFIED  ·  **Executed by** operator

### Purpose and expected outcome

Portal Rate Limits is filtered on every request to every endpoint; Portal Verification Proofs on every proof presentation; Portal Support Cases on every case; Global Tracking Queue.RefIDD by every assignment (15,804 items at capture); DGO DIGITAL OPS.RefIDD by the flag, status-transition and document-read paths (21,249 items at capture).

**Expected outcome.** The provisioner's index pass reports 5 already indexed. A single assignment against a known reference returns 200; a flag write against a known reference returns 200.

### Prerequisites and dependencies

- None beyond the environment in §1 and, for anything that transmits, CFG-1 and CFG-2.

**Constraints**

- SharePoint's 5,000-item view threshold makes an unindexed equality filter FAIL, not merely slow.

### Execution steps

1. Run §2 of docs/deployment/PORTAL-TENANT-RUNBOOK.md against the sites.
2. Confirm the index pass reports 5 already indexed on a second run.

**Execution detail**

**The nine columns to index**

| List | Column | Items at capture | Site |
|---|---|---|---|
| Portal Rate Limits | `Title` | 0 | `Global_Digital_Documents_Centre` |
| Portal Verification Proofs | `Title` | 0 | `Global_Digital_Documents_Centre` |
| Portal Support Cases | `Title` | 0 | `Global_Digital_Documents_Centre` |
| Global Tracking Queue | `RefIDD` | 15,804 | `NITDADGO-EAAACTIVITYTRACKING` |
| DGO DIGITAL OPS | `RefIDD` | 21,249 | `NITDADGO-EAAACTIVITYTRACKING` |
| Global Tracking Queue | `DueDate` | 15,804 | `NITDADGO-EAAACTIVITYTRACKING` |
| Global Tracking Queue | `Acknowledgement_x0020_Due_x0020_` | 15,804 | `NITDADGO-EAAACTIVITYTRACKING` |
| Global Tracking Queue | `Modified` | 15,804 | `NITDADGO-EAAACTIVITYTRACKING` |
| DGO DIGITAL OPS | `Modified` | 21,249 | `NITDADGO-EAAACTIVITYTRACKING` |

**Sites**

| Key | URL | Lists |
|---|---|---|
| `DGO_ECM_GOVERNANCE` | https://nitdanigeria.sharepoint.com/sites/DGO_ECM_GOVERNANCE | 31 |
| `DRC` | https://nitdanigeria.sharepoint.com/sites/DRC | 33 |
| `Global_Digital_Documents_Centre` | https://nitdanigeria.sharepoint.com/sites/Global_Digital_Documents_Centre | 46 |
| `NEDMS` | https://nitdanigeria.sharepoint.com/sites/NEDMS | 25 |
| `NITDADGO-EAAACTIVITYTRACKING` | https://nitdanigeria.sharepoint.com/sites/NITDADGO-EAAACTIVITYTRACKING | 191 |

**Automated path — a console run, no PowerShell.** Provisioning the portal columns is
**§2 of [`PORTAL-TENANT-RUNBOOK.md`](./PORTAL-TENANT-RUNBOOK.md)**, which is the only document
that carries the step. The lists and columns it covers are below.

It creates missing columns first, then makes a second pass that sets the indexes, reading each
field first and skipping any already indexed — so it is safe to run twice, and **the second run
is the verification**: the index pass must report every target already indexed.

You need **Manage Lists** on each site; Site Owner is enough and tenant admin is not used. This
file exists precisely so the PnP PowerShell path and its Entra app registration are not needed.

**A refusal is not a pass.** SharePoint can decline to index a column on a list already past
5,000 items, and two of these lists were at 15,804 and 21,249 at capture. The script reports the
refusal and exits non-zero. Indexing is necessary and not sufficient: an indexed clause still has
to return under 5,000 rows, which is why DGO_SCHEDULED_SWEEP asks for a 30-day window.

**If a target is refused**, the run says so rather than passing. Do not record ITEM-23 as closed
while any target is red.

### Verification

Re-run the provisioner and read the index pass count; then exercise one assignment and one flag write end to end.

### If verification fails

- This is the only pending provisioning item that breaks behaviour today rather than failing closed: a known reference answers 404 and a single assignment against a known reference does not return 200.
- Never mark the item closed on an expectation. `APPLIED_UNVERIFIED` is an open status: an
  action performed without its effect observed is not complete.

### Completion criteria

The provisioner's index pass reports 5 already indexed. A single assignment against a known reference returns 200; a flag write against a known reference returns 200.

---

## 6. ITEM-30 — Portal Flow Telemetry.RunRecordJson is not provisioned, so no run record is retained

**Status** APPLIED_UNVERIFIED  ·  **Executed by** operator + author

### Purpose and expected outcome

Every generated package composes Compose_Flow_Run_Record. Nothing writes it, because the designer validates item/<Column> against the list at save time and would refuse the flow outright with WorkflowOperationParametersExtraParameter. The generator strips any write naming a column the spec marks PENDING, so the packages save and the compose wires itself the moment the column exists.

**Expected outcome.** A telemetry row carries a run record.

### Prerequisites and dependencies

- Same provisioner run as ITEM-23 and ITEM-8.

**Constraints**

- The order matters: provision the column, THEN flip PENDING, THEN rebuild. Flipping first produces packages that cannot be saved.

### Execution steps

1. Run §2 of docs/deployment/PORTAL-TENANT-RUNBOOK.md.
2. Flip RunRecordJson out of PENDING in portal-field-spec.json.
3. Run npm run build:internalpaste && npm run build:designerpaste.
4. Confirm the write reappears in the packages, then re-paste.

### Verification

npm run designerpaste after the rebuild shows the item/RunRecordJson write present; then one live run produces a Portal Flow Telemetry row carrying it.

### If verification fails

- Low. Nothing breaks while it is absent; only run-record retention is lost.
- Never mark the item closed on an expectation. `APPLIED_UNVERIFIED` is an open status: an
  action performed without its effect observed is not complete.

### Completion criteria

A telemetry row carries a run record.

---

## 7. ITEM-43 — The portal still calls the defective OTP flow — the new one is deployed but not in service

**Status** READY  ·  **Executed by** operator

### Purpose and expected outcome

IP_OTP_VERIFY (c5e314c7-68b5-4bdc-8935-0954ef6a256d) was built on 2026-09-04 and carries the hardening. It is a NEW flow, so it has a NEW trigger URL. config/endpoints.config.js resolves OTP_VERIFY from the deployed configuration, which still names 3e201620-f1e8-4c17-a90a-4d95b94a24c2 — the flow that carries all five defects and is still running. Until the configuration is repointed and the old flow disabled, the hardening is deployed and unused: a code issued to one person still verifies anyone, and the agency's Office 365 connection is still an open relay, for anyone holding the old URL.

**Expected outcome.** The deployed configuration resolves OTP_VERIFY to the flow the tenant register names (b372d45e4b2a47d88b8e8b032da67fcd, serving OTP_GENERATE and OTP_VERIFY), the OTP tests pass against it, and no configuration resolves OTP_VERIFY to a flow absent from the register. Re-based: the register contains neither c5e314c7-68b5-4bdc-8935-0954ef6a256d nor the defective 3e201620-f1e8-4c17-a90a-4d95b94a24c2, so the original criteria cannot be evaluated.

### Prerequisites and dependencies

- ITEM-38 — the new flow exists and is verified. Met 2026-09-04.

**Constraints**

- The new trigger URL carries a sig= bearer token. It goes into the deployed configuration and nowhere else — not into this repository, a ticket, an email or a chat message.

### Execution steps

1. Put the new flow's trigger URL into the deployed configuration as OTP_VERIFY.
2. Exercise the four tests in BUILD.md against it: verify as another caller must fail; verify as the right caller must succeed; verify with no valid code must send no mail; six wrong guesses must return 429.
3. Turn off 3e201620-f1e8-4c17-a90a-4d95b94a24c2. Not delete — off, so it can be re-enabled if step 2 finds something.
4. Re-run npm run test:triggerauth and npm run wiring, and record the outcome as an evidence file.

**Execution detail**

**The flow the register names**

| Key | Workflow id | Flow |
|---|---|---|
| `DGO_ENDPOINT_OTP_VERIFY` | `b372d45e4b2a47d88b8e8b032da67fcd` | IP_OTP_VERIFY |

`DGO_ENDPOINT_OTP_GENERATE` resolves to the same workflow. The deployed configuration must
resolve OTP_VERIFY to this id and to no other.

```bash
npm run reconcile -- --check
npm run test:otpdeployed
npm run test:otpprotocol
```

### Verification

The four live calls in step 2, and a tenant read showing the old flow stopped.

### If verification fails

- While both flows run, the defective one is the one being called. Disabling it is the step that ends the exposure; repointing alone leaves it reachable by anyone who kept the old URL.
- Never mark the item closed on an expectation. `APPLIED_UNVERIFIED` is an open status: an
  action performed without its effect observed is not complete.

### Completion criteria

The deployed configuration resolves OTP_VERIFY to the flow the tenant register names (b372d45e4b2a47d88b8e8b032da67fcd, serving OTP_GENERATE and OTP_VERIFY), the OTP tests pass against it, and no configuration resolves OTP_VERIFY to a flow absent from the register. Re-based: the register contains neither c5e314c7-68b5-4bdc-8935-0954ef6a256d nor the defective 3e201620-f1e8-4c17-a90a-4d95b94a24c2, so the original criteria cannot be evaluated.

---

## 8. ITEM-56 — The OTP allow-list row is set — applied by the operator, not verified here

**Status** APPLIED_UNVERIFIED  ·  **Executed by** operator

### Purpose and expected outcome

Both OTP flows now resolve Access-Control-Allow-Origin from Select_Allowed_Origins_Otp, which reads ConfigValue items from a SharePoint configuration list via Get_Allowed_Origins_Otp. This is the right design — the origins are tenant-configured rather than compiled in — but it moves the value out of the flow and into data. If that list does not contain the portal's deployed origin, Compose_Allowed_Origin_Otp falls back to the first entry, or to an empty string when the list is empty, and the browser rejects the response exactly as it did under the placeholder. Nothing in this repository can read that list.

**Expected outcome.** A live OTP verify call from the portal returns Access-Control-Allow-Origin equal to the portal's origin.

### Prerequisites and dependencies

- The portal's deployed origin must be known
- ITEM-55 (the flow must read the allow-list at all) — met
- ITEM-2 — the resolution criterion is a live OTP verify call, and whyOpen states ITEM-2 remains the first such proof. Recorded here 2026-09-15 for the same reason as ITEM-12: stated in prose, absent from this field, so it read as released.

**Constraints**

- An empty or wrong allow-list fails identically to the placeholder, and just as silently, because no live OTP call has been made

### Execution steps

1. Open the configuration list the OTP flows read via Get_Allowed_Origins_Otp.
2. Confirm a ConfigValue row carries the portal's deployed origin, exactly as the browser sends it in the Origin header — scheme and host, no trailing slash.
3. Make the first live OTP call (ITEM-43 / R-3) and confirm the response carries that origin back.

**Execution detail**

**Automated path**

```bash
npm run prove:tenant -- --only ITEM-56
```

Sends a request carrying `Origin: https://activityweb.page.gd` and asserts that
`Access-Control-Allow-Origin` comes back equal to it. This proof is read-only.

### Verification

the first live OTP call — no repository gate can establish this

### If verification fails

- Cutting over (ITEM-43) with the portal origin absent from the list produces the same CORS failure ITEM-55 described, from a different cause
- Never mark the item closed on an expectation. `APPLIED_UNVERIFIED` is an open status: an
  action performed without its effect observed is not complete.

### Completion criteria

A live OTP verify call from the portal returns Access-Control-Allow-Origin equal to the portal's origin.

---

## 9. ITEM-44 — The client spoke a protocol neither OTP flow reads — reconciled on the client side

**Status** APPLIED_UNVERIFIED  ·  **Executed by** operator (the live proof); the direction is settled

### Purpose and expected outcome

core/data-client.js sends action "otpGenerate"/"otpVerify" from config/endpoints.config.js and nests everything under payload; both OTP flows switch on action "generate"/"verify" and read identifier and otp_code at the top level. Neither the dispatch value nor the field placement matches, so the Switch matches no case and neither OTP path runs. This is not caused by the ITEM-43 cutover — the old flow has the same case values — and is consistent with ITEM-2: nothing has ever exercised it. It fails loudly: varStatusCode starts at 500 and the Response returns it, so an unmatched request answers 500 rather than a misleading 200. Two further findings sit alongside it: Case_Generate mails the code to the literal dgsregistry@nitda.gov.ng rather than to the requester, and writes item/Title from identifier with no coalesce while the verify lookup binds on Title through a four-way one — so a caller sending userEmail writes an empty Title that verify can never match.

**Expected outcome.** One request from the platform reaches a matched Switch case, and one code issued through the platform verifies through the platform. Until both are observed against the tenant, this is unresolved regardless of what the code reads like.

### Prerequisites and dependencies

- None beyond the environment in §1 and, for anything that transmits, CFG-1 and CFG-2.


### Execution steps

1. Done — the client sends action generate/verify with identifier and otp_code at the top level.
2. Done — npm run test:otpprotocol asserts the seam against the deployed definitions.
3. Outstanding — the recipient decision: Case_Generate mails the code to the literal dgsregistry@nitda.gov.ng rather than to the requester. That is a flow change and an agency call.
4. Outstanding — run the four cutover tests through the platform (ITEM-43).

**Execution detail**

**Automated path**

```bash
npm run prove:tenant -- --include-writes --only ITEM-44
```

Requests a code through OTP_GENERATE. Take the code delivered to that mailbox and verify it
through OTP_VERIFY in the same session. A 200 from generate alone does not meet the criterion.

### Verification

Read from the tenant, not from the repository: a run history entry on IP_OTP_VERIFY showing Case_Verify entered, and an OTP_Transactions row whose Title equals the identity the platform sent.

### If verification fails

- Do not advance the item. Record the observed output and the command that produced it, and refer it to the party named above.
- Never mark the item closed on an expectation. `APPLIED_UNVERIFIED` is an open status: an
  action performed without its effect observed is not complete.

### Completion criteria

One request from the platform reaches a matched Switch case, and one code issued through the platform verifies through the platform. Until both are observed against the tenant, this is unresolved regardless of what the code reads like.

---

## 10. ITEM-11 — An unknown reference is answered 200 and rendered as found

**Status** APPLIED_UNVERIFIED  ·  **Executed by** operator

### Purpose and expected outcome

The denial path fails open: any reference reads as a match, and the uniform denial that stops a stranger distinguishing 'no such reference' from 'not yours' never fires. Now specified — the filter matches the indexed ReferenceId and the owning SenderEmail in one query, so a miss and a mismatch are the same empty result and answer the same 404.

**Expected outcome.** An unknown reference returns 404, a wrong-email pairing returns a materially identical 404, and track.js shows the denial.

### Prerequisites and dependencies

- ITEM-2 (the patch route must be proven) or the paste route via ITEM-35.
- ITEM-23 — the filter reads the indexed ReferenceId.

**Constraints**

- INT-006 requires a byte-identical 404 for a miss and a mismatch.

### Execution steps

1. Apply the artifact (patch or paste).
2. Test an unknown reference.
3. Test a valid reference with the wrong email.
4. Compare the two responses byte for byte.

**Execution detail**

**Automated path**

```bash
npm run prove:tenant -- --only ITEM-11
```

Calls STATUS with a reference that cannot exist and asserts HTTP 404. A 200 here is the defect
this item records. Evidence is written to `docs/deployment/evidence/`.

### Verification

Two live requests, responses compared byte for byte.

### If verification fails

- Until applied, a stranger can confirm whether a reference exists.
- Never mark the item closed on an expectation. `APPLIED_UNVERIFIED` is an open status: an
  action performed without its effect observed is not complete.

### Completion criteria

An unknown reference returns 404, a wrong-email pairing returns a materially identical 404, and track.js shows the denial.

---

## 11. ITEM-12 — A submission reports delivered while its attachments are never sent

**Status** APPLIED_UNVERIFIED  ·  **Executed by** operator

### Purpose and expected outcome

The original trigger expected one inline base64 file; the client sends an attachments[] array with no bytes. Both halves are now specified: field names reconciled, and cardinality carried by the two-phase upload — SUBMISSION issues one ticket per declared attachment and returns uploads[]; UPLOAD redeems a ticket, writes the bytes and creates the Portal Attachments row.

**Expected outcome.** A submission with an attachment produces a row in Portal Attachments.

### Prerequisites and dependencies

- ITEM-35 (SUBMISSION and UPLOAD targets are among the five resolved) — met.
- ITEM-2 — whyOpen states it outright: 'Verification is blocked on ITEM-2 and on nothing else.' Recorded here 2026-09-15 because it was stated in prose and not in this field, so every instrument reading dependencies structurally read this item as released when it is not.


### Execution steps

1. Paste Portal_SUBMISSION_ECM_DOCS onto de9ef13b.
2. Paste Portal_UPLOAD_ECM_DOCS onto df7ddff1.
3. Submit with an attachment end to end.

**Execution detail**

**Automated path**

```bash
npm run prove:tenant -- --include-writes --only ITEM-12
```

Submits one marked record carrying an attachment. **The HTTP status is not the criterion.** Read
back `Portal Attachments` on
`https://nitdanigeria.sharepoint.com/sites/Global_Digital_Documents_Centre` and confirm one row
carries the run id the proof prints.

### Verification

End-to-end submission with one attachment; assert the Portal Attachments row exists and carries StoredName.

### If verification fails

- Until applied, a citizen is told a submission with attachments succeeded and no attachment exists.
- Never mark the item closed on an expectation. `APPLIED_UNVERIFIED` is an open status: an
  action performed without its effect observed is not complete.

### Completion criteria

A submission with an attachment produces a row in Portal Attachments.

---

## 12. ITEM-6 — Visit 4 — ECM_DOCS_INTAKE write-back

**Status** APPLIED_UNVERIFIED  ·  **Executed by** operator

### Purpose and expected outcome

Placements are done and the flow's shape is clean. Closes circle C7 — the first point at which a citizen's status can change.

**Expected outcome.** Circle C7 closes: a citizen's status can change.

### Prerequisites and dependencies

- ITEM-2


### Execution steps

1. Patch.
2. Verify.
3. Apply.
4. Re-export and sweep.

**Execution detail**

**Automated path**

```bash
npm run prove:tenant -- --include-writes --only ITEM-6
```

Writes a status transition through WRITEBACK, then track the same reference on the portal and
confirm the status it shows changed. Both halves are the criterion.

### Verification

npm run wiring after re-export, then one status change observed end to end.

### If verification fails

- Do not advance the item. Record the observed output and the command that produced it, and refer it to the party named above.
- Never mark the item closed on an expectation. `APPLIED_UNVERIFIED` is an open status: an
  action performed without its effect observed is not complete.

### Completion criteria

Circle C7 closes: a citizen's status can change.

---

## 13. ITEM-25 — 15 of 19 DYNAMIC_ACTIONS operations answer 501

**Status** EVIDENCE_INCOMPLETE  ·  **Executed by** operator + author

### Purpose and expected outcome

The operations are recognised, authorised and audited, then explicitly refused. The client sees 501 NOT_IMPLEMENTED with applied: false, which is the truth; it previously saw 200 with applied: true. The column evidence was captured on 2026-08-31 and splits the fifteen into three different problems.

**Expected outcome.** npm run designerpaste builds the writes and no operation answers 501.

### Prerequisites and dependencies

- None beyond the environment in §1 and, for anything that transmits, CFG-1 and CFG-2.

**Constraints**

- The designer validates item/<Column> at save time, so writing an absent column produces a package that cannot be saved. Twelve correct columns and three wrong ones is not a partial write.

### Execution steps

1. Read the Global Tracking Queue column set in the tenant and check for Status, NVERSE and Category.
2. Confirm or correct the category -> Category0 pairing on DGO DIGITAL OPS.
3. Decide the dispatch and archive question; if they persist, name the list and capture its columns.
4. Record every confirmed column in internal-field-evidence.json with its evidence class.
5. Build the write bodies; npm run test:listcolumns must stay green.

### Verification

npm run test:listcolumns and npm run designerpaste both green after the evidence is added; then each formerly-501 operation exercised end to end.

### If verification fails

- Building on the reasoned Category0 pairing would repeat the mis-attribution class already found once in this file.
- Never mark the item closed on an expectation. `APPLIED_UNVERIFIED` is an open status: an
  action performed without its effect observed is not complete.

### Completion criteria

npm run designerpaste builds the writes and no operation answers 501.

---

## 14. ITEM-40 — Four endpoint triggers answer every verb and five carry no request schema

**Status** EVIDENCE_INCOMPLETE  ·  **Executed by** operator

### Purpose and expected outcome

Every trigger has now been READ — npm run triggerauth reports unread: 0 across the contract keys, each resolved from a dated export. What the reading found is worse than the title this item carried until 2026-09-14 ('thirteen of fourteen unverified, and the fourteenth answers any verb'): FOUR keys answer every verb, not one — GET_DOCS, FETCH_EMAIL_ATTACHMENTS, AI_DOC_ANALYSIS and AI_CHAT — and FIVE carry no request schema, including CG_Upload_Endpoint and IP_SCAN_INTAKE. The measurement half of this item is closed; the posture half is not, and it was understated.

**Expected outcome.** Every endpoint trigger is recorded as method POST with a request schema, read from an export taken after the change. Currently answeringEveryVerb: 4 and withNoRequestSchema: 5 against a target of 0 and 0; unread is already 0.

### Prerequisites and dependencies

- None beyond the environment in §1 and, for anything that transmits, CFG-1 and CFG-2.

**Constraints**

- Changing the trigger URL is forbidden — setting Method on an existing trigger does not change it, replacing the trigger does.

### Execution steps

1. Set Method = POST on IP_Get_Docs_Endpoint.
2. Export each of the other thirteen and run npm run compare:export, which reports the trigger.
3. Set Method = POST wherever it is unset.

### Verification

A GET against a trigger URL is refused by the platform rather than answered.

### If verification fails

- A signed URL that answers four verbs where the contract uses one.
- Never mark the item closed on an expectation. `APPLIED_UNVERIFIED` is an open status: an
  action performed without its effect observed is not complete.

### Completion criteria

Every endpoint trigger is recorded as method POST with a request schema, read from an export taken after the change. Currently answeringEveryVerb: 4 and withNoRequestSchema: 5 against a target of 0 and 0; unread is already 0.

---

## 15. ITEM-48 — The portal promises citizens four emails the estate does not send

**Status** SPECIFIED_NOT_APPLIED  ·  **Executed by** Platform technical owner for the sends; the agency for any decision to withdraw a promise instead.

### Purpose and expected outcome

The public portal states, at the moment of submission and in three other places, that a confirmation carrying the tracking ID is on its way, and that the decision outcome is emailed. No submission-confirmation and no decision-outcome mail action exists in any flow export or paste package. The submission screen additionally instructs the citizen to contact the helpdesk if no confirmation arrives within one working day, which every submitter will satisfy.

**Expected outcome.** npm run notifications -- --check derives PROVISIONED for RN-001 and RN-005, and either PROVISIONED for RN-003 and RN-004 or the portal copy no longer makes the promise, which the verifier's portal-promise checks will detect and fail on until the matrix is updated.

### Prerequisites and dependencies

- ITEM-39 — the decision and action-required mails need a status-change or scheduled carrier, which does not exist

**Constraints**

- The submission confirmation itself does not need ITEM-39: CG_Submission_Endpoint already holds the sender address and can send inline.

### Execution steps

1. Decide build or withdraw, per promise.
2. For the confirmation: add the send and the outbox receipt to the SUBMISSION package, regenerate, and re-run npm run designerpaste.
3. For the outcome mails: land ITEM-39 first, then key the sends off the status write.
4. Re-run npm run notifications -- --check; RN-001 and RN-005 must derive PROVISIONED.

**Execution detail**

**The rows this item must move to PROVISIONED**

| Row | State now | Event | Audience |
|---|---|---|---|
| RN-001 | ABSENT | A submission is accepted and a reference is minted | citizen |
| RN-003 | PROVISIONED | A decision is issued — the record moves to approved | citizen |
| RN-004 | PROVISIONED | A decision is issued — the record moves to declined | citizen |
| RN-005 | ABSENT | A submitter withdraws their own submission | citizen |

**Automated path — generated, and not yet owned**

A console snippet for this package exists:
`docs/deployment/internal/flows/console-apply/DGO_SEND_EMAIL.console-apply.js`. At
`DRY_RUN = true` it reports WOULD REPLACE and changes nothing; one flow at a time, exercised
before the next, is the safe unit of work. **No runbook carries the step that applies it** —
`docs/reference/document-ownership.json` records why, and what would give it an owner.

```bash
npm run notifications -- --check
```

**Three limits.** A snippet replaces only a top-level scope that already exists, and stops if
the flow does not have one of that name — placement is not guessed. Connection references are
not patched, so a scope using a connector the flow does not already hold saves and then fails at
run time unbound; apply that one in the designer. And `DGO_SCHEDULED_SWEEP` has no snippet at
all: it is a recurrence flow with no HTTP endpoint, so no contract key names it.

### Verification

npm run notifications -- --check

### If verification fails

- Every submitter is told to contact the helpdesk after one working day. The helpdesk load this creates is the whole submitter population, not an exception path.
- Never mark the item closed on an expectation. `APPLIED_UNVERIFIED` is an open status: an
  action performed without its effect observed is not complete.

### Completion criteria

npm run notifications -- --check derives PROVISIONED for RN-001 and RN-005, and either PROVISIONED for RN-003 and RN-004 or the portal copy no longer makes the promise, which the verifier's portal-promise checks will detect and fail on until the matrix is updated.

---

## 16. ITEM-50 — Verification codes, correspondence, reports and assignment notices go to a fixed mailbox

**Status** SPECIFIED_NOT_APPLIED  ·  **Executed by** Platform technical owner

### Purpose and expected outcome

Of 76 live mail actions across 41 flows, four are addressed solely to the person concerned. The live VERIFY endpoint (Portal_Verify, 86897b2f) sends every citizen's verification code to the literal address dgsregistry@nitda.gov.ng, so a citizen cannot complete verification unless someone forwards it from a shared mailbox. Web - OTP Generate does the same for every officer's sign-in code. Web - Send Email, the flow behind the internal EMAIL endpoint, hard-codes To = dgsRegistry@nitda.gov.ng on all three of its sends with no Cc, no Bcc and no dynamic recipient, so every outward official correspondence, report and statistics email an officer composes is delivered to the registry mailbox instead of the addressee while the sent register displays the intended recipient. The bulk assignment flows send nine notices to one named individual, and the three flows that announce a new portal submission all address that same individual rather than a monitored registry address.

**Expected outcome.** npm run notifications -- --check derives PROVISIONED for RN-006, RN-016, RN-021, RN-024 and RN-037, and the fixed-recipient census shows no verification, correspondence, report or assignment send hard-coding an address.

### Prerequisites and dependencies

- ITEM-4 — the internal packages are pasted as part of the same visit

**Constraints**

- The payload already carries the recipient in every case; the carrier discards it. No new contract is needed, only the expression.

### Execution steps

1. Paste Portal_VERIFY_ECM_DOCS and Portal_VERIFY_CONFIRM_ECM_DOCS per Session 4 — this closes RN-006.
2. Paste DGO_SEND_EMAIL into the flow the configured EMAIL URL resolves to — it addresses every send from the payload, gates on DGO_UserDirectory, and writes a Portal Outbox Receipts row per send. The paste target is not settled in this repository: read the workflows/<32-hex> segment off the EMAIL entry in config/config.local.js first (EXECUTION_GUIDE §6.7).
3. Alternatively, if the deployed Web - Send Email body is to be kept, apply docs/deployment/sharepoint/remediation/patched/Web_Send_Email.definition.json — but correct its recipient expression first: it reads triggerBody()?['to'], and every one of the three desks nests the recipient under payload.
4. Patch Web - OTP Generate to use the same dynamic form its sibling Web - OTP Verify already carries.
5. Paste DGO_OTP, DGO_SINGLE_ASSIGNMENT and DGO_BULK_ASSIGNMENT per the walkthrough.
6. Re-address the new-submission notice and add it to the SUBMISSION package.
7. Re-run npm run notifications -- --check.

**Execution detail**

**The rows this item must move to PROVISIONED**

| Row | State now | Event | Audience |
|---|---|---|---|
| RN-006 | MISADDRESSED | A citizen requests a verification code | citizen |
| RN-016 | MISADDRESSED | An officer requests a sign-in code | assignee |
| RN-021 | MISADDRESSED | An officer sends an outward official correspondence | correspondent |
| RN-024 | MISADDRESSED | An officer sends a report or statistics email | originator |
| RN-037 | MISADDRESSED | A new portal submission arrives and the registry must pick it up | administrator |

**Automated path — generated, and not yet owned**

There is one snippet per flow in `docs/deployment/internal/flows/console-apply/`, which is also
the safe unit of work. **No runbook carries the step that applies them**, and this document
records rather than restates: see `docs/reference/document-ownership.json`.

```bash
npm run notifications -- --check
npm run test:sendemail
```

MISADDRESSED means the send happens but goes to a fixed mailbox rather than the audience the row
names. `npm run sendemail` regenerates the recipient patch these packages carry.

### Verification

npm run notifications -- --check

### If verification fails

- Officer sign-in codes for every officer arrive in one shared mailbox, which is an authentication concern as well as a notification one.
- Citizen verification codes do the same, so the public verification journey cannot be completed as built.
- The arrival of a new submission depends on one named individual reading their mail.
- Never mark the item closed on an expectation. `APPLIED_UNVERIFIED` is an open status: an
  action performed without its effect observed is not complete.

### Completion criteria

npm run notifications -- --check derives PROVISIONED for RN-006, RN-016, RN-021, RN-024 and RN-037, and the fixed-recipient census shows no verification, correspondence, report or assignment send hard-coding an address.

---

## 17. ITEM-51 — Officers are never told of assignments, approvals, escalations, delegations or reminders

**Status** SPECIFIED_NOT_APPLIED  ·  **Executed by** Platform technical owner, with the correspondence lifecycle process owner for the copy-to decision

### Purpose and expected outcome

The assignment cascade requires an assignee and a due date on every assignment and resolves a supporting unit, a supporting assignee and a copy-to list besides. Neither DGO_SINGLE_ASSIGNMENT nor DGO_BULK_ASSIGNMENT carries any mail action, so none of those audiences is written to. pending_review can only be left by a second person's decision and nothing tells that person it is waiting. The acknowledgment module's remind-assignee and FastTrack's notify-owner both report to the operator that a notice was queued; both write only to browser storage, and notify-owner has no action-ownership spec at all, so it runs with no declared backend or audit vocabulary. The FastTrack escalation action writes an escalation record with status Open that reaches nobody, escalate-non-ack is a charter capability with no implementation at all, the orchestrator's set-reminder records a dueAt that nothing reads at that time, and executive-escalate delegates a record to a person who is never told it is theirs. Separately, Deployed - Create Task DOES notify the assignee from the task's AssignedTo field — and DGO_SINGLE_ASSIGNMENT, the package that replaces it, carries no mail action, so pasting it removes the one working assignment notice in the estate.

**Expected outcome.** npm run notifications -- --check derives PROVISIONED for RN-009, RN-011, RN-012, RN-015, RN-017, RN-018, RN-019, RN-034, RN-036 and RN-038, and no runtime action reports a notification as queued that has no carrier.

### Prerequisites and dependencies

- GAP-001 must be closed for notify-owner before its audit trail can be trusted

**Constraints**

- RN-020 is in-app only and is bounded by the notification centre being device-local by design.

### Execution steps

1. Port the assignee recipient expression from Deployed - Create Task into DGO_SINGLE_ASSIGNMENT — this is a regression to prevent, not a feature to design.
2. Add 'notify-owner' to config/action-ownership.config.js with an owner, service, audit event and mandatory backend.
3. Change remind-assignee's backend from DYNAMIC_ACTIONS.optional to mandatory, or change the operator copy so it does not claim the assignee was reminded.
4. Add the assignment and approval sends to the packages.
5. Re-run npm run notifications -- --check and npm run test:governance.

**Execution detail**

**The rows this item must move to PROVISIONED**

| Row | State now | Event | Audience |
|---|---|---|---|
| RN-009 | REGRESSION | A record is assigned to one officer | assignee |
| RN-011 | ABSENT | A record is assigned with supporting and copy-to parties | supporting |
| RN-012 | LOCAL_ONLY | An officer is reminded to acknowledge an assignment | assignee |
| RN-015 | LOCAL_ONLY | An operator notifies the owner of a fast-tracked record | owner |
| RN-017 | ABSENT | A record enters pending_review and awaits an approval | approver |
| RN-018 | ABSENT | An approval is decided, returned or rejected | originator |
| RN-019 | ABSENT | A record is reassigned from one officer to another | assignee |
| RN-034 | LOCAL_ONLY | An escalation is raised on a record | owner |
| RN-036 | ABSENT | The executive authority delegates a record to someone else | assignee |
| RN-038 | ABSENT | A record is assigned and the cascade resolves a copy-to and inform list | copyTo |

**Automated path — generated, and not yet owned**

There is one snippet per flow in `docs/deployment/internal/flows/console-apply/`. **No runbook
carries the step that applies them**; `docs/reference/document-ownership.json` records that, and
what would change it.

```bash
npm run notifications -- --check
```

LOCAL_ONLY rows (RN-012, RN-015, RN-034) are recorded in browser storage and leave the device
for nobody. They need a carrier built, not a recipient corrected — those three are the ones the
packages cannot supply.

### Verification

npm run notifications -- --check

### If verification fails

- An assignee can breach an acknowledgement and a due date without ever having been told the record exists.
- Never mark the item closed on an expectation. `APPLIED_UNVERIFIED` is an open status: an
  action performed without its effect observed is not complete.

### Completion criteria

npm run notifications -- --check derives PROVISIONED for RN-009, RN-011, RN-012, RN-015, RN-017, RN-018, RN-019, RN-034, RN-036 and RN-038, and no runtime action reports a notification as queued that has no carrier.

---

## 18. ITEM-9 — 39 of 77 flows have no stated purpose

**Status** BLOCKED  ·  **Executed by** someone who knows the estate

### Purpose and expected outcome

The generated catalogue reports 77 definitions catalogued, 38 attributed to a contract key, and 39 unattributed. The catalogue records what each flow does — trigger, parameters, lists, structure — but not why it exists. This item read '27 of 58' from 2026-09-05 until 2026-09-14, a figure that never matched the catalogue it cited and had since moved further: the gap is wider, not narrower. Eight GOV definitions exported on 2026-09-09 landed unattributed, and attribution has not moved at all — 38 then, 38 now.

**Expected outcome.** The catalogue reports 0 with no stated purpose.

### Prerequisites and dependencies

- None beyond the environment in §1 and, for anything that transmits, CFG-1 and CFG-2.

**Constraints**

- Must come from knowledge of the estate, not from the definition.

### Execution steps

1. Identify who knows the estate's history.
2. Walk the 27 unattributed flows with them.
3. Record one sentence each into the contract register.
4. Regenerate the catalogue.

### Verification

npm run catalogue regenerates and the summary line reads 0.

### If verification fails

- 27 flows nobody can account for is 27 flows nobody can safely decommission.
- Never mark the item closed on an expectation. `APPLIED_UNVERIFIED` is an open status: an
  action performed without its effect observed is not complete.

### Completion criteria

The catalogue reports 0 with no stated purpose.

---

## 19. MANUAL-2 — Test records must be cleared before real correspondence arrives

**Status** READY  ·  **Executed by** operator

### Purpose and expected outcome

Commissioning verification writes real rows into the Correspondence list, and a reference sequence that has issued test numbers keeps issuing from there.

**Expected outcome.** No test row remains, and the sequence counter's starting value is recorded.

### Prerequisites and dependencies

- CFG-1
- CFG-2
- all commissioning verification

**Constraints**

- The reference sequence continues from wherever the test numbers left it. Clearing rows does not reset the counter.

### Execution steps

1. Complete commissioning verification.
2. Identify every test row.
3. Remove them.
4. Decide and record the sequence-counter treatment.

### Verification

A read of the Correspondence list showing no test rows, plus the recorded counter value.

### If verification fails

- Live correspondence interleaved with test rows, and a reference series that starts mid-range with no explanation.
- Never mark the item closed on an expectation. `APPLIED_UNVERIFIED` is an open status: an
  action performed without its effect observed is not complete.

### Completion criteria

No test row remains, and the sequence counter's starting value is recorded.

---

## 20. MANUAL-3 — The browser suite must run against the deployed build

**Status** BLOCKED  ·  **Executed by** operator

### Purpose and expected outcome

The suite covers boot, accessibility, all 29 routes, themes and the portal — but against a local server. Deployment is exactly where config.local.js presence differs.

**Expected outcome.** All ten spec files pass against the deployed hostname.

### Prerequisites and dependencies

- CFG-1
- CFG-2

**Constraints**

- In this container the suite needs DGO_CHROME_PATH set, because the pinned Playwright build and the pre-installed browser differ.

### Execution steps

1. Deploy.
2. Point baseURL at the deployed hostname.
3. Run all ten spec files.
4. Record the result.

**Execution detail**

**The browser suite, against the deployed hostname**

```bash
npm install
npx playwright install chromium
DGO_BASE_URL=https://activityweb.page.gd npm run test:smoke
```

`https://activityweb.page.gd` is the deployed internal site, recorded at
`docs/deployment/PORTAL-TENANT-RUNBOOK.md` line 550. In a container, set `DGO_CHROME_PATH`
to the Chromium binary. The suite covers boot, accessibility, all 29 routes, the themes and the
portal, across these twelve spec files:

```
tests/admin-console.spec.js      tests/audit-remediation.spec.js
tests/closure.spec.js            tests/containment.spec.js
tests/correspondence.spec.js     tests/document-flags.spec.js
tests/dual-spine.spec.js         tests/endpoint-console.spec.js
tests/lookup-commits.spec.js     tests/portal.spec.js
tests/scan-intake.spec.js        tests/smoke.spec.js
```

A local pass does not discharge this: deployment is where `config.local.js` presence differs.

### Verification

The Playwright run output against the deployed hostname, retained.

### If verification fails

- A defect that only appears when config.local.js is present would not be caught by any local run.
- Never mark the item closed on an expectation. `APPLIED_UNVERIFIED` is an open status: an
  action performed without its effect observed is not complete.

### Completion criteria

All ten spec files pass against the deployed hostname.

---

## 21. G-04 — No server-side enforcement is running anywhere

**Status** BLOCKED  ·  **Executed by** Power Automate (implementation); agency (posture decision)

### Purpose and expected outcome

The authenticating proxy that would have validated tokens, derived roles and authorised actions was built and then removed rather than deployed. Every obligation it carried now belongs to each flow individually: token validation, role derivation, per-action authorisation, rate limiting, reference minting, upload ticketing and the filename policy. Caller identity is a client-asserted userEmail read from localStorage.

**Expected outcome.** A flow called directly with a forged or absent identity refuses the action.

### Prerequisites and dependencies

- Each flow must be reachable and editable, which depends on ITEM-2 and ITEM-35.

**Constraints**

- The generated packages already carry rate limiting, reference minting and upload ticketing. Token validation, role derivation and per-action authorisation are not in them.

### Execution steps

1. Decide the posture (pilot vs enforced) — agency.
2. If enforced: implement token validation, role derivation and per-action authorisation in each flow.
3. Set auth.enabled and supply tenant configuration.
4. Verify a viewer cannot reach a privileged action by editing local storage.

### Verification

A direct call to each endpoint bypassing the client, asserting refusal. npm run commission ceases to report the pilot-posture warning.

### If verification fails

- npm run commission states this posture is 'Acceptable for an internal pilot. Not for citizens' personal data at scale.' MANUAL-4 records that roughly 785 individuals' personal data is in scope.
- Never mark the item closed on an expectation. `APPLIED_UNVERIFIED` is an open status: an
  action performed without its effect observed is not complete.

### Completion criteria

A flow called directly with a forged or absent identity refuses the action.

---

## 22. ITEM-37 — The sanctioned crossing does not exist in the live flow — restore it, or amend D1 and D6

**Status** DECISION_REQUIRED  ·  **Executed by** agency

### Purpose and expected outcome

Workflow df7ddff1-9275-4f23-acf6-e169525f4e2f was in docs/reference/flow-contracts/deployed/ TWICE: as ECM_DOCS_INTAKE, captured 2026-08-19 with 2 actions and a Portal Registry GetItems, and as CG_Upload_Endpoint, captured 2026-08-24 with 9 actions and no Portal Registry operation. That was read as one flow renamed and rebuilt in place, destroying the single sanctioned crossing D1 and D6 rest on. IT WAS NOT. The second file was CG_Upload_Endpoint's definition stamped with ECM_DOCS_INTAKE's workflow id — one corrupted export, not two states of one flow. It was removed on 2026-09-03 and the finding withdrawn.

**Expected outcome.** The agency records one of two things: that D1 and D6 stand as written, given the crossing measures present — which closes this item with no work — or that the crossing should sit somewhere other than where it does, which makes it a change request rather than a restoration.

### Prerequisites and dependencies

- ITEM-35 established that this id is one flow renamed in place; that finding is the input to this one.
- The UPLOAD paste (EXECUTION_GUIDE 5.2) targets this same workflow and must not proceed until this is settled.

**Constraints**

- Record workflow ids only. A trigger URL carries a bearer credential and must not be pasted into a ticket, an email or a chat message.

### Execution steps

1. Read item37_settled in the 2026-09-01 paste-state evidence.
2. Decide whether the portal must feed the internal registry at all.
3. Record the decision, and amend D1 and D6 if it is not to be restored.

### Verification

npm run test:wiringspec — the bridge state recorded in the specification must equal what the sweep measures, in both directions.

### If verification fails

- D1 and D6 both rest on this crossing existing and being read-only. If the intake feed is gone, the one-crossing argument rests on something that is not there.
- Pasting Portal_UPLOAD_ECM_DOCS onto this workflow overwrites whatever remains of the bridge, and the paste would look successful.
- Never mark the item closed on an expectation. `APPLIED_UNVERIFIED` is an open status: an
  action performed without its effect observed is not complete.

### Completion criteria

The agency records one of two things: that D1 and D6 stand as written, given the crossing measures present — which closes this item with no work — or that the crossing should sit somewhere other than where it does, which makes it a change request rather than a restoration.

---

## 23. ITEM-41 — Content approval is still ON for Global Tracking Queue — 12,079 of 15,936 rows are invisible to readers

**Status** DECISION_REQUIRED  ·  **Executed by** agency

### Purpose and expected outcome

Measured 2026-09-01 by a complete tally: Global Tracking Queue holds 15,936 items of which 12,079 are Pending and therefore not visible to readers. Four of the seven internal flows write to this list, so rows they create land Pending and invisible. DGO DIGITAL OPS carried the same setting with 14,882 of 21,532 hidden and was turned off on agency direction, confirmed by read-back. Global Tracking Queue was deliberately not changed: it carries its own decision and the script will not make it by implication.

**Expected outcome.** A recorded decision, and if it is to be off, a read-back confirming EnableModeration is false.

### Prerequisites and dependencies

- None beyond the environment in §1 and, for anything that transmits, CFG-1 and CFG-2.

**Constraints**

- $filter on _ModerationStatus returns HTTP 500 on both lists — the column is unindexed and both are over the 5,000-item view threshold — so any count must page every row.

### Execution steps

1. Decide whether this list is to carry content approval.
2. If not, run the approval script with APPLY set and confirm by read-back.

### Verification

A reader-facing query returns the list's full ItemCount.

### If verification fails

- Every reader-facing query against this list returns 3,857 of 15,936 rows, and rows the flows create are invisible the moment they are written.
- Never mark the item closed on an expectation. `APPLIED_UNVERIFIED` is an open status: an
  action performed without its effect observed is not complete.

### Completion criteria

A recorded decision, and if it is to be off, a read-back confirming EnableModeration is false.

---

## 24. ITEM-52 — Read endpoints and AI flows email record payloads to a shared mailbox

**Status** DECISION_REQUIRED  ·  **Executed by** The agency for SN-001, alongside MANUAL-4; the platform technical owner for SN-002 to SN-004.

### Purpose and expected outcome

Thirteen flows send mail that nothing in the estate requires. Eight are read endpoints that email their response payload — the records they just returned — to dgsRegistry@nitda.gov.ng or [a personal mailbox, resolved from DGO_UserDirectory] on every call. Two are AI chat and preprocessing flows that mail their results out with subjects reading 'Result--Hugging Face --' and 'HTTP CHAT FLOW'. Two mail an audit line to one individual that the audit and Portal Audit Events lists already hold. One, in Web - Email To Task Processing, carries an empty recipient and an empty subject and cannot succeed on any run that reaches it.

**Expected outcome.** Each of the four surplus patterns carries a recorded decision, and any send decided against is gone from the exported estate.

### Prerequisites and dependencies

- MANUAL-4 — the posture decision on citizens' personal data covers the read-endpoint payload sends
- ITEM-9 — 39 of 77 flows have no stated purpose, which is what makes 'is this still wanted' unanswerable from the repository (corrected 2026-09-15 from '27 of 58', the count this citation carried after ITEM-9's own text had been corrected against docs/reference/FLOW_CATALOGUE.md)

**Constraints**

- Removing a send is a flow edit and needs the same paste or patch path as any other change.

### Execution steps

1. Review SN-001 alongside MANUAL-4 — they are the same posture decision.
2. Confirm with estate knowledge whether SN-002's AI sends are still wanted in production.
3. Retire SN-004's audit mails in favour of the audit lists, or record why the mail copy is needed.
4. Remove or complete the SN-003 empty send.
5. Update SurplusNotifications and re-run npm run notifications -- --check.

### Verification

npm run notifications -- --check

### If verification fails

- Record payloads for roughly 785 individuals are mailed to a shared mailbox on every read-endpoint call.
- Never mark the item closed on an expectation. `APPLIED_UNVERIFIED` is an open status: an
  action performed without its effect observed is not complete.

### Completion criteria

Each of the four surplus patterns carries a recorded decision, and any send decided against is gone from the exported estate.

---

## 25. ITEM-57 — The archived design system is unreconciled against the live one, and nobody has said which is authoritative

**Status** DECISION_REQUIRED  ·  **Executed by** the agency, with whoever owns the platform's visual design

### Purpose and expected outcome

docs/archive/dgo-ops/design-system/ is a 105-file design system documentation set dated 2026-07-30 — foundations, tokens, colour, typography, spacing, iconography, motion, elevation, accessibility, i18n/RTL, 27 component pages, styles and a legacy-v1. The platform's live design system is document-portal/ds/ (ds.css, colors_and_type.css, fonts, icons, logo), gated by npm run test:tokens and npm run test:breakpoints. The two were built separately and neither defers to the other. dgoeaa/BESPOKE_DSIGN_SYSTEM is not a third source: it holds one file, an unextracted zip. Nothing depends on resolving this — the archived set is not wired to anything and not served by any page — but it is the only copy of a substantial body of design work, and leaving it unlabelled invites someone to treat it as current.

**Expected outcome.** The archived design system carries an explicit statement of its standing relative to document-portal/ds/, decided rather than inferred.

### Prerequisites and dependencies

- None beyond the environment in §1 and, for anything that transmits, CFG-1 and CFG-2.

**Constraints**

- The archived set is dated before main (63d79ee, 2026-08-16), so it predates the current platform

### Execution steps

1. Read docs/archive/dgo-ops/design-system/docs/01-tokens.md against document-portal/ds/colors_and_type.css.
2. Either mark the archived set superseded in its README, or raise a scoped item for the parts worth promoting.
3. If superseded, nothing further is required — it stays as a record.

### Verification

none — this is a decision, and no gate can hold it

### If verification fails

- A reader treats the archived documentation as the current specification because it is more complete than the live one, and builds against tokens nothing enforces
- Never mark the item closed on an expectation. `APPLIED_UNVERIFIED` is an open status: an
  action performed without its effect observed is not complete.

### Completion criteria

The archived design system carries an explicit statement of its standing relative to document-portal/ds/, decided rather than inferred.

---

## 26. Final completion criteria

The estate reaches production operations when all of the following hold, each with named evidence.

```bash
npm run test:node          # 138 stages, exit 0
npm run readiness          # 10 passed, 0 failed
npm run commission         # no blockers
npm run tenant:validate -- --strict   # ready: true
npm run disposition        # every remaining item has an accountable party
```

- [ ] `npm run commission` reports no blockers, closing CFG-1 and CFG-2.
- [ ] `npm run tenant:validate -- --strict` returns `ready: true` with no errors and no blocked
      items. It currently returns `ready: false` with 61 blocked, which is the true state of the
      tenant backlog rather than a defect.
- [ ] Every item above is `RESOLVED` in the register, or carries a risk-acceptance record.
- [ ] The production authorisation record is complete (P-17 of
      `docs/deployment/notification-instrument/tenant-execution/PENDING_WORK_REGISTER.md`).
- [ ] `npm run test:smoke` passes against the deployed hostname.

**What no command can settle.** G-04 requires enforcement implemented in Power Automate and a
posture decision by the agency. Until both hold, caller identity is a client-asserted
`userEmail` from `localStorage`, RBAC is advisory, and a flow called directly answers whoever
calls it. No repository check can verify a Power Automate flow, so the gate reports this as a
standing manual obligation in every posture.
