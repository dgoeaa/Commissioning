# Closed — what this estate has discharged

**Generated by `npm run closed` from [`PRODUCTION_READINESS_REGISTER.json`](./PRODUCTION_READINESS_REGISTER.json)
and [`governance-estate-position.json`](../reference/governance-estate-position.json).**
Do not edit by hand — `npm run test:closed` fails on drift.

> **29 items are closed.** 26 in the readiness register,
> 3 in the governance estate. 28 of them record the date they
> closed; the rest state the evidence without one, and no date has been invented for them here.

This is the counterpart to [`OUTSTANDING.md`](./OUTSTANDING.md). Every other instrument in this
estate reports what is left: `npm run readiness` counts open items, `npm run commission` lists
open obligations, `CLOSURE_DISPOSITION.md` dispositions the open ones. Nothing recorded what had
been discharged, so closed work existed as a subtraction and nowhere as a record — and a closure
with no record is indistinguishable from an oversight. **`GOV-08` spent five days filed as open
while reading "RESOLVED IN THE TENANT"; `CFG-2` spent eleven telling operators to record workflow
ids that were already recorded.** Both survived because checking a closure meant re-deriving it,
and the cheap move is to re-raise instead.

## What each closed status claims

| Status | Count | What it asserts |
|---|---:|---|
| `RESOLVED` | 23 | The item's own resolution criteria are met, with the evidence named. |
| `ACCEPTED` | 1 | Known, reasoned, and deliberately not being changed. The requirement stands and cannot be met. |
| `DISCHARGED_UNTRACKABLE` | 2 | Done and evidenced once, and permanently unverifiable from this repository — the proof is a credential or a git-ignored file. A gate here will go on reporting it as outstanding forever. |
| governance `CLOSED` | 3 | Moved to `closedFindings` — resolved in the repository and in the tenant, or superseded by another finding. |

---

## Readiness register — 26 closed

| Item | Status | Closed | Discharged by | Why it closed |
|---|---|---|---|---|
| **ITEM-22** Rotate the disclosed trigger tokens — done; the disclosed set is superseded | `RESOLVED` | 2026-09-03 | security authority | Closed. It was open because rotation can only be performed in Power Automate and no repository action could close it; that rotation has since been performed and reconciled against the tenant endpoint register. |
| **CFG-1** Internal runtime is not configured | `DISCHARGED_UNTRACKABLE` | 2026-09-13 | operator | Closed 2026-09-13 by a live run, and unverifiable here forever. The operator harvested all 25 signatures and ran setup: 18 of 18 internal keys wired, 0 automated commissioning blockers. The values file was then shredded and confi… |
| **CFG-2** Public portal is not configured | `DISCHARGED_UNTRACKABLE` | 2026-09-13 | operator | Closed 2026-09-13 by the same live run: 7 of 7 portal keys wired, 0 automated commissioning blockers. document-portal/config.local.js is git-ignored, so this is unverifiable here for the same reason as CFG-1. The reason recorded … |
| **ITEM-8** The allowed origin is listed but has never been confirmed against the platf… | `RESOLVED` | 2026-09-01 | operator + author | The row was added on 2026-09-01. Nothing in this repository can verify that https://activityweb.page.gd is the origin the platform actually serves from, and a mismatch of scheme or a trailing slash fails the browser CORS check ex… |
| **ITEM-34** The list holding the allowed origins is unowned and unaccounted for | `RESOLVED` | 2026-08-31 | operator + author | Resolved 2026-08-31. The agency decided to keep Flow Configuration as the list the allowed origins are read from, and D15 records it with the tenant reading it was taken on. Moving the control would have been a rebuild of fourtee… |
| **ITEM-7** Third-party API keys rotated | `RESOLVED` | 2026-09-08 | operator | Closed. The agency confirmed all rotations complete on 2026-09-08. |
| **ITEM-35** Two paste targets unconfirmed; five resolved | `RESOLVED` | 2026-08-31 | operator | Resolved 2026-08-31. Both ids were exported from the tenant and resolve to exactly the flow the 2026-08-27 run records name. Neither id appears elsewhere in deployed/, so both are flows never previously captured rather than renam… |
| **ITEM-17** WRITEBACK — client and specification done, flow and endpoint key outstanding | `RESOLVED` | 2026-09-01 | operator | Closed. It was open because WRITEBACK had a client and a specification but no wired flow; the reconciliation wired it. |
| **ITEM-3** Portal_Verify_Confirm is patched and verified on disk but never applied | `RESOLVED` | 2026-09-01 | operator | Closed. It was open because the patch existed on disk with no evidence it had been applied; the reconciled wiring is that evidence. |
| **ITEM-4** Visit 2 — the two submission flows | `RESOLVED` | 2026-09-01 | operator | Closed. The route was chosen and applied; the reconciled wiring reflects it. |
| **ITEM-18** The upload SHA-256 cannot be computed on this tenant — the digest is record… | `ACCEPTED` | 2026-09-03 | author | Accepted 2026-09-03, not closed by work. The requirement stands and cannot be met on this tenant. What is in place instead is disclosed above and is deliberately not described as an integrity control. |
| **MANUAL-1** Routing table approved by the agency | `RESOLVED` | 2026-09-08 | agency | Closed. The agency approved and supplied the matrix on 2026-09-08. |
| **MANUAL-4** Personal data disposition decided: identity lives in DGO_UserDirectory, not… | `RESOLVED` | 2026-09-08 | agency | Closed by the agency decision of 2026-09-08. |
| **G-08** The overrides cascade debt is measured and ratcheted — 89 contended pairs, … | `RESOLVED` | 2026-09-03 | author | Closed 2026-09-03. The finding was that the cascade producing the asserted appearance was not measured. It is measured now, and the measurement is a ratchet in the gate rather than a number in a comment. |
| **ITEM-36** A public portal endpoint reads the internal operations estate, against the … | `RESOLVED` | 2026-08-31 | agency + author | Resolved 2026-08-31. The agency accepted the crossing and D16 records it, for all seven portal endpoints since all seven packages read the same list for the same value. The principle is not amended; this is its one named exceptio… |
| **ITEM-38** The OTP verify hardening is deployed — built fresh as IP_OTP_VERIFY on 2026… | `RESOLVED` | 2026-09-04 | operator | Resolved 2026-09-04. The hardening no longer exists only on disk: it is deployed, in a flow built by hand from the designer-paste kit, and the operator's export proves it action by action. Two things this does NOT mean, and ITEM-… |
| **ITEM-39** OTP_Transactions.Attempts is provisioned, and the list is inside the provis… | `RESOLVED` | 2026-09-03 | operator | Resolved 2026-09-03. The operator re-ran the provisioner as a dry run, which agreed with 2026-09-02 that exactly one column was missing, then ran it with DRY_RUN = false. It created Attempts and skipped the other 102. The specifi… |
| **ITEM-42** The portal sitemap and robots.txt need the deployed origin before the porta… | `RESOLVED` | 2026-09-04 | operator | Closed. The deployed origin was set and both files now name it absolutely. |
| **ITEM-45** The scheduled sweep completes: the list view threshold failure is closed | `RESOLVED` | 2026-09-05 | operator | Resolved 2026-09-05. Run 08584130061891154567402797399CU18 completed in 9m50s with every reported scope Succeeded and an empty error list. Get_Due_Breached and Get_Ack_Overdue — the two actions that returned 400 on the first live… |
| **ITEM-46** Two detectors for the threshold read only equality filters, which is why th… | `RESOLVED` | 2026-09-02 | Platform technical owner | Resolved 2026-09-02. Recorded because the fix to ITEM-45 is worth nothing if the detector that missed it stays as it was. |
| **ITEM-47** The four index targets ITEM-45 declared are applied and recorded | `RESOLVED` | 2026-09-05 | operator | Resolved 2026-09-05 by the operator. DueDate and Acknowledgement_x0020_Due_x0020_ were indexed on Global Tracking Queue; Modified read back already indexed on both Global Tracking Queue and DGO DIGITAL OPS, which is an observed o… |
| **ITEM-49** The estate can fire on a schedule — AUTO_SCHEDULED_SWEEP is deployed | `RESOLVED` | 2026-09-05 | Operational owner | Resolved 2026-09-05 by the operator. The export carries a Recurrence trigger, hourly, W. Central Africa Standard Time. This was the estate's largest structural gap: every deferred, breach-driven and retry notification needed a me… |
| **ITEM-53** CG_SEND_EMAIL no longer accepts anonymous callers | `RESOLVED` | 2026-09-05 | operator | Resolved 2026-09-05 by the operator. triggerAuthenticationType is Tenant, matching the flow it replaces. |
| **ITEM-54** IP_OTP_Endpoint no longer returns officer claims with a wildcard origin | `RESOLVED` | 2026-09-05 | operator | Resolved 2026-09-05 by the operator. Access-Control-Allow-Origin is now @outputs('Compose_Allowed_Origin_Otp'), not *. |
| **ITEM-55** IP_OTP_VERIFY's placeholder origin is replaced by the allow-list its siblin… | `RESOLVED` | 2026-09-05 | operator | Resolved 2026-09-05 by the operator, 36 minutes after the 06:17:06 export that introduced it. Access-Control-Allow-Origin is now @{outputs('Compose_Allowed_Origin_Otp')}, and that Compose exists in this flow: it echoes the reques… |
| **ITEM-58** Unresolved DSU keys in the approved matrix — closed by agency direction | `RESOLVED` | 2026-09-08 | agency | Closed by agency direction of 2026-09-08. |

---

## Governance estate — 3 closed

| Finding | Closed | Why it closed |
|---|---|---|
| **GOV-00** The seven HTTP flow registry lists are specified but not provisioned | _not stated_ | docs/reference/http-flow-registry-spec.json declares 7 lists and 101 fields on DGO_ECM_GOVERNANCE and states that none is provisioned. The 2026-08-18 capture confirms all seven are absent. Specification and reality agree; nothing is outstanding until the provisioning flow is run. |
| **GOV-07** DGO_AccessScopes appeared damaged in the tenant | 2026-09-09 | Recorded from the live run of 2026-09-09 as a damaged list, on the evidence that its columns read back while every operation touching its item store returned 500. That diagnosis was wrong. The list loads normally in the browser. The cause is GOV-08: ScopeId is a SharePoint reserved field name, the … |
| **GOV-08** One specification column used a name SharePoint reserves, and was reported present … | 2026-09-09 | ScopeId is a SharePoint system field: every list carries a hidden one holding the item's security scope. The specification declares a custom Text column of the same name on DGO_AccessScopes, so the provisioner's presence check answered yes against SharePoint's field, reported the column present, an… |

---

## The evidence behind each closure

Every file and command a closed item cites. A closure that cites nothing would appear here with an
empty list, which is why the list is printed rather than summarised.

### ITEM-22 · Rotate the disclosed trigger tokens — done; the disclosed set is superseded

| Cited | What it establishes |
|---|---|
| `docs/deployment/rotation/ROTATION.md` | the rotation broken down by endpoint key rather than by token count: 17 of 25 keys need a new URL, served by 15 distinct flows; 24 of the 39 token-bearing workflows sit behind no endpoint key at all … |
| `npm run commission` _(command)_ | WARNING: 43 signed trigger URL(s) are committed to this repository, across 28 tracked file(s) |
| `docs/deployment/sharepoint/OPEN_ITEMS.md` | item 22 records 56 tokens across 39 workflows and the four leaking flows |
| `docs/STATUS_REPORT.md` | finding G-03, corrected 6 Aug to 55 distinct signatures across 28 files, and again 2026-09-03 to 43 once the counters stopped indexing one credential under several strings |
| `npm run designerpaste` _(command)_ | check 9b passes: all 15 generated packages blank both gateway headers |
| `npm run test:rotation` _(command)_ | 18 assertions holding the worksheet to the endpoint map and the disclosed set, including that a key with no recorded workflow id is never called unaffected, and that no generated file contains a sign… |
| `npm run commission` _(command)_ | The estate has since been rotated and reconciled against docs/reference/endpoint-register.json: none of these signatures appears in the current configuration, so none authenticates an endpoint this p… |
| `docs/reference/endpoint-register.json` | post-rotation tenant export, redistribution_status shareable_credentials_removed; a scan of all 43 disclosed signatures against it returns 0 matches |

### CFG-1 · Internal runtime is not configured

| Cited | What it establishes |
|---|---|
| `evidence/email-related-task-workflow-remediation.md` | the live run of 2026-09-13: 25 harvested values, 25 of 25 complete signatures, internal runtime configuration 18 of 18, automated commissioning blockers 0. Records that the harvester and the values f… |
| `npm run commission` _(command)_ | BLOCKER: 'Internal runtime: not configured — config/config.local.js does not exist, so every endpoint resolves to "" and the platform runs in demo mode' |
| `docs/deployment/rotation/ROTATION.md` | the 17 internal keys that need a new URL, and the order that gets there |

### CFG-2 · Public portal is not configured

| Cited | What it establishes |
|---|---|
| `evidence/email-related-task-workflow-remediation.md` | the live run of 2026-09-13: public portal configuration 7 of 7, automated commissioning blockers 0. |
| `docs/reference/endpoint-workflow-ids.json` | schema v2, exported from the tenant post-rotation: all 25 keys carry a workflowId and a urlTemplate ending at sig=, SUBMISSION, SUPPORT and UPLOAD included. |
| `npm run commission` _(command)_ | BLOCKER: 'Public portal: not configured — document-portal/config.local.js does not exist' |
| `docs/deployment/rotation/ROTATION.md` | which portal keys rotation actually touches, and which three are undetermined |

### ITEM-8 · The allowed origin is listed but has never been confirmed against the platform

| Cited | What it establishes |
|---|---|
| `docs/deployment/sharepoint/evidence/2026-09-01-paste-state-by-flow.json` | CLOSED_2026_09_01_03_26.remaining: ALLOWED_ORIGIN_1 in Flow Configuration reads https://activityweb.page.gd. The row exists; what is unconfirmed is that it equals location.origin on the platform exac… |
| `docs/deployment/sharepoint/OPEN_ITEMS.md` | items 8 and 34 |
| `docs/deployment/sharepoint/evidence/2026-08-30-flow-configuration-list.json` | the list was created 2025-10-30, has no description, holds one item unchanged in over nine months, and is named by none of the 58 exported flows |
| `docs/deployment/sharepoint/evidence/2026-08-31-provisioning-run.json` | Flow Configuration.ConfigValue created 2026-08-31 — the column exists; no ALLOWED_ORIGIN row has been added |
| `docs/deployment/sharepoint/DECISIONS.md` | D15 ratifies Flow Configuration as the list, clearing what ITEM-34 blocked |
| `docs/deployment/sharepoint/evidence/2026-09-04-origin-confirmed.json` | location.origin read from the running platform on 2026-09-04 printed https://activityweb.page.gd, matching ALLOWED_ORIGIN_1 exactly — scheme, no trailing slash, no path |
| `docs/deployment/internal/evidence/2026-09-04-origin-check-not-delegable.json` | an approved attempt to settle this from a session on 2026-09-04, BEFORE the operator's reading closed it. The egress proxy refused CONNECT with 403, so nothing about the origin was learned that way. … |

### ITEM-34 · The list holding the allowed origins is unowned and unaccounted for

| Cited | What it establishes |
|---|---|
| `docs/deployment/sharepoint/evidence/2026-08-30-flow-configuration-list.json` | read from the tenant on 2026-08-30 |
| `docs/deployment/sharepoint/evidence/2026-08-31-provisioning-run.json` | ConfigValue was created on Flow Configuration on 2026-08-31, before any decision was recorded |
| `docs/deployment/sharepoint/DECISIONS.md` | D15 records the decision to keep Flow Configuration, what was known when it was taken, and that no secret may go in that list |

### ITEM-7 · Third-party API keys rotated

| Cited | What it establishes |
|---|---|
| `docs/deployment/sharepoint/OPEN_ITEMS.md` | item 7, listing the seven providers |
| `docs/deployment/EXECUTION_GUIDE.md` | Appendix B: 'Redaction is not rotation. A redacted key in a document is still live at the provider.' |
| `docs/deployment/CLOSURE_DISPOSITION.md` | agency confirmation of 2026-09-08: all rotations complete |

### ITEM-35 · Two paste targets unconfirmed; five resolved

| Cited | What it establishes |
|---|---|
| `docs/deployment/sharepoint/evidence/2026-08-31-portal-paste-target-resolution.json` | 5 resolved, 2 open, with the derivation |
| `docs/deployment/sharepoint/evidence/2026-08-30-portal-paste-target-conflict.json` | the original conflict record it supersedes |
| `docs/deployment/EXECUTION_GUIDE.md` | section 5.2 and Appendix A now carry the run-record ids and the flows not to paste on |
| `docs/reference/flow-contracts/deployed/CG_Writeback_Endpoint__21d4bfd3-f595-46fa-81bb-c29dabc12e7a__full_definition.json` | exported 2026-08-31; workflow_identity names it CG_Writeback_Endpoint, matching the run record |
| `npm run wiring` _(command)_ | 24/59 operations, 3 of 7 endpoints fully wired — was 7/59 and 0 |

### ITEM-17 · WRITEBACK — client and specification done, flow and endpoint key outstanding

| Cited | What it establishes |
|---|---|
| `docs/deployment/sharepoint/evidence/2026-09-01-paste-state-by-flow.json` | CG_Writeback_Endpoint is verified current, so the flow half of this item is applied. What remains is the endpoint key in document-portal/config.local.js, which is CFG-2. |
| `docs/deployment/sharepoint/evidence/2026-09-01-paste-state-by-flow.json` | CLOSED_2026_09_01_03_26: all fourteen flows verified current against the tenant — every action present, every parameter identical, zero differences on every one. |
| `docs/deployment/sharepoint/remediation/06-writeback-citizen-actions.json` | the specification covering all ten operations |
| `docs/deployment/sharepoint/flows/designer-paste/Portal_WRITEBACK_ECM_DOCS.designer-paste.json` | the paste package, 58 actions, passing npm run designerpaste |
| `document-portal/config.example.js` | declares the empty seventh slot |
| `npm run wiring` _(command)_ | 58/58 required operations in place, 7/7 endpoints fully wired; WRITEBACK 10/10 flows read CG_Writeback_Endpoint |

### ITEM-3 · Portal_Verify_Confirm is patched and verified on disk but never applied

| Cited | What it establishes |
|---|---|
| `docs/deployment/sharepoint/evidence/2026-09-01-paste-state-by-flow.json` | CG_Verification_Confirmation_Endpoint is verified current. The OTP email body defect it carried is confirmed resolved against the tenant: 403 characters carrying the generated code, where it was the … |
| `docs/deployment/sharepoint/evidence/2026-09-01-paste-state-by-flow.json` | CLOSED_2026_09_01_03_26: all fourteen flows verified current against the tenant — every action present, every parameter identical, zero differences on every one. |
| `docs/deployment/sharepoint/remediation/patched/README.md` | the apply procedure |
| `docs/deployment/sharepoint/OPEN_ITEMS.md` | item 3: 'Patched and verified at 7/7 on disk; the file is in the repository.' |
| `npm run wiring` _(command)_ | VERIFY_CONFIRM 7/7 operations, flows read CG_Verification_Confirmation_Endpoint; 58/58 overall, 7/7 endpoints, 6 boundary crossings |

### ITEM-4 · Visit 2 — the two submission flows

| Cited | What it establishes |
|---|---|
| `docs/deployment/sharepoint/evidence/2026-09-01-paste-state-by-flow.json` | CG_Submission_Endpoint is verified current. Claim_Sequence_Counter now reads ?['CurrentSequence'] from the row it patches, where it read ?['CurrrentSequence'] — three r's, no such column — nulling th… |
| `docs/deployment/sharepoint/evidence/2026-09-01-paste-state-by-flow.json` | CLOSED_2026_09_01_03_26: all fourteen flows verified current against the tenant — every action present, every parameter identical, zero differences on every one. |
| `docs/deployment/sharepoint/remediation/02-submission-registry-write.json` | 13 actions; appliesTo names 270fb295 and de9ef13b |
| `npm run test:remediation` _(command)_ | passes |
| `npm run wiring` _(command)_ | WIRED SUBMISSION 12/12 operation(s), flows read CG_Submission_Endpoint |

### ITEM-18 · The upload SHA-256 cannot be computed on this tenant — the digest is recorded, not enforced

| Cited | What it establishes |
|---|---|
| `docs/deployment/sharepoint/evidence/2026-09-03-inline-code-unavailable.json` | the operator's answer, attributed and dated, recorded as an external dependency rather than as a measurement — it is the one fact here not derived from this repository |
| `docs/deployment/sharepoint/portal-field-spec.json` | DeclaredSha256 is declared on Portal Attachments and on Portal Upload Tickets, and the 2026-09-02 dry run reported both present in the tenant — so the declared digest is persisted |
| `document-portal/js/submit.js` | digestOf() computes a real SHA-256 through crypto.subtle and declares one per attachment; it returns the empty string where crypto.subtle is absent, and an empty digest is not refused |
| `docs/deployment/power-automate-flows/02-portal-upload.flow.json` | Verify_Upload_Checksum, typed Javascript — not a Power Automate action type — comparing against an uninterpolated string literal. It has never run and must not be ported |
| `npm run test:claims` _(command)_ | passes: no document claims the digest is verified, and the claim is mutation-tested |

### MANUAL-1 · Routing table approved by the agency

| Cited | What it establishes |
|---|---|
| `npm run commission` _(command)_ | SETTLED: 'routing table needs approval — MANUAL-1 — RESOLVED'. The gate reads the register and stops reporting a closed item as outstanding; until 2026-09-14 it hardcoded the manual and went on print… |
| `docs/deployment/MINIMAL-PILOT.md` | the routing table itself |
| `docs/reference/approved/category-matrix.csv` | the approved matrix as supplied by the agency: 45 rows, 16 categories, primary/supporting/inform units per row. Two rows share a Title/Subcategory pair, so npm run test:orgrouting reports 44 distinct… |
| `docs/reference/approved/organizational-units.csv` | the approved departments list, 50 units, personal addresses removed |
| `npm run test:categories` _(command)_ | 22 passed, 0 failed against the approved matrix |

### MANUAL-4 · Personal data disposition decided: identity lives in DGO_UserDirectory, not in the repository

| Cited | What it establishes |
|---|---|
| `npm run commission` _(command)_ | SETTLED: 'personal data of ~785 individuals is in scope — MANUAL-4 — RESOLVED'. The gate reads the register; until 2026-09-14 it hardcoded the manual and went on reporting the disposition as an outst… |
| `docs/STATUS_REPORT.md` | finding R-01, closed as an exposure by making the repository private |
| `npm run test:orgrouting` _(command)_ | 50 units, 44 matrix rows, 16 categories, 8 adopted corrections, 0 personal addresses; the generator exits non-zero if a personal address reaches configuration. 44, not 45: the approved CSV carries 45… |
| `config/organizational-units.config.js` | role mailboxes only; individual identity resolved at runtime from DGO_UserDirectory |

### G-08 · The overrides cascade debt is measured and ratcheted — 89 contended pairs, and it can no longer grow

| Cited | What it establishes |
|---|---|
| `tests/css-cascade-contract.mjs` | the measurement the comment promised: override-overlap is 89 contended (selector, property) pairs between the two overrides sheets, budgeted and allowed only to shrink |
| `npm run test:cascade` _(command)_ | 10 assertions — the budget, a guard against the budget becoming stale slack, the layer preconditions the analysis rests on, and that no missing tool is cited as current evidence |
| `styles/index.css` | the reasoning kept, the imaginary evidence removed, and a dated correction note saying what was corrected |

### ITEM-36 · A public portal endpoint reads the internal operations estate, against the estate's own principle

| Cited | What it establishes |
|---|---|
| `docs/deployment/sharepoint/portal-wiring.json` | the principle, and declaredCrossings[0] recording this crossing with state UNRESOLVED |
| `npm run wiring` _(command)_ | BOUNDARY - 1 crossing(s): WRITEBACK CG_Writeback_Endpoint read NITDADGO-EAAACTIVITYTRACKING/Flow Configuration |
| `npm run test:wiringspec` _(command)_ | passes: every measured crossing of the portal boundary is declared |
| `docs/reference/flow-contracts/deployed/CG_Writeback_Endpoint__21d4bfd3-f595-46fa-81bb-c29dabc12e7a__full_definition.json` | reads list GUID 9bc168c3-06e5-4d58-982b-0df06205fd35 |
| `docs/deployment/sharepoint/DECISIONS.md` | D16 accepts the crossing for all seven portal endpoints, bounded to one list, one operation, one column, and names what would reverse it |

### ITEM-38 · The OTP verify hardening is deployed — built fresh as IP_OTP_VERIFY on 2026-09-04

| Cited | What it establishes |
|---|---|
| `docs/deployment/sharepoint/evidence/2026-09-04-otp-verify-fresh-build.json` | the operator's package export of the flow they built from the kit, and what reading it establishes: all five defects closed, all four of the 2026-09-03 defects closed, the paste complete at 36 action… |
| `docs/reference/flow-contracts/deployed/IP_OTP_VERIFY__c5e314c7-68b5-4bdc-8935-0954ef6a256d__full_definition.json` | the deployed definition itself, so every claim above is checkable against the artefact rather than a summary of it |
| `npm run test:otpdeployed` _(command)_ | 17 assertions read that export on every build, nine of them naming the defect each exists to catch. If the flow is edited and re-exported, this is what notices the hardening going backwards. |
| `docs/deployment/power-automate-flows/otp-verify-patched-definition.json` | the complete replacement workflow definition — $schema, contentVersion, parameters, triggers, actions — not a patch |
| `scripts/apply-otp-verify-patch.browser.js` | the complete definition embedded in a browser console patcher, generated from it so it cannot drift. It sends the identical PATCH the PowerShell route sends — properties.definition and nothing else —… |
| `npm run test:otppatch` _(command)_ | the patcher is run against a stubbed ProcessSimple API before it is ever pasted: 33 assertions covering token capture over both fetch and XMLHttpRequest, a regional host being honoured rather than th… |
| `docs/deployment/power-automate-flows/OTP-VERIFY-SECURITY-PATCH.md` | the five defects, their effects, and the apply procedure |
| `npm run test:flowpatch` _(command)_ | the patch builder walks the runAfter graph and refuses a definition where an action reads an output not yet produced — it caught defect 3 in the patch itself |
| `docs/deployment/sharepoint/evidence` | no evidence record in this directory names the OTP patch as applied; the estate convention is that a tenant change is recorded here |
| `docs/deployment/sharepoint/evidence/2026-09-03-provisioning-apply-run.json` | the column this definition writes now exists in the tenant — one created, 0 failed — which is what the save-time validation would otherwise reject the whole flow on |
| `docs/deployment/power-automate-flows/fresh-flow/BUILD.md` | a second route, added 2026-09-03 after the flow-management API proved unreachable from the operator's session: build the flow fresh and paste the patched body in through the designer's own clipboard,… |
| `npm run test:freshflow` _(command)_ | 29 assertions holding the kit to the patched definition: the pasted scope byte-identical, no Initialize variable inside a package, every variable the paste touches present in the build table and none… |

### ITEM-39 · OTP_Transactions.Attempts is provisioned, and the list is inside the provisioning specification

| Cited | What it establishes |
|---|---|
| `docs/deployment/sharepoint/evidence/2026-09-03-provisioning-apply-run.json` | the apply pass of 2026-09-03: one column created, 102 already present, 0 failed, and a ledger row reading 'created' for NITDADGO-EAAACTIVITYTRACKING / OTP_Transactions / Attempts / Number. The provis… |
| `docs/deployment/sharepoint/evidence/2026-09-02-provisioning-dry-run.json` | DRY_RUN read of the tenant 2026-09-02: 102 of the specification's 103 columns present, and Attempts on OTP_Transactions is the one that is not. 0 sites unreachable, so the account that ran it can cre… |
| `docs/deployment/sharepoint/portal-field-spec.json` | OTP_Transactions and its Attempts column, declared capturedState PENDING with no provisionedBy |
| `npm run test:sharepoint` _(command)_ | the specification suite now covers this column: type mapping, PENDING justification and the requirement that a PENDING column claiming provisioning names a run record |
| `scripts/provision-sharepoint-fields.browser.js` | the sanctioned provisioner, regenerated from the specification and idempotent — it reads each field first and creates only what is absent |
| `scripts/lib/designer-paste-builder.mjs` | the PENDING strip, and the save-time failure it exists to prevent |
| `docs/deployment/sharepoint/portal-field-spec.json` | Attempts now carries provisionedBy naming that run record. capturedState stays PENDING because that is what the 2026-08-14 capture found; provisionedBy is how a column says it has since been created,… |

### ITEM-42 · The portal sitemap and robots.txt need the deployed origin before the portal is indexable

| Cited | What it establishes |
|---|---|
| `document-portal/sitemap.xml` | the four relative <loc> values and the deploy-time note above them |
| `document-portal/robots.txt` | the relative Sitemap: directive and the same note |
| `docs/deployment/COMMISSIONING.md` | places hosting outside this repository, which is why the origin is not here |
| `docs/deployment/sharepoint/evidence/2026-09-04-origin-confirmed.json` | location.origin read from the running platform on 2026-09-04 printed https://activityweb.page.gd, matching ALLOWED_ORIGIN_1 exactly — scheme, no trailing slash, no path |
| `npm run test:portalseo` _(command)_ | both files are absolute against the confirmed origin, AND neither still instructs a reader to make them so — the check fails on a comment that survives its own cause, because following it would produ… |
| `npm run test:portalseo` _(command)_ | portal SEO: sitemap.xml and robots.txt are absolute, and say so; Sitemap: https://activityweb.page.gd/sitemap.xml |

### ITEM-45 · The scheduled sweep completes: the list view threshold failure is closed

| Cited | What it establishes |
|---|---|
| `docs/deployment/sharepoint/evidence/2026-09-05-scheduled-sweep-run.json` | the run id, its timings, five scopes Succeeded, no errors — and what it does not establish |
| `docs/reference/flow-contracts/deployed/AUTO_SCHEDULED_SWEEP__1cb48715-b1e4-4b9b-8740-fb3ee8c4d6f1__full_definition.json` | the bounded filters the run exercised |
| `docs/deployment/sharepoint/evidence/2026-09-05-provisioning-index-run.json` | the index pass that was the other half |

### ITEM-46 · Two detectors for the threshold read only equality filters, which is why this shipped

| Cited | What it establishes |
|---|---|
| `scripts/build-internal-preflight.mjs` | every operator, $orderby, and mustIndex driven by itemsAtCapture over 5,000 |
| `tests/index-targets.test.mjs` | the same widening; it found five undeclared columns immediately |
| `scripts/preflight-internal-flows.browser.js` | regenerated: Global Tracking Queue now names four columns to index, DGO DIGITAL OPS two |

### ITEM-47 · The four index targets ITEM-45 declared are applied and recorded

| Cited | What it establishes |
|---|---|
| `docs/deployment/sharepoint/evidence/2026-09-05-provisioning-index-run.json` | the indexRun ledger: nine targets, two indexed, seven already indexed, none failed |
| `npm run test:sharepoint` _(command)_ | passes; the assertion that names these four reads the outcome from the committed record rather than being told |

### ITEM-49 · The estate can fire on a schedule — AUTO_SCHEDULED_SWEEP is deployed

| Cited | What it establishes |
|---|---|
| `docs/reference/flow-contracts/deployed/AUTO_SCHEDULED_SWEEP__1cb48715-b1e4-4b9b-8740-fb3ee8c4d6f1__full_definition.json` | the Recurrence trigger, and eight mail actions of which seven address a composed recipient; the one literal address is Send_Outbox_Alert, an operations alert about undelivered notifications |
| `config/notification-matrix.config.js` | the rows and preconditions that depended on a scheduling mechanism existing — PRE-2 is what this item gated |

### ITEM-53 · CG_SEND_EMAIL no longer accepts anonymous callers

| Cited | What it establishes |
|---|---|
| `docs/reference/flow-contracts/deployed/CG_SEND_EMAIL__63a7f2d7-d137-467a-9578-8099c52407dc__full_definition.json` | triggerAuthenticationType: Tenant on the manual trigger, and both mail actions addressing Compose_SendEmail_Recipients rather than a literal |

### ITEM-54 · IP_OTP_Endpoint no longer returns officer claims with a wildcard origin

| Cited | What it establishes |
|---|---|
| `docs/reference/flow-contracts/deployed/IP_OTP_Endpoint__5d0d0a72-1467-4949-a808-61b58cdea0ce__full_definition.json` | the Response headers carrying a composed origin, and the mail action addressing Compose_Otp_Email |

### ITEM-55 · IP_OTP_VERIFY's placeholder origin is replaced by the allow-list its sibling uses

| Cited | What it establishes |
|---|---|
| `docs/reference/flow-contracts/deployed/IP_OTP_VERIFY__c5e314c7-68b5-4bdc-8935-0954ef6a256d__full_definition.json` | the only Access-Control-Allow-Origin value in the definition is the composed expression; Compose_Allowed_Origin_Otp, Compose_Request_Origin_Otp and Select_Allowed_Origins_Otp are all present |

### ITEM-58 · Unresolved DSU keys in the approved matrix — closed by agency direction

| Cited | What it establishes |
|---|---|
| `npm run test:categories` _(command)_ | unresolved DSU keys: (empty), HRA, ODG, PADGCEO, PATODGCEO, PRS |
| `docs/reference/approved/category-matrix.csv` | the rows as approved |
| `docs/reference/approved/dsu-key-corrections.csv` | the five adopted key corrections and one spelling correction, each with its rationale and adoption date |
| `npm run test:categories` _(command)_ | 23 passed, 0 failed; every row that names a unit resolves to one, and only Unclassifieds and Sponsorship And Collaborations / PROPOSALS name none |
| `npm run test:categories` _(command)_ | 24 passed, 0 failed; every approved row names a unit the departments list defines, and no category/subcategory pair repeats |
| `docs/reference/approved/dsu-key-corrections.csv` | eight corrections, each with its rationale and adoption date |


---

## How to check any of this

Every closure above is a claim about a register, and every register is read by a command. Where
this document and a command disagree, **the command governs**.

```bash
npm run readiness     # the register's own tally, and whether it is pinned to this commit
npm run closed        # regenerate this document
npm run test:closure  # no document, and not the commissioning gate, presents a closed item as open
npm run outstanding   # the counterpart: what is still open, across all five registers
```

A closure you believe is wrong is a register edit, not a document edit. Change the item's status
and evidence in the register it lives in, then regenerate.
