# Rotation, and the two configuration items behind it

> GENERATED by `scripts/build-rotation-worksheet.mjs` from [`docs/reference/endpoint-workflow-ids.json`](../../reference/endpoint-workflow-ids.json)
> and the disclosed signatures under `docs/reference/foundational`. Do not edit. `npm run test:rotation` fails if it drifts.

## The size of it

39 workflows carry a disclosed signature — 53 distinct signatures across
28 files. What matters for sequencing is not that number but this one:

| | Endpoint keys |
| --- | --- |
| **Must be re-captured** — the flow's token is disclosed, so rotating it changes the URL | **1** |
| Unaffected by rotation — the flow carries no disclosed token | 24 |
| Undetermined — no workflow id is recorded for the key | 0 |
| **Total** | **25** |

Those 1 keys are served by **1 distinct flows** — two flows back two keys each.
And **38 of the 39 token-bearing workflows sit behind no endpoint key at all**: rotating them
cannot break the internal runtime or the portal, because neither client calls them. That is not the
same as saying they are unused — something outside this repository may. It means they need no
re-capture and no configuration change, so they can be rotated first, on their own, at no risk.

## What this corrects

CFG-2's stated reason for being blocked is "same as CFG-1: depends on rotated URLs". For the
portal that is only partly true. 24 of the 7 portal keys point at flows carrying no disclosed
token, so their URLs do not change when rotation happens. 0 portal keys have no workflow id
recorded at all, so their status is genuinely unknown — and settling that is a small, bounded task
(read the workflow id out of each flow's trigger URL and add it to `docs/reference/endpoint-workflow-ids.json`), not a
dependency on the whole rotation.

## Order of work

1. **Rotate the 38 workflows behind no endpoint key.** No client calls them; nothing to re-capture.
   This shrinks the disclosed surface without touching either runtime.
2. **Record the missing workflow ids** for the 0 undetermined keys, and regenerate this
   worksheet. Either they join the re-capture list or they leave it; both are better than unknown.
3. **Rotate the 1 flows behind the 1 keys, capturing each new URL as you go.** Regenerating a
   trigger URL and not writing it down means the endpoint is lost until you open the flow again.
4. **Fill in `values.template.txt`** and run:
   ```
   npm run setup -- --values <file> --force
   ```
   `--force` matters on any run after the first. Without it `setup` reports
   *"already exists — left untouched (pass --force to replace)"* and keeps the old file, so a
   post-rotation re-run would leave the revoked URLs in place. It says so plainly; this is a note,
   not a defect.
5. **Verify**, and this is the step that catches the failure hand-editing cannot:
   ```
   node scripts/check-config-local.mjs
   node scripts/check-config-local.mjs --portal
   npm run commission
   ```
   `check-config-local.mjs` compares the workflow id inside each URL against the id that key is
   supposed to reach, so a well-formed URL under the wrong key is caught. It prints keys, ids and
   verdicts and never a URL.
6. **Delete the values file.** It is a list of live bearer credentials.

## Keys that must be re-captured

| Key | Scope | Flow | Workflow id |
| --- | --- | --- | --- |
| `EMAIL_RELATED_TASK` | internal | IP_Create_Email_Assignment_Endpoint | `a942d230337c4ddfa9a386e92bbd048b` |

## Keys unaffected by the rotation

| Key | Scope | Flow | Workflow id |
| --- | --- | --- | --- |
| `FETCH_ACTIVITIES` | internal | IP_Get_Docs_Endpoint | `c4c26f93ba1e4d7db5247536c30cdc11` |
| `SUBSIDIARY_ACTIONS` | internal | IP_Dynamic_Global_Actions_Endpoint | `d5e4b3da41e34819b3f953d2acbc2dd7` |
| `FETCH_ALL` | internal | FETCH_ALL | `aa662769f13a4666bfadf3039cd8d247` |
| `REFERENCE_DATA` | internal | IP_Reference_Data_Endpoint | `885fad5ec91f460490e8c772d5d10cb8` |
| `GET_DOCS` | internal | IP_Get_Docs_Endpoint | `c4c26f93ba1e4d7db5247536c30cdc11` |
| `FETCH_EMAIL_ATTACHMENTS` | internal | IP_Retrieve_Email_Attachment_Endpoint | `d2c773332f6f4fbf97ee4a5baa01f70b` |
| `SINGLE_ASSIGNMENT` | internal | Single_Task_Assignment | `72acfc6a694f414f9fe39c939ead7f08` |
| `BULK_ASSIGNMENT` | internal | BULK OPS DATA RETRIEVAL HTTP | `a705e453f922457c95ca19f32731e4d7` |
| `BULK_ASSIGNMENT_DIRECT` | internal | IP_Bulk_Assign_Endpoint | `8f6a40a682bb4e79acd2ac5d42f15705` |
| `DYNAMIC_ACTIONS` | internal | IP_Dynamic_Global_Actions_Endpoint | `d5e4b3da41e34819b3f953d2acbc2dd7` |
| `EMAIL` | internal | IP_SEND_EMAIL | `378b491ec39a4beca886ddec250e6961` |
| `AI_EMAIL_ANALYSIS` | internal | AI_Document_Processing | `3f018fea1031490fb73dff6a8d6341f2` |
| `AI_DOC_ANALYSIS` | internal | AI_Document_Processing | `3f018fea1031490fb73dff6a8d6341f2` |
| `AI_CHAT` | internal | AI_Document_Processing | `3f018fea1031490fb73dff6a8d6341f2` |
| `OTP_GENERATE` | internal | IP_OTP_VERIFY | `b372d45e4b2a47d88b8e8b032da67fcd` |
| `OTP_VERIFY` | internal | IP_OTP_VERIFY | `b372d45e4b2a47d88b8e8b032da67fcd` |
| `SCAN_INTAKE` | internal | IP_SCAN_INTAKE | `88401ce1f44148539c0da53c9491f8e6` |
| `SUBMISSION` | portal | CG_Submission_Endpoint | `1041ed37ce924e3c886d891f23e8142c` |
| `UPLOAD` | portal | CG_Upload_Endpoint | `62fe121e5a95416bb91275e43dd0e37e` |
| `SUPPORT` | portal | CG_Portal_Support_Endpoint | `052013da80724713a4285edee72ccb4a` |
| `VERIFY` | portal | CG_Portal_Verification_Endpoint | `c0d58004c53348539d0d5aeaf49dcded` |
| `VERIFY_CONFIRM` | portal | CG_Portal_Verification_Confirmation_Endpoint | `b270894ff410499ebbc090e32ffe899b` |
| `STATUS` | portal | CG_Portal_Status_Check_Endpoint | `34dd28b4a5664927b66e581c74a0ab94` |
| `WRITEBACK` | portal | CG_Portal_Writeback_Endpoint | `abf3a3ca53e64ba58c9ce5933e4e97e3` |

## Keys with no workflow id recorded

_(none)_

These are undetermined, not clean. Step 2 settles them.

## This chain was run before you run it

Every step above was exercised locally on 2026-09-04 against a synthetic values file — 25
well-formed URLs carrying invented signatures, outside the repository, deleted afterwards. It
established three things:

- `setup --values --force` writes both config files and wires all 25 keys.
- `check-config-local.mjs` accepts a correct mapping and exits 0.
- **The negative control fires.** Two internal keys were swapped so each pointed at the other's
  flow. The checker refused both by name — *"FETCH_ALL points at the WRONG FLOW — expected
  31e02518…, got 818ec405… (that is GET_DOCS)"* — and exited 1. That is the failure hand-editing
  cannot show you, and it is caught.

What it cannot check is the signature. A URL under the right key with a revoked token reads as
correct here, because the workflow id is still right. Only a live call proves a token; that is
what step 5's `npm run commission` and the endpoint probes are for.

## The rule that applies to all of it

A trigger URL is a bearer credential. It goes into a `config.local.js` — which is git-ignored —
and into nothing else. Not this repository, not a ticket, not an email, not a chat message. Every
tool named on this page prints keys, ids and verdicts only, so their output is safe to paste
anywhere.
