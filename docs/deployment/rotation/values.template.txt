# Rotated trigger URLs — one KEY=value line per endpoint.
#
# Hand this to:  npm run setup -- --values <this file>
# Then verify:   node scripts/check-config-local.mjs   and   --portal
#
# THIS FILE HOLDS BEARER CREDENTIALS. Keep it outside the repository, and delete it once
# both config.local.js files are written and verified. Every URL below carries a sig= token;
# a file of them is a file of live keys to the estate.
#
# 1 keys need a NEW url — their flow's token is disclosed and must be rotated first.
# 24 keys are unaffected by the rotation, but still need their url to be configured at all.
# 0 keys have no workflow id recorded, so whether rotation changes them is UNKNOWN.

# ---- must be re-captured after rotating the flow -------------------------------------
EMAIL_RELATED_TASK=

# ---- flow not among the disclosed set; url unchanged by rotation ----------------------
FETCH_ACTIVITIES=
SUBSIDIARY_ACTIONS=
FETCH_ALL=
REFERENCE_DATA=
GET_DOCS=
FETCH_EMAIL_ATTACHMENTS=
SINGLE_ASSIGNMENT=
BULK_ASSIGNMENT=
BULK_ASSIGNMENT_DIRECT=
DYNAMIC_ACTIONS=
EMAIL=
AI_EMAIL_ANALYSIS=
AI_DOC_ANALYSIS=
AI_CHAT=
OTP_GENERATE=
OTP_VERIFY=
SCAN_INTAKE=
SUBMISSION=
UPLOAD=
SUPPORT=
VERIFY=
VERIFY_CONFIRM=
STATUS=
WRITEBACK=

# ---- workflow id not recorded; see ROTATION.md ----------------------------------------
